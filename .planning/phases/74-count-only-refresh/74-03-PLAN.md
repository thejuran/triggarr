---
phase: 74-count-only-refresh
plan: 03
type: execute
wave: 3
depends_on: [74-02]
files_modified:
  - triggarr/templates/partials/app_card.html
  - tests/test_refresh_counts.py
autonomous: true
requirements: [CNT-05]
must_haves:
  truths:
    - "The connected app-card footer shows two side-by-side flex-1 buttons: Search Now (primary) + Refresh counts (secondary)"
    - "Refresh counts htmx-posts to refresh_counts, targets the card, swaps outerHTML, disables itself in-flight — mirroring Search Now exactly"
    - "The disconnected app-card footer is unchanged: single full-width Retry Connection button, no Refresh counts"
  artifacts:
    - path: "triggarr/templates/partials/app_card.html"
      provides: "Connected footer split into Search Now + Refresh counts; disconnected footer unchanged"
      contains: "refresh_counts"
    - path: "tests/test_refresh_counts.py"
      provides: "Button presence (connected) / absence (disconnected) tests"
      contains: "Refresh counts"
  key_links:
    - from: "triggarr/templates/partials/app_card.html Refresh counts button"
      to: "refresh_counts route"
      via: "request.url_for('refresh_counts', ...)"
      pattern: "url_for\\('refresh_counts'"
    - from: "Refresh counts button"
      to: "the card element"
      via: "hx-target outerHTML swap"
      pattern: "hx-swap=\"outerHTML\""
---

<objective>
Split the single full-width "Search Now" button in the connected footer of `triggarr/templates/partials/app_card.html` into two side-by-side `flex-1` buttons: "Search Now" stays primary (current styling, app-colored hover magnifying-glass), and a new secondary "Refresh counts" button (lighter style, `ph-arrows-clockwise` icon) htmx-posts to the Plan 02 `refresh_counts` endpoint. The disconnected footer (single "Retry Connection" button) is left exactly as-is.

Purpose: Give users the count-only refresh affordance directly on each app card, updating counts/health in place via the full-card partial swap — no new visual design, the button's design contract is fully locked in CONTEXT.md D-09..D-14. (CNT-05)
Output: Modified connected-footer block in `app_card.html`; button presence/absence tests in `tests/test_refresh_counts.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase source artifacts (read before implementing)
@.planning/phases/74-count-only-refresh/74-CONTEXT.md
@.planning/phases/74-count-only-refresh/74-PATTERNS.md
@.planning/phases/74-count-only-refresh/74-VALIDATION.md

# Plan 02 registered the route this button posts to
@.planning/phases/74-count-only-refresh/74-02-SUMMARY.md

<interfaces>
<!-- Confirmed live line anchors. app_card.html footer block. -->

triggarr/templates/partials/app_card.html footer is at lines 110-126:
- Footer wrapper `<div class="p-3 bg-triggarr-bg/30 ...">` at line 110.
- `{% if app.connected == false %}` branch (lines 111-117): single full-width "Retry Connection" button — `w-full ... bg-triggarr-card ... text-triggarr-muted`, icon `ph ph-arrows-clockwise`, posts to `search_now`. LEAVE THIS BRANCH UNCHANGED (D-10).
- `{% else %}` connected branch (lines 118-125): single full-width "Search Now" button — `w-full flex items-center justify-center gap-2 py-2 rounded-md bg-triggarr-elevated hover:bg-triggarr-border border border-triggarr-border text-xs font-semibold transition-colors text-triggarr-text group disabled:opacity-50 disabled:cursor-not-allowed`, icon `ph ph-magnifying-glass` with app-colored `group-hover:` classes; htmx attrs hx-post→search_now, hx-target="#{{ app.card_id }}-card", hx-swap="outerHTML", hx-disabled-elt="this". THIS IS THE BRANCH TO SPLIT.
- `{% endif %}` at line 126.

Locked design contract (CONTEXT.md D-09..D-14): wrap the two connected buttons in `<div class="flex gap-2">`; Search Now `w-full` → `flex-1` (only change to it); Refresh counts is `flex-1`, lighter (`bg-triggarr-card hover:bg-triggarr-elevated`, `text-triggarr-muted`, no app-colored group-hover), icon `ph ph-arrows-clockwise`, label exactly "Refresh counts", mirrors Search Now's hx-post (→ `refresh_counts`)/hx-target/hx-swap="outerHTML"/hx-disabled-elt="this". No spinner, no success cue, no sibling-disable.

The full target markup is in 74-PATTERNS.md "Target: two side-by-side buttons" (lines ~413-434) — copy it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button presence/absence tests (RED) then split the connected footer (GREEN)</name>
  <files>tests/test_refresh_counts.py, triggarr/templates/partials/app_card.html</files>
  <read_first>
    - triggarr/templates/partials/app_card.html (footer block lines 110-126)
    - .planning/phases/74-count-only-refresh/74-PATTERNS.md (app_card.html section lines ~395-440 — current block + exact target markup + key copy decisions)
    - .planning/phases/74-count-only-refresh/74-CONTEXT.md (D-09..D-14 — the locked design contract)
    - tests/test_refresh_counts.py (refresh_test_app/refresh_client fixtures from Plan 02 — the connected GET /partials/app-card test pattern lines ~707-721 in 74-PATTERNS.md)
  </read_first>
  <action>
    First add two tests to `tests/test_refresh_counts.py` (copy the scaffold from 74-PATTERNS.md): `test_app_card_connected_has_refresh_counts_button` (GET /partials/app-card/radarr/Default → 200, body contains "Refresh counts") and `test_app_card_disconnected_no_refresh_counts_button` (set `refresh_test_app.state.triggarr_state["radarr"]["Default"]["connected"] = False`, GET → 200, body does NOT contain "Refresh counts" but DOES contain "Retry Connection"). Run them to confirm the connected test is RED (button absent) while the disconnected test passes. Then edit ONLY the `{% else %}` connected branch (lines 118-125) of `app_card.html`: wrap the two buttons in `<div class="flex gap-2">...</div>`; change Search Now's `w-full` to `flex-1` (every other class on Search Now stays identical — same `bg-triggarr-elevated`, app-colored `group-hover` magnifying-glass, htmx attrs); add the secondary Refresh counts button (`flex-1 flex items-center justify-center gap-2 py-2 rounded-md bg-triggarr-card hover:bg-triggarr-elevated border border-triggarr-border text-xs font-semibold transition-colors text-triggarr-muted disabled:opacity-50 disabled:cursor-not-allowed`, icon `<i class="ph ph-arrows-clockwise"></i>`, label "Refresh counts", `hx-post="{{ request.url_for('refresh_counts', app_name=app.name, instance_name=app.instance) }}"`, `hx-target="#{{ app.card_id }}-card"`, `hx-swap="outerHTML"`, `hx-disabled-elt="this"`). Do NOT touch the `{% if app.connected == false %}` Retry Connection branch (D-10). Run the tests + full suite; commit `feat(74-03): split connected footer into Search Now + Refresh counts buttons`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_refresh_counts.py -k "button or disconnected" -x -q && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_app_card_connected_has_refresh_counts_button` and `test_app_card_disconnected_no_refresh_counts_button` both pass.
    - Source assertion (CNT-05 / D-09): `grep -c "url_for('refresh_counts'" triggarr/templates/partials/app_card.html` == 1; the connected branch contains `<div class="flex gap-2">` and two `flex-1` buttons; Search Now no longer has `w-full`.
    - Source assertion (D-11): the Refresh counts button uses `ph ph-arrows-clockwise` and the exact label `Refresh counts`.
    - Source assertion (D-12): the Refresh counts button mirrors Search Now's htmx attrs — `hx-target="#{{ app.card_id }}-card"`, `hx-swap="outerHTML"`, `hx-disabled-elt="this"` all present.
    - Source assertion (D-10): the `{% if app.connected == false %}` branch is unchanged — still a single `w-full` "Retry Connection" button, no "Refresh counts". `git diff` shows changes only within the connected `{% else %}` branch.
    - `uv run pytest tests/ -x -q` passes (984 baseline + all Phase 74 tests; the existing `test_app_card_partial_*` web tests still green).
    - Commit message starts with `feat(74-03):`.
  </acceptance_criteria>
  <done>Connected footer shows Search Now + Refresh counts side by side; disconnected footer unchanged; button tests + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → htmx POST to `refresh_counts` | The button issues the request handled by the Plan 02 endpoint (which owns all input validation, rate-limit, and auth); the template adds no new server-side trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-10 | Information Disclosure | secret/credential rendered into the card partial | mitigate | The button swaps `partials/app_card.html` via `_build_app_context`, the same context as the existing periodic poll and `search_now` — no `api_key`/secret field is in the card context. No new template variable introduced. |
| T-74-11 | Tampering | htmx attrs diverging from the hardened Search Now affordance | accept | Refresh counts mirrors Search Now's `hx-disabled-elt="this"` dim-only in-flight pattern verbatim (hardened in the v2.9 walkthrough); no new client behavior to abuse. |
</threat_model>

<verification>
- `uv run pytest tests/test_refresh_counts.py -k "button or disconnected" -x -q` — presence + absence tests green.
- `uv run pytest tests/ -x -q` — full suite green.
- `git diff triggarr/templates/partials/app_card.html` — changes confined to the connected `{% else %}` footer branch; disconnected branch untouched.
</verification>

<success_criteria>
- The connected app-card footer renders Search Now (primary) + Refresh counts (secondary) side by side as `flex-1` buttons; Refresh counts htmx-posts to `refresh_counts`, swaps the full card, dims itself in-flight. (CNT-05)
- The disconnected footer is unchanged (single Retry Connection button, no Refresh counts). (D-10)
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/74-count-only-refresh/74-03-SUMMARY.md` when done.
</output>
