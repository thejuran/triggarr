---
phase: 28-fix-code-review-findings-from-v2-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/templates/settings.html
  - triggarr/config.py
  - triggarr/search/scheduler.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Settings checkbox and description text are wrapped in a single container div matching other settings pattern"
    - "No print() calls exist in triggarr/ source code"
    - "Callable type annotation uses collections.abc.Callable, not lowercase builtin"
  artifacts:
    - path: "triggarr/templates/settings.html"
      provides: "Skip unreleased checkbox with proper container wrapping"
    - path: "triggarr/config.py"
      provides: "Loguru logger.warning instead of print()"
    - path: "triggarr/search/scheduler.py"
      provides: "Callable return type from collections.abc"
  key_links: []
---

<objective>
Fix settings template structure (F2) and three deferred Phase 16 findings (M3, M5, M6).

Purpose: Clean up minor cosmetic and code quality issues found during v2.2 code review. These are mechanical fixes with no behavioral change beyond the template improvement and lint compliance.

Output: Cleaner settings template, no print() calls, proper type annotations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-fix-code-review-findings-from-v2-2/28-RESEARCH.md

<interfaces>
<!-- Current code that needs fixing -->

From triggarr/templates/settings.html (lines 62-71):
```html
<div class="flex items-center gap-3 mt-2">
    <input type="checkbox" name="skip_unreleased" ...>
    <label for="skip_unreleased" class="text-sm">Skip Unreleased Movies</label>
</div>
<p class="text-xs text-triggarr-muted mt-1">
    Skip Radarr movies without a past digital or physical release date.
</p>
```

From triggarr/config.py (lines 88-95):
```python
if not config_path.exists():
    generate_default_config(config_path)
    print(
        f"Default config written to {config_path}\n"
        "Edit the config file and restart Triggarr.",
        file=sys.stderr,
    )
    sys.exit(1)
```

From triggarr/search/scheduler.py (line 106):
```python
def create_lifespan(
    settings: Settings, state_path: Path, config_path: Path
) -> callable:  # type: ignore[type-arg]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix settings template wrapping (F2)</name>
  <files>triggarr/templates/settings.html</files>
  <action>
    Wrap the skip_unreleased checkbox div (lines 62-68) and its description paragraph (lines 69-71) in a single container `<div>`, matching the pattern used by other settings fields (e.g., Tracking Window at lines 55-61 uses `<div>` containing `<label>` + `<input>` + `<p>`).

    Replace lines 62-71:
    ```html
    <div>
        <div class="flex items-center gap-3 mt-2">
            <input type="checkbox" name="skip_unreleased"
                   {% if skip_unreleased %}checked{% endif %}
                   class="accent-triggarr-green w-4 h-4"
                   id="skip_unreleased">
            <label for="skip_unreleased" class="text-sm">Skip Unreleased Movies</label>
        </div>
        <p class="text-xs text-triggarr-muted mt-1">
            Skip Radarr movies without a past digital or physical release date.
        </p>
    </div>
    ```

    This ensures the checkbox and description are siblings within one container, keeping them visually associated on narrow screens.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_web.py -k "settings" -x -q</automated>
  </verify>
  <done>Checkbox and description wrapped in container div matching other settings fields.</done>
</task>

<task type="auto">
  <name>Task 2: Fix deferred findings M5 and M6</name>
  <files>triggarr/config.py, triggarr/search/scheduler.py</files>
  <action>
    **M5 - Replace print() with Loguru in config.py:**
    In triggarr/config.py around line 90, replace:
    ```python
    print(
        f"Default config written to {config_path}\n"
        "Edit the config file and restart Triggarr.",
        file=sys.stderr,
    )
    ```
    With:
    ```python
    logger.warning(
        "Default config written to {path} -- edit the config file and restart Triggarr",
        path=config_path,
    )
    ```
    Ensure `from loguru import logger` is imported at top of file (check if already present; if not, add it).

    **M6 - Fix lowercase callable type annotation in scheduler.py:**
    In triggarr/search/scheduler.py line 106, change:
    ```python
    ) -> callable:  # type: ignore[type-arg]
    ```
    To:
    ```python
    ) -> Callable[..., AsyncIterator[None]]:
    ```
    The function returns an `@asynccontextmanager` which yields `AsyncIterator[None]`. `Callable` is already imported from `collections.abc` at line 13. Remove the `# type: ignore[type-arg]` comment since we're providing proper type args now.

    **M3 - contextlib.suppress(Exception) too broad:** Research shows `contextlib.suppress(Exception)` no longer exists in the codebase (grep returned no matches). Skip this fix -- it was already addressed in a prior phase.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/config.py --select T201 && uv run ruff check triggarr/search/scheduler.py --select UP006 && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>No print() calls in triggarr/ (ruff T201 clean). Callable type annotation uses collections.abc.Callable with proper type args (ruff UP006 clean). All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` -- full suite green
- `uv run ruff check triggarr/ tests/` -- no lint violations
- `uv run ruff check triggarr/ --select T201` -- no print() calls
- `uv run ruff check triggarr/ --select UP006` -- no lowercase type aliases
</verification>

<success_criteria>
- Settings template checkbox wrapped in container div
- Zero print() calls in triggarr/ source
- Proper Callable type annotation in scheduler.py
- All 270+ existing tests pass
- No ruff violations
</success_criteria>

<output>
After completion, create `.planning/phases/28-fix-code-review-findings-from-v2-2/28-02-SUMMARY.md`
</output>
