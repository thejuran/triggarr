---
phase: 50-app-cards-services-grid
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - tests/test_app_cards.py
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05
  - CARD-06
  - CARD-07
  - LAYOUT-01

must_haves:
  truths:
    - "Tests verify unified connection pill markup for all three states"
    - "Tests verify schedule row with Last Run / Next Run formatting"
    - "Tests verify pass pill badges appear only when pass > 0"
    - "Tests verify hover and shadow CSS classes on all cards"
    - "Tests verify danger-stripes and Retry button on unreachable cards"
    - "Tests verify dot-pulse on Connected pill only"
    - "Tests verify xl:grid-cols-3 in dashboard grid wrapper"
  artifacts:
    - path: "tests/test_app_cards.py"
      provides: "Comprehensive test coverage for CARD-01..07 and LAYOUT-01"
      min_lines: 150
  key_links:
    - from: "tests/test_app_cards.py"
      to: "triggarr/templates/partials/app_card.html"
      via: "HTTP response assertions on HTML content"
      pattern: "response\\.text"
---

<objective>
Create comprehensive tests validating all Phase 50 app card features — connection pills, schedule row, pass pills, hover elevation, danger stripes, Retry button, live-refresh dot, and 3-column grid.

Purpose: Verify every CARD-01..07 and LAYOUT-01 requirement is correctly implemented with automated assertions.
Output: tests/test_app_cards.py with tests covering all eight requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-app-cards-services-grid/50-CONTEXT.md
@.planning/phases/50-app-cards-services-grid/50-01-SUMMARY.md

@triggarr/templates/partials/app_card.html
@triggarr/templates/dashboard.html
@triggarr/static/css/input.css
@triggarr/web/routes.py
@tests/test_web.py
@tests/test_stats_health.py

<interfaces>
<!-- Test fixtures from test_web.py that test_app_cards.py should reuse -->
From tests/test_web.py:
```python
# The test fixtures `client` and `test_app` are defined in conftest.py or test_web.py
# client: TestClient for the FastAPI app
# test_app: the FastAPI app instance with mocked state
# test_app.state.triggarr_state["radarr"]["Default"] = {...}  # dict of app state
# test_app.state.settings  # mocked settings object
```

<!-- HTML patterns to assert from Plan 01 -->
Connection pill (Connected): `inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full bg-triggarr-green/15 text-triggarr-green`
Connection pill (Unreachable): `rounded-full bg-red-500/15 text-red-400`
Connection pill (Waiting): `rounded-full bg-triggarr-border/40 text-triggarr-muted`
Dot pulse: `dot-pulse`
Schedule row: `Last run`, `Next run`
Pass pill: `text-[10px] bg-triggarr-border/60`
Danger stripes: `danger-stripes`
Retry button: `Retry`
Card hover: `card-hover`
Shadow: `shadow-sm`
Grid: `xl:grid-cols-3`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_app_cards.py with comprehensive card feature tests</name>
  <files>tests/test_app_cards.py</files>
  <read_first>
    - tests/test_web.py (fixture patterns: how `client`, `test_app` are used, how state is set up, assertion patterns)
    - tests/test_stats_health.py (recent Phase 49 test patterns for reference)
    - triggarr/templates/partials/app_card.html (the actual template being tested — know the exact HTML to assert)
    - triggarr/templates/dashboard.html (grid wrapper markup to verify)
    - triggarr/static/css/input.css (verify .card-hover and .danger-stripes exist)
    - triggarr/web/routes.py lines 147-200 (_build_app_context fields that tests manipulate)
  </read_first>
  <action>
Create `tests/test_app_cards.py` — a new test file (per Claude's discretion in CONTEXT.md, use separate file not extending test_web.py).

Import the same fixtures used by test_web.py (`client`, `test_app` from conftest or inline). Check test_web.py to see if fixtures are in conftest.py or defined inline — follow the same pattern.

**Tests to write (one test function per requirement):**

1. **test_connected_pill_unified_shape (CARD-01):**
   - GET `/partials/app-card/radarr/Default` with `connected=True` in state
   - Assert response contains `rounded-full bg-triggarr-green/15 text-triggarr-green`
   - Assert response contains the text `Connected`
   - Assert response contains `inline-flex items-center gap-1.5`

2. **test_unreachable_pill_unified_shape (CARD-01):**
   - Set `connected=False`, `unreachable_since` to an ISO timestamp in state
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `rounded-full bg-red-500/15 text-red-400`
   - Assert response contains the text `Unreachable`
   - Assert response contains `inline-flex items-center gap-1.5`

3. **test_waiting_pill_unified_shape (CARD-01):**
   - Set `connected=None` in state (remove the key or set to None)
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `rounded-full bg-triggarr-border/40 text-triggarr-muted`
   - Assert response contains `Waiting...`

4. **test_schedule_row_present (CARD-02):**
   - Set `last_run` to `"2026-04-13T14:32:10"` and ensure `next_run` is set via scheduler mock
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `Last run` and `Next run`
   - Assert response contains `border-b border-triggarr-border/50 pb-3`
   - Assert response contains `14:32:10` (HH:MM:SS from last_run)

5. **test_schedule_row_unreachable_next_run_dash (CARD-02):**
   - Set `connected=False`
   - GET `/partials/app-card/radarr/Default`
   - Assert the Next run value area contains `&mdash;` or `—` (em dash)

6. **test_pass_pill_displayed (CARD-03):**
   - Set `missing_pass=2` in state
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `pass 2`
   - Assert response contains `text-[10px] bg-triggarr-border/60`

7. **test_pass_pill_hidden_when_zero (CARD-03):**
   - Set `missing_pass=0`, `cutoff_pass=0` in state
   - GET `/partials/app-card/radarr/Default`
   - Assert response does NOT contain `pass 0`
   - Assert response does NOT contain `bg-triggarr-border/60` (no pill rendered)

8. **test_card_has_hover_classes (CARD-04):**
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `card-hover`
   - Assert response contains `shadow-sm`

9. **test_unreachable_card_danger_stripes (CARD-05):**
   - Set `connected=False` in state
   - GET `/partials/app-card/radarr/Default`
   - Assert response contains `danger-stripes`
   - Assert response contains `relative overflow-hidden`

10. **test_connected_card_no_danger_stripes (CARD-05):**
    - Set `connected=True` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response does NOT contain `danger-stripes`

11. **test_unreachable_card_retry_button (CARD-06):**
    - Set `connected=False` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response contains `Retry`
    - Assert response contains `bg-red-500/15 text-red-400`
    - Assert response does NOT contain `Search Now`

12. **test_connected_card_search_now_button (CARD-06):**
    - Set `connected=True` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response contains `Search Now`
    - Assert response does NOT contain `Retry`

13. **test_connected_pill_has_dot_pulse (CARD-07):**
    - Set `connected=True` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response contains `dot-pulse`

14. **test_unreachable_pill_no_dot_pulse (CARD-07):**
    - Set `connected=False` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response does NOT contain `dot-pulse`

15. **test_dashboard_grid_three_columns (LAYOUT-01):**
    - GET `/` (dashboard)
    - Assert response contains `xl:grid-cols-3`

16. **test_unreachable_stats_opacity (CARD-05):**
    - Set `connected=False` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response contains `opacity-60`

17. **test_css_has_card_hover_rule:**
    - Read `triggarr/static/css/input.css`
    - Assert it contains `.card-hover`
    - Assert it contains `background-color: #233346`

18. **test_css_has_danger_stripes_rule:**
    - Read `triggarr/static/css/input.css`
    - Assert it contains `.danger-stripes`
    - Assert it contains `repeating-linear-gradient`

19. **test_tag_warning_uses_svg_icon (D-25):**
    - Set `tag_warnings` to `[{"field": "missing", "tag": "hdr"}]` in state
    - GET `/partials/app-card/radarr/Default`
    - Assert response contains `<svg` within the warning badge area
    - Assert response does NOT contain `&#9888;`
  </action>
  <verify>
    <automated>uv run pytest tests/test_app_cards.py -x -q 2>&1 | tail -10 && uv run pytest tests/test_web.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_app_cards.py exists with at least 17 test functions
    - All tests in test_app_cards.py pass
    - All tests in test_web.py still pass (no regressions)
    - Tests cover: connected pill (CARD-01), unreachable pill (CARD-01), waiting pill (CARD-01), schedule row (CARD-02), pass pills shown and hidden (CARD-03), card-hover and shadow-sm (CARD-04), danger-stripes present and absent (CARD-05), Retry vs Search Now (CARD-06), dot-pulse present and absent (CARD-07), xl:grid-cols-3 (LAYOUT-01)
    - `uv run ruff check tests/test_app_cards.py` passes
  </acceptance_criteria>
  <done>All 19 tests pass covering every requirement CARD-01..07 and LAYOUT-01 with automated assertions, no regressions in existing test suite</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test-only plan, no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | N/A | Test file | accept | Tests only — no new attack surface introduced |
</threat_model>

<verification>
1. `uv run pytest tests/test_app_cards.py -x -q` — all tests pass
2. `uv run pytest tests/test_web.py -x -q` — no regressions
3. `uv run ruff check tests/test_app_cards.py` — no lint violations
</verification>

<success_criteria>
- tests/test_app_cards.py exists with 17+ passing tests
- Every requirement (CARD-01..07, LAYOUT-01) has at least one dedicated test
- No regressions in existing test suite
- Ruff lint passes on the new test file
</success_criteria>

<output>
After completion, create `.planning/phases/50-app-cards-services-grid/50-02-SUMMARY.md`
</output>
