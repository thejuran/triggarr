---
phase: 50-app-cards-services-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/static/css/input.css
  - triggarr/templates/partials/app_card.html
  - triggarr/templates/dashboard.html
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05
  - CARD-06
  - CARD-07
  - LAYOUT-01

must_haves:
  truths:
    - "User sees a unified connection pill (Connected/Unreachable/Waiting) in every app card header"
    - "User sees Last Run and Next Run in a dedicated schedule row below the card header"
    - "User sees compact 'pass N' pill badges instead of ordinal text"
    - "User sees cards elevate on hover with animated background and shadow transition"
    - "User sees diagonal red danger stripes on unreachable instance cards"
    - "User sees a red Retry button instead of Search Now on unreachable cards"
    - "User sees a pulsing green dot inside the Connected pill during live-refresh"
    - "User sees a 3-column grid at xl: breakpoint with 3+ configured instances"
  artifacts:
    - path: "triggarr/static/css/input.css"
      provides: ".card-hover and .danger-stripes CSS rules"
      contains: ".card-hover"
    - path: "triggarr/templates/partials/app_card.html"
      provides: "Redesigned app card template with all CARD-01..07 features"
      contains: "rounded-full"
    - path: "triggarr/templates/dashboard.html"
      provides: "3-column grid layout at xl breakpoint"
      contains: "xl:grid-cols-3"
  key_links:
    - from: "triggarr/templates/partials/app_card.html"
      to: "triggarr/static/css/input.css"
      via: "CSS classes card-hover and danger-stripes"
      pattern: "card-hover|danger-stripes"
    - from: "triggarr/templates/partials/app_card.html"
      to: "triggarr/web/routes.py"
      via: "Jinja context variables from _build_app_context"
      pattern: "app\\.connected|app\\.unreachable_since|app\\.missing_pass"
---

<objective>
Redesign app service cards with unified connection pills, schedule rows, pass pills, hover elevation, unreachable danger stripes, Retry button, live-refresh dot, and 3-column xl grid.

Purpose: Transform the services section from functional-but-plain cards to the v2.5 mockup's polished, information-dense design with clear visual hierarchy for connection state, scheduling, and queue progress.
Output: Updated app_card.html template, CSS additions to input.css, grid update in dashboard.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-app-cards-services-grid/50-CONTEXT.md

@triggarr/templates/partials/app_card.html
@triggarr/templates/dashboard.html
@triggarr/static/css/input.css
@triggarr/web/routes.py
@.aidesigner/enhanced-mockup-v3.html

<interfaces>
<!-- Key data contract from routes.py _build_app_context() — executor needs these field names -->
From triggarr/web/routes.py _build_app_context():
```python
return {
    "name": app_name,                       # "radarr", "sonarr", "lidarr"
    "instance": instance_name,              # "Default", "4K", etc.
    "card_id": _sanitize_card_id(...),      # safe HTML id
    "last_run": app_state.get("last_run"),  # ISO string or None
    "next_run": next_run,                   # ISO string or None
    "missing_cursor": ...,                  # int
    "cutoff_cursor": ...,                   # int
    "missing_pass": ...,                    # int (0 = no pass pill)
    "cutoff_pass": ...,                     # int (0 = no pass pill)
    "connected": ...,                       # True, False, or None (Waiting)
    "unreachable_since": ...,               # ISO string or None
    "missing_count": ...,                   # int or None
    "missing_eligible": ...,                # int or None
    "missing_monitored": ...,               # int or None
    "missing_searchable": ...,              # int or None
    "cutoff_count": ...,                    # int or None
    "cutoff_searchable": ...,               # int or None
    "total_items": ...,                     # int or None
    "skip_unreleased": ...,                 # bool
    "tag_warnings": ...,                    # list of dicts with "field" and "tag"
}
```

From triggarr/web/routes.py:
```python
def _relative_time(dt: datetime | None) -> str:
    """Format a datetime as a short relative string (e.g. '2s ago', '1m ago')."""
```

From triggarr/static/css/input.css (existing Phase 48 utilities):
```css
.dot-pulse { position: relative; }
.dot-pulse::after { /* pulsing green ring animation */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .card-hover and .danger-stripes CSS rules to input.css</name>
  <files>triggarr/static/css/input.css</files>
  <read_first>
    - triggarr/static/css/input.css (current CSS with @theme tokens and existing utilities)
    - .aidesigner/enhanced-mockup-v3.html lines 43-44 (card-hover spec) and lines 64-73 (danger-stripes spec)
  </read_first>
  <action>
Add two new CSS rule blocks to the end of `triggarr/static/css/input.css` (after the existing `.mini-bar` rules):

1. **`.card-hover` rule (per D-12, D-13):**
```css
/* App card hover elevation — CARD-04 */
.card-hover {
  transition: background-color 150ms ease, box-shadow 150ms ease;
}
.card-hover:hover {
  background-color: #233346;
  box-shadow: 0 8px 24px -12px rgba(0, 0, 0, 0.6);
}
```
Note: Uses the `triggarr-card-elevated` token value `#233346` directly in CSS (not Tailwind class) so the background-color transition animates smoothly. Per D-12, do NOT add `transform` to the transition — the mockup includes it but the effect is barely visible, keep it simple.

2. **`.danger-stripes` rule (per D-15):**
```css
/* Diagonal red danger stripes for unreachable cards — CARD-05 */
.danger-stripes {
  background-image: repeating-linear-gradient(
    135deg,
    rgba(239, 68, 68, 0.05) 0px,
    rgba(239, 68, 68, 0.05) 6px,
    transparent 6px,
    transparent 12px
  );
}
```
Note: 5% opacity red — intentionally very subtle per CONTEXT specifics.
  </action>
  <verify>
    <automated>grep -c "card-hover" triggarr/static/css/input.css && grep -c "danger-stripes" triggarr/static/css/input.css && grep "background-color: #233346" triggarr/static/css/input.css</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/static/css/input.css contains `.card-hover {` with `transition: background-color 150ms ease, box-shadow 150ms ease;`
    - triggarr/static/css/input.css contains `.card-hover:hover {` with `background-color: #233346;` and `box-shadow: 0 8px 24px -12px rgba(0, 0, 0, 0.6);`
    - triggarr/static/css/input.css contains `.danger-stripes {` with `repeating-linear-gradient(135deg`
    - The danger-stripes gradient uses `rgba(239, 68, 68, 0.05)` (5% opacity, not higher)
  </acceptance_criteria>
  <done>Both .card-hover and .danger-stripes CSS rules exist in input.css matching the mockup spec exactly</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app_card.html with unified pills, schedule row, pass pills, danger stripes, and Retry button</name>
  <files>triggarr/templates/partials/app_card.html, triggarr/templates/dashboard.html</files>
  <read_first>
    - triggarr/templates/partials/app_card.html (current template to be replaced)
    - triggarr/templates/dashboard.html (grid wrapper div to update)
    - triggarr/web/routes.py lines 147-200 (_build_app_context return dict — field names are the contract)
    - .aidesigner/enhanced-mockup-v3.html lines 264-417 (complete app card markup for all states)
    - .planning/phases/50-app-cards-services-grid/50-CONTEXT.md (all D-01 through D-25 decisions)
  </read_first>
  <action>
**Part A: Rewrite `triggarr/templates/partials/app_card.html`**

Replace the entire file with the redesigned template. The structure from top to bottom:

**Outer div** (per D-12, D-14, D-15, D-16):
```html
<div id="{{ app.card_id }}-card"
     hx-get="{{ request.url_for('partial_app_card', app_name=app.name, instance_name=app.instance) }}"
     hx-trigger="every 5s"
     hx-swap="outerHTML"
     class="bg-triggarr-card rounded-lg border border-triggarr-border p-5 shadow-sm card-hover
            {% if app.connected == false %}border-l-4 border-l-red-500 danger-stripes relative overflow-hidden{% else %}border-l-4 border-l-triggarr-green{% endif %}">
```
Key points: `shadow-sm` on ALL cards (D-14), `card-hover` on ALL cards (D-12), `danger-stripes relative overflow-hidden` only when `app.connected == false` (D-15, D-16).

**Header row** with unified connection pill (D-01 through D-04):
```html
<div class="flex items-center justify-between">
  <h2 class="text-lg font-semibold">{{ app.name | capitalize }}{% if app.instance != 'Default' %} <span class="text-sm font-normal text-triggarr-muted">/ {{ app.instance }}</span>{% endif %}</h2>
  {% if app.connected == true %}
    <span class="inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full bg-triggarr-green/15 text-triggarr-green">
      <span class="w-1.5 h-1.5 rounded-full bg-triggarr-green dot-pulse"></span>
      Connected
    </span>
  {% elif app.connected == false %}
    <span class="inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full bg-red-500/15 text-red-400">
      <span class="w-1.5 h-1.5 rounded-full bg-red-500"></span>
      Unreachable
    </span>
  {% else %}
    <span class="inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full bg-triggarr-border/40 text-triggarr-muted">
      <span class="w-1.5 h-1.5 rounded-full bg-triggarr-muted"></span>
      Waiting...
    </span>
  {% endif %}
</div>
```
Note: All three states use the SAME `inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full` pill shape. Connected state has `dot-pulse` on inner dot (D-03). Unreachable has NO pulse (D-02). Waiting has NO pulse (D-02).

**Tag warning badge** (D-24, D-25) — keep existing logic, update styling and icon:
```html
{% if app.tag_warnings %}
<div class="bg-amber-500/15 text-amber-400 text-xs px-3 py-1.5 rounded mt-3 flex items-center gap-2">
  <svg class="w-3.5 h-3.5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z"/><line x1="12" y1="9" x2="12" y2="13"/><circle cx="12" cy="17" r="0.5"/></svg>
  <span>
    {% if app.tag_warnings | length == 1 %}
      {{ app.tag_warnings[0].field | capitalize }} tag '{{ app.tag_warnings[0].tag }}' not found
    {% else %}
      Tags not found: {% for w in app.tag_warnings %}'{{ w.tag }}' ({{ w.field }}){% if not loop.last %}, {% endif %}{% endfor %}
    {% endif %}
  </span>
</div>
{% endif %}
```
Note: Changed from `&#9888;` HTML entity to SVG warning triangle icon per D-25. Background changed from `bg-amber-500/20` to `bg-amber-500/15` per mockup L315.

**Schedule row** (D-05 through D-08) — NEW section between header/tag-warning and stats grid:
```html
<div class="mt-3 flex items-center justify-between text-xs text-triggarr-muted border-b border-triggarr-border/50 pb-3">
  <span>Last run <span class="text-triggarr-text">{% if app.last_run %}{{ app.last_run[11:19] }}{% else %}Never{% endif %}</span></span>
  <span>Next run <span class="text-triggarr-text">{% if app.connected == false %}&mdash;{% elif app.next_run %}{{ app.next_run[11:16] }}{% else %}&mdash;{% endif %}</span></span>
</div>
```
Per D-08: Last run shows HH:MM:SS (slice [11:19] from ISO string), Next run shows HH:MM (slice [11:16]). Per D-07: When unreachable, Next Run shows em dash.

**Stats grid** (D-09 through D-11, D-17) — restructured with pass pills, opacity when unreachable:
```html
<div class="grid grid-cols-2 gap-4 mt-3{% if app.connected == false %} opacity-60{% endif %}">
```
Per D-17: `opacity-60` when unreachable.

For Missing section, the cursor line changes from ordinal text to pass pill:
```html
<p class="text-xs text-triggarr-muted mt-0.5 flex items-center gap-1.5">
  <span>{{ app.missing_cursor }} of {{ app.missing_searchable if app.missing_searchable is not none else (app.missing_eligible if app.missing_eligible is not none else (app.missing_count if app.missing_count is not none else '?')) }}</span>
  {%- if app.missing_pass is defined and app.missing_pass > 0 %}
  <span class="text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded">pass {{ app.missing_pass }}</span>
  {%- endif %}
</p>
```
Per D-09: pill badge `text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded`. Per D-10: inline-flex via parent `flex items-center gap-1.5`. Per D-11: only when pass > 0.

Same pattern for Cutoff cursor line — replace ordinal text with pill:
```html
<p class="text-xs text-triggarr-muted mt-0.5 flex items-center gap-1.5">
  <span>{{ app.cutoff_cursor }} of {{ app.cutoff_searchable if app.cutoff_searchable is not none else (app.cutoff_count if app.cutoff_count is not none else '?') }}</span>
  {%- if app.cutoff_pass is defined and app.cutoff_pass > 0 %}
  <span class="text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded">pass {{ app.cutoff_pass }}</span>
  {%- endif %}
</p>
```

Keep the existing Missing count line (`{{ app.missing_count }} of {{ app.total_items }}`), Cutoff count line, skip-unreleased amber line — these are unchanged.

**Controls section** (D-18, D-19, D-06) — conditional Search Now vs Retry:
```html
<div class="flex items-center gap-3 mt-4 pt-3 border-t border-triggarr-border/50">
  {% if app.connected == false %}
    <button hx-post="{{ request.url_for('search_now', app_name=app.name, instance_name=app.instance) }}"
            hx-target="#{{ app.card_id }}-card"
            hx-swap="outerHTML"
            class="bg-red-500/15 text-red-400 text-xs font-medium px-3 py-1.5 rounded hover:bg-red-500/25 transition-colors flex items-center gap-1.5">
      <svg class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M20.49 9A9 9 0 0 0 5.64 5.64L1 10m22 4-4.64 4.36A9 9 0 0 1 3.51 15"/></svg>
      Retry
    </button>
  {% else %}
    <button hx-post="{{ request.url_for('search_now', app_name=app.name, instance_name=app.instance) }}"
            hx-target="#{{ app.card_id }}-card"
            hx-swap="outerHTML"
            class="bg-triggarr-green/20 text-triggarr-green text-xs font-medium px-3 py-1.5 rounded hover:bg-triggarr-green/30 transition-colors">
      Search Now
    </button>
  {% endif %}
</div>
```
Per D-18: red Retry button with refresh SVG icon. Per D-19: reuses same `search_now` POST endpoint.

**Part B: Update `triggarr/templates/dashboard.html`**

Change line 25 (the services grid wrapper) from:
```html
<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
```
to:
```html
<div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 mb-6">
```
Per D-21: add `xl:grid-cols-3` for 3-column layout at >=1280px viewport.

**Part C: Rebuild output.css**

Run: `uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --minify`
  </action>
  <verify>
    <automated>grep "rounded-full bg-triggarr-green/15" triggarr/templates/partials/app_card.html && grep "rounded-full bg-red-500/15" triggarr/templates/partials/app_card.html && grep "rounded-full bg-triggarr-border/40" triggarr/templates/partials/app_card.html && grep "xl:grid-cols-3" triggarr/templates/dashboard.html && grep "danger-stripes" triggarr/templates/partials/app_card.html && grep "Retry" triggarr/templates/partials/app_card.html && grep 'class="text-\[10px\]' triggarr/templates/partials/app_card.html && grep "shadow-sm card-hover" triggarr/templates/partials/app_card.html && grep "dot-pulse" triggarr/templates/partials/app_card.html && grep "opacity-60" triggarr/templates/partials/app_card.html && uv run pytest tests/test_web.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app_card.html contains three connection pill variants all using `inline-flex items-center gap-1.5 text-[11px] font-medium px-2 py-0.5 rounded-full`
    - Connected pill: `bg-triggarr-green/15 text-triggarr-green` with inner `dot-pulse` dot
    - Unreachable pill: `bg-red-500/15 text-red-400` with static red dot (no pulse)
    - Waiting pill: `bg-triggarr-border/40 text-triggarr-muted` with static muted dot
    - Schedule row exists with `border-b border-triggarr-border/50 pb-3` containing "Last run" and "Next run"
    - Last run slices ISO string at [11:19] for HH:MM:SS format
    - Next run slices ISO string at [11:16] for HH:MM format
    - Next Run shows em dash when `app.connected == false`
    - Pass pills use `text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded` class
    - Pass pills only render when pass > 0 (no "pass 0")
    - Outer card div includes `shadow-sm card-hover` on ALL cards
    - Outer card div includes `danger-stripes relative overflow-hidden` when `app.connected == false`
    - Stats grid gets `opacity-60` when unreachable
    - Retry button with refresh SVG icon appears when unreachable, Search Now when connected
    - Retry button uses same `search_now` endpoint as Search Now
    - Tag warning badge uses SVG triangle icon instead of &#9888;
    - dashboard.html grid wrapper contains `xl:grid-cols-3`
    - All existing tests in test_web.py pass
  </acceptance_criteria>
  <done>Complete app card redesign with all CARD-01..07 and LAYOUT-01 features implemented, output.css rebuilt, existing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jinja template rendering | User-controlled instance names and tag values rendered in HTML |
| htmx POST (search_now) | Reused existing endpoint, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Spoofing | app_card.html Retry button | accept | Reuses existing `search_now` endpoint which already validates app_name is in APP_TYPES and instance_name length <= 64 chars |
| T-50-02 | Information Disclosure | Schedule row time display | accept | Times are ISO string slices from server state, no user secrets — only shows HH:MM:SS of last/next run |
| T-50-03 | Injection | Tag warning badge text | mitigate | Tag names and field names are already Jinja2-autoescaped in the template; no `|safe` filter used |
| T-50-04 | Tampering | CSS class injection via card_id | accept | card_id is sanitized by existing `_sanitize_card_id()` which strips special characters — no change needed |
</threat_model>

<verification>
1. `grep "card-hover" triggarr/static/css/input.css` returns the CSS rule
2. `grep "danger-stripes" triggarr/static/css/input.css` returns the CSS rule
3. `grep "rounded-full" triggarr/templates/partials/app_card.html` shows unified pill markup
4. `grep "xl:grid-cols-3" triggarr/templates/dashboard.html` confirms 3-col grid
5. `uv run pytest tests/test_web.py -x -q` passes all existing tests
6. `uv run ruff check triggarr/ tests/` passes lint
</verification>

<success_criteria>
- All 8 requirements (CARD-01..07, LAYOUT-01) implemented per CONTEXT.md decisions D-01 through D-25
- Connection pill uses identical shape for all three states with state-specific colors
- Schedule row shows formatted times below card header
- Pass pills replace ordinal text with compact badges
- Card hover transitions background and shadow smoothly
- Unreachable cards show danger stripes, opacity-60 stats, red Retry button
- Connected pill pulsing dot visible during htmx polling
- 3-column grid activates at xl: breakpoint
- All existing tests pass, no ruff violations
</success_criteria>

<output>
After completion, create `.planning/phases/50-app-cards-services-grid/50-01-SUMMARY.md`
</output>
