---
phase: 75-drain-timeout-config-parity-deferred-record-correction
plan: 04
type: execute
wave: 3
depends_on: ["75-02", "75-03"]
files_modified:
  - .planning/STATE.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "D-07: The STATE.md deferred table no longer mis-records DEBT-07/08/03 as parked and shows DEBT-06 as shipped in Phase 75"
    - "D-08: README documents the new settings-UI / config-field path for the drain timeout and the config-default-vs-env-override precedence, without removing the stop_grace_period guidance"
    - "D-09: CHANGELOG.md has a v2.10 section covering all three tracks (password recovery, refresh-counts, drain-timeout knob)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "corrected deferred record (DEBT-07/08/03 shipped; DEBT-06 shipped)"
      contains: "DEBT-06"
    - path: "README.md"
      provides: "drain-timeout settings-UI path + precedence note"
      contains: "shutdown_drain_timeout"
    - path: "CHANGELOG.md"
      provides: "v2.10 in-app changelog section"
      contains: "v2.10"
  key_links:
    - from: "CHANGELOG.md v2.10 section"
      to: "triggarr/changelog.py::read_changelog()"
      via: "in-app changelog modal parser (## vX.Y.Z (YYYY-MM-DD) format)"
      pattern: "## v2\\.10"
---

<objective>
Correct the stale deferred record and document the shipped drain-timeout knob across the three
DOCS-01 record-correction surfaces (D-07, D-08, D-09). The fourth DOCS-01 surface — the
settings.html field help text (D-10) — already shipped in Plan 02. This plan runs last because it
documents the now-shipped knob (Plans 01-03) and the milestone's other two tracks (72-74,
already shipped).

Purpose: Make the project record match shipped reality — DEBT-07 (request timeout), DEBT-08 (page
size), DEBT-03 (search-history cap) were already shipped (config fields + settings inputs), and
DEBT-06 (drain timeout) is now shipped — and surface the new config path to operators (README +
in-app changelog). This is record-correction and documentation, not feature work.
Output: Corrected STATE.md deferred rows, extended README drain docs, a new CHANGELOG.md v2.10
section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-CONTEXT.md
@.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md

<notes>
This plan touches only docs/record artifacts plus the repo-root CHANGELOG.md (consumed by the
in-app changelog modal). No production code, no tests. The drain-timeout config field, settings
input + help text, and scheduler precedence are delivered by Plans 01-03 (this wave depends on
02 and 03). Tracks A and B (Phases 72-74) already shipped this milestone — the CHANGELOG v2.10
entry covers all three per spec §5 / D-09.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the STATE.md deferred-record table (D-07)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the Deferred Items table, STATE.md:101-123 — the in-table note at STATE.md:103 says "DEBT-07/08/03/06 leave this table at v2.10 close"; the two rows at STATE.md:107-108)
    - .planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md (the STATE.md section — D-07)
    - .planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-CONTEXT.md (D-07 — keep the edit minimal and factual; GSD's complete-milestone may rewrite STATE.md at archive, so this is in-flight bookkeeping correctness)
  </read_first>
  <action>
    In .planning/STATE.md, update the two deferred rows (STATE.md:107-108) to reflect shipped
    reality per the in-table note (STATE.md:103). Mark the DEBT-07/08/03 record-correction row as
    resolved (already shipped as both config fields, config.py:128-130, AND settings inputs) and
    the DEBT-06 row as shipped in Phase 75 (CFG-03/CFG-04). Either move them out of the parked list
    or annotate them clearly as shipped/resolved — the goal is the table no longer asserts these
    four items are parked. Do NOT touch the genuinely-deferred rows (UI-01/02/03, PERF-*, SCALE-*,
    AUDIT-01, OBS-01, the v2.9-audit follow-ups, the cosmetic primaryDark token). Keep the edit
    minimal and factual.
  </action>
  <verify>
    <automated>grep -n "DEBT-06\|DEBT-07/08/03" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md no longer records DEBT-07/08/03 as parked/pending — they read as already shipped (config fields + settings inputs)
    - .planning/STATE.md records DEBT-06 as shipped in Phase 75 (CFG-03/CFG-04), not "in scope" / "shipping in v2.10"
    - The genuinely-deferred rows (UI-01/02/03, PERF-01/02/03, SCALE-01/02, AUDIT-01, OBS-01) are unchanged
    - `grep -n "DEBT-06" .planning/STATE.md` shows the DEBT-06 row marked shipped/resolved
  </acceptance_criteria>
  <done>The STATE.md deferred table reflects that DEBT-07/08/03 were already shipped and DEBT-06 is now shipped; unrelated deferred items are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend README drain docs + add the v2.10 CHANGELOG section (D-08, D-09)</name>
  <files>README.md, CHANGELOG.md</files>
  <read_first>
    - README.md (the existing drain/env docs at README.md:86, 95, 140 — the stop_grace_period guidance, the prose drain note, the systemd comment; read all three so none are removed)
    - CHANGELOG.md (the v2.9.0 section header + bulleted categories, CHANGELOG.md:3-22 — mirror the structure and the `## vX.Y.Z (YYYY-MM-DD)` header format the parser expects)
    - triggarr/changelog.py (read_changelog at changelog.py:32, 40 — confirm the header format the in-app modal parser requires; no code change needed)
    - .planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md (the README.md and CHANGELOG.md sections — D-08/D-09)
    - docs/superpowers/specs/2026-06-02-recovery-counts-config-design.md (§5 Cross-Track Close — the v2.10 entry covers all three tracks)
  </read_first>
  <action>
    README (D-08): Near README.md:95 (the prose drain note), add a sentence documenting that the
    drain timeout is now a persisted config field (general.shutdown_drain_timeout, settable in
    Settings, default 60s) and that TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT OVERRIDES the configured value
    when set (config default, env overrides — D-06). Keep the >= 1.0 clamp note and the
    `stop_grace_period > drain` deployment guidance at README.md:86/140 intact — do NOT remove the
    existing env-var documentation.
    CHANGELOG (D-09): Add a new top section above the v2.9.0 entry for the v2.10 release, using the
    `## vX.Y.Z (YYYY-MM-DD)` header format the parser requires (mirror CHANGELOG.md:3-22 structure
    with bulleted categories). Per spec §5 / D-09, the v2.10 entry covers all three tracks shipped
    this milestone: password recovery (Track A, Phases 72-73), Refresh-counts (Track B, Phase 74),
    and the drain-timeout settings knob (Track C, Phase 75). Exact wording/ordering and the version
    string (e.g. v2.10.0) / date are Claude's discretion; keep it factual to shipped behavior. No
    code change to changelog.py is needed — read_changelog() renders this section into the in-app
    modal.
  </action>
  <verify>
    <automated>grep -n "shutdown_drain_timeout\|TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT" README.md && grep -n "## v2.10" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md documents general.shutdown_drain_timeout as a settings-UI / config-field path AND states the env var overrides the configured value
    - README.md still contains the stop_grace_period deployment guidance and the existing TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT env documentation (not removed)
    - CHANGELOG.md has a new `## v2.10` section above v2.9.0, using the `## vX.Y.Z (YYYY-MM-DD)` header format
    - The v2.10 CHANGELOG section references all three tracks (password recovery, refresh-counts, drain-timeout knob)
    - `grep -n "## v2.10" CHANGELOG.md` matches
  </acceptance_criteria>
  <done>README documents the new config-field/settings path and the env-override precedence without dropping existing guidance; CHANGELOG has a parser-compatible v2.10 section covering all three tracks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md → in-app changelog modal | repo-root markdown is parsed by read_changelog() and rendered into the app UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-11 | Cross-Site Scripting | CHANGELOG.md rendered in the in-app modal | accept | CHANGELOG content is maintainer-authored (not user input) and rendered through the existing read_changelog()/template path whose escaping is unchanged; this plan adds no new render mechanism |
| T-75-12 | Information Disclosure | README / CHANGELOG / STATE docs | accept | Docs describe a numeric drain knob and shipped features only — no token/hash/secret/PII; the password-recovery changelog line describes the flow without exposing any token or secret value (SecretStr discipline unaffected — no secret is written to any doc) |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; docs-only plan, no dependency or code change |
</threat_model>

<verification>
- `grep -n "DEBT-06" .planning/STATE.md` shows the row marked shipped/resolved
- `grep -n "shutdown_drain_timeout" README.md` matches the new settings-UI/config-field documentation
- `grep -n "## v2.10" CHANGELOG.md` matches the new section header
- `uv run pytest tests/ -x -q` exits 0 (docs-only change must not break the changelog parser tests or any other test)
- `uv run ruff check triggarr/ tests/` exits 0 (no code touched, but confirm the tree is clean)
</verification>

<success_criteria>
- STATE.md, README, and CHANGELOG correctly state that DEBT-07/08/03 were already shipped and DEBT-06 is now shipped (DOCS-01)
- README documents the config-field/settings-UI path and the config-default / env-override precedence without dropping existing guidance
- CHANGELOG has a parser-compatible v2.10 section covering all three milestone tracks
- The full suite stays green (the changelog parser still reads the file); ruff clean
</success_criteria>

<output>
Create `.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-04-SUMMARY.md` when done
</output>
