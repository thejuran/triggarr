---
phase: 75-drain-timeout-config-parity-deferred-record-correction
plan: 03
type: tdd
wave: 2
depends_on: ["75-01"]
files_modified:
  - triggarr/search/scheduler.py
  - tests/test_scheduler.py
autonomous: true
requirements: [CFG-04]
must_haves:
  truths:
    - "D-04: _read_shutdown_drain_timeout(configured) resolves the drain timeout from the configured GeneralConfig value, with TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT overriding when set"
    - "D-06: The >= 1.0 clamp applies to BOTH the configured value and the env override (env wins over config); a malformed env value falls back to the configured default"
    - "D-06: A non-finite resolved value (env nan/inf/-inf, or a non-finite configured value) is treated as malformed and falls back to a finite default — the helper NEVER returns nan/inf, so asyncio.timeout always gets a finite bound"
    - "D-05: The shutdown path reads the drain timeout from app.state.settings (or the lifespan-closure settings) at shutdown time, not from import-time module state"
    - "D-05: A discriminating fast test proves the CONFIGURED value reaches the shutdown block — it sets a distinctive shutdown_drain_timeout (7.0, not the 60.0 default and not the 1.0 minimum), runs WITHOUT holding the lock (so asyncio.timeout(7.0) returns instantly on a successful acquire, no real wait), captures the loguru drain-entry INFO log, and asserts it contains `timeout=7.0s` — a hardcoded 60.0 would log `timeout=60.0s` and fail, so the test genuinely discriminates config-read from config-ignored"
  artifacts:
    - path: "triggarr/search/scheduler.py"
      provides: "config-aware, finite-guaranteed _read_shutdown_drain_timeout + local drain resolution in the shutdown block"
      contains: "def _read_shutdown_drain_timeout(configured: float = 60.0) -> float"
    - path: "tests/test_scheduler.py"
      provides: "precedence-matrix tests (incl. non-finite) + migrated holder-identity test (1.0) + a discriminating fast test that asserts the logged drain value equals a distinctive configured value (7.0)"
      contains: "_read_shutdown_drain_timeout"
  key_links:
    - from: "scheduler.py shutdown-drain block"
      to: "app.state.settings.general.shutdown_drain_timeout"
      via: "drain = _read_shutdown_drain_timeout(...) at the top of the block"
      pattern: "_read_shutdown_drain_timeout\\(.*shutdown_drain_timeout"
    - from: "scheduler.py asyncio.timeout"
      to: "drain local"
      via: "drain replaces _SHUTDOWN_DRAIN_TIMEOUT in the shutdown path"
      pattern: "asyncio\\.timeout\\(drain\\)"
    - from: "triggarr/search/scheduler.py::_read_shutdown_drain_timeout"
      to: "math.isfinite"
      via: "non-finite resolved value falls back to a finite default before clamping"
      pattern: "math\\.isfinite"
    - from: "tests/test_scheduler.py discriminating drain test"
      to: "the INFO drain-entry log timeout= value"
      via: "asserts the logged drain equals the distinctive configured 7.0 (not 60.0)"
      pattern: "timeout=7\\.0s"
---

<objective>
Refactor the scheduler shutdown drain so the timeout is resolved from the configured value at
shutdown time with the env var overriding it (D-04/D-05/D-06), delivering the precedence half of
CFG-04. This is the one real wiring change in the phase: (1) make `_read_shutdown_drain_timeout`
accept a `configured` default, apply `TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT` on top with a `>= 1.0`
clamp on both sources, AND guarantee the result is FINITE (a `nan`/`inf` from either source —
which `float()` parses without raising and which survives `max(value, 1.0)` — must fall back to a
finite default); and (2) resolve a single local `drain` at the top of the shutdown-drain block
and replace every `_SHUTDOWN_DRAIN_TIMEOUT` reference in the shutdown path with it.

The refactor is behavior-preserving for the shutdown mechanism (`asyncio.timeout()`, WR-01) —
only the timeout VALUE source changes. The full existing suite must stay green.

Purpose: Remove the import-time-staleness footgun (the drain was read once at module import) so
the operator's configured value (and env override) is honored at actual shutdown time, where
`app.state.settings` is already in scope — and guarantee `asyncio.timeout(drain)` always receives
a finite value (an `inf` would never bound the drain; a `nan` would time out immediately —
either way the drain is effectively disabled, the failure the >=1.0 clamp was meant to prevent).
Output: A config-aware, finite-guaranteed helper, a local-resolved `drain` in the shutdown block,
precedence-matrix tests (including non-finite), a migrated holder-identity test, and a
discriminating fast test that proves the configured value reaches the shutdown block by asserting
the LOGGED drain value equals a distinctive configured value.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-CONTEXT.md
@.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md

<interfaces>
<!-- Contract from Plan 75-01 and the existing scheduler shutdown internals. -->

From Plan 75-01: `GeneralConfig.shutdown_drain_timeout: float`
(default 60.0, ge=1.0, allow_inf_nan=False). The model boundary already rejects a non-finite
CONFIGURED value at construction, so in practice `app.state.settings.general.shutdown_drain_timeout`
is finite. The helper STILL guards non-finite defensively (defense in depth — the helper is also
called directly in tests and is the single choke point that feeds `asyncio.timeout`).

From triggarr/search/scheduler.py — current helper (scheduler.py:58-75):
```python
def _read_shutdown_drain_timeout() -> float:
    raw = os.environ.get("TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT", "60.0")
    try:
        value = float(raw)
    except (ValueError, TypeError):
        value = 60.0
    return max(value, 1.0)
```
Target signature: `def _read_shutdown_drain_timeout(configured: float = 60.0) -> float`.
Behavior: read TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT; if unset, use `configured`; if set, env wins
(coerce via float(), falling back to `configured` on (ValueError, TypeError)). THEN, before
clamping, if the resolved `value` is non-finite (`not math.isfinite(value)` — note `float("nan")`,
`float("inf")`, `float("-inf")` all parse WITHOUT raising and would survive the clamp:
`max(nan, 1.0) == nan`, `max(inf, 1.0) == inf`), fall back to `configured`; and if `configured`
itself is somehow non-finite, fall back to the module default 60.0 — so the helper NEVER returns
nan/inf. Finally return `max(value, 1.0)` so the clamp applies to BOTH sources. Add
`import math` to the module (ruff I). Keep the existing stop_grace_period deployment-guidance
docstring tail.

Module constant (scheduler.py:81): `_SHUTDOWN_DRAIN_TIMEOUT: float = _read_shutdown_drain_timeout()`.
RETAIN this constant as a no-arg call (env-unset → 60.0) so test_shutdown_timeout_default_is_60s
and test_shutdown_timeout_env_var_override stay green for free (FLAG 1). The shutdown PATH must no
longer reference it.

Shutdown-drain block (scheduler.py:598-657) — `scheduler.shutdown(wait=False)` at 598, then the
`holder = getattr(app.state, "search_lock_holder", None)` line at 605. Five `_SHUTDOWN_DRAIN_TIMEOUT`
references at lines 611, 618, 634 (`asyncio.timeout(...)`), 648, 656. The drain value is embedded in
the logs at TWO seams a test can assert on: the drain-ENTRY `logger.info(...)` (the holder branch at
609-614 with `t=...` at 611, and the no-holder branch at 616-619 with `t=...` at 618 — both render
`timeout={t}s`), and the timeout `logger.warning(...)` (648, 656, rendering `... in {timeout}s ...`).
The drain-ENTRY info log fires BEFORE the `asyncio.timeout` wait, so its `timeout=` value is
observable even when the lock is uncontested and `acquire()` returns instantly (no real wait).

CONFIRMED — FLAG 2: `app.state.settings = settings` is bound during lifespan startup at
scheduler.py:473, before the shutdown block runs. The block already reads `app.state.search_lock`
and `app.state.search_lock_holder`. Reading `app.state.settings.general.shutdown_drain_timeout`
at shutdown is therefore safe and introduces no new state-access pattern. The closure-scoped
`settings` var is also in scope inside `create_lifespan` (scheduler.py:405) as an equivalent safe
read path.

CONFIRMED — make_settings + the holder-identity test seam (FLAG 1 fix). From tests/conftest.py,
`make_settings(general=...)` (conftest.py:29, 42) accepts a `GeneralConfig` via the `general=` kwarg.
The migrated holder-identity test forces the fast drain via
`make_settings(general=GeneralConfig(shutdown_drain_timeout=1.0), radarr_enabled=False, sonarr_enabled=False)`.
CRITICAL CONSISTENCY NOTE: the value MUST be `1.0`, NOT `0.1`. Plan 75-01 adds
`Field(ge=1.0, allow_inf_nan=False)`, so `GeneralConfig(shutdown_drain_timeout=0.1)` raises
`pydantic.ValidationError` at construction — a `0.1` here would make the RED test error in
settings construction instead of exercising the shutdown block, and GREEN could never pass without
weakening the core `ge=1.0` bound. `1.0` is the field minimum and is the smallest value that
satisfies `GeneralConfig`. The holder-identity test acquires the lock and never releases it, so
the drain ALWAYS times out regardless of the value — `1.0` proves the shutdown block READS the
configured value and logs holder identity exactly as `0.1` would, just with a ~1.0s wait instead
of ~0.1s (acceptable for a single test; this is the pattern the other shutdown tests use too —
they rely on the timeout firing, not on a sub-second value). Do NOT introduce any config value
below 1.0 anywhere in this plan.

CONFIRMED — the discriminating-test seam (FINDING A — Codex second pass). The migrated
holder-identity test holds the lock and never releases it, so the drain ALWAYS times out
REGARDLESS of whether the shutdown block read the configured value or a hardcoded default — it
proves the holder LOGGING works, but it does NOT prove the CONFIGURED value flowed into the block.
A second, separate test closes that gap by asserting on the LOGGED drain value:
- Set a DISTINCTIVE configured value `shutdown_drain_timeout=7.0` (clearly not the 60.0 default,
  clearly not the 1.0 minimum) via `make_settings(general=GeneralConfig(shutdown_drain_timeout=7.0), ...)`.
- Do NOT acquire the lock and do NOT inject a holder. The lock is uncontested, so the no-holder
  branch's drain-entry `logger.info("Shutdown: draining search lock (timeout={t}s); no current holder", t=drain)`
  fires with `t=7.0`, rendering `timeout=7.0s`, and `asyncio.timeout(7.0)` then succeeds INSTANTLY
  on `await app.state.search_lock.acquire()` — there is NO multi-second wait (the 7.0 is logged but
  never waited on, because the acquire returns immediately on the uncontested lock).
- Capture loguru output with the SAME `io.StringIO()` + `logger.add(sink, format="{level} | {message}", level="INFO")`
  mechanism the holder-identity test uses, and assert the captured output contains `timeout=7.0s`
  (the drain-entry INFO line). A shutdown block that read a hardcoded `60.0` would log
  `timeout=60.0s` and the assertion would FAIL — so the test genuinely discriminates
  "config was read" from "config was ignored", and it runs fast (no real timeout wait).
This is option (ii)/(i) from FINDING A: keep the lock-held holder-identity test at 1.0 for the
LOGGING proof, and add a SEPARATE fast lock-UNHELD test at 7.0 for the CONFIG-READ proof. Do NOT
patch `asyncio.timeout` — the live log seam is cleaner and asserts the real rendered value.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Refactor _read_shutdown_drain_timeout to config-default-with-env-override + finite guard + precedence tests (D-04, D-06)</name>
  <files>triggarr/search/scheduler.py, tests/test_scheduler.py</files>
  <read_first>
    - triggarr/search/scheduler.py (_read_shutdown_drain_timeout at scheduler.py:58-75 and the module constant at scheduler.py:81 — read both, plus the docstring deployment-guidance tail to preserve it; confirm the module's import block so `import math` sorts under ruff I)
    - tests/test_scheduler.py (test_shutdown_timeout_default_is_60s at test_scheduler.py:628 and test_shutdown_timeout_env_var_override at test_scheduler.py:636 — the existing constant tests that MUST stay green; FLAG 1)
    - .planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md (the scheduler.py section — D-04/D-05/D-06 — and the scheduler precedence-test assignment, the full precedence matrix)
    - docs/superpowers/specs/2026-06-02-recovery-counts-config-design.md (§4.2 "bounded to defend against a typo disabling the drain"; §4.3 precedence — the one real decision — and §4.4 tests)
    - /Users/julianamacbook/triggarr/CLAUDE.md (no bare except — catch (ValueError, TypeError))
  </read_first>
  <behavior>
    - Test: env unset → returns configured. `_read_shutdown_drain_timeout(45.0) == 45.0`
    - Test: env set → env wins over configured. monkeypatch.setenv("TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT", "15.0"); `_read_shutdown_drain_timeout(45.0) == 15.0`
    - Test: clamp on configured source. `_read_shutdown_drain_timeout(0.5) == 1.0`
    - Test: clamp on env source. monkeypatch.setenv(..., "0"); `_read_shutdown_drain_timeout(45.0) == 1.0`
    - Test: malformed env falls back to configured. monkeypatch.setenv(..., "abc"); `_read_shutdown_drain_timeout(45.0) == 45.0`
    - Test (non-finite env, FINITE GUARANTEE): monkeypatch.setenv(..., "nan"); `_read_shutdown_drain_timeout(45.0) == 45.0` (nan resolved value is non-finite → falls back to configured; assert math.isfinite(result) is True)
    - Test (non-finite env, FINITE GUARANTEE): monkeypatch.setenv(..., "inf"); `_read_shutdown_drain_timeout(45.0) == 45.0` (inf survives max(inf,1.0)==inf without the guard; the guard must catch it)
    - Test (non-finite env, FINITE GUARANTEE): monkeypatch.setenv(..., "-inf"); `_read_shutdown_drain_timeout(45.0) == 45.0`
    - Test (non-finite configured, defense in depth): `_read_shutdown_drain_timeout(float("inf"))` returns a finite value (60.0) and `math.isfinite(_read_shutdown_drain_timeout(float("inf"))) is True` — even though 75-01's allow_inf_nan=False makes a non-finite CONFIGURED value unreachable from the model, the helper must not return inf if called with one directly
    - Test (must stay green, FLAG 1): _read_shutdown_drain_timeout() with no arg and env unset returns 60.0 (preserves the module-constant default so test_shutdown_timeout_default_is_60s passes)
  </behavior>
  <action>
    RED: In tests/test_scheduler.py, add precedence-matrix tests calling _read_shutdown_drain_timeout
    directly (pure function — no lifespan needed) for each behavior case above: env unset → configured,
    env overrides, clamp on configured, clamp on env, malformed env → configured, AND the four
    non-finite cases (env "nan"/"inf"/"-inf" → configured; and configured=float("inf") → finite 60.0,
    each asserting `math.isfinite(result)` is True). Use monkeypatch.setenv / monkeypatch.delenv.
    Run them; the configured-arg cases AND the non-finite cases must fail (the function takes no arg
    yet, and the current body has no isfinite guard so `inf` would pass through).
    GREEN: In triggarr/search/scheduler.py, add `import math` to the module (ruff I will sort it among
    the stdlib imports). Change the signature to
    `def _read_shutdown_drain_timeout(configured: float = 60.0) -> float`. Read
    os.environ.get("TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT") with no default; if None, value = configured;
    else try float(raw) and on (ValueError, TypeError) fall back to configured. THEN add the finite
    guard: `if not math.isfinite(value): value = configured if math.isfinite(configured) else 60.0`
    (so a non-finite env value OR a non-finite configured value resolves to a finite default — the
    helper NEVER returns nan/inf). Return max(value, 1.0) so the clamp applies to both sources. Update
    the docstring to document the D-06 precedence (config default, env overrides), the >=1.0 clamp,
    and that non-finite (nan/inf) values are rejected so the returned drain is always finite; KEEP the
    existing stop_grace_period deployment-guidance tail. Leave the module constant scheduler.py:81 as
    `_SHUTDOWN_DRAIN_TIMEOUT: float = _read_shutdown_drain_timeout()` (no-arg call → env-unset → 60.0)
    so the two existing constant tests stay green (FLAG 1 — retain). Run the precedence tests AND the
    two existing constant tests; all must pass.
    Add a RED commit `test(75-03): add drain-timeout precedence-matrix tests (incl. non-finite)` and a
    GREEN commit `refactor(75-03): config-default-with-env-override + finite-guaranteed drain timeout`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "shutdown_timeout or drain" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/search/scheduler.py contains `def _read_shutdown_drain_timeout(configured: float = 60.0) -> float`
    - triggarr/search/scheduler.py _read_shutdown_drain_timeout falls back to `configured` on a malformed env value and catches only (ValueError, TypeError)
    - triggarr/search/scheduler.py _read_shutdown_drain_timeout guards non-finite resolved values with `math.isfinite` (env nan/inf/-inf OR a non-finite configured value → a finite default) and the module imports `math`; it never returns nan/inf
    - triggarr/search/scheduler.py retains the module constant `_SHUTDOWN_DRAIN_TIMEOUT` as a no-arg call (FLAG 1)
    - tests/test_scheduler.py contains precedence tests covering: env unset → configured, env overrides, clamp on both sources, malformed env → configured, env nan/inf/-inf → configured (each asserting math.isfinite(result)), and configured=inf → finite
    - test_shutdown_timeout_default_is_60s and test_shutdown_timeout_env_var_override still pass unchanged
    - `uv run pytest tests/test_scheduler.py -k "shutdown_timeout or drain" -x -q` exits 0
  </acceptance_criteria>
  <done>The helper resolves config-default-with-env-override, clamps both sources to >= 1.0, falls back on malformed AND non-finite env/configured values to a finite default (never returns nan/inf), retains the legacy module constant, and the precedence + non-finite + existing constant tests pass.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Resolve drain locally in the shutdown block + migrate holder-identity test (1.0) + add a discriminating config-read test (7.0) (D-05, FLAG 1, FLAG 2, FINDING A)</name>
  <files>triggarr/search/scheduler.py, tests/test_scheduler.py</files>
  <read_first>
    - triggarr/search/scheduler.py (the shutdown-drain block scheduler.py:598-657 — the five _SHUTDOWN_DRAIN_TIMEOUT references at 611, 618, 634, 648, 656; the drain-ENTRY info log at 609-619 — holder branch `t=` at 611, no-holder branch `t=` at 618 — both render `timeout={t}s`; the lifespan binding `app.state.settings = settings` at scheduler.py:473; create_lifespan at scheduler.py:405)
    - tests/test_scheduler.py (test_shutdown_timeout_logs_holder_identity at test_scheduler.py:665 — the BREAKING test that monkeypatches sched._SHUTDOWN_DRAIN_TIMEOUT = 0.1 at test_scheduler.py:678, and its loguru capture idiom `io.StringIO()` + `logger.add(sink, format="{level} | {message}", level="INFO")` at 687-688; test_shutdown_drains_search_lock at test_scheduler.py:564 — must stay green; the module imports `io`, `re`, `time`, and `from loguru import logger` at the top, but NOT GeneralConfig — only `from triggarr.models.config import InstanceConfig, Settings` at line 32)
    - tests/conftest.py (make_settings at conftest.py:29 — accepts a GeneralConfig via general=; use it to set shutdown_drain_timeout=1.0 for the holder-identity test and 7.0 for the discriminating test, both >= the 1.0 field minimum)
    - .planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-PATTERNS.md (the scheduler.py shutdown-block section — D-05 — and the "must stay green / must update" test notes; FLAG 1 + FLAG 2)
  </read_first>
  <behavior>
    - Test (migrated holder-identity, FLAG 1 — LOGGING proof): forcing a fast drain via make_settings(general=GeneralConfig(shutdown_drain_timeout=1.0), radarr_enabled=False, sonarr_enabled=False) still produces the INFO-on-entry log naming the holder job_id + elapsed, and the WARNING-on-timeout log — without monkeypatching the module constant, and WITHOUT using any config value below 1.0 (0.1 would raise ValidationError under 75-01's ge=1.0; 1.0 is the field minimum and the test still times out because the lock is held and never released)
    - Test (NEW discriminating config-read, FINDING A — CONFIG-READ proof): with make_settings(general=GeneralConfig(shutdown_drain_timeout=7.0), radarr_enabled=False, sonarr_enabled=False) and the lock UNHELD (no acquire, no holder injected), the shutdown block resolves drain=7.0 from app.state.settings, logs the no-holder drain-entry INFO line `Shutdown: draining search lock (timeout=7.0s); no current holder`, then `asyncio.timeout(7.0)` succeeds INSTANTLY on the uncontested acquire (no real wait). The captured loguru output contains `timeout=7.0s`. A hardcoded 60.0 would log `timeout=60.0s` and the assertion would FAIL — proving the configured value reached the block. The test runs in well under a second (no timeout fires)
    - Test (must stay green): test_shutdown_drains_search_lock still drains and proceeds
    - Behavior-preserving: the asyncio.timeout() mechanism is unchanged; only the value source moves from the module constant to the config-resolved local
  </behavior>
  <action>
    RED (holder-identity migration): Update test_shutdown_timeout_logs_holder_identity
    (test_scheduler.py:665) to remove the `monkeypatch.setattr(sched, "_SHUTDOWN_DRAIN_TIMEOUT", 0.1)`
    line (test_scheduler.py:678) and instead force the fast drain through config: construct the
    settings via make_settings(general=GeneralConfig(shutdown_drain_timeout=1.0), radarr_enabled=False,
    sonarr_enabled=False) so the shutdown block resolves drain=1.0 from app.state.settings. USE 1.0,
    NOT 0.1 — 0.1 raises pydantic.ValidationError under 75-01's Field(ge=1.0). The test still times
    out and logs the holder identity because the lock is acquired and never released (the drain always
    fires); 1.0 just means a ~1.0s wait. Add `GeneralConfig` to the
    `from triggarr.models.config import ...` line at the top of the test file (currently
    `InstanceConfig, Settings`; add `GeneralConfig` — ruff I sorts it first).
    RED (NEW discriminating test): Add a new async test `test_shutdown_drain_reads_configured_value`
    immediately after the holder-identity test. Build settings via
    make_settings(general=GeneralConfig(shutdown_drain_timeout=7.0), radarr_enabled=False,
    sonarr_enabled=False), build the lifespan/app exactly like the holder-identity test, but do NOT
    acquire the lock and do NOT inject a holder. Capture loguru with the SAME idiom
    (`sink = io.StringIO(); sink_id = logger.add(sink, format="{level} | {message}", level="INFO")`,
    `logger.remove(sink_id)` in finally). After `async with lifespan_fn(app):` exits, assert the
    captured output contains the substring `"timeout=7.0s"` (the no-holder drain-entry INFO line) — e.g.
    `assert "timeout=7.0s" in output` with a message showing `output!r`. Use 7.0 (distinctive: not the
    60.0 default, not the 1.0 minimum). Run BOTH tests; both must FAIL because the shutdown block still
    reads the module constant (60.0): the holder-identity test no longer times out in ~1s, and the
    discriminating test sees `timeout=60.0s` not `timeout=7.0s`.
    GREEN: In triggarr/search/scheduler.py, at the top of the shutdown-drain block — right after
    `scheduler.shutdown(wait=False)` (scheduler.py:598) and before the
    `holder = getattr(app.state, "search_lock_holder", None)` line (scheduler.py:605) — add one
    local: `drain = _read_shutdown_drain_timeout(app.state.settings.general.shutdown_drain_timeout)`
    with a brief comment noting env overrides, that settings is already in scope here (FLAG 2 —
    confirmed bound at scheduler.py:473), and that the helper guarantees a finite value so
    asyncio.timeout always gets a bounded argument. Then replace ALL five `_SHUTDOWN_DRAIN_TIMEOUT`
    references in the shutdown path with `drain`: the two `t=` log args (611, 618), the
    `asyncio.timeout(...)` (634), and the two `timeout=` warning args (648, 656). Do NOT change the
    asyncio.timeout mechanism or any other logic. Run the migrated holder-identity test, the new
    discriminating test, and test_shutdown_drains_search_lock; all three must pass (the discriminating
    test now logs `timeout=7.0s` and returns instantly; the holder-identity test times out in ~1s and
    logs the holder).
    Add a RED commit `test(75-03): migrate holder-identity (1.0) + add discriminating config-read drain test (7.0)`
    and a GREEN commit `feat(75-03): resolve shutdown drain from config at shutdown time`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "shutdown" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/search/scheduler.py shutdown block defines `drain = _read_shutdown_drain_timeout(app.state.settings.general.shutdown_drain_timeout)` near the top (after scheduler.shutdown, before the holder read)
    - triggarr/search/scheduler.py shutdown block contains `asyncio.timeout(drain)` and no longer references `_SHUTDOWN_DRAIN_TIMEOUT` anywhere in the shutdown path (`grep -n "_SHUTDOWN_DRAIN_TIMEOUT" triggarr/search/scheduler.py` shows only the helper docstring/comment and the retained module-constant definition — not the drain block)
    - tests/test_scheduler.py test_shutdown_timeout_logs_holder_identity forces the fast drain via make_settings(general=GeneralConfig(shutdown_drain_timeout=1.0)), NOT via monkeypatching _SHUTDOWN_DRAIN_TIMEOUT and NOT with any value below 1.0
    - tests/test_scheduler.py contains test_shutdown_drain_reads_configured_value that sets shutdown_drain_timeout=7.0, runs with the lock UNHELD (no acquire, no injected holder), and asserts the captured loguru output contains `timeout=7.0s` — discriminating config-read from config-ignored, and completing without a real timeout wait
    - `GeneralConfig` is imported in tests/test_scheduler.py
    - test_shutdown_drains_search_lock still passes
    - `uv run pytest tests/test_scheduler.py -k shutdown -x -q` exits 0
  </acceptance_criteria>
  <done>The shutdown block resolves the drain from config (env-override, finite-guaranteed) at shutdown time, no longer reads the import-time constant in the drain path; the migrated holder-identity test (config-driven via the valid 1.0 minimum) proves holder logging, the new discriminating test (7.0, lock unheld) proves the CONFIGURED value reaches the block by asserting the logged `timeout=7.0s` and runs fast, and the existing drain test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var / config → drain resolution | operator-supplied TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT and the persisted config value cross into the shutdown timeout |
| app.state.settings → shutdown block | the typed config is read at shutdown to bound the drain wait |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07 | Denial of Service | drain resolution (both sources) | mitigate | `max(value, 1.0)` clamps BOTH the configured value and the env override; a 0/negative value cannot disable the drain — the in-flight search cycle still gets a bounded grace window before SIGKILL |
| T-75-07b | Denial of Service | non-finite drain value | mitigate | The `math.isfinite` guard rejects a non-finite resolved value (env nan/inf/-inf, or a non-finite configured value) and falls back to a finite default. Without it: `asyncio.timeout(inf)` never bounds the drain (waits until SIGKILL — drain effectively disabled), and `asyncio.timeout(nan)` can time out immediately (drain effectively disabled). `max(nan,1.0)==nan` / `max(inf,1.0)==inf` means the clamp alone does NOT neutralize them — closing the "typo disabling the drain" vector end-to-end |
| T-75-08 | Denial of Service | malformed env var at shutdown | mitigate | A non-float TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT falls back to the configured default (caught via (ValueError, TypeError)) instead of crashing the shutdown path |
| T-75-09 | Tampering | app.state.settings read at shutdown | accept | FLAG 2 confirmed: app.state.settings is bound at lifespan startup (scheduler.py:473) before shutdown; no untrusted mutation path between bind and read; closure `settings` var is an equivalent in-scope read. The configured value is additionally finite-bounded at the model (75-01 allow_inf_nan=False) |
| T-75-10 | Information Disclosure | shutdown drain logging | accept | The drain logs the timeout value and the holder job_id/elapsed only — no token/hash/secret/PII; SecretStr discipline unaffected (no secret fields in scope). The new discriminating test asserts on the logged numeric timeout value (7.0s) only |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; reuses os.environ, math (stdlib), asyncio.timeout (stdlib), io.StringIO + loguru (already used by the existing holder-identity test), and the existing scheduler shutdown path |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -x -q` exits 0 (precedence + non-finite + migrated holder-identity + new discriminating config-read + existing drain tests all green)
- `uv run pytest tests/ -x -q` exits 0 (full suite stays green — behavior-preserving refactor, the 1044 baseline holds plus the new tests)
- `uv run ruff check triggarr/ tests/` exits 0 (confirm `import math` sorts among stdlib imports; confirm `GeneralConfig` sorts in the test config import)
- `grep -n "_SHUTDOWN_DRAIN_TIMEOUT" triggarr/search/scheduler.py` shows references only in the helper/comment/module-constant definition — NOT in the shutdown-drain block (the block uses the `drain` local)
- The discriminating test completes in well under a second (lock unheld → no real timeout wait), proving the assertion is on the LOGGED value, not on a timeout firing
</verification>

<success_criteria>
- The configured drain timeout is the default at shutdown; TRIGGARR_SHUTDOWN_DRAIN_TIMEOUT overrides it; the >= 1.0 clamp applies to both sources (CFG-04 precedence)
- The helper is finite-guaranteed: a non-finite env or configured value falls back to a finite default; asyncio.timeout never receives nan/inf
- The shutdown path reads from app.state.settings (FLAG 2 confirmed safe), not import-time module state
- The breaking holder-identity test is migrated to drive the drain via config using the VALID 1.0 minimum (FLAG 1) — internally consistent with 75-01's Field(ge=1.0), no value below 1.0 fed to GeneralConfig; the module constant is retained so the two existing constant tests stay green
- A SEPARATE discriminating test (FINDING A) proves the CONFIGURED value reaches the shutdown block by asserting the LOGGED drain equals a distinctive 7.0 (not the 60.0 default), with the lock unheld so it runs fast — the test would fail if the block read a hardcoded default, so it genuinely distinguishes config-read from config-ignored
- The full existing suite stays green (behavior-preserving); ruff clean
</success_criteria>

<output>
Create `.planning/phases/75-drain-timeout-config-parity-deferred-record-correction/75-03-SUMMARY.md` when done
</output>
</content>
