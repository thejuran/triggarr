---
phase: 56-first-run-setup-login
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/templates/base-auth.html
  - triggarr/templates/login.html
  - triggarr/templates/setup.html
  - triggarr/templates/base.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - LOGIN-06

must_haves:
  truths:
    - "Login page renders as a centered card on dark background with no nav bar"
    - "Setup page renders as a centered card with form fields for username, password, confirm password"
    - "Setup success state shows API key in monospace with copy button and continue button"
    - "Logout link appears in nav bar only when auth is active (Forms or Basic mode)"
    - "All templates use request.url_for('static', path='...') for asset URLs"
  artifacts:
    - path: "triggarr/templates/base-auth.html"
      provides: "Standalone auth layout with centered flexbox, no nav bar"
      contains: "flex items-center justify-center"
    - path: "triggarr/templates/login.html"
      provides: "Login form extending base-auth.html"
      contains: "{% extends \"base-auth.html\" %}"
    - path: "triggarr/templates/setup.html"
      provides: "Setup form and success state extending base-auth.html"
      contains: "{% extends \"base-auth.html\" %}"
    - path: "triggarr/templates/base.html"
      provides: "Conditional logout form in nav bar"
      contains: "auth_active"
  key_links:
    - from: "triggarr/templates/login.html"
      to: "triggarr/templates/base-auth.html"
      via: "Jinja2 extends"
      pattern: "extends.*base-auth"
    - from: "triggarr/templates/setup.html"
      to: "triggarr/templates/base-auth.html"
      via: "Jinja2 extends"
      pattern: "extends.*base-auth"
---

<objective>
Create the Jinja2 templates for login, setup, and setup-success pages, plus the nav bar logout modification. All visual design follows the 56-UI-SPEC.md contract and will be generated by AIDesigner MCP then ported pixel-exact.

Purpose: The route handlers (Plan 03) need templates to render. These templates define the user-facing experience for first-run setup and login. The nav bar logout link completes the session lifecycle UI.

Output: base-auth.html, login.html, setup.html templates; modified base.html with conditional logout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-first-run-setup-login/56-CONTEXT.md
@.planning/phases/56-first-run-setup-login/56-UI-SPEC.md
@.planning/phases/56-first-run-setup-login/56-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs from the existing codebase -->

From triggarr/templates/base.html (head block pattern, lines 1-14):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}Triggarr{% endblock %}</title>
  <link rel="icon" type="image/x-icon" href="{{ request.url_for('static', path='favicon.ico') }}">
  <link rel="icon" type="image/png" sizes="32x32" href="{{ request.url_for('static', path='favicon-32x32.png') }}">
  <link rel="icon" type="image/png" sizes="16x16" href="{{ request.url_for('static', path='favicon-16x16.png') }}">
  <link rel="apple-touch-icon" sizes="180x180" href="{{ request.url_for('static', path='apple-touch-icon.png') }}">
  <link rel="manifest" href="{{ request.url_for('static', path='site.webmanifest') }}">
  <link rel="stylesheet" href="{{ request.url_for('static', path='css/output.css') }}">
</head>
```

From triggarr/templates/base.html (nav items pattern, lines 38-51):
```html
<div class="flex items-center gap-6 text-sm">
    <a href="{{ dashboard_url }}" class="...">Dashboard</a>
    <a href="{{ history_url }}" class="...">History</a>
    <a href="{{ settings_url }}" class="...">Settings</a>
</div>
```

From triggarr/web/routes.py (Jinja2 globals pattern, lines 53-58):
```python
templates.env.globals["triggarr_version"] = get_display_version()
update_info: dict = {}
templates.env.globals["update_info"] = update_info
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base-auth.html and auth page templates</name>
  <files>triggarr/templates/base-auth.html, triggarr/templates/login.html, triggarr/templates/setup.html</files>
  <read_first>
    - triggarr/templates/base.html (head block pattern for favicon, CSS, meta tags -- replicate exactly for base-auth.html)
    - .planning/phases/56-first-run-setup-login/56-UI-SPEC.md (full visual contract: colors, spacing, typography, element inventory, copywriting, interaction contract, accessibility)
    - .planning/phases/56-first-run-setup-login/56-CONTEXT.md (D-01 through D-08: setup flow, copy button, password validation, login errors, template structure)
    - .planning/phases/56-first-run-setup-login/56-PATTERNS.md (base-auth.html pattern assignment, static asset URL pattern)
    - triggarr/static/css/input.css (existing Tailwind custom tokens: triggarr-bg, triggarr-card, triggarr-border, triggarr-green, triggarr-muted, triggarr-text, triggarr-card-elevated, triggarr-green-dark)
  </read_first>
  <action>
    **Use AIDesigner MCP to generate the HTML artifacts** per D-08. The AIDesigner output is the hard visual spec. Then port into Jinja2 templates.

    If AIDesigner MCP is unavailable, implement directly from the 56-UI-SPEC.md contract.

    **base-auth.html** -- Standalone minimal auth layout:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>{% block title %}Triggarr{% endblock %}</title>
      <!-- Same favicon links as base.html using request.url_for('static', path='...') -->
      <link rel="icon" type="image/x-icon" href="{{ request.url_for('static', path='favicon.ico') }}">
      <link rel="icon" type="image/png" sizes="32x32" href="{{ request.url_for('static', path='favicon-32x32.png') }}">
      <link rel="icon" type="image/png" sizes="16x16" href="{{ request.url_for('static', path='favicon-16x16.png') }}">
      <link rel="apple-touch-icon" sizes="180x180" href="{{ request.url_for('static', path='apple-touch-icon.png') }}">
      <link rel="manifest" href="{{ request.url_for('static', path='site.webmanifest') }}">
      <link rel="stylesheet" href="{{ request.url_for('static', path='css/output.css') }}">
      <!-- NO htmx script -- not needed for auth pages -->
    </head>
    <body class="bg-triggarr-bg text-triggarr-text min-h-screen flex items-center justify-center">
      {% block content %}{% endblock %}
    </body>
    </html>
    ```
    Key: No nav bar, no sidebar, no max-w-7xl container. Body uses flexbox centering per D-07.

    **login.html** -- Extends base-auth.html:
    - `{% block title %}Login - Triggarr{% endblock %}`
    - Card: max-w-[420px] w-full, bg-triggarr-card, border border-triggarr-border, rounded-lg, p-6 (24px)
    - Brand header: "Triggarr" in text-triggarr-green text-xl font-medium, centered
    - Page heading: "Sign In" in text-xl font-medium text-triggarr-text, centered, mt-4
    - Error area: `{% if error %}` block with red text (#ef4444 = text-red-500), text-sm, centered, mt-4, with `aria-live="polite"`
    - Form: `method="post" action="{{ request.url_for('login_post') }}"`, mt-6
    - Hidden input: `<input type="hidden" name="next" value="{{ next_url | default('', true) }}">`
    - Username field: Label "Username" (text-sm font-medium text-triggarr-muted), input with name="username" value="{{ username | default('', true) }}", autocomplete="username", autofocus (or autofocus on password if username pre-filled)
    - Password field: Label "Password", type=password, name="password", autocomplete="current-password", always empty on re-render
    - CTA button: "Sign In", type=submit, full-width, bg-triggarr-green hover:bg-triggarr-green-dark, text-white text-sm font-medium, rounded, py-2
    - Input styling: bg-triggarr-bg border border-triggarr-border rounded px-3 py-2 text-sm text-triggarr-text placeholder-triggarr-muted w-full, focus:ring-2 focus:ring-triggarr-green focus:outline-none
    - Field spacing: space-y-4 on the form fields container

    **setup.html** -- Extends base-auth.html, two states:
    - `{% block title %}Setup - Triggarr{% endblock %}`
    - Same card styling as login.html
    - Brand header: "Triggarr" in text-triggarr-green text-xl font-medium, centered

    **Setup form state** (`{% if not setup_complete %}`):
    - Heading: "Welcome to Triggarr" -- text-xl font-medium, centered, mt-4
    - Subheading: "Create your account to get started" -- text-sm text-triggarr-muted, centered, mt-1
    - Form: `method="post" action="{{ request.url_for('setup_post') }}"`, mt-6
    - Username field: Label "Username", name="username", value="{{ username | default('admin', true) }}", autocomplete="username"
    - Password field: Label "Password", type=password, name="password", autocomplete="new-password"
    - Confirm password field: Label "Confirm Password", type=password, name="confirm_password", autocomplete="new-password"
    - Inline errors: `{% if errors.username %}` red text below username, `{% if errors.password %}` below password, `{% if errors.confirm_password %}` below confirm. Text-sm text-red-500 mt-1.
    - CTA button: "Create Account", same styling as login CTA
    - All inputs same styling as login.html

    **Setup success state** (`{% if setup_complete %}`):
    - Heading: "Account Created" -- text-xl font-medium, centered, mt-4
    - Instruction: "Save your API key -- you will not be able to see it again." -- text-sm text-triggarr-muted, centered, mt-1
    - API key display: `<code id="api-key-display" class="block w-full bg-triggarr-bg border border-triggarr-border rounded px-3 py-2 text-sm text-triggarr-text select-all mt-6">{{ api_key }}</code>`
    - Copy button: `<button type="button" onclick="copyApiKey(this)" class="mt-2 bg-triggarr-card-elevated border border-triggarr-border rounded px-3 py-1 text-sm text-triggarr-text hover:text-white transition-colors cursor-pointer">Copy</button>`
    - Continue button: `<a href="{{ request.url_for('dashboard') }}" class="block w-full text-center bg-triggarr-green hover:bg-triggarr-green-dark text-white text-sm font-medium rounded py-2 mt-6 transition-colors">Continue to Dashboard</a>`

    - Inline `<script>` for copy functionality per D-02:
    ```javascript
    function copyApiKey(btn) {
        var text = document.getElementById('api-key-display').textContent;
        if (navigator.clipboard && navigator.clipboard.writeText) {
            navigator.clipboard.writeText(text).then(function() {
                btn.textContent = 'Copied!';
                setTimeout(function() { btn.textContent = 'Copy'; }, 2000);
            });
        } else {
            var range = document.createRange();
            range.selectNodeContents(document.getElementById('api-key-display'));
            window.getSelection().removeAllRanges();
            window.getSelection().addRange(range);
            btn.textContent = 'Copied!';
            setTimeout(function() { btn.textContent = 'Copy'; }, 2000);
        }
    }
    ```

    All `<label>` elements must have matching `for`/`id` attributes. Error messages use `aria-live="polite"`.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && python -c "
import jinja2, sys
env = jinja2.Environment(loader=jinja2.FileSystemLoader('triggarr/templates'), autoescape=True)
for name in ['base-auth.html', 'login.html', 'setup.html']:
    t = env.get_template(name)
    print(f'{name}: OK')
print('All templates parse successfully')
" && uv run ruff check triggarr/templates/ 2>/dev/null; echo "Template validation complete"</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/templates/base-auth.html exists and contains `flex items-center justify-center` in body class
    - triggarr/templates/base-auth.html contains `request.url_for('static', path='favicon.ico')` (root_path-aware)
    - triggarr/templates/base-auth.html does NOT contain `htmx` or `<nav`
    - triggarr/templates/login.html contains `{% extends "base-auth.html" %}` and `{% block title %}Login - Triggarr{% endblock %}`
    - triggarr/templates/login.html contains `name="username"`, `name="password"`, `type="hidden" name="next"`, `autocomplete="current-password"`
    - triggarr/templates/login.html contains `aria-live="polite"` for error message area
    - triggarr/templates/setup.html contains `{% extends "base-auth.html" %}` and `{% block title %}Setup - Triggarr{% endblock %}`
    - triggarr/templates/setup.html contains `{% if not setup_complete %}` and `{% if setup_complete %}`
    - triggarr/templates/setup.html contains `id="api-key-display"` and `copyApiKey` function
    - triggarr/templates/setup.html contains `name="confirm_password"` and `autocomplete="new-password"`
    - triggarr/templates/setup.html contains `navigator.clipboard.writeText` (D-02 copy functionality)
    - All three templates parse without Jinja2 syntax errors
  </acceptance_criteria>
  <done>Three Jinja2 templates created: base-auth.html (minimal centered layout), login.html (sign-in form with error display and ?next= hidden input), setup.html (credential creation form + success state with API key display and copy button). All follow UI-SPEC color, spacing, typography, and accessibility contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional logout link to nav bar and auth_active Jinja2 global</name>
  <files>triggarr/templates/base.html, triggarr/web/routes.py</files>
  <read_first>
    - triggarr/templates/base.html (full file -- nav bar structure at lines 38-51, conditional pattern at lines 25-32)
    - triggarr/web/routes.py (Jinja2 globals pattern at lines 53-58)
    - .planning/phases/56-first-run-setup-login/56-CONTEXT.md (D-09, D-10, D-11, D-12)
    - .planning/phases/56-first-run-setup-login/56-UI-SPEC.md (nav bar modification section)
  </read_first>
  <action>
    **1. Add `auth_active` Jinja2 global in routes.py** (after line 58, near the other globals):

    The global needs to be a callable (or use a dict reference) so it reads live state at render time, not a static value. Use a dict reference pattern like `update_info`:

    ```python
    # Auth state for conditional nav rendering (logout link visibility per D-11).
    # Uses a mutable dict so template reads live state without per-route wiring.
    auth_state: dict = {}
    templates.env.globals["auth_state"] = auth_state
    ```

    Then in the app lifespan or startup, the `auth_state` dict gets populated. BUT since routes.py globals are set at import time and `app.state.settings` isn't available yet, use a simpler approach: make `auth_active` a function that templates can call.

    Actually, the cleanest approach matching the existing pattern: add a context processor. But FastAPI/Jinja2Templates doesn't have built-in context processors. The simplest approach that matches the project's existing pattern (update_info is a shared mutable dict assigned in lifespan):

    Add near line 58 in routes.py:
    ```python
    # Shared mutable dict for auth display state. The app lifespan populates it
    # from settings. Templates use auth_state.active for conditional logout.
    auth_state: dict = {"active": False}
    templates.env.globals["auth_state"] = auth_state
    ```

    Then add a helper function that gets called after settings load/reload to update auth_state. Add near the globals:
    ```python
    def _update_auth_state(settings: SettingsModel) -> None:
        """Update the shared auth_state dict for template rendering."""
        method = settings.auth.method
        auth_state["active"] = method in ("Forms", "Basic")
    ```

    The route handlers for setup_post and any future settings save will call `_update_auth_state(request.app.state.settings)` after reloading settings. The existing `save_settings` handler should also call it (but that's a one-line addition to an existing function).

    Also add a call in the dashboard route (or better, as a startup hook). For now, the simplest approach: call `_update_auth_state` at the start of each page render that uses base.html. But that's invasive. Better: call it once on app startup via the lifespan, and again after any config change (setup_post, save_settings).

    For the initial template rendering, add to the existing `startup` event or router `on_startup`. Since this project uses lifespan context managers, add the call in `triggarr/web/routes.py` by hooking into the first dashboard request. Actually, the simplest non-invasive approach: make `auth_state` a dict that templates check, and populate it on first request via a simple check. OR even simpler -- since the template just needs to know if auth is Forms/Basic, and the middleware already reads settings, just pass it as a template context variable in every route that uses base.html.

    **Simplest correct approach:** Use a Jinja2 global function (callable):
    ```python
    def _is_auth_active() -> bool:
        """Check if auth requires logout link (Forms or Basic mode)."""
        # This is called during template rendering. If app.state isn't set yet, return False.
        # The function is replaced with a proper one during app startup.
        return False

    templates.env.globals["is_auth_active"] = _is_auth_active
    ```

    Then in the app startup/lifespan (which is in `triggarr/__main__.py` or the lifespan function), replace:
    ```python
    from triggarr.web.routes import templates
    def _auth_active_check() -> bool:
        return app.state.settings.auth.method in ("Forms", "Basic")
    templates.env.globals["is_auth_active"] = _auth_active_check
    ```

    **BUT** this requires modifying `__main__.py` which is not in this plan's scope. So use the mutable dict pattern instead, which the setup_post and login handlers will update:

    In routes.py after line 58:
    ```python
    auth_state: dict = {"active": False, "method": "Forms"}
    templates.env.globals["auth_state"] = auth_state
    ```

    In base.html, use `{% if auth_state.active %}`. The dict will be updated by:
    - Plan 03's setup_post handler after writing config
    - Plan 03's app startup hook (one line in the dashboard route or a lazy-init pattern)

    For now in Plan 02, just wire the template conditional. Plan 03 will handle populating `auth_state` when routes load settings.

    **2. Add conditional logout to base.html nav bar** (inside the nav items div, after the Settings link, around line 50):

    ```html
    {% if auth_state.active %}
    <span class="text-triggarr-border">|</span>
    <form method="post" action="{{ request.url_for('logout') }}" class="inline">
      <button type="submit"
              class="text-triggarr-muted hover:text-white text-sm transition-colors cursor-pointer">
        Logout
      </button>
    </form>
    {% endif %}
    ```

    This uses POST form submission per D-10, is only visible when auth is active per D-11, and uses the same text styling as nav links. The `|` separator provides visual distinction from navigation links.

    **Import note for routes.py:** Add `from triggarr.models.config import Settings as SettingsModel` if not already imported (it's imported as `SettingsModel` in the existing save_settings handler area). Check existing imports first.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && python -c "
import jinja2
env = jinja2.Environment(loader=jinja2.FileSystemLoader('triggarr/templates'), autoescape=True)
t = env.get_template('base.html')
print('base.html parses OK')
" && grep -c 'auth_state' triggarr/templates/base.html && grep -c 'auth_state' triggarr/web/routes.py</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/templates/base.html contains `{% if auth_state.active %}` conditional block
    - triggarr/templates/base.html contains `method="post"` inside the auth_state conditional (POST form per D-10)
    - triggarr/templates/base.html contains `Logout` button text
    - triggarr/templates/base.html contains `request.url_for('logout')` (root_path-aware URL)
    - triggarr/web/routes.py contains `auth_state: dict = ` and `templates.env.globals["auth_state"]`
    - base.html parses without Jinja2 syntax errors
  </acceptance_criteria>
  <done>Nav bar shows conditional logout POST form button when auth_state.active is True. auth_state Jinja2 global dict registered in routes.py for Plan 03's route handlers to populate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logout form submission | POST to /logout, protected by existing OriginCheckMiddleware against CSRF |
| Template rendering | Jinja2 autoescape=True prevents XSS in template variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | Tampering | Logout via GET | mitigate | Use POST form (D-10), OriginCheckMiddleware blocks cross-origin POST. Button styled as link but submits form. |
| T-56-04 | Information Disclosure | API key in setup success | mitigate | API key displayed once, user instructed to save. Copy button uses clipboard API (no server logging). Session cookie httpOnly prevents JS theft. |
| T-56-05 | Spoofing | XSS in error messages or username | mitigate | Jinja2 autoescape=True on all templates. Username and error values auto-escaped. |
</threat_model>

<verification>
- All four template files parse without Jinja2 syntax errors
- base-auth.html has flexbox centering, no nav bar, no htmx
- login.html has form fields, error area, ?next= hidden input, accessibility attributes
- setup.html has both form and success states, API key display, copy JS
- base.html has conditional logout form using POST method
- auth_state global registered in routes.py
</verification>

<success_criteria>
- Templates created matching 56-UI-SPEC.md visual contract
- Login page: centered card, sign-in form, error display, ?next= support
- Setup page: credential creation form + success view with API key and copy button
- Nav bar: conditional POST-based logout link visible only for Forms/Basic auth
- All templates use request.url_for() for URLs and have proper accessibility attributes
</success_criteria>

<output>
After completion, create `.planning/phases/56-first-run-setup-login/56-02-SUMMARY.md`
</output>
