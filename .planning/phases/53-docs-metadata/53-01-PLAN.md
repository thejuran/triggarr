---
phase: 53-docs-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "User reading PROJECT.md Key Decisions finds a new entry explaining the rationale for the sticky Recent Activity rail and expandable Application Log"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "New Key Decisions entry for rail + expandable log"
      contains: "Recent Activity rail"
  key_links: []
---

<objective>
Add an architecture decision record to PROJECT.md Key Decisions for the v2.5 sticky Recent Activity rail and expandable Application Log terminal pane.

Purpose: Captures the design rationale so future contributors understand why these UI patterns were chosen over alternatives.
Output: Updated PROJECT.md with new Key Decisions table row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rail + expandable log Key Decisions entry to PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>
Read .planning/PROJECT.md and locate the Key Decisions table (line ~158). Append a new row to the table with these exact values:

| Decision | Rationale | Outcome |
| Sticky Recent Activity rail + expandable terminal log (vanilla JS, no framework) | Rail reuses existing search_log data via htmx polling -- no new backend endpoint. Sticky positioning keeps activity visible during scroll. Expandable log uses fixed bottom-pinned pane with vanilla JS toggle -- avoids Alpine/React dependency. Data reuse from search_log means zero schema changes. Rail hidden below xl: breakpoint so narrow screens use History page instead. | Pending -- v2.5 |

The entry must follow the existing table format exactly (pipe-delimited, single line per row). Place it after the last existing row (the HX-Request header CSRF row).

Per DOCS-03: the entry must explain (1) sticky positioning rationale, (2) data reuse from existing search_log, (3) vanilla JS only -- no framework.
  </action>
  <verify>
    <automated>grep -c "Recent Activity rail" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md Key Decisions table contains a new row mentioning "Recent Activity rail"
    - The entry explains sticky positioning, search_log data reuse, and vanilla JS (no framework)
    - The Outcome column shows "Pending -- v2.5" (since the milestone is not yet shipped)
    - Existing table rows are unchanged
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions table has one new row documenting the rail + expandable log architecture decision with rationale covering sticky positioning, data reuse, and vanilla JS approach</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan modifies only internal planning documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | I (Information Disclosure) | PROJECT.md | accept | Planning docs are internal; no secrets or PII involved |
</threat_model>

<verification>
- `grep "Recent Activity rail" .planning/PROJECT.md` returns a match
- `grep "search_log" .planning/PROJECT.md` returns a match in the new row
- `grep "vanilla JS" .planning/PROJECT.md` returns a match in the new row
</verification>

<success_criteria>
PROJECT.md Key Decisions contains the new rail + expandable log architecture entry with full rationale per DOCS-03.
</success_criteria>

<output>
After completion, create `.planning/phases/53-docs-metadata/53-01-SUMMARY.md`
</output>
