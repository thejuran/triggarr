---
phase: 64-data-safety-config-integrity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - triggarr/config.py
  - tests/test_config.py
autonomous: true
requirements:
  - SAFETY-04
tags:
  - python
  - config
  - atomic-write
  - error-handling

must_haves:
  truths:
    - "A non-FileNotFoundError OSError raised during temp file cleanup in _atomic_toml_write is logged via loguru.error before being suppressed"
    - "A FileNotFoundError raised during temp file cleanup remains silently suppressed and produces no log line"
    - "An OSError raised by os.replace() (or surrounding fsync calls) is logged with the config path then re-raised so the route handler sees the failure"
    - "Existing test test_atomic_toml_write_cleans_temp_on_failure continues to pass (TypeError still propagates, no temp files left behind)"
    - "Existing test test_save_settings_propagates_write_failure continues to pass (OSError from atomic write still produces a 500)"
  artifacts:
    - path: "triggarr/config.py"
      provides: "Hardened _atomic_toml_write except branches that distinguish FileNotFoundError from other OSError during cleanup, and log OSError raised by os.replace"
      contains: "logger.error"
    - path: "tests/test_config.py"
      provides: "Three new test cases asserting log emission and propagation invariants"
      contains: "test_atomic_toml_write_logs_cleanup_oserror"
  key_links:
    - from: "triggarr/config.py::_atomic_toml_write"
      to: "loguru.logger.error"
      via: "structured log call with path and exc kwargs"
      pattern: "logger\\.error\\("
    - from: "tests/test_config.py"
      to: "triggarr.config._atomic_toml_write"
      via: "patch + StringIO sink"
      pattern: "logger\\.add\\(sink"
---

<objective>
Patch `_atomic_toml_write` in `triggarr/config.py` so that OSError raised during the `os.replace()` / fsync step is logged before re-raise, and the cleanup `contextlib.suppress(OSError)` is replaced with a discriminating handler that keeps `FileNotFoundError` silent but logs any other `OSError` (PermissionError, ENOSPC, EROFS, etc.). Cover the new behavior with three unit tests using the loguru-sink capture pattern from `tests/test_startup.py:261-267`.

Purpose: Closes SAFETY-04. Today the bare `contextlib.suppress(OSError)` at `config.py:113-115` swallows `PermissionError` and other real-OS failures during temp cleanup with zero observability; a failed `os.replace()` re-raises but emits no log line from inside the writer. After this patch, every non-trivial OS failure during a config write produces at least one structured loguru line containing the config path.

Output: Modified `triggarr/config.py` (only the except / cleanup region of `_atomic_toml_write`), plus three new test functions in `tests/test_config.py`. No new imports in `config.py` (loguru already imported at line 14). New `import io` and `from loguru import logger` in `tests/test_config.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-data-safety-config-integrity/64-RESEARCH.md
@.planning/phases/64-data-safety-config-integrity/64-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From triggarr/config.py (lines 1-16, current imports — already present, do NOT re-add):
```python
import contextlib
import os
import shutil
import sys
import tempfile
import tomllib
from pathlib import Path

import tomli_w
from loguru import logger
```

From triggarr/config.py:92-119 (CURRENT state of _atomic_toml_write — this is the function to patch):
```python
def _atomic_toml_write(path: Path, data: dict) -> None:
    """Write *data* to *path* atomically using a temp file + os.replace (BUG-06)."""
    dir_fd = None
    fd, tmp_path = tempfile.mkstemp(dir=path.parent, suffix=".tmp")
    try:
        with os.fdopen(fd, "wb") as f:
            tomli_w.dump(data, f)
            f.flush()
            os.fsync(f.fileno())
        os.replace(tmp_path, path)
        # fsync the directory to ensure rename is durable
        dir_fd = os.open(path.parent, os.O_RDONLY)
        os.fsync(dir_fd)
    except Exception:
        with contextlib.suppress(OSError):
            os.unlink(tmp_path)
        raise
    finally:
        if dir_fd is not None:
            os.close(dir_fd)
```

From tests/test_config.py:670-680 (analog test — copy this `with patch(...), pytest.raises(...):` shape):
```python
def test_atomic_toml_write_cleans_temp_on_failure(tmp_path: Path) -> None:
    """_atomic_toml_write removes temp file when tomli_w.dump raises (BUG-06)."""
    config_file = tmp_path / "test.toml"
    with patch("triggarr.config.tomli_w.dump", side_effect=TypeError("bad data")), \
         pytest.raises(TypeError):
        _atomic_toml_write(config_file, {"key": "value"})
    remaining = list(tmp_path.glob("*.tmp"))
    assert remaining == [], f"Temp files should be cleaned up, found: {remaining}"
```

From tests/test_startup.py:261-267 (loguru-sink capture analog — copy this try/finally shape):
```python
sink = io.StringIO()
handler_id = logger.add(sink, format="{message}", level="WARNING")
try:
    result = await sonarr.detect_api_version()
finally:
    logger.remove(handler_id)
assert "version detection failed" in sink.getvalue()
```

Loguru kwargs idiom used in this file (config.py:230-233):
```python
logger.warning(
    "Default config written to {path} -- edit the config file and restart Triggarr",
    path=config_path,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add three failing tests for SAFETY-04 invariants in tests/test_config.py</name>
  <files>tests/test_config.py</files>
  <read_first>
    - tests/test_config.py (full file, lines 1-26 for imports and lines 653-726 for the existing atomic-write/TOML-error test region — the new tests must be appended in the same neighborhood and follow the same shape)
    - tests/test_startup.py:255-275 (loguru-sink capture pattern — copy verbatim)
    - .planning/phases/64-data-safety-config-integrity/64-RESEARCH.md (sections "Test Strategy" lines 643-678 and "Recommended Approach #2" lines 247-303)
    - .planning/phases/64-data-safety-config-integrity/64-PATTERNS.md (section "tests/test_config.py — TEST-02 + SAFETY-04 cases" lines 237-330)
    - CLAUDE.md (loguru-only logging, no print/logging module)
  </read_first>
  <behavior>
    - Test 1 (test_atomic_toml_write_logs_cleanup_oserror): patches `triggarr.config.tomli_w.dump` to raise `TypeError("fail")` AND patches `triggarr.config.os.unlink` to raise `PermissionError("denied")`. Calls `_atomic_toml_write(config_file, {"k": "v"})`. Expects:
        * `pytest.raises(TypeError)` — the original TypeError from dump still propagates
        * loguru sink (added at level ERROR) contains the substring `"Failed to clean up temp file"`
        * loguru sink contains the substring `"denied"`
    - Test 2 (test_atomic_toml_write_suppresses_filenotfound_silently): patches `triggarr.config.tomli_w.dump` to raise `TypeError("fail")` AND patches `triggarr.config.os.unlink` to raise `FileNotFoundError`. Calls `_atomic_toml_write(...)`. Expects:
        * `pytest.raises(TypeError)`
        * loguru sink does NOT contain `"Failed to clean up"`
    - Test 3 (test_atomic_toml_write_logs_os_replace_failure): patches `triggarr.config.os.replace` to raise `OSError("EROFS")`. Calls `_atomic_toml_write(...)`. Expects:
        * `pytest.raises(OSError)` — the OSError propagates
        * loguru sink contains `"Config write failed"`
        * loguru sink contains `"EROFS"`
        * loguru sink contains the config path string
  </behavior>
  <action>
Append three new test functions to `tests/test_config.py` (after the existing `test_atomic_toml_write_cleans_temp_on_failure` block near line 680). At the top of the file, add `import io` (sort alongside existing stdlib imports) and `from loguru import logger` (sort alongside existing third-party imports). Do NOT add `import sys` or `@pytest.mark.asyncio` — these tests are synchronous.

Test 1 — `test_atomic_toml_write_logs_cleanup_oserror(tmp_path: Path) -> None`:
  1. `config_file = tmp_path / "test.toml"`
  2. Open a loguru sink: `sink = io.StringIO(); handler_id = logger.add(sink, format="{message}", level="ERROR")`
  3. Wrap in `try: ... finally: logger.remove(handler_id)` to guarantee sink removal even on test failure
  4. Inside try: `with patch("triggarr.config.tomli_w.dump", side_effect=TypeError("bad data")), patch("triggarr.config.os.unlink", side_effect=PermissionError("denied")), pytest.raises(TypeError): _atomic_toml_write(config_file, {"k": "v"})`
  5. After the patch/raises block but still inside try: `text = sink.getvalue()` then `assert "Failed to clean up temp file" in text` and `assert "denied" in text`

Test 2 — `test_atomic_toml_write_suppresses_filenotfound_silently(tmp_path: Path) -> None`:
  1. Same scaffold as Test 1 (loguru sink at ERROR)
  2. `with patch("triggarr.config.tomli_w.dump", side_effect=TypeError("bad data")), patch("triggarr.config.os.unlink", side_effect=FileNotFoundError), pytest.raises(TypeError): _atomic_toml_write(config_file, {"k": "v"})`
  3. `assert "Failed to clean up" not in sink.getvalue()`

Test 3 — `test_atomic_toml_write_logs_os_replace_failure(tmp_path: Path) -> None`:
  1. Same scaffold (loguru sink at ERROR)
  2. `with patch("triggarr.config.os.replace", side_effect=OSError("EROFS")), pytest.raises(OSError): _atomic_toml_write(config_file, {"k": "v"})`
  3. `text = sink.getvalue()` then `assert "Config write failed" in text` and `assert "EROFS" in text` and `assert str(config_file) in text`

Run `uv run pytest tests/test_config.py::test_atomic_toml_write_logs_cleanup_oserror tests/test_config.py::test_atomic_toml_write_suppresses_filenotfound_silently tests/test_config.py::test_atomic_toml_write_logs_os_replace_failure -x` — ALL THREE MUST FAIL (RED phase) because the production code at config.py:113-115 still uses `contextlib.suppress(OSError)` and emits no log lines.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "test_atomic_toml_write_logs_cleanup_oserror or test_atomic_toml_write_suppresses_filenotfound_silently or test_atomic_toml_write_logs_os_replace_failure" -x 2>&1 | grep -E "FAILED|ERROR" | wc -l | awk '$1>=3{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_atomic_toml_write_logs_cleanup_oserror" tests/test_config.py` returns 1
    - `grep -c "def test_atomic_toml_write_suppresses_filenotfound_silently" tests/test_config.py` returns 1
    - `grep -c "def test_atomic_toml_write_logs_os_replace_failure" tests/test_config.py` returns 1
    - `grep -c "^import io" tests/test_config.py` returns 1
    - `grep -c "^from loguru import logger" tests/test_config.py` returns 1
    - `uv run pytest tests/test_config.py -k "test_atomic_toml_write_logs_cleanup_oserror" -x` exits non-zero (test fails — RED)
    - `uv run pytest tests/test_config.py -k "test_atomic_toml_write_logs_os_replace_failure" -x` exits non-zero (test fails — RED)
    - Existing tests still parseable: `uv run pytest tests/test_config.py --collect-only -q` exits 0
    - `uv run ruff check tests/test_config.py` exits 0
  </acceptance_criteria>
  <done>Three new tests exist in tests/test_config.py, are collected by pytest, and all three fail when run against the current (un-patched) _atomic_toml_write. Existing tests still collect (no syntax errors). Ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Patch _atomic_toml_write in triggarr/config.py to satisfy the three new tests</name>
  <files>triggarr/config.py</files>
  <read_first>
    - triggarr/config.py:1-16 (current imports — DO NOT add duplicates; contextlib, os, tempfile, tomli_w, logger are already imported)
    - triggarr/config.py:92-119 (current `_atomic_toml_write` implementation — replace ONLY the except/raise region; preserve the `finally: os.close(dir_fd)` block exactly)
    - triggarr/config.py:230-233 (existing `logger.{level}("... {path} ...", path=value)` kwargs idiom — copy verbatim style)
    - tests/test_config.py:670-680 (existing test_atomic_toml_write_cleans_temp_on_failure — the patch must keep this test passing)
    - tests/test_web.py:772-799 (test_save_settings_propagates_write_failure — must keep passing)
    - .planning/phases/64-data-safety-config-integrity/64-RESEARCH.md sections "Success #2: SAFETY-04" lines 247-303 (recommended patch shape)
    - .planning/phases/64-data-safety-config-integrity/64-PATTERNS.md section "_atomic_toml_write SAFETY-04 patch" lines 36-121 (exact patch shape)
    - CLAUDE.md (no bare except, specific exceptions, loguru-only)
  </read_first>
  <behavior>
    - After patch: TypeError from tomli_w.dump propagates and on cleanup PermissionError is logged via `logger.error` with "Failed to clean up temp file" and the exception text
    - FileNotFoundError during cleanup is silently suppressed — no log emission
    - OSError from os.replace is logged via `logger.error` with "Config write failed", the config path, and the exception, then propagated unchanged
    - All three Task 1 tests pass after the patch
    - Existing test_atomic_toml_write_cleans_temp_on_failure still passes (no .tmp files leak, TypeError still propagates)
    - Existing test_save_settings_propagates_write_failure (tests/test_web.py:772-799) still passes (route handler still sees the OSError)
  </behavior>
  <action>
Replace `triggarr/config.py:112-117` (the `except Exception: with contextlib.suppress(OSError): os.unlink(tmp_path); raise` block) with two explicit except branches. The `try:` body (lines 105-111) and the `finally:` body (lines 118-119) are UNCHANGED.

Resulting except-region structure:
  - `except OSError as exc:` — handles OSError raised by `os.replace`, `os.open`, or `os.fsync`. Emit `logger.error("Config write failed: {path} - {exc}", path=path, exc=exc)`. Then attempt cleanup of `tmp_path`:
      - `try: os.unlink(tmp_path)`
      - `except FileNotFoundError: pass` (expected — temp may already be moved or never existed)
      - `except OSError as cleanup_exc: logger.error("Failed to clean up temp file {tmp} during config write: {exc}", tmp=tmp_path, exc=cleanup_exc)`
    Then `raise` (re-raise the original OSError, not cleanup_exc).
  - `except Exception:` — handles non-OSError failures (TypeError from tomli_w.dump, ValueError, etc.). Do NOT add a log line here (per Patterns line 116-117 invariant: TypeError from tomli_w.dump is not logged here, only the cleanup OSError if it occurs). Same cleanup attempt as above:
      - `try: os.unlink(tmp_path)`
      - `except FileNotFoundError: pass`
      - `except OSError as cleanup_exc: logger.error("Failed to clean up temp file {tmp} during config write: {exc}", tmp=tmp_path, exc=cleanup_exc)`
    Then bare `raise`.

Remove the unused `import contextlib` from config.py:1 ONLY IF no other code in the file uses it — run `grep -n "contextlib" triggarr/config.py` after the patch to verify; if zero other hits, delete the import to satisfy ruff F401. Otherwise leave the import.

Do not change the function signature, the docstring, or any line outside the except region. Do not touch `generate_default_config` (config.py:187-211) — RESEARCH line 40 explicitly says leave the near-duplicate write path alone.

Run `uv run pytest tests/test_config.py -k "test_atomic_toml_write" -x` — all 4 tests (3 new + 1 existing) MUST pass. Then run `uv run pytest tests/test_web.py::test_save_settings_propagates_write_failure -x` — MUST pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "test_atomic_toml_write" -x && uv run pytest tests/test_web.py::test_save_settings_propagates_write_failure -x && uv run ruff check triggarr/config.py tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' triggarr/config.py | grep -c "contextlib.suppress(OSError)"` returns 0 (the broad suppress is gone)
    - `grep -c "except OSError as exc:" triggarr/config.py` is at least 1 (new branch added)
    - `grep -c "except FileNotFoundError:" triggarr/config.py` is at least 1 (FNF kept silent)
    - `grep -c "Config write failed" triggarr/config.py` returns 1
    - `grep -c "Failed to clean up temp file" triggarr/config.py` returns at least 1
    - `uv run pytest tests/test_config.py -k "test_atomic_toml_write" -x` exits 0 (all 4 tests pass)
    - `uv run pytest tests/test_config.py::test_atomic_toml_write_suppresses_filenotfound_silently -x` exits 0
    - `uv run pytest tests/test_web.py::test_save_settings_propagates_write_failure -x` exits 0
    - `uv run ruff check triggarr/config.py tests/test_config.py` exits 0
    - The `finally: if dir_fd is not None: os.close(dir_fd)` block is unchanged: `grep -A1 "if dir_fd is not None:" triggarr/config.py` shows `os.close(dir_fd)` immediately after
  </acceptance_criteria>
  <done>SAFETY-04 patch applied. All 4 atomic-write tests pass. Route-handler propagation test passes. Ruff clean. `contextlib.suppress(OSError)` gone from config.py (replaced with discriminating handlers). FileNotFoundError cleanup still silent. OSError from os.replace logged with path before re-raise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR + full suite): Run full test suite and ruff over the phase scope to catch regressions</name>
  <files>triggarr/config.py, tests/test_config.py</files>
  <read_first>
    - triggarr/config.py (full file — confirm no `contextlib` import remains if unused, no stray debug prints, no missing newlines)
    - tests/test_config.py (full file — confirm sink try/finally cleanup is correct and no sinks leak across tests)
    - .planning/phases/64-data-safety-config-integrity/64-RESEARCH.md "Pitfall 6" lines 492-495
  </read_first>
  <behavior>
    - All existing config and web tests still pass — no regression
    - Ruff is clean across triggarr/config.py and tests/test_config.py with the project ruleset (E, F, I, UP, B, SIM, line-length 120)
    - No leaked loguru sinks: re-running the full config test file twice gives identical output (idempotent)
  </behavior>
  <action>
This is the refactor pass. Inspect the patched `_atomic_toml_write` for clarity:
  1. If the two `except` branches share identical cleanup logic, consider extracting a small inner helper `_cleanup_temp(tmp_path: str) -> None` that does the `try/except FileNotFoundError/except OSError as cleanup_exc: logger.error(...)` dance. ONLY extract if it reduces line count without obscuring the control flow — otherwise leave the duplication (two short branches is clearer than one helper plus two calls for code this short).
  2. Verify no stale `import contextlib` remains if no other call site uses it. Run `grep -n "contextlib" triggarr/config.py` — if zero hits in the function bodies, remove the import line.
  3. Confirm every log line uses the `{name}` placeholder + kwarg style (no f-strings, no `%` formatting) per CLAUDE.md loguru convention.

Then run the broader regression sweep:
  - `uv run pytest tests/test_config.py -x` (entire file passes)
  - `uv run pytest tests/test_web.py -x -q` (no web regressions)
  - `uv run pytest tests/ -x -q` (full suite green — Triggarr's full suite is ~857 tests; allow ~2 minutes)
  - `uv run ruff check triggarr/ tests/` (entire repo clean)

If any pre-existing unrelated test fails, do NOT fix it in this plan — record in SUMMARY.md and continue. Only block on regressions in config-related or web-routes tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py tests/test_web.py -x -q && uv run ruff check triggarr/config.py tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py -x -q` exits 0
    - `uv run pytest tests/test_web.py -x -q` exits 0
    - `uv run ruff check triggarr/config.py tests/test_config.py` exits 0
    - `uv run pytest tests/ -x -q` exits 0 (full suite — note this in SUMMARY if it diverges from the 857 baseline)
    - `grep -v '^#' triggarr/config.py | grep -c "contextlib.suppress"` returns 0
    - If `grep -c "^import contextlib" triggarr/config.py` returns 1, then `grep -c "contextlib\\." triggarr/config.py` must be at least 2 (import + usage); otherwise the import was removed
  </acceptance_criteria>
  <done>Full test suite green. Ruff clean over the entire repo. Patched _atomic_toml_write is idiomatic loguru-style with discriminating exception handlers. No regressions in config or web tests. SAFETY-04 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> writer | `_atomic_toml_write` is called from request handlers; the filesystem can fail (ENOSPC, EROFS, EPERM) and that failure must be observable to the operator |
| writer -> caller | The route handler in `triggarr/web/routes.py` (and the scheduler in `triggarr/search/scheduler.py`) expects OSError to propagate so it can return 500 / abort the cycle; silent swallowing creates a phantom-success surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02 | Repudiation / Tampering | `triggarr/config.py::_atomic_toml_write` | mitigate | Replace `contextlib.suppress(OSError)` with `try/except FileNotFoundError: pass` + `except OSError as cleanup_exc: logger.error(...)`. Add `except OSError as exc: logger.error("Config write failed: {path} - {exc}", ...)` around `os.replace` so a real I/O failure is recorded with the path. Tested by `test_atomic_toml_write_logs_cleanup_oserror` and `test_atomic_toml_write_logs_os_replace_failure`. |
| T-64-INFO | Information Disclosure | log lines emitted by `_atomic_toml_write` | accept | Log lines include the config path (acceptable per CLAUDE.md — path is operator-relevant) and the OSError message. They do NOT include file contents — config dict is never passed to `logger.{level}`. SecretStr discipline preserved (no API keys touched in this function). |
| T-64-SC | Tampering | npm/pip/cargo installs | n/a | This phase adds zero packages. No package legitimacy gate required (verified by RESEARCH.md "Package Legitimacy Audit" section, line 220-222). |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -k "test_atomic_toml_write" -x` exits 0 (4 tests: 3 new + 1 existing)
- `uv run pytest tests/test_web.py::test_save_settings_propagates_write_failure -x` exits 0
- `uv run pytest tests/ -x -q` exits 0 (full suite — no regressions)
- `uv run ruff check triggarr/config.py tests/test_config.py` exits 0
- `grep -v '^#' triggarr/config.py | grep -c "contextlib.suppress"` returns 0 (broad suppress eliminated)
- `grep -c "Config write failed" triggarr/config.py` returns 1
- `grep -c "Failed to clean up temp file" triggarr/config.py` is at least 1
</verification>

<success_criteria>
SAFETY-04 closed:
- Non-FileNotFoundError OSError during temp cleanup is logged via loguru.error (proved by test_atomic_toml_write_logs_cleanup_oserror)
- FileNotFoundError during temp cleanup remains silently suppressed (proved by test_atomic_toml_write_suppresses_filenotfound_silently)
- OSError raised by os.replace is logged via loguru.error with config path before re-raise (proved by test_atomic_toml_write_logs_os_replace_failure)
- Existing tests for cleanup behavior (test_atomic_toml_write_cleans_temp_on_failure) and route propagation (test_save_settings_propagates_write_failure) still pass — no regression
</success_criteria>

<output>
Create `.planning/phases/64-data-safety-config-integrity/64-01-SUMMARY.md` when done:
- What was patched in config.py (lines + before/after of the except region)
- Which tests were added (names + what each proves)
- Confirmation that the full pytest suite is green (note any pre-existing unrelated failures)
- Confirmation that ruff check is clean over the changed files
- Whether `import contextlib` was removed or retained
</output>
