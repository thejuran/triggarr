---
phase: 65-scheduler-hardening-resilience
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler.py
  - triggarr/search/scheduler.py
autonomous: true
requirements: [SAFETY-02]
tags: [scheduler, exception-handling, apscheduler, observability]

must_haves:
  truths:
    - "RuntimeError (and other non-narrow-tuple exceptions) raised inside cycle_fn propagates out of make_search_job() to APScheduler"
    - "httpx.HTTPError (and the other three narrow-tuple types) raised inside cycle_fn is still caught inside make_search_job() and logged"
    - "When APScheduler receives a propagated unexpected exception, the EVENT_JOB_ERROR listener logs a single line containing the job_id, the exception type name, and a sanitized exception message"
    - "All existing scheduler tests except the inverted test_make_search_job_exception_swallowed continue to pass"
  artifacts:
    - path: "triggarr/search/scheduler.py"
      provides: "narrow exception tuple at the make_search_job outer except + EVENT_JOB_ERROR listener registered before scheduler.start()"
      contains: "except (httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error, OSError) as exc:"
    - path: "tests/test_scheduler.py"
      provides: "inverted propagation test + httperror swallow test + EVENT_JOB_ERROR listener test"
      contains:
        - "test_make_search_job_unexpected_exception_propagates"
        - "test_make_search_job_httperror_swallowed"
        - "test_event_job_error_listener_logs_unexpected_exception"
  key_links:
    - from: "triggarr/search/scheduler.py::make_search_job"
      to: "apscheduler EVENT_JOB_ERROR"
      via: "narrowed except tuple no longer catches RuntimeError → APScheduler invokes registered _on_job_error listener"
      pattern: "scheduler\\.add_listener\\(_on_job_error,\\s*EVENT_JOB_ERROR\\)"
    - from: "triggarr/search/scheduler.py::_on_job_error"
      to: "loguru.logger.error"
      via: "logger.error('Job {job} failed unexpectedly: {type}: {exc}', ...)"
      pattern: "failed unexpectedly:"
---

<objective>
Narrow the over-broad `except Exception` at the outer try in `make_search_job` to the
canonical four-type tuple `(httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error,
OSError)` used 22 times in `engine.py`, and wire an APScheduler `EVENT_JOB_ERROR`
listener so that exceptions which now propagate (RuntimeError, KeyError, MemoryError,
etc.) become operator-visible at ERROR level instead of disappearing into APScheduler's
stdlib-logging silence.

Purpose: SAFETY-02. Today, a code-bug exception (e.g. AttributeError from a refactor)
is silently swallowed by `except Exception`. After this plan, code bugs surface in the
log; transient external-service failures (the four narrow types) continue to be caught
gracefully so the scheduler keeps running.

Output:
- `triggarr/search/scheduler.py` — narrowed outer except tuple in `make_search_job`,
  module-level imports for `time` (used by 65-03 but harmless here) and
  `apscheduler.events.EVENT_JOB_ERROR`, plus `scheduler.add_listener(_on_job_error,
  EVENT_JOB_ERROR)` call before `scheduler.start()`.
- `tests/test_scheduler.py` — existing `test_make_search_job_exception_swallowed`
  renamed and inverted to `test_make_search_job_unexpected_exception_propagates`; new
  sibling `test_make_search_job_httperror_swallowed`; new
  `test_event_job_error_listener_logs_unexpected_exception` covering the listener.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-scheduler-hardening-resilience/65-RESEARCH.md
@.planning/phases/65-scheduler-hardening-resilience/65-PATTERNS.md
@.planning/phases/65-scheduler-hardening-resilience/65-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from triggarr/search/scheduler.py:1-50 (current state) -->

Current imports at scheduler.py top (lines 18-23):
- import aiosqlite
- import httpx
- import pydantic
- from apscheduler.schedulers.asyncio import AsyncIOScheduler
- from fastapi import FastAPI
- from loguru import logger

Existing module-level helper to reuse for safe exception logging
(from triggarr/search/engine.py:30-44):

```python
def _sanitize_exc(exc: Exception) -> str:
    if isinstance(exc, httpx.HTTPStatusError):
        return f"HTTP {exc.response.status_code}"
    if isinstance(exc, httpx.TimeoutException):
        return "request timeout"
    if isinstance(exc, httpx.HTTPError):
        return f"HTTP error: {type(exc).__name__}"
    if isinstance(exc, pydantic.ValidationError):
        return f"validation error ({exc.error_count()} issues)"
    return type(exc).__name__
```

Precedent for cross-module `_`-prefixed import inside scheduler.py
(see scheduler.py:232): `from triggarr.web.routes import _sync_auth_state`.

APScheduler 3.x event API (verified locally by REPL):

```python
from apscheduler.events import EVENT_JOB_ERROR, JobExecutionEvent
# EVENT_JOB_ERROR = 8192
# JobExecutionEvent(code, job_id, jobstore, scheduled_run_time, retval=None, exception=None, traceback=None)
```

Tests fixture (tests/conftest.py:29-77):
- `make_settings(...)` returns a Settings object usable as `app.state.settings`.

Test infra constraints (pyproject.toml:38): `asyncio_mode = "auto"` — `async def
test_...` functions need no `@pytest.mark.asyncio` decorator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Wave 0 tests — invert existing swallow test + add narrow-tuple swallow test + add EVENT_JOB_ERROR listener test</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - .planning/phases/65-scheduler-hardening-resilience/65-PATTERNS.md  (analog + patch shapes for all three tests)
    - .planning/phases/65-scheduler-hardening-resilience/65-RESEARCH.md  (sections §A and §B for SAFETY-02 rationale; Pitfall 6 for inversion necessity)
    - tests/test_scheduler.py  (read the entire file; the existing
      test_make_search_job_exception_swallowed at lines 35-55 is being inverted, not
      replaced from scratch; the loguru-sink pattern at lines 226-266 is reused)
    - triggarr/search/scheduler.py  (read make_search_job 43-131 and create_lifespan in
      full to know the lifespan signature for the listener test)
    - tests/conftest.py  (make_settings factory)
  </read_first>
  <behavior>
    Three tests written BEFORE the production change. They MUST fail against the
    current scheduler.py (which still has `except Exception` and no listener):

    1. `test_make_search_job_unexpected_exception_propagates` — REPLACES (renames +
       inverts) the existing `test_make_search_job_exception_swallowed`. Same
       fixture skeleton (FastAPI() app, AsyncMock radarr_clients, asyncio.Lock,
       triggarr_state, settings) plus initialize
       `app.state.search_failures = {}` and `app.state.search_lock_holder = None`
       (these will be set for real in plan 65-02 / 65-03; pre-initializing here
       prevents AttributeError when the wrapper writes to them later, and is harmless
       under the inverted assertion). Patches `triggarr.search.scheduler.run_radarr_cycle`
       to raise `RuntimeError("boom")`; asserts `pytest.raises(RuntimeError, match="boom")`
       when `await job()` is called.

    2. `test_make_search_job_httperror_swallowed` — SIBLING new test. Same fixture
       shape. Patches `run_radarr_cycle` to raise `httpx.ConnectError("connection
       refused")`. Asserts that `await job()` does NOT raise (httpx.ConnectError IS
       in the narrow tuple). Does not assert on log message here — that's
       SAFETY-03's concern in plan 65-02.

    3. `test_event_job_error_listener_logs_unexpected_exception` — NEW. Builds a
       real `AsyncIOScheduler`, registers the `_on_job_error` listener by
       importing it from `triggarr.search.scheduler` (the production code under
       test exposes it as a module-level function for testability), then synthesizes
       a `JobExecutionEvent(code=EVENT_JOB_ERROR, job_id="radarr_Default_search",
       jobstore="default", scheduled_run_time=datetime.now(UTC),
       exception=RuntimeError("synthetic boom"))` and dispatches it via
       `scheduler._dispatch_event(event)`. Captures loguru output with the standard
       sink-add/remove pattern (`format="{level} | {message}"`, `level="ERROR"`).
       Asserts the captured output contains
       `"radarr_Default_search"` AND `"RuntimeError"` AND `"synthetic boom"` AND
       starts with `"ERROR | "`. The listener function MUST be importable as
       `from triggarr.search.scheduler import _on_job_error` — if it isn't, the
       test fails at import (which is correct RED state).

    All three tests use `async def` (no decorator — asyncio_mode=auto). Patches use
    the `with patch(...)` context-manager form per repo convention. New imports
    needed at top of test_scheduler.py: `import io`, `import httpx`, `import pytest`,
    `from loguru import logger`, `from apscheduler.events import EVENT_JOB_ERROR,
    JobExecutionEvent`. Pre-existing imports already cover `asyncio`,
    `datetime.UTC/datetime`, `FastAPI`, `AsyncMock`, `MagicMock`, `patch`, `Path`,
    `make_settings`, `_default_state`.
  </behavior>
  <action>
    Edit tests/test_scheduler.py. Add new imports near existing ones (io, httpx,
    pytest, loguru.logger, apscheduler.events.{EVENT_JOB_ERROR, JobExecutionEvent}).
    RENAME `test_make_search_job_exception_swallowed` to
    `test_make_search_job_unexpected_exception_propagates` and invert its assertion
    to `pytest.raises(RuntimeError, match="boom")` wrapped around the `await job()`
    call. Inside its `app.state` setup, add `app.state.search_failures = {}` and
    `app.state.search_lock_holder = None`. Add `test_make_search_job_httperror_swallowed`
    immediately after it, mirroring the same fixture shape but using
    `side_effect=httpx.ConnectError("connection refused")` and asserting `await job()`
    completes without raising. Add `test_event_job_error_listener_logs_unexpected_exception`
    that imports `_on_job_error` from `triggarr.search.scheduler`, builds an
    `AsyncIOScheduler()`, calls `scheduler.add_listener(_on_job_error, EVENT_JOB_ERROR)`,
    then dispatches a synthetic `JobExecutionEvent` carrying a RuntimeError and asserts
    loguru sink output contains `"ERROR | "`, `"radarr_Default_search"`, `"RuntimeError"`,
    `"synthetic boom"`. The dispatch path uses `scheduler._dispatch_event(event)` (the
    public-but-underscored API APScheduler uses internally — verified available on
    AsyncIOScheduler). Do NOT start the scheduler; the listener fires synchronously
    on dispatch. Tests MUST fail at this point because (a) the import of `_on_job_error`
    will raise ImportError, (b) the RuntimeError test will see it swallowed, and
    (c) the httperror test will pass on the existing broad except but the assertion
    of "no raise" still holds — only (a) and (b) are required RED.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py::test_make_search_job_unexpected_exception_propagates tests/test_scheduler.py::test_make_search_job_httperror_swallowed tests/test_scheduler.py::test_event_job_error_listener_logs_unexpected_exception -x 2>&amp;1 | tee /tmp/65-01-red.log; grep -qE "(FAILED|ImportError|ERRORS)" /tmp/65-01-red.log</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `grep -c "test_make_search_job_unexpected_exception_propagates" tests/test_scheduler.py` returns at least 1.
    - File assertion: `grep -c "test_make_search_job_httperror_swallowed" tests/test_scheduler.py` returns at least 1.
    - File assertion: `grep -c "test_event_job_error_listener_logs_unexpected_exception" tests/test_scheduler.py` returns at least 1.
    - File assertion: `grep -c "test_make_search_job_exception_swallowed" tests/test_scheduler.py` returns 0 (renamed away).
    - File assertion: `grep -c "from apscheduler.events import" tests/test_scheduler.py` returns at least 1 (covers EVENT_JOB_ERROR + JobExecutionEvent).
    - Behavior: `uv run pytest tests/test_scheduler.py::test_make_search_job_unexpected_exception_propagates -x` MUST exit non-zero (RED).
    - Behavior: `uv run pytest tests/test_scheduler.py::test_event_job_error_listener_logs_unexpected_exception -x` MUST exit non-zero (RED — _on_job_error import fails).
    - Ruff clean on the test file: `uv run ruff check tests/test_scheduler.py` returns exit 0.
  </acceptance_criteria>
  <done>
    Three new/renamed tests exist in tests/test_scheduler.py. The RED state is
    confirmed: the propagation test and the listener test both fail against the
    current scheduler.py. Commit message: `test(65-01): add failing tests for SAFETY-02
    narrow exception tuple + EVENT_JOB_ERROR listener`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Narrow scheduler.py outer except tuple + register EVENT_JOB_ERROR listener</name>
  <files>triggarr/search/scheduler.py</files>
  <read_first>
    - .planning/phases/65-scheduler-hardening-resilience/65-PATTERNS.md  (PATTERNS sections "narrow exception (SAFETY-02)" and "EVENT_JOB_ERROR listener (SAFETY-02 sibling)" — copy the patch shapes exactly)
    - .planning/phases/65-scheduler-hardening-resilience/65-RESEARCH.md  (sections §A, §B, Implementation Approach Success #1, Pitfall 2 + Pitfall 7)
    - triggarr/search/scheduler.py  (read the whole file — the change touches imports near line 18-23, the except branch at lines 124-129, and a new listener registration near scheduler.start() around line 254-255)
    - triggarr/search/engine.py  (read lines 30-44 to verify `_sanitize_exc` signature before importing it; read one instance of the narrow-tuple usage around line 426 for confirmation of the exact tuple shape)
  </read_first>
  <action>
    Edit triggarr/search/scheduler.py. Add three new module-level imports at the top
    of the file alongside existing ones (placing in alphabetical group per ruff `I`):
      - `import time`  (used by RES-01 in plan 65-03; harmless here, avoids a churn
        commit later)
      - `from apscheduler.events import EVENT_JOB_ERROR`
      - `from triggarr.search.engine import _sanitize_exc`
    Replace the `except Exception as exc:` block (currently at scheduler.py:124-129)
    with `except (httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error, OSError) as exc:`.
    Inside that branch, KEEP the existing `logger.error("{app}: Unhandled error in
    search cycle -- {exc}", app=app_name.title(), exc=exc)` call for now — plan
    65-02 will replace its body with the failure-counter + escalation logic. The
    only SAFETY-02 change here is the exception tuple itself.
    Define a module-level function `_on_job_error(event)` (not a closure — must be
    importable from tests):
      ```python
      def _on_job_error(event) -> None:
          """APScheduler EVENT_JOB_ERROR listener (SAFETY-02).

          Fires for exceptions that escape the narrowed try/except in make_search_job
          — i.e., code bugs (RuntimeError, KeyError, AttributeError) and system
          failures (MemoryError). Sanitizes httpx exceptions to avoid leaking
          credentials embedded in request URLs; uses str() for other types because
          our non-httpx exceptions do not carry secrets.
          """
          exc = event.exception
          if isinstance(exc, (httpx.HTTPError, pydantic.ValidationError)):
              exc_repr = _sanitize_exc(exc)
          else:
              exc_repr = str(exc)
          logger.error(
              "Job {job} failed unexpectedly: {type}: {exc}",
              job=event.job_id,
              type=type(exc).__name__,
              exc=exc_repr,
          )
      ```
    Place `_on_job_error` at module scope above `create_lifespan`. Inside
    `create_lifespan`, AFTER all `add_job` calls and BEFORE `scheduler.start()`
    (around current line 254-255), add:
      `scheduler.add_listener(_on_job_error, EVENT_JOB_ERROR)`
    Do NOT modify the inner tracking-check `try/except` at scheduler.py:119 — it
    already uses the correct narrow tuple. Do NOT add `asyncio.CancelledError` to
    the tuple (it is `BaseException`, not `Exception`; shutdown drain relies on its
    propagation — RESEARCH Pitfall 7).
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py::test_make_search_job_unexpected_exception_propagates tests/test_scheduler.py::test_make_search_job_httperror_swallowed tests/test_scheduler.py::test_event_job_error_listener_logs_unexpected_exception -x &amp;&amp; uv run pytest tests/test_scheduler.py -x -q &amp;&amp; uv run ruff check triggarr/search/scheduler.py tests/test_scheduler.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "except (httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error, OSError) as exc:" triggarr/search/scheduler.py` returns at least 2 (one outer make_search_job branch added; one pre-existing inner tracking branch).
    - Source assertion: `grep -v '^#' triggarr/search/scheduler.py | grep -c "except Exception as exc:"` returns 0 (the broad outer catch in make_search_job is gone; verify no comment lines confound the count).
    - Source assertion: `grep -c "from apscheduler.events import EVENT_JOB_ERROR" triggarr/search/scheduler.py` returns 1.
    - Source assertion: `grep -c "from triggarr.search.engine import _sanitize_exc" triggarr/search/scheduler.py` returns 1.
    - Source assertion: `grep -c "^import time$" triggarr/search/scheduler.py` returns 1.
    - Source assertion: `grep -c "def _on_job_error" triggarr/search/scheduler.py` returns 1.
    - Source assertion: `grep -c "scheduler.add_listener(_on_job_error, EVENT_JOB_ERROR)" triggarr/search/scheduler.py` returns 1.
    - Behavior: `uv run pytest tests/test_scheduler.py::test_make_search_job_unexpected_exception_propagates -x` exits 0 (GREEN).
    - Behavior: `uv run pytest tests/test_scheduler.py::test_make_search_job_httperror_swallowed -x` exits 0.
    - Behavior: `uv run pytest tests/test_scheduler.py::test_event_job_error_listener_logs_unexpected_exception -x` exits 0.
    - Regression: `uv run pytest tests/test_scheduler.py -x -q` exits 0 (no other scheduler tests broken).
    - Ruff: `uv run ruff check triggarr/search/scheduler.py tests/test_scheduler.py` exits 0.
  </acceptance_criteria>
  <done>
    Outer except in `make_search_job` is the canonical narrow tuple. RuntimeError now
    propagates to APScheduler and is logged by `_on_job_error` listener. All three new
    tests pass GREEN; existing scheduler tests still pass. Commit message:
    `feat(65-01): narrow scheduler exception handler + wire EVENT_JOB_ERROR listener (SAFETY-02)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR): Confirm full suite + ruff clean; harden listener comment</name>
  <files>triggarr/search/scheduler.py</files>
  <read_first>
    - triggarr/search/scheduler.py  (re-read make_search_job + _on_job_error + create_lifespan around the listener registration)
  </read_first>
  <action>
    Sanity pass after GREEN. If `_on_job_error` is missing a brief docstring or the
    narrowed except branch is missing an inline comment referencing SAFETY-02, add
    them. No behavioral change. The listener docstring already added in Task 2 is
    sufficient; this task verifies and adds a one-line `# SAFETY-02:` comment beside
    the narrowed `except (httpx.HTTPError, ...)` tuple in `make_search_job` if not
    already present. Then run the full test suite and ruff to confirm no regression.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q &amp;&amp; uv run ruff check triggarr/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "SAFETY-02" triggarr/search/scheduler.py` returns at least 1.
    - Behavior: `uv run pytest tests/ -x -q` exits 0 (full suite green).
    - Behavior: `uv run ruff check triggarr/ tests/` exits 0.
  </acceptance_criteria>
  <done>
    Refactor pass complete. Full test suite green, ruff clean. Commit message:
    `refactor(65-01): document SAFETY-02 narrow exception tuple inline`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → operator log stream | Exception messages cross from internal types into structured log records; HTTP exceptions may carry request URLs containing `apikey=` query parameters |
| Cycle function → APScheduler executor | Previously: all exceptions caught inside coroutine, EVENT_JOB_ERROR never fired. After SAFETY-02: non-narrow-tuple exceptions propagate, exposing internal type names to the operator (intended; not externally observable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Information Disclosure | `_on_job_error` listener logging `event.exception` | mitigate | Listener routes `httpx.HTTPError`/`pydantic.ValidationError` through `_sanitize_exc()` (engine.py:30) which strips `request.url` (may contain `apikey=`); other exception types log `str(exc)` because Triggarr's non-httpx exceptions do not carry secrets |
| T-65-02 | Denial of Service | A listener function that raises would propagate to APScheduler's dispatcher | accept | APScheduler 3.x isolates listener exceptions in a try/except internally (verified against installed apscheduler>=3.11,<4); listener body is one `logger.error` call — minimal failure surface |
| T-65-03 | Tampering | Adding `asyncio.CancelledError` to the narrow tuple would break graceful shutdown | mitigate | Inline `# SAFETY-02:` comment in scheduler.py + Pitfall 7 in RESEARCH.md document why CancelledError MUST NOT be added; tests/test_scheduler.py shutdown tests would catch regression |
</threat_model>

<verification>
- All three new/inverted tests pass: `uv run pytest tests/test_scheduler.py -k "unexpected_exception or httperror_swallowed or event_job_error_listener" -x`
- Full suite green: `uv run pytest tests/ -x -q`
- Ruff clean: `uv run ruff check triggarr/ tests/`
- AST: outer `except Exception` no longer present in `make_search_job` (grep negative)
- APScheduler listener registered exactly once: grep `scheduler.add_listener(_on_job_error, EVENT_JOB_ERROR)` returns 1
</verification>

<success_criteria>
- A `RuntimeError` raised by a cycle function reaches APScheduler and is logged by
  `_on_job_error` at ERROR level with the job_id and type name.
- An `httpx.ConnectError` raised by a cycle function is still caught silently inside
  `make_search_job` (same as before SAFETY-02, modulo the upcoming SAFETY-03 counter
  in plan 65-02).
- No existing scheduler test regresses; no other test file regresses.
- `scheduler.py` imports `time` (used by 65-03 RES-01), `EVENT_JOB_ERROR`, and
  `_sanitize_exc` cleanly; ruff passes.
- The inline `# SAFETY-02:` comment is present beside the narrowed tuple.
</success_criteria>

<output>
Create `.planning/phases/65-scheduler-hardening-resilience/65-01-SUMMARY.md` when
done, following the standard SUMMARY template. Include:
- Files changed (+/− line counts)
- Test results: 3 new/inverted tests + full suite count
- Key decisions: kept `time` import in this plan to avoid churn in 65-03; module-level
  `_on_job_error` (not a closure) so tests can import it; preserved existing
  `logger.error` body inside narrowed except — plan 65-02 will replace that body with
  the failure-counter + escalation logic.
</output>
