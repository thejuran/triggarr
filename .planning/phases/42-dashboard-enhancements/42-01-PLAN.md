---
phase: 42-dashboard-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/state.py
  - triggarr/search/engine.py
  - triggarr/web/routes.py
  - tests/test_web.py
  - tests/test_search.py
autonomous: true
requirements: [INST-07, TAG-05, OBS-03]

must_haves:
  truths:
    - "Health summary route returns connected/disconnected counts for enabled instances only"
    - "Tag resolution stores warning dicts in ist['tag_warnings'] when configured tags are not found"
    - "Stats-row route accepts ?instance= query param and passes instance_id to get_dashboard_stats"
    - "_build_app_context includes tag_warnings from instance state"
  artifacts:
    - path: "triggarr/state.py"
      provides: "AppState TypedDict with tag_warnings field"
      contains: "tag_warnings"
    - path: "triggarr/search/engine.py"
      provides: "Tag warning storage in ist dict during search cycles"
      contains: "tag_warnings"
    - path: "triggarr/web/routes.py"
      provides: "Health summary route, stats filter query param, tag_warnings in app context"
      exports: ["partial_health_summary", "partial_stats_row"]
    - path: "tests/test_web.py"
      provides: "Tests for health summary, stats instance filter"
      contains: "health_summary"
    - path: "tests/test_search.py"
      provides: "Tests for tag warning state storage"
      contains: "tag_warning"
  key_links:
    - from: "triggarr/search/engine.py"
      to: "triggarr/state.py"
      via: "ist['tag_warnings'] list population"
      pattern: "ist\\[.tag_warnings.\\]"
    - from: "triggarr/web/routes.py"
      to: "triggarr/state.py"
      via: "_build_app_context reads tag_warnings from app_state"
      pattern: "tag_warnings"
    - from: "triggarr/web/routes.py"
      to: "triggarr/db.py"
      via: "get_dashboard_stats(instance_id=instance_param)"
      pattern: "get_dashboard_stats.*instance_id"
---

<objective>
Add backend data paths for all three Phase 42 dashboard features: tag warning state storage in the search engine, health summary computation from triggarr_state, and instance-filtered stats queries.

Purpose: Provides the data layer that templates (Plan 02) will render. Separates backend logic from presentation.
Output: Updated state model, engine tag warning storage, new health summary route, extended stats-row route, tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-dashboard-enhancements/42-CONTEXT.md
@.planning/phases/42-dashboard-enhancements/42-RESEARCH.md
@triggarr/state.py
@triggarr/search/engine.py
@triggarr/web/routes.py
@triggarr/db.py
@tests/test_web.py
@tests/test_search.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From triggarr/state.py:
```python
class AppState(TypedDict, total=False):
    missing_cursor: int
    cutoff_cursor: int
    missing_pass: int
    cutoff_pass: int
    last_run: str | None
    connected: bool | None
    unreachable_since: str | None
    missing_count: int | None
    missing_eligible: int | None
    missing_monitored: int | None
    missing_searchable: int | None
    cutoff_count: int | None
    cutoff_searchable: int | None
```

From triggarr/models/config.py:
```python
class Settings:
    def get_enabled_instances(self, app_type: str) -> dict[str, InstanceConfig]:
        """Return only enabled instances with non-empty URLs for an app type."""
```

From triggarr/db.py:
```python
async def get_dashboard_stats(db: aiosqlite.Connection, *, instance_id: str | None = None) -> dict:
```

From triggarr/web/routes.py:
```python
def _build_app_context(request: Request, app_name: str, instance_name: str | None = None) -> dict | None:
def _sanitize_card_id(raw: str) -> str:
def _format_duration(seconds: float | None) -> str:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tag warning state storage and AppState update</name>
  <files>triggarr/state.py, triggarr/search/engine.py, tests/test_search.py</files>
  <behavior>
    - Test: AppState TypedDict includes tag_warnings field typed as list[dict]
    - Test: run_radarr_cycle stores tag_warnings=[] in ist when no tags configured
    - Test: run_radarr_cycle stores tag_warnings with {tag, field} dicts when tag not found and tag_fetch_ok=True
    - Test: run_radarr_cycle stores empty tag_warnings when tags resolve successfully
    - Test: run_sonarr_cycle stores tag_warnings with {tag, field} dicts when tag not found and tag_fetch_ok=True
    - Test: tag_warnings cleared at start of each cycle (not accumulated across cycles)
  </behavior>
  <action>
    1. In triggarr/state.py, add `tag_warnings: list[dict]` to AppState TypedDict (total=False, so optional).

    2. In triggarr/search/engine.py, in BOTH run_radarr_cycle and run_sonarr_cycle:
       - At the start of the tag resolution block (line ~339 for Radarr, ~541 for Sonarr), add `ist["tag_warnings"] = []` to clear previous warnings.
       - After each `if missing_tag_id is None and tag_fetch_ok:` block, append `{"tag": instance_config.missing_tag, "field": "missing"}` to `ist["tag_warnings"]`.
       - After each `if cutoff_tag_id is None and tag_fetch_ok:` block, append `{"tag": instance_config.cutoff_tag, "field": "cutoff"}` to `ist["tag_warnings"]`.
       - Also set `ist["tag_warnings"] = []` when no tags are configured (the else case — before the tag resolution block, ensure tag_warnings is always set).

    3. In tests/test_search.py, add tests:
       - test_tag_warning_state_stored_when_tag_not_found: Mock client.get_tags() to return tags that don't include the configured tag name. Verify ist["tag_warnings"] contains the expected warning dict.
       - test_tag_warning_state_empty_when_tags_resolve: Mock client.get_tags() to return matching tags. Verify ist["tag_warnings"] == [].
       - test_tag_warning_state_cleared_each_cycle: Run cycle twice, verify warnings don't accumulate.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_search.py -x -q -k tag_warning</automated>
  </verify>
  <done>AppState has tag_warnings field. Both run_radarr_cycle and run_sonarr_cycle store tag warning dicts in ist["tag_warnings"]. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Health summary route, stats filter route, and app context tag passthrough</name>
  <files>triggarr/web/routes.py, tests/test_web.py</files>
  <behavior>
    - Test: GET /partials/health-summary returns HTML with connected/disconnected counts
    - Test: Health summary excludes disabled instances from count
    - Test: Health summary treats connected=None as "pending" (separate from disconnected)
    - Test: GET /partials/stats-row?instance=radarr/Default calls get_dashboard_stats with instance_id="Default"
    - Test: GET /partials/stats-row without ?instance= calls get_dashboard_stats with instance_id=None
    - Test: Stats-row route determines instance_app_type from settings for card visibility
    - Test: _build_app_context includes tag_warnings from app_state (defaults to [])
    - Test: Dashboard route includes health context and all_instances list
  </behavior>
  <action>
    1. In triggarr/web/routes.py, add `_build_health_summary(request)` helper:
       - Iterate settings.get_enabled_instances() for radarr and sonarr.
       - For each instance, read ist.get("connected") from triggarr_state.
       - Count: connected (True), disconnected (False), pending (None).
       - Return dict: {"connected": N, "disconnected": N, "pending": N, "total": N}.

    2. Add new route `GET /partials/health-summary`:
       - Call _build_health_summary(request).
       - Return TemplateResponse with "partials/health_summary.html" and health context.
       - Use function name `partial_health_summary`.

    3. Extend `partial_stats_row` route to accept `?instance=` query param:
       - Parse `instance_param = request.query_params.get("instance")`.
       - If present and contains "/", split on "/" to get (app_type, instance_name). Pass instance_name as instance_id to get_dashboard_stats.
       - If present without "/", pass as-is (backward compat).
       - Determine instance_app_type by checking which app's instances dict contains the name.
       - Pass instance_app_type to template context.

    4. Build an `all_instances` list for the stats filter dropdown:
       - In both the `dashboard` route and `partial_stats_row`, build a list of {"value": "radarr/Main", "label": "Radarr / Main"} for each enabled instance.
       - Pass to template context as `all_instances`.
       - Also pass `selected_instance` (the current ?instance= value or empty string).

    5. Extend `_build_app_context` to include tag_warnings:
       - Add `"tag_warnings": app_state.get("tag_warnings", [])` to the return dict.

    6. Update `dashboard` route to pass health summary and all_instances.

    7. In tests/test_web.py, add tests:
       - test_health_summary_counts: Set up state with mixed connected/disconnected/None. Assert counts.
       - test_health_summary_excludes_disabled: Disabled instance not counted.
       - test_stats_row_instance_filter: Mock get_dashboard_stats, verify instance_id passed.
       - test_stats_row_no_filter: Verify instance_id=None when no param.
       - test_app_context_includes_tag_warnings: Verify tag_warnings in context.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_web.py -x -q -k "health_summary or stats_instance or tag_warning"</automated>
  </verify>
  <done>Health summary route returns correct counts excluding disabled instances. Stats-row route filters by instance via query param. _build_app_context passes tag_warnings. All tests pass.</done>
</task>

</tasks>

<verification>
cd /Users/julianamacbook/triggarr && uv run pytest tests/test_web.py tests/test_search.py -x -q
cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/state.py triggarr/search/engine.py triggarr/web/routes.py
</verification>

<success_criteria>
- Health summary helper computes correct connected/disconnected/pending counts from triggarr_state
- Tag warnings stored in ist["tag_warnings"] during both Radarr and Sonarr search cycles
- Stats-row route accepts ?instance=app/name and passes instance_id to DB query
- _build_app_context includes tag_warnings field
- All new tests pass, existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/42-dashboard-enhancements/42-01-SUMMARY.md`
</output>
