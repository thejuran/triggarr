---
phase: 42-dashboard-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - triggarr/templates/partials/health_summary.html
  - triggarr/templates/partials/app_card.html
  - triggarr/templates/partials/stats_row.html
  - triggarr/templates/dashboard.html
autonomous: false

must_haves:
  truths:
    - "Dashboard shows a health summary card above app cards with connected/disconnected counts"
    - "App card shows amber warning badge when tag_warnings is non-empty"
    - "Stats filter dropdown appears above stats row with all enabled instances"
    - "Selecting an instance in dropdown swaps stats row with instance-scoped data"
    - "Movies card hidden when filtered to a Sonarr instance; Episodes card hidden for Radarr"
  artifacts:
    - path: "triggarr/templates/partials/health_summary.html"
      provides: "Health summary card with htmx polling"
      contains: "health-summary"
    - path: "triggarr/templates/partials/app_card.html"
      provides: "Tag warning amber badge"
      contains: "tag_warnings"
    - path: "triggarr/templates/partials/stats_row.html"
      provides: "Conditional card visibility based on instance_app_type"
      contains: "instance_app_type"
    - path: "triggarr/templates/dashboard.html"
      provides: "Health summary include, stats filter dropdown"
      contains: "health_summary"
  key_links:
    - from: "triggarr/templates/dashboard.html"
      to: "triggarr/web/routes.py"
      via: "health_summary partial include and stats filter dropdown"
      pattern: "health_summary|instance"
    - from: "triggarr/templates/partials/stats_row.html"
      to: "triggarr/web/routes.py"
      via: "hx-include preserves dropdown selection across polls"
      pattern: "hx-include"
    - from: "triggarr/templates/partials/app_card.html"
      to: "triggarr/web/routes.py"
      via: "tag_warnings from _build_app_context"
      pattern: "tag_warnings"
---

<objective>
Create and update Jinja2 templates for all three dashboard features: health summary card, tag warning badges on app cards, and instance filter dropdown for stats.

Purpose: Renders the backend data from Plan 01 into the dashboard UI using established htmx partial patterns.
Output: New health_summary.html partial, updated app_card.html with tag badge, updated stats_row.html with conditional cards, updated dashboard.html with filter dropdown.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-dashboard-enhancements/42-CONTEXT.md
@.planning/phases/42-dashboard-enhancements/42-RESEARCH.md
@.planning/phases/42-dashboard-enhancements/42-01-SUMMARY.md
@triggarr/templates/dashboard.html
@triggarr/templates/partials/app_card.html
@triggarr/templates/partials/stats_row.html
@triggarr/templates/base.html
@triggarr/web/routes.py

<interfaces>
<!-- Context from Plan 01 outputs that this plan consumes -->

From triggarr/web/routes.py (after Plan 01):
```python
# Health summary route
@router.get("/partials/health-summary")
async def partial_health_summary(request: Request) -> HTMLResponse:
    # Returns template with context: {"health": {"connected": N, "disconnected": N, "pending": N, "total": N}}

# Stats row route (extended)
@router.get("/partials/stats-row")
async def partial_stats_row(request: Request) -> HTMLResponse:
    # Accepts ?instance=radarr/Main query param
    # Returns template with context: {"stats": ..., "time_to_grab": ..., "instance_app_type": "radarr"|"sonarr"|None, "all_instances": [...], "selected_instance": ""}

# Dashboard route (extended)
@router.get("/")
async def dashboard(request: Request) -> HTMLResponse:
    # Context includes: {"health": {...}, "all_instances": [...], ...}

# _build_app_context returns (extended):
# {"tag_warnings": [{"tag": "name", "field": "missing"|"cutoff"}, ...], ...existing keys...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health summary partial, tag warning badge, stats filter dropdown, and conditional stats cards</name>
  <files>triggarr/templates/partials/health_summary.html, triggarr/templates/partials/app_card.html, triggarr/templates/partials/stats_row.html, triggarr/templates/dashboard.html</files>
  <action>
    1. Create triggarr/templates/partials/health_summary.html:
       - Div with id="health-summary", hx-get to partial_health_summary route, hx-trigger="every 30s", hx-swap="outerHTML".
       - Use standard card styling: bg-triggarr-card rounded-lg border border-triggarr-border p-4 mb-4.
       - Display "Instance Health" label (text-xs uppercase tracking-wide text-triggarr-muted).
       - When health.disconnected == 0 and health.pending == 0: show "N/N connected" in green (text-triggarr-green).
       - When health.disconnected > 0: show "X connected, Y down" with green/red colors.
       - When health.pending > 0: include "Z pending" in text-triggarr-muted after connected/down counts.
       - Use 30s polling per research recommendation (health changes infrequently).

    2. Update triggarr/templates/partials/app_card.html:
       - After the header div (after line 20 in current template, before the stats grid), add tag warning badge block.
       - Condition: {% if app.tag_warnings %}
       - Single warning: use HTML entity &#9888; then "Missing tag 'x' not found" or "Cutoff tag 'y' not found" (capitalize the field name).
       - Two warnings: use HTML entity &#9888; then "Tags not found: 'x' (missing), 'y' (cutoff)" — combined single line per user decision.
       - Styling: bg-amber-500/20 text-amber-400 text-xs px-3 py-1.5 rounded mb-3.

    3. Update triggarr/templates/partials/stats_row.html:
       - Add hx-include="[name='instance']" to the outer div's hx-get attribute to preserve dropdown selection across 30s polls.
       - Wrap the Movies card in {% if instance_app_type != 'sonarr' %} ... {% endif %} to hide when filtered to Sonarr.
       - Wrap the Episodes card in {% if instance_app_type != 'radarr' %} ... {% endif %} to hide when filtered to Radarr.
       - Adjust grid: use grid-cols-2 md:grid-cols-{{ 3 if instance_app_type else 4 }} (3 columns when one card hidden, 4 when all visible). Alternatively, use a simpler approach: keep grid-cols-2 md:grid-cols-4 and use an empty placeholder div when a card is hidden to maintain alignment, OR just let the grid reflow naturally with grid-cols-2 md:grid-cols-3 lg:grid-cols-4 and let the 3-card case render in a 3-col grid. Use the simplest approach: conditionally set the grid class.

    4. Update triggarr/templates/dashboard.html:
       - Add the stats filter dropdown ABOVE the stats_row include but inside the {% if apps %} block.
       - The dropdown is a select element with name="instance", styled with bg-triggarr-card border border-triggarr-border rounded text-sm px-2 py-1 text-white.
       - hx-get to partial_stats_row route, hx-target="#stats-row", hx-swap="outerHTML", hx-include="this".
       - Options: first option "All instances" with value="" (selected by default). Then loop over all_instances to create option elements with value="{{ inst.value }}" and label="{{ inst.label }}".
       - Place the dropdown in a flex container with justify-end and mb-2 for alignment.
       - Add {% include "partials/health_summary.html" %} ABOVE the app cards grid div (but after the stats row and dropdown).
       - Order: stats dropdown + stats row -> health summary -> app cards grid -> search log -> log viewer.
       - IMPORTANT: The dropdown must be OUTSIDE the #stats-row div to avoid being replaced on poll. Place it in dashboard.html directly, not inside stats_row.html.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_web.py -x -q && uv run ruff check triggarr/</automated>
  </verify>
  <done>Health summary card renders at top of dashboard. Tag warning badge appears on app cards with amber styling. Stats filter dropdown swaps stats row content. Movies/Episodes cards hidden when filtered to wrong app type. Dropdown selection preserved across 30s polls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard enhancements</name>
  <action>
    Verify all three dashboard features visually in the browser:
    1. Health summary card at top of dashboard
    2. Tag warning badges on app cards
    3. Stats filter dropdown with instance scoping
  </action>
  <verify>Human visual inspection confirms all three features render correctly</verify>
  <done>All three dashboard features confirmed working in browser</done>
  <what-built>Dashboard now shows: (1) health summary card with connected/disconnected/pending counts, (2) tag warning badges on app cards when configured tags not found, (3) instance filter dropdown above stats row that scopes stats to a single instance and hides irrelevant cards.</what-built>
  <how-to-verify>
    1. Start the app: cd /Users/julianamacbook/triggarr && uv run python -m triggarr
    2. Open http://localhost:8080 in browser
    3. Verify health summary card appears above app cards with instance counts
    4. If you have a tag configured that doesn't exist in your *arr instance, verify amber warning badge appears on the app card after a search cycle runs
    5. Verify stats filter dropdown appears above the stats row
    6. Select a specific instance from the dropdown — verify stats row updates, and Movies or Episodes card hides appropriately
    7. Select "All instances" — verify all 4 stats cards appear again
    8. Wait 30+ seconds — verify the dropdown selection persists after the stats row polls
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
cd /Users/julianamacbook/triggarr && uv run pytest tests/ -x -q
cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/ tests/
</verification>

<success_criteria>
- Health summary card visible at dashboard top with correct connected/disconnected/pending counts
- Tag warning badge appears in amber on affected app cards
- Stats filter dropdown swaps stats row content for selected instance
- Irrelevant stats cards hidden when filtering by instance
- Dropdown selection persists across 30s htmx polls
- All tests pass, no ruff violations
</success_criteria>

<output>
After completion, create `.planning/phases/42-dashboard-enhancements/42-02-SUMMARY.md`
</output>
