---
phase: 76-never-searched-first-search-queue
plan: 03
type: execute
wave: 3
depends_on: ["76-01", "76-02"]
files_modified:
  - triggarr/search/engine.py
  - tests/test_search.py
  - tests/test_refresh_counts.py
  - tests/test_web.py
  - tests/test_app_cards.py
  - tests/test_stats_health.py
  - tests/test_ui_foundations.py
  - tests/test_activity_rail.py
  - tests/test_header_redesign.py
  - tests/test_log_viewer.py
autonomous: true
requirements: [QUEUE-06, QUEUE-07]
user_setup: []

must_haves:
  truths:
    - "slice_batch is deleted from engine.py and its 5 unit tests are removed; no slice_batch reference survives anywhere in triggarr/ or tests/"
    - "A post-deletion fixed-expectation cold-start test preserves the cold-start-equivalence guarantee after slice_batch is gone"
    - "The refresh-counts queue-independence invariant is re-expressed against the searched-log: refresh_*_counts neither reads nor writes missing_searched/cutoff_searched (Radarr/Sonarr/Lidarr)"
    - "Every incidental UI/route test fixture drops the removed cursor keys; no *_cursor reference survives in tests/; *_pass references are kept"
    - "No *_cursor reference survives in triggarr/state.py or triggarr/search/engine.py (the v2.2-format-detector strings excepted, confirmed by reading)"
    - "The full test suite is green and ruff is clean"
    - "D-05: cursor references in tests/ are classified by role and migrated-or-stripped (no blanket find-and-replace, no tombstoned cursor fields)"
    - "D-07: the per-file behavioral-vs-incidental classification is enumerated (test_refresh_counts re-expressed; 7 incidental UI/route fixtures stripped) per the RESEARCH grep-verified table"
    - "D-12: no new dashboard/UI surface is added (*_pass stays the only dashboard signal); failed individual searches still log a WARNING + write a failed search-history row exactly as today"
  artifacts:
    - path: "triggarr/search/engine.py"
      provides: "slice_batch removed; docstring cursor references in refresh_*_counts updated to searched-log language"
      contains: "prioritize_batch"
    - path: "tests/test_refresh_counts.py"
      provides: "re-expressed queue-independence tests asserting searched-log unchanged after refresh"
      contains: "missing_searched"
    - path: "tests/test_search.py"
      provides: "post-deletion fixed-expectation cold-start test; slice_batch unit tests + import removed"
      contains: "prioritize_batch"
  key_links:
    - from: "tests/test_refresh_counts.py"
      to: "refresh_*_counts queue-independence"
      via: "set <q>_searched=[...]; refresh; assert unchanged"
      pattern: "missing_searched"
    - from: "static guard"
      to: "slice_batch / *_cursor removal"
      via: "grep gate in verification"
      pattern: "slice_batch"
---

<objective>
Remove the dead cursor machinery, re-express the queue-independence invariant, and lock the static guards.

This plan deletes `slice_batch` from `engine.py` (now that all 6 callers have migrated in Plan 02) along with its 5 unit tests and import in `test_search.py`, and adds a post-deletion fixed-expectation cold-start test so the cold-start-equivalence guarantee survives `slice_batch`'s removal. It re-expresses the three `test_refresh_counts.py` "cursor unchanged" tests as "searched-log unchanged after refresh" (D-06 — preserving the v2.10 queue-independence invariant under the new state model). It strips the now-removed cursor keys from the seven incidental UI/route test fixtures. Finally it asserts the static guards: no `slice_batch` survivor anywhere, no `*_cursor` survivor in `state.py`/`engine.py` (outside the v2.2-format-detector strings).

Purpose: no dead code, no tombstone (D-09/D-10). The cold-start guarantee must outlive its oracle. The queue-independence invariant must hold under the new model. The static guards make the removal verifiable.

Output: `slice_batch` gone; post-deletion cold-start test in `test_search.py`; re-expressed invariant in `test_refresh_counts.py`; cursor keys stripped from 7 incidental files; static guards green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/76-never-searched-first-search-queue/76-CONTEXT.md
@.planning/phases/76-never-searched-first-search-queue/76-RESEARCH.md
@.planning/phases/76-never-searched-first-search-queue/76-PATTERNS.md
@.planning/phases/76-never-searched-first-search-queue/76-VALIDATION.md
@docs/superpowers/specs/2026-06-04-search-queue-priority-design.md
@.planning/phases/76-never-searched-first-search-queue/76-01-SUMMARY.md
@.planning/phases/76-never-searched-first-search-queue/76-02-SUMMARY.md

<interfaces>
<!-- Per-file ref counts are grep-verified in RESEARCH "Test Surface Classification" — trust that table, do NOT re-grep to discover them. -->
<!-- Note: counts were taken before Plans 01/02 migrated test_search.py + test_state.py; the incidental files below are untouched by Plans 01/02, so their counts hold. -->

Incidental fixture files (strip the missing_cursor=/cutoff_cursor= keyword args from AppState fixtures; assert NOTHING about cursors; KEEP *_pass):
  | File                       | missing_cursor | cutoff_cursor | Notes |
  | tests/test_web.py          | 9 | 8 | fixtures :46-69, :1070-1077; :1850 `== 0` → empty/absent-log assertion (RESEARCH A1); doc-comment :385-387 |
  | tests/test_activity_rail.py | 5 | 5 | fixture cursor keys |
  | tests/test_app_cards.py    | 3 | 3 | KEEP its 5 *_pass refs |
  | tests/test_stats_health.py | 3 | 3 | fixture cursor keys |
  | tests/test_ui_foundations.py | 3 | 3 | fixture cursor keys |
  | tests/test_log_viewer.py   | 3 | 3 | fixture cursor keys |
  | tests/test_header_redesign.py | 2 | 2 | fixture cursor keys |

refresh-counts invariant analog (tests/test_refresh_counts.py):
  test_refresh_radarr_counts_does_not_advance_cursor :129 (+ Sonarr ~244, Lidarr ~303)
  TODAY: set missing_cursor=N → refresh → assert == N
  TARGET (D-06): set missing_searched=["1","2","3"], cutoff_searched=["9"] → refresh → assert UNCHANGED
  Do NOT touch _does_not_stamp_last_run / _sets_connected_true tests. _make_test_state @ :48 auto-inherits new fields; strip literal cursor keys in route fixtures (:754-781).

slice_batch removal:
  def at engine.py:133; 5 unit tests at test_search.py:69/76/83/90/105; import at test_search.py:38.
  3 docstring mentions in refresh_*_counts (engine.py:1027/1163/1302) say "Does NOT call slice_batch, does NOT write missing_cursor/cutoff_cursor" → update to "Does NOT read/write missing_searched/cutoff_searched".
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Delete slice_batch + its tests; add post-deletion fixed-expectation cold-start test; update refresh-counts docstrings</name>
  <files>triggarr/search/engine.py, tests/test_search.py</files>
  <read_first>
    - triggarr/search/engine.py:133-156 (slice_batch def to delete), :1023-1030 + :1159-1166 + :1298-1305 (the three refresh_*_counts docstrings mentioning slice_batch/cursor)
    - tests/test_search.py:38 (slice_batch import), :65-109 (the 5 slice_batch unit tests to delete), the cold-start-equivalence oracle test authored in Plan 01 (now needs its fixed-expectation sibling)
    - docs/superpowers/specs/2026-06-04-search-queue-priority-design.md §3 D-10 (slice_batch removed, no dead code), §8 (cold-start test), §10
    - .planning/phases/76-never-searched-first-search-queue/76-PATTERNS.md "NEW prioritize_batch unit matrix" (cold-start equivalence — post-deletion fixed-expectation variant), "State of the Art" (deprecated slice_batch)
    - .planning/phases/76-never-searched-first-search-queue/76-RESEARCH.md Pitfall 4 (cold-start), "Test Surface Classification" (test_search.py slice_batch refs)
  </read_first>
  <action>
    Add the post-deletion cold-start FIRST: in tests/test_search.py add a fixed-expectation cold-start test asserting prioritize_batch(items, [], N, key_fn)[0] equals a hardcoded first-N-in-fetch-order expectation (no slice_batch reference) — this preserves the QUEUE-06 guarantee once slice_batch is gone. THEN delete the 5 slice_batch unit tests (test fns at test_search.py:69/76/83/90/105) and remove `slice_batch` from the engine import block (test_search.py:38) — but only if Plan 01's oracle test was the only other user; if the Plan 01 oracle test still references slice_batch, convert/delete it now that the fixed-expectation variant covers the guarantee. THEN delete the `def slice_batch(...)` function from triggarr/search/engine.py:133-156. Update the three refresh_*_counts docstrings (engine.py:1027/1163/1302) that say "Does NOT call slice_batch, does NOT write missing_cursor/cutoff_cursor" to read "Does NOT read or write missing_searched/cutoff_searched" (queue-independence under the new model). Do NOT touch the refresh_*_counts function BODIES.
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py -k "cold_start" -x -q && ! grep -rq "slice_batch" triggarr/ tests/ && uv run ruff check triggarr/search/engine.py tests/test_search.py</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rq "slice_batch" triggarr/ tests/` (zero survivors anywhere — def, calls, tests, import, docstrings)
    - `uv run pytest tests/test_search.py -k "cold_start" -x -q` exits 0 with a fixed-expectation (slice_batch-free) cold-start test present
    - `grep -q "def prioritize_batch(" triggarr/search/engine.py` (replacement function intact)
    - `! grep -E "missing_cursor|cutoff_cursor" triggarr/search/engine.py` (refresh-counts docstrings re-expressed; no cursor string survives in engine.py)
    - `uv run pytest tests/test_search.py -x -q` exits 0 (whole file green without the slice_batch tests)
    - `uv run ruff check triggarr/search/engine.py tests/test_search.py` exits 0 (no unused-import F401 from the removed slice_batch import)
  </acceptance_criteria>
  <done>slice_batch and its tests are gone; a slice_batch-free fixed-expectation cold-start test holds the QUEUE-06 guarantee; refresh-counts docstrings re-expressed against the searched-log; test_search.py green and ruff clean.</done>
</task>

<task type="execute">
  <name>Task 2: Re-express the refresh-counts queue-independence invariant (Radarr/Sonarr/Lidarr)</name>
  <files>tests/test_refresh_counts.py</files>
  <read_first>
    - tests/test_refresh_counts.py:48 (_make_test_state — auto-inherits searched-log fields), :129-148 (test_refresh_radarr_counts_does_not_advance_cursor), :244-262 (Sonarr equivalent), :303-321 (Lidarr equivalent), :754-781 (route fixtures with literal cursor keys to strip); also the _does_not_stamp_last_run / _sets_connected_true tests (DO NOT touch)
    - .planning/phases/76-never-searched-first-search-queue/76-CONTEXT.md D-06 (re-express, not delete; preserves v2.10 CNT-02 queue-independence)
    - .planning/phases/76-never-searched-first-search-queue/76-PATTERNS.md "re-express queue-independence invariant" (exact migration shape)
    - .planning/phases/76-never-searched-first-search-queue/76-RESEARCH.md "Test Surface Classification" (test_refresh_counts.py: 14+14 cursor refs, 4 *_pass refs KEEP), "Verified current refresh-counts queue-independence"
  </read_first>
  <behavior>
    - For each of Radarr/Sonarr/Lidarr: seed missing_searched=["1","2","3"] and cutoff_searched=["9"] on the instance state, call refresh_*_counts, assert both searched-logs are UNCHANGED (refresh neither reads nor writes them — the queue-independence invariant under the new model).
    - The existing _does_not_stamp_last_run and _sets_connected_true assertions in those tests stay intact (counts/health update; last_run/last_success and the searched-log do not).
  </behavior>
  <action>
    In tests/test_refresh_counts.py, re-express the three "does_not_advance_cursor" tests (:129 Radarr, :244 Sonarr, :303 Lidarr): replace the `state[...]["missing_cursor"] = N` seeds and `assert ... ["missing_cursor"] == N` checks with `state[...]["missing_searched"] = ["1","2","3"]` / `state[...]["cutoff_searched"] = ["9"]` seeds and `assert ... unchanged` after the refresh call (D-06). Keep the assertions that prove counts/health updated and that last_run/last_success are NOT stamped. Strip any literal cursor keyword args from the route fixtures (:754-781) — swap to searched-log fields or drop them. KEEP the 4 *_pass references (pass semantics preserved). Do NOT touch the _does_not_stamp_last_run / _sets_connected_true tests. Consider renaming the three tests to reflect the new invariant (e.g. *_does_not_touch_searched_log) following the existing convention — your discretion.
  </action>
  <verify>
    <automated>uv run pytest tests/test_refresh_counts.py -x -q && uv run ruff check tests/test_refresh_counts.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "missing_searched" tests/test_refresh_counts.py` (invariant re-expressed against the searched-log)
    - `! grep -E "missing_cursor|cutoff_cursor" tests/test_refresh_counts.py` (no cursor references survive, including route fixtures)
    - `grep -c "_pass" tests/test_refresh_counts.py` is >= 4 (the *_pass references are kept)
    - `uv run pytest tests/test_refresh_counts.py -x -q` exits 0 (all three apps prove searched-log unchanged after refresh; last_run/health assertions intact)
    - `uv run ruff check tests/test_refresh_counts.py` exits 0
  </acceptance_criteria>
  <done>refresh_*_counts proven queue-independent under the new model for all three apps (searched-log unchanged after refresh); cursor references gone; *_pass refs and last_run/health assertions preserved; file green and ruff clean.</done>
</task>

<task type="execute">
  <name>Task 3: Strip cursor keys from the 7 incidental UI/route fixtures + full-suite static guards</name>
  <files>tests/test_web.py, tests/test_app_cards.py, tests/test_stats_health.py, tests/test_ui_foundations.py, tests/test_activity_rail.py, tests/test_header_redesign.py, tests/test_log_viewer.py</files>
  <read_first>
    - .planning/phases/76-never-searched-first-search-queue/76-RESEARCH.md "Test Surface Classification" (exact per-file ref lines — do NOT re-grep to discover them) + Pitfall 6 (test_web.py was omitted from CONTEXT — it IS incidental; :1850 `== 0` → empty/absent-log assertion per A1)
    - .planning/phases/76-never-searched-first-search-queue/76-PATTERNS.md "Incidental fixture strips (7 files)"
    - tests/test_web.py:46-69 + :1070-1077 (fixture cursor keys), :385-387 (doc-comment), :1850 (the one assertion — read the surrounding test to confirm A1 disposition: default-state path → migrate to empty/absent-log)
    - tests/test_app_cards.py (cursor fixtures + the 5 *_pass refs to KEEP)
  </read_first>
  <action>
    In each of the seven incidental files (test_web.py, test_app_cards.py, test_stats_health.py, test_ui_foundations.py, test_activity_rail.py, test_header_redesign.py, test_log_viewer.py), delete the `missing_cursor=`/`cutoff_cursor=` keyword args from the AppState fixture literals (the ref lines are enumerated in the RESEARCH classification table — use them, do not re-discover). Assert nothing about cursors (they were never the subject under test). KEEP test_app_cards.py's 5 *_pass references. For tests/test_web.py specifically: strip the fixture cursor keys at :46-69 and :1070-1077; update the doc-comment at :385-387; and for the assertion at :1850 (`missing_cursor == 0`) read the surrounding test — it targets a default-state path (A1), so migrate it to assert the searched-log is empty/absent (e.g. missing_searched == [] or `"missing_searched" not in ...`) rather than asserting a cursor. After editing all seven, run the full suite and the static guards.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q && ! grep -rq "slice_batch" triggarr/ tests/ && ! grep -rqE "missing_cursor|cutoff_cursor" tests/ && uv run ruff check triggarr/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rqE "missing_cursor|cutoff_cursor" tests/` (no cursor reference survives anywhere in tests/)
    - `! grep -rq "slice_batch" triggarr/ tests/` (slice_batch gone everywhere)
    - `! grep -rqE "missing_cursor|cutoff_cursor" triggarr/state.py triggarr/search/engine.py | grep -v "_v22\|v2.2"` — i.e. no cursor reference in state.py/engine.py except the v2.2-format-detector strings (confirm those by reading _is_v22_state_format/_migrate_v22_state)
    - `grep -c "_pass" tests/test_app_cards.py` is >= 5 (pass refs kept)
    - `uv run pytest tests/ -x -q` exits 0 (FULL suite green — 924-fn baseline must stay green)
    - `uv run ruff check triggarr/ tests/` exits 0
  </acceptance_criteria>
  <done>All seven incidental fixtures drop the removed cursor keys (test_web.py:1850 migrated to an empty/absent-log assertion); *_pass refs preserved; the full suite is green; the static guards (no slice_batch, no *_cursor in tests/, none in state.py/engine.py outside the v2.2 detector) all pass; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Dead-code removal + test migration. No runtime behavior changes beyond what Plans 01/02 already shipped; no new endpoint, network call, user input, or secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Tampering | refresh_*_counts queue-independence | mitigate | The re-expressed invariant tests (×3 apps) prove refresh_*_counts neither reads nor writes the searched-log, preserving the v2.10 CNT-02 boundary that count-only refresh never mutates the search queue. |
| T-76-08 | (regression) | static guards | mitigate | The grep gates (no slice_batch, no *_cursor in tests/state.py/engine.py) make the removal verifiable and prevent a tombstoned/dead cursor field from silently surviving (D-09/D-10). |
| T-76-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed this plan. |

Block-on severity: high — none expected. No new attack surface; SecretStr discipline unchanged.
</threat_model>

<verification>
- `uv run pytest tests/ -x -q` (FULL suite) green
- `uv run ruff check triggarr/ tests/` clean
- Static guards: `! grep -rq "slice_batch" triggarr/ tests/`; `! grep -rqE "missing_cursor|cutoff_cursor" tests/`; no cursor in state.py/engine.py outside the v2.2 detector
- refresh-counts queue-independence re-expressed and green for all three apps
</verification>

<success_criteria>
- slice_batch and its 5 unit tests removed; a slice_batch-free fixed-expectation cold-start test holds QUEUE-06.
- refresh-counts queue-independence re-expressed against the searched-log (Radarr/Sonarr/Lidarr), green.
- All 7 incidental fixtures drop the removed cursor keys; *_pass refs kept.
- Full suite green; ruff clean; all static guards pass.
</success_criteria>

<output>
Create `.planning/phases/76-never-searched-first-search-queue/76-03-SUMMARY.md` when done.
</output>
