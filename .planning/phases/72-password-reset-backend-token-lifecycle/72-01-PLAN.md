---
phase: 72-password-reset-backend-token-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_reset.py
  - triggarr/auth.py
  - triggarr/web/middleware.py
  - triggarr/web/routes.py
  - triggarr/search/scheduler.py
  - triggarr/templates/reset.html
autonomous: true
requirements: [RCOV-02, RCOV-03, RCOV-04, RCOV-05, RCOV-06]
nyquist_compliant: true
wave_0_complete: true

must_haves:
  truths:
    - "tests/test_reset.py exists with all 20 VALIDATION.md test stubs, collectible by pytest (RED — failing until handlers exist)"
    - "generate_reset_token() returns a URL-safe CSPRNG string (~43 chars) via secrets.token_urlsafe(32)"
    - "GET /reset/request is reachable without a session cookie (routes are exempt from AuthMiddleware)"
    - "No authenticated route other than /reset/* becomes reachable by the /reset exemption"
    - "app.state.reset_token initializes to None and app.state.last_reset_time to {} at lifespan startup"
  artifacts:
    - path: "tests/test_reset.py"
      provides: "All 20 Phase 72 test functions (RED specs from VALIDATION.md) + _make_reset_app helper"
      contains: "def _make_reset_app"
    - path: "triggarr/auth.py"
      provides: "generate_reset_token() CSPRNG helper"
      contains: "def generate_reset_token"
    - path: "triggarr/web/middleware.py"
      provides: "/reset added to EXEMPT_PREFIXES"
      contains: "/reset"
    - path: "triggarr/web/routes.py"
      provides: "RESET_REQUEST_RATE_LIMIT_SECONDS and RESET_CONFIRM_RATE_LIMIT_SECONDS constants"
      contains: "RESET_REQUEST_RATE_LIMIT_SECONDS"
    - path: "triggarr/search/scheduler.py"
      provides: "app.state.reset_token=None and app.state.last_reset_time={} init"
      contains: "app.state.reset_token"
    - path: "triggarr/templates/reset.html"
      provides: "Minimal functional form shell for request + confirm steps"
      min_lines: 15
  key_links:
    - from: "triggarr/web/middleware.py"
      to: "AuthMiddleware prefix-match"
      via: "EXEMPT_PREFIXES tuple membership"
      pattern: "EXEMPT_PREFIXES.*reset"
    - from: "triggarr/search/scheduler.py"
      to: "app.state"
      via: "create_lifespan init block"
      pattern: "app\\.state\\.reset_token"
---

<objective>
Lay the Phase 72 foundation: the RED test file (`tests/test_reset.py`, the only Wave 0 gap), the `generate_reset_token()` auth helper, the `/reset` middleware exemption, the two rate-limit constants, the new `app.state` fields initialized at startup, and a minimal `reset.html` form shell. After this plan all 20 tests collect, and the contracts that Plans 02 and 03 implement against exist.

Purpose: Establish every contract and scaffold downstream handlers consume — no scavenger hunt for executors of Plans 02/03. The test file is RED (handlers do not exist yet); the middleware exemption, app.state init, and auth helper are GREEN and independently importable now.
Output: `tests/test_reset.py` (20 tests + helper), `generate_reset_token()`, `EXEMPT_PREFIXES += "/reset"`, two rate-limit constants in routes.py, two new app.state fields in scheduler.py, `triggarr/templates/reset.html`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-password-reset-backend-token-lifecycle/72-CONTEXT.md
@.planning/phases/72-password-reset-backend-token-lifecycle/72-RESEARCH.md
@.planning/phases/72-password-reset-backend-token-lifecycle/72-PATTERNS.md
@.planning/phases/72-password-reset-backend-token-lifecycle/72-VALIDATION.md

<interfaces>
<!-- Contracts extracted from the live codebase. Use these directly — no exploration needed. -->

triggarr/auth.py (existing, verified):
- COOKIE_MAX_AGE = 30 * 24 * 60 * 60  (line 10)
- def hash_password(plaintext: str) -> str  (raises ValueError if > 72 bytes)
- def generate_api_key() -> str  (line 52)
- def generate_session_secret() -> str  (line 61; body: return secrets.token_hex(32))
- def sign_session(username: str, secret: str) -> str  (line 70)
- def validate_session(cookie_value: str | None, secret: str) -> str | None  (line 88)
- `import secrets` already present (line 3 of auth.py)

triggarr/web/middleware.py (existing, verified):
- EXEMPT_PREFIXES = ("/health", "/static", "/login", "/setup")  (line 22)
- prefix-match: `if any(path.startswith(prefix) for prefix in EXEMPT_PREFIXES):`  (line 112)

triggarr/web/routes.py (existing, verified):
- SEARCH_RATE_LIMIT_SECONDS = 10  (line 143)
- templates = Jinja2Templates(...)  (line 81) — module-level object; call templates.TemplateResponse(request=request, name=..., context=...)
- @router.get("/") name="dashboard" (line 367) — referenceable via request.url_for("dashboard")
- already imported: asyncio, os, secrets, time (lines 10,12,14,15); HTMLResponse, RedirectResponse (line 24); is_secure_request (line 54); _atomic_toml_write, load_settings (line 43); collect_secrets (line 51); setup_logging (line 46); generate_session_secret, hash_password, sign_session, COOKIE_MAX_AGE (lines 29-37)
- tempfile is NOT yet imported (Plan 02 adds it)

triggarr/search/scheduler.py (existing, verified — create_lifespan app.state block):
- line 479: app.state.config_path = config_path
- line 491: app.state.search_lock = asyncio.Lock()
- line 500: app.state.last_search_time = {}
- line 501: app.state.last_health_check = None

tests/test_auth_routes.py (existing, verified — analog patterns):
- _make_route_app(auth_config, config_path) at lines 82-108 builds FastAPI app with AuthMiddleware + router + app.state.{settings, config_path, search_lock}; sets auth_state["active"]/["method"]
- monkeypatch.setattr(time, "monotonic", lambda: fake_time) for TTL tests (lines 1120-1133)
- _set_cookie_has_secure_attribute(set_cookie) helper (lines 111-113)
- session rotation assertion: tomllib.loads(config_file.read_text())["auth"]["session_secret"] != original; validate_session(old_cookie, new_secret) is None (lines 631-638)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generate_reset_token() + /reset exemption + rate-limit constants + app.state init</name>
  <files>triggarr/auth.py, triggarr/web/middleware.py, triggarr/web/routes.py, triggarr/search/scheduler.py</files>
  <read_first>
    - triggarr/auth.py lines 52-67 (generate_api_key / generate_session_secret — the symmetry to mirror; `import secrets` already at line 3)
    - triggarr/web/middleware.py lines 22 and 107-113 (EXEMPT_PREFIXES tuple + the `path.startswith(prefix)` match logic)
    - triggarr/web/routes.py line 143 (SEARCH_RATE_LIMIT_SECONDS = 10 — adjacency for the two new constants)
    - triggarr/search/scheduler.py lines 491-501 (app.state init block: search_lock, last_search_time, last_health_check)
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-PATTERNS.md sections "generate_reset_token()", "EXEMPT_PREFIXES", "rate-limit constants", "app.state init" (exact adaptations)
  </read_first>
  <action>
    In triggarr/auth.py: add `generate_reset_token() -> str` immediately after `generate_session_secret()` (after line 67, before `sign_session`), body `return secrets.token_urlsafe(32)`, with a two-line docstring mirroring `generate_session_secret`'s "Returns:" convention (per D-22 — thin CSPRNG wrapper; storage/validation stays in the route layer, NOT here). No new import (secrets already present).

    In triggarr/web/middleware.py line 22: append `"/reset"` as the fifth element of EXEMPT_PREFIXES → `("/health", "/static", "/login", "/setup", "/reset")` (per D-21). The existing `startswith` match (line 112) makes this cover `/reset`, `/reset/request`, `/reset/confirm`.

    In triggarr/web/routes.py: immediately after line 143 (`SEARCH_RATE_LIMIT_SECONDS = 10`) add two module-level constants: `RESET_REQUEST_RATE_LIMIT_SECONDS = 60` (comment: prevent log/file flooding, ~1 mint/min) and `RESET_CONFIRM_RATE_LIMIT_SECONDS = 5` (comment: throttle token-guessing attempts) — per D-15, Claude's-discretion names/values within the spec windows.

    In triggarr/search/scheduler.py: in create_lifespan, immediately after `app.state.last_search_time = {}` (line 500) and before `app.state.last_health_check = None` (line 501), add `app.state.reset_token = None` and `app.state.last_reset_time = {}` (per D-04, D-14). Precede each with a comment-style type annotation matching the convention on line 500: reset_token is `tuple[str, float] | None` (CSPRNG token string, expiry as monotonic ts) set by reset_request_post, cleared by reset_confirm_post, reset to None on restart (D-04, D-18); last_reset_time is `dict[str, float]` keyed "request"/"confirm" → monotonic ts (D-14, D-15).
  </action>
  <verify>
    <automated>uv run ruff check triggarr/auth.py triggarr/web/middleware.py triggarr/web/routes.py triggarr/search/scheduler.py && uv run python -c "from triggarr.auth import generate_reset_token; t=generate_reset_token(); assert isinstance(t,str) and len(t)>=40; from triggarr.web.middleware import EXEMPT_PREFIXES; assert '/reset' in EXEMPT_PREFIXES; from triggarr.web.routes import RESET_REQUEST_RATE_LIMIT_SECONDS, RESET_CONFIRM_RATE_LIMIT_SECONDS; assert RESET_REQUEST_RATE_LIMIT_SECONDS==60 and RESET_CONFIRM_RATE_LIMIT_SECONDS==5; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from triggarr.auth import generate_reset_token` imports; two calls return distinct ~43-char URL-safe strings.
    - `"/reset" in EXEMPT_PREFIXES` is True; tuple length is 5.
    - `RESET_REQUEST_RATE_LIMIT_SECONDS == 60` and `RESET_CONFIRM_RATE_LIMIT_SECONDS == 5` importable from routes.
    - `grep -n "app.state.reset_token" triggarr/search/scheduler.py` and `grep -n "app.state.last_reset_time" triggarr/search/scheduler.py` each return exactly one init line, positioned after last_search_time and before last_health_check.
    - ruff clean on all four files.
  </acceptance_criteria>
  <done>generate_reset_token() exists in auth.py; /reset is in EXEMPT_PREFIXES; both rate-limit constants exist in routes.py; reset_token/last_reset_time initialized in scheduler create_lifespan. ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal reset.html form shell</name>
  <files>triggarr/templates/reset.html</files>
  <read_first>
    - triggarr/templates/login.html (structure of an unauthenticated form page — extends/base block usage, form action/method, CSP-nonce handling)
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-CONTEXT.md D-20 (context-key contract: generic `error` for token failures, per-field `errors` dict for password fields) and the "minimal form-shell" discretion note
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-RESEARCH.md Open Question 2 (single reset.html with a conditional `step` block serving both request and confirm forms)
  </read_first>
  <action>
    Create a minimal FUNCTIONAL `reset.html` (NOT styled — Phase 73 owns the polished pages matching login.html/setup.html; do not build the polished UI). Single template with a `step` conditional: when `step == "request"`, render a POST form to `/reset/request` (submit button plus a neutral confirmation message area). When `step == "confirm"`, render a POST form to `/reset/confirm` with three inputs named EXACTLY `token`, `new_password`, `confirm_password`. The template MUST render the generic token error from context key `error` (when present) and per-field errors from context key `errors` (a dict keyed `new_password`, `confirm_password`) so it matches change_password's `errors: dict[str,str]` contract (D-20). The template MUST NOT receive or echo a `token` value — the token never appears in any HTTP response (D-02). Mirror login.html's base/extends and CSP-nonce conventions so it renders under the existing Jinja2Templates env. Keep markup minimal; field names and context keys matter, styling does not.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; t=Path('triggarr/templates/reset.html').read_text(); assert 'new_password' in t and 'confirm_password' in t and 'token' in t; assert '/reset/request' in t and '/reset/confirm' in t; assert 'error' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `triggarr/templates/reset.html` exists, >= 15 lines.
    - Contains form inputs named `token`, `new_password`, `confirm_password` and form actions `/reset/request` and `/reset/confirm`.
    - References context key `error` (generic token error) and `errors` (per-field dict); does NOT echo a `token` value back into the page.
    - Mirrors login.html base/extends conventions (renders without Jinja errors).
  </acceptance_criteria>
  <done>Minimal reset.html shell exists with request/confirm conditional, correct field names, and the D-20 error/errors context-key contract. No token echoed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/test_reset.py — all 20 RED tests + _make_reset_app helper</name>
  <files>tests/test_reset.py</files>
  <behavior>
    All 20 VALIDATION.md tests, RED now (handlers absent). After Plan 02 the mint/request tests go GREEN; after Plan 03 the confirm tests go GREEN. Test names below MUST match VALIDATION.md exactly:
    - test_request_mints_token: POST /reset/request → app.state.reset_token is set (tuple); token string NOT in response.text
    - test_token_file_written_0600: after POST /reset/request, config_path.parent/reset-token.txt exists with mode 0o600
    - test_token_not_in_response: minted token value absent from response.text, set-cookie header, and str(dict(response.headers))
    - test_token_ttl_stored_correctly: stored expiry == monotonic_at_mint + 900 (monkeypatch time.monotonic)
    - test_expired_token_rejected: token with past expiry → POST /reset/confirm returns generic "Invalid or expired", no state change
    - test_new_mint_supersedes_prior: two POST /reset/request calls (rate-limit cleared between) → app.state.reset_token holds only the second token; first no longer validates
    - test_token_single_use: successful confirm, then re-POST same token → rejected (app.state.reset_token is None after first)
    - test_confirm_success_redirects_with_cookie: valid token + matching password → 303 to dashboard + triggarr_session set-cookie
    - test_confirm_rotates_session_secret: persisted TOML auth.session_secret differs from original
    - test_pre_reset_cookie_invalid_after_reset: cookie signed with old secret → validate_session(old_cookie, new_secret) is None
    - test_new_cookie_validates_after_reset: new set-cookie validates under new secret
    - test_wrong_token_generic_error: wrong token → generic error, app.state.reset_token unchanged, password_hash unchanged
    - test_password_mismatch_field_error: new_password != confirm_password → per-field errors dict, no state change
    - test_empty_password_field_error: empty new_password → per-field error
    - test_password_too_long_field_error: >72-byte password → per-field error (bcrypt ValueError surfaced)
    - test_request_rate_limited: second POST /reset/request within 60s → 429
    - test_confirm_rate_limited: rapid POST /reset/confirm within 5s → 429
    - test_reset_routes_unauthenticated: GET /reset/request with no cookie → 200 (not redirected to /login)
    - test_no_other_route_exposed: GET / (dashboard) with no cookie still redirects to /login (exemption scoped to /reset only)
    - test_token_file_deleted_on_success: reset-token.txt removed after successful confirm
  </behavior>
  <read_first>
    - tests/test_auth_routes.py lines 1-113 (imports, _make_route_app helper, _set_cookie_has_secure_attribute), 604-707 (session-rotation assertions), 1120-1133 (monkeypatch time.monotonic)
    - tests/conftest.py (autouse fixtures: _reset_rate_limiter resets _login_failures only, NOT app.state.last_reset_time — each rate-limit test needs a fresh _make_reset_app)
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-VALIDATION.md (the 20-row Per-Task Verification Map — exact test names = RED specs)
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-PATTERNS.md "tests/test_reset.py" section (imports block, _make_reset_app wrapper, time injection, redaction/rotation assertion snippets)
    - .planning/phases/72-password-reset-backend-token-lifecycle/72-RESEARCH.md §5 "Test Patterns" (TestClient(app, follow_redirects=False), AuthConfig with username + password_hash + session_secret for confirm tests, tomllib TOML verification)
  </read_first>
  <action>
    Create tests/test_reset.py with the imports block from PATTERNS.md (asyncio, time, tomllib, Path, pytest, FastAPI, StaticFiles, TestClient, SecretStr; from triggarr.auth import generate_session_secret, hash_password, sign_session, validate_session; AuthConfig/GeneralConfig/Settings; AuthMiddleware; router + auth_state). Add a `_make_reset_app(auth_config=None, config_path=None)` helper that replicates test_auth_routes.py's `_make_route_app` construction locally (build FastAPI, add AuthMiddleware, include router, mount /static, set app.state.settings/config_path/search_lock, set auth_state active/method) and then sets `app.state.reset_token = None` and `app.state.last_reset_time = {}` (Pitfall 4: required or handlers raise AttributeError). For confirm tests, build AuthConfig with a known username, a real `hash_password(...)`-derived `password_hash` (SecretStr), and a known `session_secret` (SecretStr), persisted to a real `config_path = tmp_path / "triggarr.toml"` so the apply path can read/rewrite it and rotation is verifiable via tomllib.

    Implement all 20 test functions named EXACTLY per VALIDATION.md (so each `uv run pytest tests/test_reset.py::<name>` command resolves). Use `TestClient(app, follow_redirects=False)` so 303s are observable. For TTL/expiry tests use `monkeypatch.setattr(time, "monotonic", ...)` or set `app.state.reset_token = (tok, time.monotonic() - 1)` for the expired case. Construct a fresh `_make_reset_app()` per rate-limit test (conftest does NOT reset app.state.last_reset_time). For redaction, assert the token value is absent from `.text`, the `set-cookie` header, and `str(dict(response.headers))`. For rotation, read persisted TOML via `tomllib.loads(config_path.read_text())` and assert `auth.session_secret` changed and the old cookie fails `validate_session`.

    These tests are EXPECTED TO FAIL (RED) in this plan — the three route handlers do not exist yet (collection succeeds; requests 404 / assertions fail). Do NOT implement handlers here (Plans 02 and 03 do). The verify step only requires pytest COLLECTS all 20 tests without import/collection errors.
  </action>
  <verify>
    <automated>uv run ruff check tests/test_reset.py && uv run pytest tests/test_reset.py --collect-only -q 2>&1 | grep -c "::test_" | (read n; test "$n" -ge 20 && echo "COLLECTED $n" || (echo "ONLY $n"; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reset.py --collect-only` lists >= 20 tests, names matching VALIDATION.md exactly (no collection/import errors).
    - `_make_reset_app` sets both `app.state.reset_token` and `app.state.last_reset_time`.
    - ruff clean on tests/test_reset.py.
    - Tests are RED at this point (handlers absent) — this is expected and correct; do not stub handlers to force green.
  </acceptance_criteria>
  <done>tests/test_reset.py exists with all 20 named tests + _make_reset_app helper; pytest collects all 20 with no import errors; ruff clean. Tests RED pending Plans 02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → /reset/* | A locked-out (or remote) caller reaches the exempt reset routes without a session cookie |
| middleware → router | EXEMPT_PREFIXES decides which paths skip the auth gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-overexpose | Elevation of Privilege | middleware.py EXEMPT_PREFIXES `/reset` | mitigate | Prefix is `/reset` only; no existing/planned route starts with `/reset` outside the three Phase 72 handlers. test_no_other_route_exposed proves GET / still redirects to /login while GET /reset/request is reachable. |
| T-72-redaction (scaffold) | Information Disclosure | reset.html template context | mitigate | Template never receives a `token` context value (D-02); verified by the template grep in Task 2 and by test_token_not_in_response (RED here, GREEN in Plan 02). |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md "Package Legitimacy Audit: not applicable"); all stdlib + already-installed deps. |
</threat_model>

<verification>
- `uv run ruff check triggarr/ tests/` clean.
- `uv run pytest tests/test_reset.py --collect-only -q` lists all 20 tests with no import errors.
- `uv run pytest tests/ -x -q` — existing 984 tests stay green (this plan only adds a helper, a constant pair, an exemption, app.state fields, and a new template; no existing behavior changed). The new test_reset.py tests are RED (expected).
- `from triggarr.web.middleware import EXEMPT_PREFIXES; "/reset" in EXEMPT_PREFIXES` is True.
</verification>

<success_criteria>
- generate_reset_token() exists in auth.py and returns a CSPRNG URL-safe string.
- /reset is in EXEMPT_PREFIXES (RCOV-06 reachability scaffold).
- RESET_REQUEST_RATE_LIMIT_SECONDS=60 and RESET_CONFIRM_RATE_LIMIT_SECONDS=5 in routes.py (RCOV-05 scaffold).
- app.state.reset_token=None and app.state.last_reset_time={} initialized in create_lifespan (RCOV-03/RCOV-05 scaffold).
- triggarr/templates/reset.html minimal shell exists with correct field names + D-20 error context keys, no token echoed.
- tests/test_reset.py collects all 20 tests; existing 984 tests still pass.
</success_criteria>

<output>
Create `.planning/phases/72-password-reset-backend-token-lifecycle/72-01-SUMMARY.md` when done.
</output>
