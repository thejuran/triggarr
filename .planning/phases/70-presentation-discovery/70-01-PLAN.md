---
phase: 70-presentation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/70-presentation-discovery/70-CRITIQUE.md
  - .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md
  - .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md
autonomous: true
requirements: [PDISC-01, PDISC-02, PDISC-03]

must_haves:
  truths:
    - "A cynical r/selfhosted-reader teardown of Triggarr's presentation exists, structured across the three D-08 first-impression lenses, with every gripe citing a specific README section or line"
    - "A codex adversarial pass has actually run against README.md + SECURITY.md + CONTRIBUTING.md, and its findings are captured as a severity/file:line/claim/issue/correction table with a provenance line"
    - "A SeedSyncarr cross-repo divergence audit exists as a signal/Triggarr-state/SeedSyncarr-state/reconciliation table that explicitly records the absent Related Projects section"
    - "Every actionable item across all three artifacts is specific enough for Phase 71 to rewrite against without re-investigation (cited + directional)"
    - "No file outside .planning/phases/70-presentation-discovery/ was modified (discover-don't-fix)"
  artifacts:
    - path: ".planning/phases/70-presentation-discovery/70-CRITIQUE.md"
      provides: "PDISC-01 cynical-reader teardown across the three D-08 lenses"
      contains: "README"
      min_lines: 40
    - path: ".planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md"
      provides: "PDISC-02 codex adversarial-pass findings table + provenance"
      contains: "Severity"
      min_lines: 15
    - path: ".planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md"
      provides: "PDISC-03 SeedSyncarr divergence-reconciliation table"
      contains: "Related Projects"
      min_lines: 20
  key_links:
    - from: "70-CRITIQUE.md"
      to: "README.md"
      via: "section/line citations per gripe"
      pattern: "README"
    - from: "70-CODEX-REVIEW.md"
      to: "README.md / SECURITY.md / CONTRIBUTING.md"
      via: "codex exec read-only pass findings"
      pattern: "codex"
    - from: "70-CONSISTENCY-AUDIT.md"
      to: "~/seedsyncarr README/SECURITY/CONTRIBUTING"
      via: "paired-section divergence diff"
      pattern: "SeedSyncarr"
---

<objective>
Run a hostile reading of Triggarr's public presentation and capture it as three critique/audit artifacts that GATE the Phase 71 rewrite. Three independent discover-don't-fix activities:

1. PDISC-01 — a single cynical "r/selfhosted commenter" teardown of Triggarr's README/presentation, structured across the three D-08 first-impression questions.
2. PDISC-02 — a codex adversarial pass against README.md + SECURITY.md + CONTRIBUTING.md (technical-claims accuracy, broken/incomplete install/quickstart, unsupported assertions), captured as a findings table.
3. PDISC-03 — a same-author cross-repo consistency audit against the sibling SeedSyncarr repo, recorded as a divergence-reconciliation table.

Purpose: These three artifacts ARE Phase 71's PREW-01/03/04/05/07 input. Each actionable item must be specific enough (cited + directional) for Phase 71 to rewrite against without re-investigation (D-03).

Output: Three Markdown files in this phase directory — 70-CRITIQUE.md, 70-CODEX-REVIEW.md, 70-CONSISTENCY-AUDIT.md. NOTHING else. This is discover-don't-fix: no README/SECURITY/CONTRIBUTING/.github/repo-metadata edits, no screenshots, no rewritten copy.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Binding decisions and pinned facts for this phase
@.planning/phases/70-presentation-discovery/70-CONTEXT.md
@.planning/phases/70-presentation-discovery/70-RESEARCH.md
@.planning/phases/70-presentation-discovery/70-VALIDATION.md

# Precedent: the paired code-track discover-don't-fix phase
@.planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md
</context>

<scope_guard>
DISCOVER-DON'T-FIX. The ONLY writes permitted by this plan are the three artifacts in
`.planning/phases/70-presentation-discovery/`. Editing README.md, SECURITY.md, CONTRIBUTING.md,
LICENSE, `.github/` templates, `triggarr/changelog.py`, or any repo file outside this phase
directory is a SCOPE VIOLATION — do not do it. Capturing/replacing screenshots is out of scope
(Phase 71 PREW-02 at the NAS walkthrough). Drafting rewritten copy is out of scope (Phase 71).
This phase CRITIQUES and AUDITS; it does not rewrite. The ROADMAP "UI hint: yes" line is a
false-positive keyword trip (researcher landmine 5b) — there is NO frontend/UI design work; the
outputs are three `.md` files, so no UI-SPEC is needed.
</scope_guard>

<tasks>

<task type="execute">
  <name>Task 1: Write the cynical-reader teardown (70-CRITIQUE.md) — PDISC-01</name>
  <files>.planning/phases/70-presentation-discovery/70-CRITIQUE.md</files>
  <read_first>
    - .planning/phases/70-presentation-discovery/70-CONTEXT.md — D-01, D-03, D-08 (the three-lens structure and the cited-and-actionable bar)
    - .planning/phases/70-presentation-discovery/70-RESEARCH.md — section "PDISC-01: Cynical-Reader Teardown — Pre-Seeded Inputs" (the 8 pre-confirmed gripes with file:line) and "SECURITY.md Divergence from v2.8/v2.8.1 Hardening"
    - README.md (the primary subject — read in full; note the line-6 one-liner, the 2-badge header, the Table-of-Contents-then-Features-then-Screenshots ordering, README:78 Docker first-run claim, README:85 pip-whl `2.7.2` line, README:197 session-rotation mention)
    - SECURITY.md (glance — to judge the "is this trustworthy" lens and note hardening features absent from it)
    - CONTRIBUTING.md (glance — community-health seriousness signal)
    - .github/ISSUE_TEMPLATE/bug-report.yml (version dropdown tops at v2.3; App Type missing Lidarr)
    - docs/screenshots/dashboard.png, history.png, settings.png (note staleness only via timestamp — do NOT recapture)
  </read_first>
  <action>
    Write 70-CRITIQUE.md as a SINGLE cynical "r/selfhosted commenter" persona voice, structured
    under the three D-08 lens headings: (a) "Is this trustworthy / a serious project?", (b) "Can I
    get it running in ~5 minutes?", (c) "Why this over the *arr apps' native search or cron + curl?".
    Under each lens, list the gripes that persona would notice in the first 30 seconds. Every gripe
    MUST cite the specific README section or line it reacts to (e.g. the README:85 pip-whl `2.7.2`
    vs `__version__ 2.8.1` mismatch; the plain `# Triggarr` H1 + 2-badge header with no release/
    version badge and no wordmark/screenshot above the fold; screenshots below ToC+Features; the
    2026-04-14 screenshot staleness vs current v2.8.1; the bug-report.yml version dropdown topping
    at v2.3 and App Type missing Lidarr; the README:78 Docker first-run `sys.exit(1)`/restart
    behavior that a newcomer watching `docker compose logs -f` may read as a crash; the absent
    Related Projects cross-link to SeedSyncarr). Address all 8 pre-confirmed gripes from RESEARCH at
    minimum; add any others found during the read. For each gripe state the recommended direction of
    the fix (so Phase 71 gets an actionable hit-list, not vague vibes) WITHOUT writing the rewritten
    copy. Optionally open with a short provenance line (date, branch, commit). Do NOT edit README.md
    or any source file — this is critique only.
  </action>
  <verify>
    <automated>test -s .planning/phases/70-presentation-discovery/70-CRITIQUE.md && grep -q 'README' .planning/phases/70-presentation-discovery/70-CRITIQUE.md && grep -Eqi 'trustworth|serious project' .planning/phases/70-presentation-discovery/70-CRITIQUE.md && grep -Eqi '5.?min|get it running|quickstart|install' .planning/phases/70-presentation-discovery/70-CRITIQUE.md && grep -Eqi 'why this|native search|cron|curl' .planning/phases/70-presentation-discovery/70-CRITIQUE.md</automated>
    <human-check>Read 70-CRITIQUE.md: confirm all three D-08 lens sections are present, every gripe cites a specific README section/line, and each gripe states a fix direction (not the rewritten copy). Spot-check the README:85 pip-whl `2.7.2` mismatch and the absent Related Projects cross-link are both present.</human-check>
  </verify>
  <acceptance_criteria>
    - `test -s .planning/phases/70-presentation-discovery/70-CRITIQUE.md` (exists, non-empty)
    - File contains all three D-08 lens headings (grep for trust / install-in-5-min / why-this-over-alternatives keywords passes)
    - At least the 8 RESEARCH-pre-confirmed gripes are present, each citing a README section or line
    - `grep -Eqi 'related projects' 70-CRITIQUE.md` passes (the cross-link gap is recorded)
    - `git status --porcelain` shows NO modified/added file outside `.planning/phases/70-presentation-discovery/` (scope guard)
  </acceptance_criteria>
  <done>70-CRITIQUE.md exists, non-empty, covers all three D-08 lenses, every gripe cites a README §/line and states a fix direction, and no file outside the phase directory was touched.</done>
</task>

<task type="execute">
  <name>Task 2: Run the codex adversarial docs pass and finalize 70-CODEX-REVIEW.md — PDISC-02</name>
  <files>.planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md</files>
  <read_first>
    - .planning/phases/70-presentation-discovery/70-CONTEXT.md — D-04, D-05 (codex pass mechanism, in-scope docs, the SEPARATE-from-plan-review distinction)
    - .planning/phases/70-presentation-discovery/70-RESEARCH.md — section "PDISC-02: Codex CLI Invocation (Concrete and Reproducible)" (the pinned `codex exec` flags and framing prompt), "Common Pitfalls" 2/3/4 (generous timeout, do NOT use `codex exec review`, credential scrubbing), and "Assumptions Log" A1/A2 (TTY/approval + ANSI/raw-output caveats)
    - README.md, SECURITY.md, CONTRIBUTING.md (the docs under review — so the executor can sanity-check codex's findings against the cited lines, e.g. the README:85 pip-whl version mismatch and the SECURITY.md hardening gaps)
  </read_first>
  <action>
    Invoke the codex CLI NON-INTERACTIVELY from the repo root (/Users/julianamacbook/triggarr) using
    the form pinned in RESEARCH: `codex exec --sandbox read-only --ephemeral -o <raw-output-path>
    "<framing-prompt>"`. Write raw output to
    `.planning/phases/70-presentation-discovery/70-CODEX-REVIEW-raw.md`. The framing prompt scopes the
    pass to README.md + SECURITY.md + CONTRIBUTING.md and frames it for the three D-05 axes:
    (1) technical claims inaccurate/unsupported by the code, (2) install/quickstart instructions that
    are broken/incomplete/misleading to a first-time user, (3) security-behavior assertions that are
    absent/outdated/misleading — and requires findings as a Markdown table with columns
    Severity | File:Line | Claim/Instruction | Issue Type | Recommended Correction (HIGH = factually
    wrong or setup-breaking, MEDIUM = misleading/outdated, LOW = minor gap), style issues excluded.
    Use the default configured codex model (no `-m` override); if output is thin, retry once with
    `-m o3`. Set a GENEROUS timeout (~10 min) and do NOT kill/retry on 60s of silence — codex is
    processing (pitfall 2). Do NOT use the `codex exec review` subcommand — that reviews code diffs,
    not docs (pitfall 3). After the raw file lands: strip any ANSI escape codes, VERIFY no real API
    key / hostname / credential appears (placeholder strings like `<radarr-api-key>` are safe —
    pitfall 4), prepend a provenance line (codex version `codex-cli 0.133.0`, date, branch, commit),
    and finalize the cleaned content as 70-CODEX-REVIEW.md. Remove the `-raw.md` scratch file (or
    leave it untracked — do NOT commit raw scratch). If codex cannot reach completion in
    non-interactive mode (A1: TTY/approval gate), record the exact failure and the attempted command
    in 70-CODEX-REVIEW.md rather than leaving it empty, so Phase 71 knows the pass state. Do NOT edit
    any doc under review — codex runs `--sandbox read-only` and this task only writes the artifact.
  </action>
  <verify>
    <automated>test -s .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md && grep -Eqi 'severity' .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md && grep -Eqi 'codex' .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md</automated>
    <human-check>Confirm codex actually ran (provenance line present with codex version + date) and the artifact has a findings table with columns Severity · File:Line · Claim/Instruction · Issue Type · Recommended Correction. Spot-check at least one finding against the cited doc:line (e.g. the README pip-whl `2.7.2` version mismatch). Verify no real credential/hostname appears.</human-check>
  </verify>
  <acceptance_criteria>
    - `test -s .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md` (exists, non-empty)
    - File contains a findings table with a Severity column header and the file:line / claim / correction columns
    - File contains a provenance line naming codex (version + date) — proving the pass ran, not a stub
    - `grep -Eq '(sk-[A-Za-z0-9]{20,}|[0-9a-f]{32})' 70-CODEX-REVIEW.md` returns NO match that is a real key (placeholders like `<radarr-api-key>` are acceptable; verify by eye if grep flags the README TOML example)
    - No raw `-raw.md` scratch file is committed; `git status --porcelain` shows NO modified/added file outside `.planning/phases/70-presentation-discovery/` (scope guard — codex ran read-only)
  </acceptance_criteria>
  <done>70-CODEX-REVIEW.md exists, non-empty, contains a populated severity/file:line/claim/issue/correction table plus a codex provenance line, contains no real credentials, and no source doc was modified.</done>
</task>

<task type="execute">
  <name>Task 3: Write the SeedSyncarr cross-repo consistency audit (70-CONSISTENCY-AUDIT.md) — PDISC-03</name>
  <files>.planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md</files>
  <read_first>
    - .planning/phases/70-presentation-discovery/70-CONTEXT.md — D-06, D-07 (audit mechanism, reconciliation-not-homogenization, the seed divergences)
    - .planning/phases/70-presentation-discovery/70-RESEARCH.md — section "PDISC-03: SeedSyncarr Consistency Audit — Pre-Enumerated Divergences" (the 11-row divergence table including the items-8-9 note that Triggarr is AHEAD on security presentation)
    - README.md, SECURITY.md, CONTRIBUTING.md (Triggarr side of each paired comparison)
    - /Users/julianamacbook/seedsyncarr/README.md (sections: Quick Start, Features, How It Works, Installation, Configuration, Screenshots, Related Projects, Contributing, Security, License, Usage Examples; centered `<picture>` wordmark; 4-badge set)
    - /Users/julianamacbook/seedsyncarr/SECURITY.md (email-reporting vs Triggarr's GSVR; no threat model)
    - /Users/julianamacbook/seedsyncarr/CONTRIBUTING.md (structure differences)
    - /Users/julianamacbook/seedsyncarr/docs/superpowers/specs/2026-06-02-launch-hardening-design.md (the intended target framing both repos converge toward)
  </read_first>
  <action>
    Write 70-CONSISTENCY-AUDIT.md as a divergence-reconciliation table with columns
    signal · Triggarr current state · SeedSyncarr current state · recommended reconciliation
    direction. Capture the concrete divergence axes (seed from the RESEARCH 11-row table, verify each
    by reading both repos): header/wordmark (Triggarr plain `# Triggarr` H1 vs SeedSyncarr centered
    `<picture>` wordmark), screenshot position (below ToC+Features vs above the fold), badge set
    (Triggarr 2: CI+Docker vs SeedSyncarr 4: CI+Release+Docker+License — flag the MISSING Release and
    License badges), Docker badge style, section ordering, the absent dedicated Quick Start section,
    the ABSENT `Related Projects` section (explicitly record this same-author cross-link gap — it is
    the highest-signal trivially-fixable item), security reporting path (Triggarr's GSVR is more
    mature — note Triggarr is AHEAD here, SeedSyncarr aligns to Triggarr), SECURITY.md depth
    (Triggarr's threat model is materially stronger — Triggarr ahead), CONTRIBUTING.md structure
    (Triggarr has explicit conventional-commit conventions), and the "what this is" one-liner
    (Triggarr README:6 accurate-but-dry vs SeedSyncarr's benefit-led blockquote). Apply D-07:
    reconciliation of quality signals, NOT forced homogenization — each project keeps its accurate
    identity; for the security signals where Triggarr is ahead, the audit notes SeedSyncarr is the
    one with the gap (its own milestone, out of scope here). Optionally note whether `~/seedsync`
    (the separate smaller repo) warrants a one-line cross-link. Map each row to the Phase 71 PREW item
    it feeds (PREW-01/05/07) so the audit is directly actionable. Do NOT edit either repo's files —
    this is an audit only.
  </action>
  <verify>
    <automated>test -s .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md && grep -qi 'SeedSyncarr' .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md && grep -qi 'Related Projects' .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md && grep -Eqi 'badge|wordmark|one-liner|reconcil' .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md</automated>
    <human-check>Read 70-CONSISTENCY-AUDIT.md: confirm a divergence table with signal · Triggarr state · SeedSyncarr state · recommended reconciliation; confirm the absent Related Projects section is explicitly recorded; confirm the badge-set, header/wordmark, section-ordering, security-path, and one-liner signals are present; confirm the security signals where Triggarr is AHEAD are noted directionally (SeedSyncarr has the gap).</human-check>
  </verify>
  <acceptance_criteria>
    - `test -s .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md` (exists, non-empty)
    - File contains a divergence table with the four columns (signal / Triggarr / SeedSyncarr / reconciliation)
    - `grep -qi 'Related Projects'` passes (the absent cross-link section is explicitly recorded)
    - Badge set, header/wordmark, section ordering, security-reporting path, and "what this is" one-liner all appear as rows
    - The items where Triggarr is AHEAD (GSVR reporting path, SECURITY.md threat-model depth) are noted directionally
    - `git status --porcelain` shows NO modified/added file outside `.planning/phases/70-presentation-discovery/` (scope guard — both repos read-only)
  </acceptance_criteria>
  <done>70-CONSISTENCY-AUDIT.md exists, non-empty, contains the four-column divergence table covering all enumerated axes, explicitly records the missing Related Projects section, notes where Triggarr leads, and modified no repo files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codex CLI → filesystem | codex runs `--sandbox read-only --ephemeral`; it reads docs and emits a findings message. No write access to source, no network-side effect on this repo. |
| codex output → artifact file | The only content crossing into a committed file is codex's findings text, which may quote README snippets (incl. placeholder API-key strings from the TOML example). |

This phase introduces NO runtime code, NO new endpoints, NO user input handling, NO secrets handling, and NO new attack surface. It writes three Markdown files under the phase directory and runs one read-only codex pass. The STRIDE register is therefore minimal and honest.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | codex output / quoted command examples written into 70-CODEX-REVIEW.md (or any artifact) | mitigate | Before finalizing each artifact, scrub/verify no REAL API key, hostname, or credential is embedded. Placeholder strings (`<radarr-api-key>`, `<sonarr-api-key>`) quoted from the README TOML example are safe to include; real values are not. Task 2 acceptance criteria enforces this grep + eyeball check. |
| T-70-02 | Tampering | source docs (README/SECURITY/CONTRIBUTING) and any repo file outside the phase dir | accept (prevented by design) | codex runs `--sandbox read-only`; all three tasks are critique-only; the per-task scope guard (`git status --porcelain` shows no out-of-dir change) is the gate. No mitigation code needed — the discover-don't-fix contract + sandbox flag prevent modification. |
| T-70-03 | Spoofing | — | N/A | No authentication, identity, or session surface is touched by a Markdown-critique phase. |
| T-70-04 | Repudiation | — | N/A | No audit-logged runtime action occurs; artifact provenance lines (codex version/date/commit) provide reproducibility, not a security control. |
| T-70-05 | Denial of Service | — | N/A | No runtime service, request path, or resource-consuming endpoint is added or modified. |
| T-70-06 | Elevation of Privilege | — | N/A | No privilege, role, or permission logic exists in this phase's outputs. |
| T-70-SC | Tampering | npm/pip/cargo installs | N/A | This phase installs NO packages (RESEARCH "Package Legitimacy Audit: Not applicable"). No supply-chain checkpoint required. |
</threat_model>

<verification>
Phase-level gates (per 70-VALIDATION.md):
- All three artifacts exist and are non-empty:
  `test -s .planning/phases/70-presentation-discovery/70-CRITIQUE.md && test -s .planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md && test -s .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md`
- Each artifact is correctly shaped (see per-task `<acceptance_criteria>`): 70-CRITIQUE.md has the three D-08 lenses; 70-CODEX-REVIEW.md has a Severity-columned findings table + codex provenance; 70-CONSISTENCY-AUDIT.md has the four-column divergence table and the Related Projects entry.
- Scope guard: `git status --porcelain` shows NO modified/added file outside `.planning/phases/70-presentation-discovery/` (discover-don't-fix).
- No-regression sanity (this phase touches no Python, so both must stay as they were):
  `uv run pytest tests/ -x -q` green and `uv run ruff check triggarr/ tests/` clean.
- Credential scrub: no real API key/hostname/credential in any artifact (placeholder `<…-api-key>` strings are acceptable).
</verification>

<success_criteria>
- 70-CRITIQUE.md (PDISC-01): cynical-reader teardown across the three D-08 lenses; every gripe cites a README §/line and states a fix direction; the 8 RESEARCH-pre-confirmed gripes are all present.
- 70-CODEX-REVIEW.md (PDISC-02): codex `exec --sandbox read-only` pass ran against README+SECURITY+CONTRIBUTING; findings captured as a severity/file:line/claim/issue/correction table with a codex provenance line; no real credentials embedded.
- 70-CONSISTENCY-AUDIT.md (PDISC-03): four-column SeedSyncarr divergence table covering header/wordmark, screenshot position, badge set, section ordering, Quick Start, the absent Related Projects section, security-reporting path, SECURITY.md depth, CONTRIBUTING structure, and the one-liner; reconciliation-not-homogenization applied; Triggarr-ahead items noted directionally.
- Every actionable item across the three artifacts is specific + cited (D-03) so Phase 71 can rewrite without re-investigation.
- No file outside `.planning/phases/70-presentation-discovery/` was modified.
</success_criteria>

<output>
Create `.planning/phases/70-presentation-discovery/70-01-SUMMARY.md` when done.
</output>
