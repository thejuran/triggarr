---
phase: 49-stats-health-strip
plan: 03
type: execute
wave: 2
depends_on: ["49-01", "49-02"]
files_modified:
  - tests/test_stats_health.py
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04, STATS-05]
must_haves:
  truths:
    - "All STATS requirements have automated test coverage"
    - "Tests pass against the health strip and hero Grab Rate card markup"
  artifacts:
    - path: "tests/test_stats_health.py"
      provides: "HTML structure tests for health strip and stats row"
      contains: "def test_health_strip"
  key_links:
    - from: "tests/test_stats_health.py"
      to: "triggarr/templates/partials/health_summary.html"
      via: "TestClient GET /partials/health-summary"
      pattern: "/partials/health-summary"
    - from: "tests/test_stats_health.py"
      to: "triggarr/templates/partials/stats_row.html"
      via: "TestClient GET / (dashboard includes stats_row partial)"
      pattern: "client.get"
---

<objective>
Create automated tests validating the HTML structure of the health strip and hero Grab Rate card, ensuring all STATS requirements are locked by assertions.

Purpose: Nyquist verification for STATS-01 through STATS-05. Catches regressions in future phases.
Output: tests/test_stats_health.py with 6+ test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-stats-health-strip/49-CONTEXT.md

@tests/test_ui_foundations.py

<interfaces>
<!-- Test pattern from test_ui_foundations.py: -->
<!-- 1. Async fixture `test_app(tmp_path)` builds FastAPI app with mocked state -->
<!-- 2. Sync fixture `client(test_app)` wraps in TestClient -->
<!-- 3. Tests call client.get("/") or client.get("/partials/...") and assert on response.text -->
<!-- 4. Import: from tests.conftest import make_settings -->
<!-- 5. Import: from triggarr.db import init_db, insert_search_entry -->
<!-- 6. Import: from triggarr.web.routes import STATIC_DIR, router -->

<!-- To get per-app rate data, insert search entries with grabbed=1 for multiple app types -->
<!-- insert_search_entry(db, "Radarr", "missing", "Test Movie", grabbed=True) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_stats_health.py with health strip and hero card tests</name>
  <files>tests/test_stats_health.py</files>
  <read_first>
    - tests/test_ui_foundations.py (full file — copy fixture pattern exactly)
    - tests/conftest.py (for make_settings signature)
    - triggarr/db.py (for insert_search_entry signature, especially grabbed parameter)
    - triggarr/templates/partials/health_summary.html (current state after Plan 01)
    - triggarr/templates/partials/stats_row.html (current state after Plan 02)
  </read_first>
  <action>
Create `tests/test_stats_health.py` following the exact fixture pattern from `test_ui_foundations.py`.

**Fixtures:** Copy `test_app` and `client` fixtures from `test_ui_foundations.py` exactly, but with one change: insert multiple search entries with `grabbed=True` so that per-app rates are non-None. Check `insert_search_entry` signature in `triggarr/db.py` to confirm exact parameter names for the grabbed flag (it may be a `grabbed` boolean or a different mechanism — read the function first).

The fixture should insert entries like:
- `insert_search_entry(db, "Radarr", "missing", "Movie A")` — searched but not grabbed
- A grabbed entry for Radarr (check how grab is recorded — it may be via a separate `update_search_entry` or `mark_grabbed` call, or a `grabbed=True` param)
- At least one Sonarr entry (searched + grabbed) to get `sonarr_rate` data

**Test functions (minimum 6):**

1. `test_health_strip_is_not_a_card(client)` — STATS-01
   - `response = client.get("/partials/health-summary")`
   - Assert `"bg-triggarr-card" not in response.text` (strip is NOT a card)
   - Assert `"flex items-center justify-between text-xs mb-4 px-1" in response.text`

2. `test_health_strip_has_colored_dots(client)` — STATS-01
   - `response = client.get("/partials/health-summary")`
   - Assert `"bg-triggarr-green" in response.text` (green connected dot)
   - Assert `"bg-red-500" in response.text` (red disconnected dot)
   - Assert `"bg-triggarr-border" in response.text` (gray pending dot)

3. `test_health_strip_has_last_sync(client)` — STATS-01
   - `response = client.get("/partials/health-summary")`
   - Assert `"Last sync" in response.text`
   - Assert `"font-geist-mono" in response.text`

4. `test_grab_rate_hero_card_layout(client)` — STATS-02
   - `response = client.get("/")`
   - Assert `"md:col-span-2" in response.text`
   - Assert `"text-4xl font-bold" in response.text`
   - Assert `"text-2xl text-triggarr-muted" in response.text` (smaller % suffix)
   - Assert `"bg-gradient-to-br from-triggarr-green/5" in response.text`

5. `test_health_badge_renders(client)` — STATS-03
   - `response = client.get("/")`
   - Assert at least one of `"Healthy"`, `"Warn"`, `"Critical"` appears in response.text (depends on test data rate)
   - Assert `"rounded-full" in response.text` (badge shape)

6. `test_per_app_bars_with_colors(client)` — STATS-04
   - `response = client.get("/")`
   - Assert `"mini-bar" in response.text`
   - Assert `"#fb923c" in response.text` (Radarr orange) — only if test data produces a radarr_rate
   - Assert `"text-orange-400" in response.text` (Radarr label)

7. `test_stat_cards_have_shadow(client)` — STATS-05
   - `response = client.get("/")`
   - Count occurrences of `"shadow-sm"` in response.text
   - Assert count >= 5 (hero + Movies + Episodes + Albums + Time to Grab)

**Important:** Read `triggarr/db.py` carefully to understand how to insert entries that produce non-None per-app rates. The `get_dashboard_stats` function likely requires entries in the search_entries table with specific columns. The test data setup must produce at least one app type with a computable grab rate.

Run `uv run pytest tests/test_stats_health.py -x -q` to verify all tests pass.
Run `uv run pytest tests/ -x -q` to verify no regressions in the full suite.
Run `uv run ruff check tests/test_stats_health.py` to verify lint compliance.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_stats_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_stats_health.py exists
    - tests/test_stats_health.py contains `def test_health_strip_is_not_a_card`
    - tests/test_stats_health.py contains `def test_health_strip_has_colored_dots`
    - tests/test_stats_health.py contains `def test_health_strip_has_last_sync`
    - tests/test_stats_health.py contains `def test_grab_rate_hero_card_layout`
    - tests/test_stats_health.py contains `def test_health_badge_renders`
    - tests/test_stats_health.py contains `def test_per_app_bars_with_colors`
    - tests/test_stats_health.py contains `def test_stat_cards_have_shadow`
    - `uv run pytest tests/test_stats_health.py -x -q` exits 0 with 7 passed
    - `uv run pytest tests/ -x -q` exits 0 (no regressions)
    - `uv run ruff check tests/test_stats_health.py` exits 0
  </acceptance_criteria>
  <done>7 tests covering all 5 STATS requirements pass. Full test suite shows no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test file only — no production trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | N/A | test_stats_health.py | accept | Test-only file, no production impact |
</threat_model>

<verification>
1. `uv run pytest tests/test_stats_health.py -x -q` exits 0 with 7+ passed
2. `uv run pytest tests/ -x -q` exits 0 (no regressions)
3. `uv run ruff check tests/test_stats_health.py` exits 0
</verification>

<success_criteria>
- All 7 test functions pass, covering STATS-01 through STATS-05
- Full test suite passes with no regressions
- Test file follows the exact same fixture pattern as test_ui_foundations.py
</success_criteria>

<output>
After completion, create `.planning/phases/49-stats-health-strip/49-03-SUMMARY.md`
</output>
