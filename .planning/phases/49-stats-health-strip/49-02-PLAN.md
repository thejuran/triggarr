---
phase: 49-stats-health-strip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/templates/partials/stats_row.html
autonomous: true
requirements: [STATS-02, STATS-03, STATS-04, STATS-05]
must_haves:
  truths:
    - "User sees Grab Rate card spanning 2 grid columns with text-4xl headline percentage"
    - "User sees a colored health badge (Healthy/Warn/Critical) thresholded on overall rate"
    - "User sees per-app grab rates as color-coded horizontal mini-bars (orange Radarr, blue Sonarr, green Lidarr)"
    - "User sees shadow-sm elevation on every stat card in the stats row"
  artifacts:
    - path: "triggarr/templates/partials/stats_row.html"
      provides: "Hero Grab Rate card with bars, badge, and shadow-sm on all stat cards"
      contains: "md:col-span-2"
  key_links:
    - from: "triggarr/templates/partials/stats_row.html"
      to: "/partials/stats-row"
      via: "hx-get with id=stats-row preserved"
      pattern: 'id="stats-row"'
    - from: "triggarr/templates/partials/stats_row.html"
      to: "triggarr/static/css/output.css"
      via: "mini-bar CSS class used in bar chart markup"
      pattern: "mini-bar"
---

<objective>
Restructure the stats row with a hero Grab Rate card (2-col span, gradient overlay, text-4xl headline, health badge, per-app color-coded bars) and add shadow-sm elevation to all stat cards.

Purpose: STATS-02 (hero card), STATS-03 (health badge), STATS-04 (per-app bars), STATS-05 (card elevation).
Output: Rebuilt stats_row.html with hero Grab Rate card and elevated stat cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-stats-health-strip/49-CONTEXT.md
@.planning/phases/49-stats-health-strip/49-RESEARCH.md

@triggarr/templates/partials/stats_row.html

<interfaces>
<!-- stats_row.html receives these context variables from partial_stats_row route: -->
<!-- stats.overall_rate: float | None -->
<!-- stats.radarr_rate: float | None -->
<!-- stats.sonarr_rate: float | None -->
<!-- stats.lidarr_rate: float | None -->
<!-- stats.movies_found: int, stats.movies_updated: int -->
<!-- stats.episodes_found: int, stats.episodes_updated: int -->
<!-- stats.albums_found: int, stats.albums_updated: int -->
<!-- time_to_grab: str (formatted duration) -->
<!-- all_instances: list[dict] (for dropdown) -->
<!-- selected_instance: str -->
<!-- instance_app_type: str | None -->

From triggarr/web/routes.py (partial_stats_row context):
```python
context={
    "stats": stats,
    "time_to_grab": time_to_grab,
    "all_instances": all_instances,
    "selected_instance": instance_param or "",
    "instance_app_type": instance_app_type,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure stats_row.html with hero Grab Rate card, health badge, per-app bars, and shadow-sm</name>
  <files>triggarr/templates/partials/stats_row.html</files>
  <read_first>
    - triggarr/templates/partials/stats_row.html
    - .aidesigner/enhanced-mockup-v3.html (lines 214-262 for exact stats row markup)
    - .planning/phases/49-stats-health-strip/49-CONTEXT.md (D-05 through D-19)
  </read_first>
  <action>
Replace the entire content of `stats_row.html` with the restructured stats row. The root `<div id="stats-row">` with its htmx attributes MUST be preserved exactly. The instance filter dropdown (if present in current file) should be kept above the grid.

**Root div** — keep existing htmx attributes unchanged:
```html
<div id="stats-row"
     hx-get="{{ request.url_for('partial_stats_row') }}"
     hx-trigger="every 30s"
     hx-swap="outerHTML"
     hx-include="[name='instance']"
     class="grid grid-cols-2 {% if instance_app_type %}md:grid-cols-3{% else %}md:grid-cols-5{% endif %} gap-3 mb-4">
```

**Hero Grab Rate card** (first child of grid, per D-05 through D-15, mockup L217-243):
- Outer div: `md:col-span-2 bg-triggarr-card rounded-lg border border-triggarr-border p-5 shadow-sm relative overflow-hidden`
- Gradient overlay: `<div class="absolute inset-0 bg-gradient-to-br from-triggarr-green/5 to-transparent pointer-events-none"></div>` (D-06)
- Content wrapper: `<div class="relative">` (so content sits above gradient)
- Header row (`flex items-center justify-between`):
  - Left: `<span class="text-xs uppercase tracking-wide text-triggarr-muted">Grab Rate</span>`
  - Right: Health badge (D-11, D-12, D-13):
    - `{% if stats.overall_rate is not none %}` (no badge when no data, per D-13)
    - `{% if stats.overall_rate >= 70 %}` -> `<span class="text-[10px] px-2 py-0.5 rounded-full bg-triggarr-green/15 text-triggarr-green">Healthy</span>`
    - `{% elif stats.overall_rate >= 40 %}` -> `<span class="text-[10px] px-2 py-0.5 rounded-full bg-amber-500/15 text-amber-400">Warn</span>`
    - `{% else %}` -> `<span class="text-[10px] px-2 py-0.5 rounded-full bg-red-500/15 text-red-400">Critical</span>`
    - `{% endif %}`
- Headline number (D-07): `<p class="text-4xl font-bold mt-2 leading-none">{% if stats.overall_rate is not none %}{{ "%.0f" | format(stats.overall_rate) }}<span class="text-2xl text-triggarr-muted">%</span>{% else %}&mdash;{% endif %}</p>`
- Per-app bars section (`<div class="mt-4 space-y-2">`), one bar per app type with data (D-09, D-10, D-14, D-15):

  For Radarr (only if `stats.radarr_rate is not none`):
  ```html
  {% if stats.radarr_rate is not none %}
  <div class="flex items-center gap-3">
    <span class="text-[11px] w-12 text-orange-400 font-medium">Radarr</span>
    <div class="mini-bar flex-1"><span style="width: {{ "%.0f" | format(stats.radarr_rate) }}%; background: #fb923c"></span></div>
    <span class="text-[11px] text-triggarr-muted w-8 text-right">{{ "%.0f" | format(stats.radarr_rate) }}%</span>
  </div>
  {% endif %}
  ```

  For Sonarr (only if `stats.sonarr_rate is not none`):
  - Label: `text-blue-400`, bar background: `#60a5fa`

  For Lidarr (only if `stats.lidarr_rate is not none`):
  - Label: `text-green-400`, bar background: `#4ade80`

**Movies card** (per D-16, D-17 — add `shadow-sm`):
```html
{% if instance_app_type not in ('sonarr', 'lidarr') %}
<div class="bg-triggarr-card rounded-lg border border-triggarr-border p-4 shadow-sm">
  <span class="text-xs uppercase tracking-wide text-triggarr-muted">Movies</span>
  <p class="text-lg font-bold mt-1">{{ stats.movies_found + stats.movies_updated }}</p>
  <p class="text-xs text-triggarr-muted mt-1">{{ stats.movies_found }} found &middot; {{ stats.movies_updated }} upgraded</p>
</div>
{% endif %}
```

**Episodes card** — same pattern, `shadow-sm` added, hidden when filtered to radarr/lidarr.

**Albums card** — same pattern, `shadow-sm` added, hidden when filtered to radarr/sonarr.

**Time to Grab card** — same pattern, `shadow-sm` added, always visible:
```html
<div class="bg-triggarr-card rounded-lg border border-triggarr-border p-4 shadow-sm">
  <span class="text-xs uppercase tracking-wide text-triggarr-muted">Time to Grab</span>
  <p class="text-lg font-bold mt-1">{{ time_to_grab }}</p>
  <p class="text-xs text-triggarr-muted mt-1">avg search &rarr; grab</p>
</div>
```

Key constraints:
- Root div id, hx-get, hx-trigger, hx-swap, hx-include MUST stay identical (avoid breaking htmx polling)
- Hero card always `md:col-span-2` regardless of instance filter (D-10)
- Per-app bars show all apps with data regardless of instance filter (D-19)
- Bar widths via inline `style="width: N%"` not Tailwind classes (D-15)
- No sparkline (FUT-01, deferred)
- `shadow-sm` on stat cards only, NOT on app/service cards (deferred to Phase 50)
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && grep -c "shadow-sm" triggarr/templates/partials/stats_row.html</automated>
  </verify>
  <acceptance_criteria>
    - stats_row.html contains `md:col-span-2` (hero card spans 2 columns)
    - stats_row.html contains `text-4xl font-bold` (large headline number)
    - stats_row.html contains `text-2xl text-triggarr-muted` (smaller % suffix)
    - stats_row.html contains `bg-gradient-to-br from-triggarr-green/5 to-transparent pointer-events-none` (gradient overlay)
    - stats_row.html contains `bg-triggarr-green/15 text-triggarr-green">Healthy` (healthy badge)
    - stats_row.html contains `bg-amber-500/15 text-amber-400">Warn` (warn badge)
    - stats_row.html contains `bg-red-500/15 text-red-400">Critical` (critical badge)
    - stats_row.html contains `mini-bar flex-1` (per-app bar chart)
    - stats_row.html contains `background: #fb923c` (Radarr orange bar)
    - stats_row.html contains `background: #60a5fa` (Sonarr blue bar)
    - stats_row.html contains `background: #4ade80` (Lidarr green bar)
    - stats_row.html contains `text-orange-400` (Radarr label)
    - stats_row.html contains `text-blue-400` (Sonarr label)
    - stats_row.html contains `text-green-400` (Lidarr label)
    - `grep -c "shadow-sm" triggarr/templates/partials/stats_row.html` returns >= 5 (hero + Movies + Episodes + Albums + Time to Grab)
    - stats_row.html contains `id="stats-row"` (htmx id preserved)
    - stats_row.html contains `hx-trigger="every 30s"` (polling preserved)
    - stats_row.html contains `p-5` on hero card (slightly more padding per D-17)
  </acceptance_criteria>
  <done>Hero Grab Rate card with 2-col span, gradient overlay, text-4xl headline, Healthy/Warn/Critical badge, per-app color-coded mini-bars. All stat cards have shadow-sm elevation. htmx polling and instance filtering preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler -> template | Stats data (rates, counts) passed to Jinja2 template |
| template -> CSS | Inline style width values from server-computed percentages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Injection | stats_row.html inline styles | accept | Width values are server-computed floats formatted with `%.0f`, not user input. No injection vector. |
| T-49-05 | Information Disclosure | stats_row.html | accept | Only displays aggregate grab rates and counts — no API keys, no PII |
| T-49-06 | Tampering | instance filter query param | accept | Existing validation in `partial_stats_row` unchanged — parameter only scopes DB query |
</threat_model>

<verification>
1. `grep "md:col-span-2" triggarr/templates/partials/stats_row.html` returns a match
2. `grep -c "shadow-sm" triggarr/templates/partials/stats_row.html` returns >= 5
3. `grep "mini-bar" triggarr/templates/partials/stats_row.html` returns matches
4. `grep 'id="stats-row"' triggarr/templates/partials/stats_row.html` returns a match
</verification>

<success_criteria>
- Grab Rate card spans 2 columns with text-4xl headline, gradient overlay, and health badge
- Per-app bars render with correct colors (Radarr orange, Sonarr blue, Lidarr green) using mini-bar CSS
- All stat cards (including hero) have shadow-sm elevation
- Instance filter dropdown and card visibility logic unchanged
- htmx polling continues to work (same id, endpoint, trigger, include)
</success_criteria>

<output>
After completion, create `.planning/phases/49-stats-health-strip/49-02-SUMMARY.md`
</output>
