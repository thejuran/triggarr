---
phase: 51-application-log-redesign
plan: 01
type: execute
wave: 1
depends_on: [51-00]
files_modified:
  - triggarr/static/css/input.css
  - triggarr/templates/partials/log_viewer.html
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, LOG-05, LOG-06]

must_haves:
  truths:
    - "Log rows display in Geist Mono with column-aligned timestamp, level, source, message"
    - "TAILING badge with pulsing green dot visible in log header"
    - "ERROR rows have red-tinted background and red left border"
    - "DEBUG rows are dimmed with opacity-60"
    - "Colored source tags appear for Radarr (orange), Sonarr (blue), Lidarr (green)"
    - "Expand button present in header to toggle terminal pane"
    - "Pause button present with onclick='toggleLogPause(this)' and data-pause-btn attribute"
    - "Scanline overlay div present inside log body"
  artifacts:
    - path: "triggarr/static/css/input.css"
      provides: "Terminal pane, scanline, expanded-log CSS classes"
      contains: ".terminal-pane"
    - path: "triggarr/templates/partials/log_viewer.html"
      provides: "Redesigned log viewer partial"
      contains: "TAILING"
  key_links:
    - from: "triggarr/templates/partials/log_viewer.html"
      to: "triggarr/static/css/input.css"
      via: "CSS classes terminal-pane, scanline-overlay, expanded"
      pattern: "terminal-pane|scanline-overlay"
    - from: "triggarr/templates/partials/log_viewer.html"
      to: "triggarr/templates/dashboard.html"
      via: "onclick handlers toggleLogPause(this) and toggleLogExpand() defined in Plan 02"
      pattern: "toggleLogPause|toggleLogExpand"
---

<objective>
Rewrite the Application Log partial template and add supporting CSS for the terminal-pane aesthetic, scanline overlay, and expand/collapse positioning.

Purpose: Transform the basic log list into a column-aligned monospace grid with level-colored rows, per-app source tags, a TAILING indicator, and the CSS foundation for expand/collapse terminal pane behavior.

Output: Rebuilt `log_viewer.html` partial + new CSS classes in `input.css`, rebuilt `output.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-application-log-redesign/51-CONTEXT.md
@.planning/phases/51-application-log-redesign/51-RESEARCH.md

<interfaces>
<!-- Current log_viewer.html is 26 lines, will be fully rewritten -->
<!-- Current input.css has .dot-pulse, .card-hover, .danger-stripes, .mini-bar -->

From triggarr/log_buffer.py:
```python
@dataclass(frozen=True, slots=True)
class LogEntry:
    timestamp: str  # ISO format string e.g. "2026-01-15 10:30:00"
    level: str      # "INFO", "WARNING", "ERROR", "DEBUG"
    message: str    # Already formatted, no timestamp/level prefix
```

From triggarr/web/routes.py L871-879:
```python
@router.get("/partials/log-viewer", response_class=HTMLResponse)
async def partial_log_viewer(request: Request) -> HTMLResponse:
    log_entries = log_buffer.get_recent(30)
    return templates.TemplateResponse(
        request=request,
        name="partials/log_viewer.html",
        context={"log_entries": log_entries},
    )
```

From dashboard.html L32:
```html
<div class="mt-4">
  {% include "partials/log_viewer.html" %}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add terminal pane and scanline CSS to input.css</name>
  <files>triggarr/static/css/input.css</files>
  <read_first>
    - triggarr/static/css/input.css
    - .aidesigner/enhanced-mockup-v3.html (lines 76-122 for CSS reference)
  </read_first>
  <action>
Append the following CSS blocks to the end of `triggarr/static/css/input.css` (after the `.danger-stripes` block):

1. **Scanline keyframes** (per D-07, mockup L76-78):
```css
@keyframes scanline {
  0%   { transform: translateY(-100%); }
  100% { transform: translateY(100%); }
}
```

2. **Terminal pane background** (per D-07 -- #050505 near-black with subtle grid lines):
```css
.terminal-pane {
  background-color: #050505;
  background-image:
    radial-gradient(circle at center, transparent 0%, #000 120%),
    linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px);
  background-size: 100% 100%, 100% 4px;
}
```

3. **Scanline overlay** (per D-07 -- 25% opacity, mix-blend-mode overlay, pointer-events none):
```css
.scanline-overlay {
  position: absolute;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
  mix-blend-mode: overlay;
  opacity: 0.25;
}
.scanline-overlay::before {
  content: '';
  position: absolute;
  inset: -100% 0 0 0;
  background: linear-gradient(to bottom, transparent, rgba(34, 197, 94, 0.15), transparent);
  animation: scanline 8s linear infinite;
}
```

4. **Expanded log pane positioning** (per D-02, D-03 -- fixed bottom, 320px, z-40):
```css
#log-viewer.expanded {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 320px;
  z-index: 40;
  border-radius: 0;
  border-left: none;
  border-right: none;
  border-bottom: none;
  border-top: 1px solid #334155;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
}
#log-viewer.expanded .log-body {
  flex: 1;
  max-height: none;
  padding: 0 1.25rem 1.25rem 1.25rem;
}
body.log-expanded {
  padding-bottom: 320px;
}
```

After writing, rebuild the CSS output:
```bash
uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --minify
```
  </action>
  <verify>
    <automated>grep -c "terminal-pane\|scanline-overlay\|log-viewer.expanded\|@keyframes scanline\|body.log-expanded" triggarr/static/css/input.css | grep -q "5" && echo "PASS: all 5 CSS blocks present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - triggarr/static/css/input.css contains `@keyframes scanline`
    - triggarr/static/css/input.css contains `.terminal-pane` with `background-color: #050505`
    - triggarr/static/css/input.css contains `.scanline-overlay` with `pointer-events: none` and `opacity: 0.25`
    - triggarr/static/css/input.css contains `#log-viewer.expanded` with `position: fixed`, `height: 320px`, `z-index: 40`
    - triggarr/static/css/input.css contains `body.log-expanded` with `padding-bottom: 320px`
    - triggarr/static/css/output.css exists and is newer than input.css after rebuild
  </acceptance_criteria>
  <done>All five CSS blocks (scanline keyframes, terminal-pane, scanline-overlay, expanded pane, body.log-expanded) are in input.css and output.css is rebuilt successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite log_viewer.html with monospace grid, source tags, TAILING, and expand/collapse</name>
  <files>triggarr/templates/partials/log_viewer.html</files>
  <read_first>
    - triggarr/templates/partials/log_viewer.html
    - .aidesigner/enhanced-mockup-v3.html (lines 420-496 for markup reference)
    - triggarr/log_buffer.py (LogEntry dataclass fields)
    - triggarr/web/routes.py (lines 871-879 for template context variables)
    - .planning/phases/51-application-log-redesign/51-CONTEXT.md
    - .planning/phases/51-application-log-redesign/51-RESEARCH.md
  </read_first>
  <action>
Completely rewrite `triggarr/templates/partials/log_viewer.html`. The new template must implement all LOG requirements and honor D-01 through D-10.

**Outer div** -- keep the same `id="log-viewer"` and htmx attributes, add `terminal-pane` class:
```html
<div id="log-viewer"
     hx-get="{{ request.url_for('partial_log_viewer') }}{% if selected_level %}?level={{ selected_level }}{% endif %}"
     hx-trigger="every 5s"
     hx-swap="outerHTML"
     class="terminal-pane rounded-lg border border-triggarr-border p-5 shadow-sm">
```

**Header** (per LOG-02, D-05, D-06, D-02) -- flex row with:
- Left side: "Application Log" h2 + TAILING badge (per mockup L425-428):
  ```html
  <span class="text-[10px] font-geist-mono text-triggarr-green bg-triggarr-green/10 px-1.5 rounded flex items-center gap-1">
    <span class="w-1.5 h-1.5 bg-triggarr-green rounded-full dot-pulse"></span>
    TAILING
  </span>
  ```
- Right side: Level filter `<select>` (per D-06) + Pause button (per D-05) + Expand button (per D-02):
  - Level filter -- a `<select>` styled like a button, with options: All (value=""), ERROR, WARNING, INFO, DEBUG. Selected state from `selected_level` template variable. Uses `htmx.ajax` on change to immediately fetch with filter:
    ```html
    <select onchange="var url='{{ request.url_for('partial_log_viewer') }}' + (this.value ? '?level=' + this.value : ''); var v=this.closest('#log-viewer'); v.setAttribute('hx-get', url); htmx.ajax('GET', url, {target:'#log-viewer', swap:'outerHTML'});"
            class="bg-transparent border border-triggarr-border/40 text-triggarr-muted text-xs rounded px-1.5 py-1 font-geist-mono focus:outline-none focus:border-triggarr-green">
      <option value=""{% if not selected_level %} selected{% endif %}>All</option>
      <option value="ERROR"{% if selected_level == 'ERROR' %} selected{% endif %}>ERROR</option>
      <option value="WARNING"{% if selected_level == 'WARNING' %} selected{% endif %}>WARNING</option>
      <option value="INFO"{% if selected_level == 'INFO' %} selected{% endif %}>INFO</option>
      <option value="DEBUG"{% if selected_level == 'DEBUG' %} selected{% endif %}>DEBUG</option>
    </select>
    ```

  - **Pause button** (per D-05) -- MUST include `onclick="toggleLogPause(this)"` and `data-pause-btn` attribute. Complete markup:
    ```html
    <button type="button" title="Pause stream" data-pause-btn onclick="toggleLogPause(this)"
            class="p-1.5 text-triggarr-muted hover:text-white hover:bg-triggarr-border/40 rounded transition-colors">
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="6" y="4" width="4" height="16"/><rect x="14" y="4" width="4" height="16"/>
      </svg>
    </button>
    ```

  - **Expand button** (per D-02) -- MUST include `onclick="toggleLogExpand()"`. Complete markup:
    ```html
    <button type="button" title="Expand log to terminal" onclick="toggleLogExpand()"
            class="p-1.5 text-triggarr-muted hover:text-white hover:bg-triggarr-border/40 rounded transition-colors">
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <polyline points="15 3 21 3 21 9"/><polyline points="9 21 3 21 3 15"/>
        <line x1="21" y1="3" x2="14" y2="10"/><line x1="3" y1="21" x2="10" y2="14"/>
      </svg>
    </button>
    ```

  CRITICAL: The `onclick="toggleLogPause(this)"` and `data-pause-btn` on the pause button, and `onclick="toggleLogExpand()"` on the expand button, are interface contracts required by Plan 02's JS functions. They MUST be present exactly as shown.

**Log body** (per LOG-01, LOG-03, LOG-04, LOG-05):
```html
<div class="log-body relative max-h-64 overflow-y-auto">
  <div class="scanline-overlay" aria-hidden="true"></div>
  <div class="space-y-0 font-geist-mono text-xs relative">
```

**Each log row** -- render with column alignment (per LOG-01, mockup L446-493). For each `entry` in `log_entries[:30]`:

1. **Source tag extraction** (per D-01) -- inline Jinja2 logic at top of loop:
   ```jinja2
   {% set source = '' %}
   {% set source_color = '' %}
   {% if entry.message.lower().startswith('radarr') %}
     {% set source = 'Radarr' %}
     {% set source_color = 'text-orange-400' %}
   {% elif entry.message.lower().startswith('sonarr') %}
     {% set source = 'Sonarr' %}
     {% set source_color = 'text-blue-400' %}
   {% elif entry.message.lower().startswith('lidarr') %}
     {% set source = 'Lidarr' %}
     {% set source_color = 'text-green-400' %}
   {% endif %}
   ```

2. **Row div** -- apply level-specific styling:
   - Base classes: `flex items-start gap-2 py-1 border-b border-triggarr-border/30`
   - ERROR (per LOG-03): add `bg-red-500/5 border-l-2 border-l-red-500 pl-2 -ml-[2px]`
   - DEBUG (per LOG-03): add `opacity-60`
   - WARNING: no extra classes (standard rendering)
   - INFO: no extra classes

3. **Columns** (per LOG-01):
   - Timestamp: `<span class="text-triggarr-muted whitespace-nowrap">{{ entry.timestamp.split(' ')[-1] if ' ' in entry.timestamp else entry.timestamp }}</span>` (time-only per research recommendation)
   - Level: `<span class="font-medium whitespace-nowrap w-14 shrink-0 {level_color}">{{ entry.level }}</span>` where level_color is: ERROR=`text-red-400`, WARNING=`text-yellow-400`, DEBUG=`text-triggarr-muted`, INFO/default=`text-triggarr-green`
   - Source (per LOG-04): `<span class="{{ source_color }} whitespace-nowrap w-20 shrink-0">{% if source %}[{{ source }}]{% endif %}</span>`
   - Message: `<span class="text-triggarr-text break-all">{{ entry.message }}</span>`

**Empty state** -- keep the "No log messages yet." message but style it in Geist Mono.

The template must NOT contain any `<script>` tags (per anti-pattern from research -- JS inside partial re-executes on every 5s swap). All JS will be added in Plan 02 to dashboard.html.
  </action>
  <verify>
    <automated>grep -c "TAILING\|scanline-overlay\|terminal-pane\|w-14 shrink-0\|w-20 shrink-0\|dot-pulse\|bg-red-500/5\|opacity-60\|text-orange-400\|text-blue-400\|text-green-400\|selected_level\|toggleLogPause\|data-pause-btn\|toggleLogExpand" triggarr/templates/partials/log_viewer.html | xargs test 15 -le && uv run pytest tests/test_log_viewer.py -x -q -k "not level_filter_server_side and not invalid_level" && echo "PASS" || echo "FAIL: missing expected patterns or tests failing"</automated>
  </verify>
  <acceptance_criteria>
    - log_viewer.html contains `id="log-viewer"` with `terminal-pane` class
    - log_viewer.html contains `TAILING` text with `dot-pulse` class nearby
    - log_viewer.html contains `scanline-overlay` div with `aria-hidden="true"`
    - log_viewer.html contains column widths `w-14 shrink-0` (level) and `w-20 shrink-0` (source)
    - log_viewer.html contains `bg-red-500/5` and `border-l-red-500` for ERROR rows
    - log_viewer.html contains `opacity-60` for DEBUG rows
    - log_viewer.html contains `text-orange-400` for Radarr source tags
    - log_viewer.html contains `text-blue-400` for Sonarr source tags
    - log_viewer.html contains `text-green-400` for Lidarr source tags
    - log_viewer.html contains `font-geist-mono` class
    - log_viewer.html contains a `<select>` element with ERROR, WARNING, INFO, DEBUG options
    - log_viewer.html contains pause button with `onclick="toggleLogPause(this)"` AND `data-pause-btn` attribute
    - log_viewer.html contains expand button with `onclick="toggleLogExpand()"`
    - log_viewer.html contains expand button SVG (polyline points="15 3 21 3 21 9")
    - log_viewer.html contains pause button SVG (rect x="6" y="4")
    - log_viewer.html contains `selected_level` template variable references
    - log_viewer.html does NOT contain any `<script>` tags
  </acceptance_criteria>
  <done>log_viewer.html is completely rewritten with monospace grid, TAILING indicator, level-colored rows, source tags, scanline overlay, filter dropdown, pause button with onclick="toggleLogPause(this)" and data-pause-btn, and expand button with onclick="toggleLogExpand()". No script tags in the partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> log viewer route | level query parameter is untrusted user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | level query param | mitigate | Plan 02 Task 1 validates level against VALID_LEVELS = {"ERROR", "WARNING", "INFO", "DEBUG"} -- any other value treated as no filter |
| T-51-02 | Information Disclosure | log messages | accept | Log messages already pass through redacting sink in logging.py L72-84 before reaching buffer -- no change needed |
</threat_model>

<verification>
- `uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --minify` succeeds
- `grep "terminal-pane" triggarr/static/css/input.css` returns matches
- `grep "TAILING" triggarr/templates/partials/log_viewer.html` returns matches
- `grep "scanline-overlay" triggarr/templates/partials/log_viewer.html` returns matches
- `grep "toggleLogPause" triggarr/templates/partials/log_viewer.html` returns matches
- `grep "data-pause-btn" triggarr/templates/partials/log_viewer.html` returns matches
- `grep "toggleLogExpand" triggarr/templates/partials/log_viewer.html` returns matches
- `grep "<script>" triggarr/templates/partials/log_viewer.html` returns NO matches
- `uv run pytest tests/test_log_viewer.py -x -q -k "not level_filter_server_side and not invalid_level"` passes
</verification>

<success_criteria>
- input.css has all 5 new CSS blocks (scanline keyframes, terminal-pane, scanline-overlay, expanded pane, body.log-expanded)
- output.css is rebuilt and contains the new classes
- log_viewer.html renders column-aligned rows with timestamp, level (w-14), source (w-20), message
- TAILING badge with dot-pulse visible in header
- ERROR rows styled with red tint and left border
- DEBUG rows dimmed with opacity-60
- Colored source tags for Radarr/Sonarr/Lidarr
- Expand button with onclick="toggleLogExpand()" present in header
- Pause button with onclick="toggleLogPause(this)" and data-pause-btn attribute present in header
- Level filter select present with All/ERROR/WARNING/INFO/DEBUG options
- No script tags inside the partial template
- Wave 0 tests pass (excluding server-side filter tests that need Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/51-application-log-redesign/51-01-SUMMARY.md`
</output>
