---
phase: 62
slug: activity-rail-log-viewer
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-16
---

# Phase 62 — UI Design Contract

> Visual and interaction contract for the Activity Rail & Log Viewer phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Jinja2 + Tailwind CSS v4) |
| Preset | not applicable |
| Component library | none (hand-rolled Jinja2 partials) |
| Icon library | Phosphor Icons (vendored at `static/vendor/phosphor/`) |
| Font | System sans-serif (body), Geist Mono (monospace elements) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline gaps (`gap-1`), icon-text spacing |
| sm | 8px | Compact element spacing (`gap-2`, `py-2`) |
| md | 16px | Timeline gap between dot and card (`gap-4`) |
| lg | 24px | Rail section padding (`p-6`, `px-6`, `space-y-6`) |

Exceptions: none. All formal spacing uses multiples of 4 from the standard set. Artifact-fidelity overrides (`p-3` = 12px for card internal padding and log container padding; `py-5` = 20px for rail header vertical padding; `gap-3` = 12px for log header icon spacing) are documented inline in the Component Inventory where they appear, not as formal scale tokens.

---

## Typography

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Section title | 13px (`text-[13px]`) | 700 (font-bold) | 1.5 (default) | System sans | "Recent Activity" header, "System Logs" title, activity card media title |
| Body/label | 12px (`text-xs`) | 700 (font-bold) | 1.5 | System sans | Log header title (`tracking-wide`), footer link |
| Badge text | 10px (`text-[10px]`) | 700 (font-bold) | 1.0 | Geist Mono | App badges, outcome pills, timestamps, LIVE label, TAILING badge text |
| Log body | 13px (`text-[13px]`) | 400 (normal) | relaxed (`leading-relaxed`) | Geist Mono | Log row text, timestamp, level tag, message, level filter dropdown |

Weights used: 400 (regular) and 700 (bold). No other weights permitted.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f172a` (`triggarr-bg`) | Page background, rail background, dashed card backgrounds |
| Secondary (30%) | `#1e293b` (`triggarr-card`) | Solid activity cards, timeline dot outer ring, log header bar |
| Accent (10%) | `#22c55e` (`triggarr-primary`) | Grabbed outcome dots, grabbed outcome pills, GRAB log highlight, TAILING badge dot+text, LIVE badge dot+text (override D-08) |
| Destructive | `#ef4444` (`triggarr-danger`) | Failed outcome dots |

### Semantic Colors (Phase 62 specific)

| Color | Hex | Usage |
|-------|-----|-------|
| Grabbed/primary | `#22c55e` (`triggarr-primary`) | Grabbed timeline dot, grabbed pill bg/text/border (`bg-triggarr-primary/10 text-triggarr-primary border-triggarr-primary/20`), GRAB log row highlight (`bg-triggarr-primary/10 border-l-2 border-triggarr-primary`) |
| Searched | `#eab308` (`yellow-500`) | Searched timeline dot, searched pill (`bg-yellow-500/10 text-yellow-500 border-yellow-500/20`) |
| Partial | `#fbbf24` (`amber-400`) | Partial timeline dot |
| Failed | `#ef4444` (`red-500`) | Failed timeline dot |
| Unresolved | `#6b7280` (`gray-500`) | Unresolved timeline dot |
| Sonarr | `#3b82f6` (`triggarr-sonarr`) | Sonarr app badge dot, Sonarr timeline dot (when Sonarr-originated entry in artifact), Sonarr log level tag color |
| Radarr | `#f59e0b` (`triggarr-radarr`) | Radarr app badge dot, Radarr log level tag color |
| LIVE badge | `#22c55e` (`triggarr-green`) | Override from artifact red (`#f87171`) per D-08. Dot + text use green |
| WARN log | `#eab308` (`yellow-500`) | WARN level tag in log rows |
| Log background | `#0b1120` | Log viewer container dark background (artifact `bg-[#0b1120]`) |
| Elevated | `#233346` (`triggarr-elevated`) | Card hover state (`hover:bg-triggarr-elevated`) |

Accent reserved for: Grabbed outcome indicators (timeline dot, pill badge, GRAB log row highlight), TAILING badge, LIVE badge (green override), "View full history" link hover.

Primary focal point: the most recent grabbed activity card (top timeline entry with solid background and green dot).

---

## Component Inventory

### Activity Rail (`activity_rail.html`)

#### Rail Container
- `<aside>` element with `border-l border-triggarr-border` (left border separator, not rounded card)
- `w-80 shrink-0 bg-triggarr-bg` (background matches page, not card color)
- `sticky top-[73px] h-[calc(100vh-73px)]` (sticky below header)
- `overflow-y-auto`

#### Rail Header (sticky)
- `sticky top-0 bg-triggarr-bg/95 backdrop-blur-md z-10`
- Padding: `px-6 py-5` (artifact-fidelity override: `py-5` = 20px, outside formal scale)
- Border: `border-b border-triggarr-border`
- Title: `text-[13px] font-bold uppercase tracking-widest text-triggarr-muted` -- "Recent Activity"
- LIVE badge: `flex items-center gap-1.5`
  - Dot: `w-1.5 h-1.5 rounded-full bg-triggarr-green dot-pulse` (green per D-08, NOT artifact red)
  - Label: `text-[10px] font-mono font-bold text-triggarr-green uppercase tracking-widest` -- "Live"

#### Timeline Structure
- Container: `p-6 relative flex-1`
- Vertical line: `absolute left-[38px] top-6 bottom-6 w-px bg-triggarr-border`
- Entries wrapper: `space-y-6 relative z-10`

#### Timeline Entry (per item)
- Wrapper: `flex gap-4 items-start`
- Opacity classes per D-07:
  - Entries 1-2: no opacity class (100%)
  - Entry 3: `opacity-75`
  - Entry 4+: `opacity-60`

#### Timeline Dot (double-circle, per D-04)
- Outer: `w-7 h-7 rounded-full bg-triggarr-card border-2 border-triggarr-bg flex items-center justify-center shrink-0 z-10 mt-1`
- Inner: `w-2.5 h-2.5 rounded-full` with outcome color (see Color table)

#### Activity Card (per D-01)
- **Grabbed/partial (solid):** `flex-1 min-w-0 bg-triggarr-card border border-triggarr-border rounded-lg p-3 relative` (artifact-fidelity override: `p-3` = 12px, outside formal scale)
  - Hover: `hover:bg-triggarr-elevated transition-colors`
- **Searched/failed/unresolved (dashed):** `flex-1 min-w-0 bg-triggarr-bg border border-triggarr-border border-dashed rounded-lg p-3 relative` (artifact-fidelity override: `p-3` = 12px)

#### Speech Bubble Pointer (per D-03)
- `absolute -left-[5px] top-4 w-2 h-2 rotate-45 transform`
- Solid cards: `bg-triggarr-card border-l border-b border-triggarr-border`
- Dashed cards: `bg-triggarr-bg border-l border-b border-triggarr-border border-dashed`
- Hover-aware (solid cards): use `bg-inherit` so pointer follows hover state

#### App Badge Row (per D-06)
- Container: `flex items-center gap-1.5 mb-1.5`
- App dot: `w-1.5 h-1.5 rounded-full` with app color (`bg-triggarr-radarr` / `bg-triggarr-sonarr` / `bg-triggarr-green` for Lidarr)
- Label: `text-[10px] font-mono text-triggarr-muted uppercase tracking-wider font-bold`
- Content: App name + instance name (e.g., "Radarr Main")

#### Media Title
- `text-[13px] font-bold text-triggarr-text truncate mb-2`

#### Outcome + Timestamp Row
- Container: `flex items-center justify-between`
- Outcome pill: `px-1.5 py-0.5 rounded text-[10px] font-bold uppercase`
  - Grabbed: `bg-triggarr-primary/10 text-triggarr-primary border border-triggarr-primary/20`
  - Searched: `bg-yellow-500/10 text-yellow-500 border border-yellow-500/20`
  - Failed: `bg-red-500/10 text-red-500 border border-red-500/20`
  - Partial: `bg-amber-400/10 text-amber-400 border border-amber-400/20`
  - Unresolved: `bg-gray-500/10 text-gray-500 border border-gray-500/20`
  - Upgraded (artifact variant): `bg-triggarr-sonarr/10 text-triggarr-sonarr border border-triggarr-sonarr/20`
- No SVG icons in pills (text-only, per artifact)
- Timestamp: `text-[10px] font-mono text-triggarr-muted`

#### Rail Footer
- Container: `p-6 border-t border-triggarr-border mt-auto`
- Link: `flex items-center justify-center gap-2 text-[13px] font-bold text-triggarr-muted hover:text-triggarr-text transition-colors group`
- Copy: "View full history"
- Icon: `ph ph-arrow-right` with `group-hover:translate-x-1 transition-transform`

---

### Log Viewer (`log_viewer.html`)

#### Log Container
- Outer: `bg-[#0b1120] border border-triggarr-border rounded-lg overflow-hidden flex flex-col shadow-lg`
- Replaces current `terminal-pane` class with explicit `bg-[#0b1120]`
- Top margin: `mt-2` from previous section

#### Log Header Bar
- Container: `flex items-center justify-between px-4 py-2 border-b border-triggarr-border bg-triggarr-card`
- Left group: `flex items-center gap-3` (artifact-fidelity override: `gap-3` = 12px, outside formal scale)
  - Terminal icon: `ph ph-terminal-window text-triggarr-muted text-lg` (per D-11)
  - Title: `font-bold text-xs text-triggarr-text tracking-wide` -- "System Logs" (per D-11, replaces "Application Log")
  - TAILING badge container: `flex items-center gap-1.5 px-2 py-0.5 rounded bg-triggarr-bg border border-triggarr-border` (per D-13)
    - Dot: `relative w-1.5 h-1.5 rounded-full bg-triggarr-primary pulse-dot` (uses existing `dot-pulse` or artifact's `pulse-dot`)
    - Text: `text-[10px] font-mono font-bold uppercase tracking-widest text-triggarr-primary` -- "Tailing"
- Right group: `flex items-center gap-3` (artifact-fidelity override: `gap-3` = 12px)
  - Level filter: `<select>` with `font-mono bg-triggarr-bg border border-triggarr-border text-[10px] text-triggarr-text rounded px-2 py-1 outline-none` (per D-14)
    - Options: "Level: INFO", "Level: WARN", "Level: ERROR", "Level: DEBUG" (per D-14)
  - Divider: `w-px h-4 bg-triggarr-border` (per D-15)
  - Pause button: `text-triggarr-muted hover:text-triggarr-text transition-colors` with `ph ph-pause text-[15px]` (per D-12)
  - Expand button: `text-triggarr-muted hover:text-triggarr-text transition-colors` with `ph ph-corners-out text-[15px]` (per D-12)

#### Log Body
- Container: `p-3 h-48 overflow-y-auto text-[13px] leading-relaxed font-mono` (artifact-fidelity override: `p-3` = 12px, outside formal scale)
- Replaces current `max-h-64` with `h-48` (per artifact)

#### Log Row (normal)
- Container: `flex gap-4 hover:bg-white/5 px-2 py-0.5 rounded transition-colors group` (per D-18)
- Timestamp: `text-triggarr-muted shrink-0` (format: `14:32:01.402`)
- Level tag: `font-bold w-14 shrink-0` with level-specific color:
  - INFO (Sonarr source): `text-triggarr-sonarr`
  - INFO (Radarr source): `text-triggarr-radarr`
  - WARN: `text-yellow-500`
  - ERROR: `text-red-400`
  - DEBUG: `text-triggarr-muted`
  - Format: `[INFO]`, `[WARN]`, `[ERROR]`, `[DEBUG]`
- Message: `text-triggarr-text truncate group-hover:text-white transition-colors`

#### GRAB Row Highlight (per D-17)
- Container: `flex gap-4 bg-triggarr-primary/10 hover:bg-triggarr-primary/20 px-2 py-0.5 rounded transition-colors group border-l-2 border-triggarr-primary ml-[2px]`
- Timestamp: `text-triggarr-muted shrink-0 ml-[-4px]` (negative margin compensates for border-l offset)
- Level tag: `text-triggarr-primary font-bold w-14 shrink-0` -- displays `[GRAB]`
- Message: `text-triggarr-text truncate group-hover:text-white transition-colors`
- Detection keywords: messages containing "grabbed", "found release", "sent to client", "grab" (case-insensitive)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Rail section title | "Recent Activity" (uppercase via CSS `tracking-widest`) |
| Rail live indicator | "Live" (uppercase via CSS) |
| Rail empty state heading | "No recent activity" |
| Rail empty state body | (none needed -- single line centered text) |
| Rail footer link | "View full history" |
| Log section title | "System Logs" (replaces "Application Log" per D-11) |
| Log tailing indicator | "Tailing" (uppercase via CSS) |
| Log empty state | "No log messages yet." |
| Log filter options | "Level: INFO", "Level: WARN", "Level: ERROR", "Level: DEBUG" (per D-14) |
| GRAB level label | "[GRAB]" (synthetic level for grab-highlighted rows) |
| Pause button tooltip | "Pause stream" |
| Expand button tooltip | "Expand log to terminal" |

No destructive actions in this phase. No error states beyond empty states (data comes from internal state, not user-facing API calls).

---

## Interaction Contract

### Activity Rail
- **Polling:** `hx-get` with `hx-trigger="every 5s"` and `hx-swap="outerHTML"` (existing pattern, preserved)
- **Card hover:** Solid cards (grabbed/partial) show `hover:bg-triggarr-elevated transition-colors`; speech bubble pointer uses `bg-inherit` to follow hover
- **Footer link hover:** Text transitions `text-triggarr-muted` to `text-triggarr-text`; arrow icon translates right 4px (`group-hover:translate-x-1`)
- **Opacity fading:** Position-based via Jinja2 loop index (D-07): entries 1-2 = 100%, entry 3 = 75%, entry 4+ = 60%

### Log Viewer
- **Polling:** `hx-get` with `hx-trigger="every 5s"` and `hx-swap="outerHTML"` (existing pattern, preserved)
- **Pause button:** Calls existing `toggleLogPause()` JS function (wired to new Phosphor icon)
- **Expand button:** Calls existing `toggleLogExpand()` JS function (wired to new Phosphor icon)
- **Level filter:** `onchange` handler updates `hx-get` URL and triggers htmx AJAX call (existing pattern)
- **Row hover:** `hover:bg-white/5` with `group-hover:text-white` on message text
- **GRAB row hover:** `hover:bg-triggarr-primary/20` (intensified green highlight)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Not applicable | N/A | Project uses Jinja2 + Tailwind, not shadcn |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
