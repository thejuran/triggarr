---
phase: 45-community-health-repo-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - SECURITY.md
  - LICENSE
autonomous: true
requirements:
  - COMM-01
  - COMM-02
  - COMM-03
must_haves:
  truths:
    - "A contributor can read CONTRIBUTING.md and find fork/branch/PR workflow, dev setup commands, test/lint commands, and conventional commit conventions"
    - "A security researcher can read SECURITY.md and find supported versions, private vulnerability reporting link, and a summary of every security mechanism"
    - "The repository has an MIT LICENSE file"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Contributor quick-reference guide"
      contains: "uv sync --extra dev"
    - path: "SECURITY.md"
      provides: "Security policy and model summary"
      contains: "SecretStr"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "CLAUDE.md"
      via: "dev commands match"
      pattern: "uv run pytest"
    - from: "SECURITY.md"
      to: "triggarr/models/config.py"
      via: "documents SecretStr usage"
      pattern: "SecretStr"
---

<objective>
Create the three repo-root community health files: CONTRIBUTING.md (contributor guide), SECURITY.md (security policy and model summary), and LICENSE (MIT).

Purpose: Contributors and security researchers visiting the repo find clear, accurate guidance without needing to explore source code.
Output: CONTRIBUTING.md, SECURITY.md, LICENSE at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CLAUDE.md (authoritative source for dev commands -- CONTRIBUTING.md must match exactly)
    - .github/workflows/ci.yml (CI steps that contributors need to pass locally)
  </read_first>
  <action>
Create CONTRIBUTING.md at the repo root with the following structure and content (per D-01 quick-reference style, per D-02 conventional commits):

## Contributing to Triggarr

### Prerequisites

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) package manager

### Getting Started

1. Fork the repo and clone your fork
2. Create a feature branch: `git checkout -b feat/your-feature`
3. Install dependencies: `uv sync --extra dev`

### Development Commands

(Copy exactly from CLAUDE.md):
```bash
uv sync --extra dev                    # install dependencies
uv run pytest tests/ -x -q             # run tests
uv run ruff check triggarr/ tests/     # lint
uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --watch  # dev CSS
docker build -t triggarr:local .       # local Docker build
```

### Before Opening a PR

1. All tests pass: `uv run pytest tests/ -x -q`
2. No lint errors: `uv run ruff check triggarr/ tests/`
3. Docker builds: `docker build -t triggarr:local .`

### Commit Conventions

Use conventional commit prefixes:

- `feat:` -- new feature
- `fix:` -- bug fix
- `docs:` -- documentation only
- `test:` -- adding or updating tests
- `refactor:` -- code change that neither fixes a bug nor adds a feature

### Pull Requests

1. Push your branch and open a PR against `main`
2. Fill out the PR template checklist
3. CI must pass before merge

### Reporting Issues

Use the GitHub issue templates for bug reports and feature requests.
  </action>
  <verify>
    <automated>grep -q "uv sync --extra dev" CONTRIBUTING.md && grep -q "uv run pytest" CONTRIBUTING.md && grep -q "uv run ruff check" CONTRIBUTING.md && grep -q "feat:" CONTRIBUTING.md && grep -q "docker build" CONTRIBUTING.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md exists at repo root
    - Contains "uv sync --extra dev" (dev setup)
    - Contains "uv run pytest tests/ -x -q" (test command)
    - Contains "uv run ruff check triggarr/ tests/" (lint command)
    - Contains "docker build -t triggarr:local ." (Docker build)
    - Contains "feat:" and "fix:" and "docs:" and "test:" (conventional commit prefixes per D-02)
    - Contains "Fork" or "fork" (fork workflow per D-01)
    - Contains "PR" or "pull request" (PR workflow per D-01)
  </acceptance_criteria>
  <done>CONTRIBUTING.md at repo root with fork/branch/PR workflow, exact dev commands from CLAUDE.md, conventional commit conventions, and pre-PR checklist</done>
</task>

<task type="auto">
  <name>Task 2: Create SECURITY.md and LICENSE</name>
  <files>SECURITY.md, LICENSE</files>
  <read_first>
    - triggarr/models/config.py (SecretStr usage for API keys)
    - triggarr/web/middleware.py (CSRF Origin/Referer checking)
    - triggarr/web/validation.py (SSRF validation, integer clamping)
    - triggarr/state.py (atomic write-then-rename for state persistence)
    - triggarr/config.py (atomic TOML write)
    - triggarr/logging.py (loguru redacting sink)
    - triggarr/startup.py (secret collection for redaction filter)
    - Dockerfile (multi-stage build)
    - entrypoint.sh (PUID/PGID, setpriv --no-new-privileges, privilege dropping)
  </read_first>
  <action>
Create two files:

**SECURITY.md** (per COMM-02, COMM-03):

```markdown
# Security Policy

## Supported Versions

| Version | Supported |
|---------|-----------|
| 2.x     | Yes       |
| 1.x     | No        |

## Reporting a Vulnerability

**Do not open a public issue for security vulnerabilities.**

Please use [GitHub's private vulnerability reporting](https://github.com/thejuran/triggarr/security/advisories/new) to report security issues. You will receive a response within 7 days.

## Security Model

Triggarr is a single-process automation daemon that connects to Radarr and Sonarr instances. The following security mechanisms are in place:

### Credential Protection

- **SecretStr for API keys** -- All instance API keys are stored as Pydantic `SecretStr` fields. The secret value is only unwrapped at HTTP client initialization (`get_secret_value()`), never serialized to logs, responses, or HTML.
- **Loguru redaction** -- A custom redacting sink filters all collected API key values from log output before writing. Secrets are collected at startup and passed to the redaction filter.

### Web Security

- **CSRF protection** -- `OriginCheckMiddleware` validates the `Origin` and `Referer` headers on state-changing requests, rejecting cross-origin submissions.
- **SSRF validation** -- URL inputs are validated against an allow-list of schemes (`http`, `https`) and a block-list of internal/metadata hostnames (e.g., `169.254.169.254`, `metadata.google.internal`) to prevent server-side request forgery.
- **Input clamping** -- Integer form values are clamped to safe bounds (minimum/maximum) rather than rejected, preventing overflow or abuse of numeric settings.

### Data Integrity

- **Atomic file writes** -- All config (TOML) and state (JSON) writes use a write-to-tempfile, fsync, then rename pattern. This prevents corruption from interrupted writes or container restarts.

### Container Hardening

- **Multi-stage Docker build** -- Tailwind CSS compilation happens in a builder stage; only the compiled CSS artifact is copied to the production image. Build tools are not present at runtime.
- **PUID/PGID privilege dropping** -- The entrypoint creates a non-root user with the specified UID/GID and drops privileges via `setpriv --reuid/--regid` with `--no-new-privileges` (where supported).
- **Health check** -- A built-in Docker HEALTHCHECK verifies the application is responsive.
```

After writing SECURITY.md, review the content against the actual source files read in read_first. Ensure every mechanism described is accurate -- do not describe capabilities that do not exist in the codebase.

**LICENSE** (per D-03 MIT):

Create a standard MIT License file with:
- Year: 2026
- Copyright holder: "Triggarr Contributors"

The MIT License text is the standard OSI-approved text starting with "MIT License" followed by "Copyright (c) 2026 Triggarr Contributors" and the full permission/warranty text.
  </action>
  <verify>
    <automated>grep -q "SecretStr" SECURITY.md && grep -q "CSRF" SECURITY.md && grep -q "SSRF" SECURITY.md && grep -q "clamping" SECURITY.md && grep -q "Atomic" SECURITY.md && grep -q "PUID" SECURITY.md && grep -q "redact" SECURITY.md && grep -q "private vulnerability reporting" SECURITY.md && grep -q "2.x" SECURITY.md && grep -q "MIT License" LICENSE && grep -q "2026" LICENSE && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md exists at repo root
    - Contains supported versions table with "2.x | Yes" and "1.x | No"
    - Contains GitHub private vulnerability reporting link: "github.com/thejuran/triggarr/security/advisories/new"
    - Contains "SecretStr" (credential protection per COMM-03)
    - Contains "CSRF" (web security per COMM-03)
    - Contains "SSRF" (web security per COMM-03)
    - Contains "clamping" (input clamping per COMM-03)
    - Contains "Atomic" or "atomic" (data integrity per COMM-03)
    - Contains "PUID" (Docker hardening per COMM-03)
    - Contains "redact" (loguru redaction per COMM-03)
    - LICENSE exists at repo root
    - LICENSE contains "MIT License"
    - LICENSE contains "2026"
  </acceptance_criteria>
  <done>SECURITY.md with supported versions table, private vulnerability reporting link, and complete security model summary. LICENSE with MIT text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SECURITY.md content -> public readers | Must not disclose internal paths, config file locations, or implementation details that aid attackers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | SECURITY.md | mitigate | Describe security mechanisms at abstraction level (e.g., "SSRF validation blocks metadata hostnames") without listing exact block-list entries or internal file paths. Review content against source files before committing. |
| T-45-02 | Information Disclosure | CONTRIBUTING.md | accept | Dev commands are already public in CLAUDE.md and CI workflow. No secrets exposed. |
</threat_model>

<verification>
- All three files exist at repo root: CONTRIBUTING.md, SECURITY.md, LICENSE
- Dev commands in CONTRIBUTING.md match CLAUDE.md exactly
- SECURITY.md covers all seven security mechanisms: SecretStr, CSRF, SSRF, input clamping, atomic writes, Docker hardening, loguru redaction
- LICENSE is valid MIT text
</verification>

<success_criteria>
- CONTRIBUTING.md contains fork/branch/PR workflow, dev setup, test/lint commands, conventional commits
- SECURITY.md contains supported versions, vulnerability reporting link, and all security model components
- LICENSE contains MIT License text with correct year
</success_criteria>

<output>
After completion, create `.planning/phases/45-community-health-repo-metadata/45-01-SUMMARY.md`
</output>
