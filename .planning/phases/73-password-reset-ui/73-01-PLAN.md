---
phase: 73-password-reset-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/web/routes.py
  - triggarr/templates/login.html
  - triggarr/templates/reset.html
  - tests/test_reset.py
autonomous: true
requirements: [RCOV-01]

must_haves:
  truths:
    - "D-01: D-01a: The login page shows a 'Forgot password?' link only when auth is already configured (not during first-run setup), centered and muted below the Sign In button, linking to GET /reset/request. login.html is rendered from THREE sites (login_page GET + login_post rate-limit re-render + login_post invalid-credentials re-render); ALL THREE pass needs_setup, and the template guard is defensive ({% if needs_setup is defined and not needs_setup %}) so any future render path that forgets the key fails CLOSED (link hidden)"
    - "D-02a: GET /reset/confirm is reachable unauthenticated and renders the confirm step (token + new password fields); the route inherits the existing exact-or-/reset/ middleware predicate with NO middleware change, and a /resetXYZ-style path stays gated (the exemption did not widen)"
    - "D-02: D-02b: After a successful reset request, the request step replaces the submit button with neutral confirmation text plus an onward link to the confirm step — for BOTH the first-mint path AND the H1 live-token no-op path — never showing the token in any response, header, or template context"
    - "D-04: Both reset steps (request and confirm) offer a 'Back to login' link to GET /login"
    - "D-03: D-03a: Confirm-page token errors render in the top-level generic error banner (no backend change); password errors render inline per-field via the errors dict"
  artifacts:
    - path: "triggarr/web/routes.py"
      provides: "needs_setup context key on ALL THREE login.html render sites (login_page + both login_post re-renders); new GET /reset/confirm route (reset_confirm_page)"
      contains: "async def reset_confirm_page"
    - path: "triggarr/templates/login.html"
      provides: "Conditional 'Forgot password?' link below Sign In, gated on a DEFENSIVE guard ({% if needs_setup is defined and not needs_setup %})"
      contains: "needs_setup is defined and not needs_setup"
    - path: "triggarr/templates/reset.html"
      provides: "message render in request step, onward link to confirm, Back to login on both steps"
      contains: "Back to login"
    - path: "tests/test_reset.py"
      provides: "Phase 73 UI assertions: link shown when configured / absent during setup on GET, POST invalid-credentials re-render, AND POST 429 rate-limit re-render; GET /reset/confirm reachable unauthenticated; /resetXYZ stays gated at the auth boundary (302→/login, never 404); message-state on first-mint AND live-token no-op paths including token-absence at the TemplateResponse CONTEXT boundary"
      contains: "def test_get_reset_confirm_reachable_unauthenticated"
  key_links:
    - from: "triggarr/templates/login.html"
      to: "reset_request_page route"
      via: "request.url_for('reset_request_page') inside the defensive needs_setup guard"
      pattern: "url_for\\(['\"]reset_request_page['\"]\\)"
    - from: "triggarr/web/routes.py login_page + login_post"
      to: "login.html needs_setup branch"
      via: "context key needs_setup derived from the in-scope auth local (auth.needs_setup) at all three render sites"
      pattern: "needs_setup"
    - from: "triggarr/templates/reset.html request step"
      to: "GET /reset/confirm (reset_confirm_page)"
      via: "onward link in the {% if message %} branch"
      pattern: "url_for\\(['\"]reset_confirm_page['\"]\\)"
---

<objective>
Deliver the browser-facing surface of the filesystem-token password recovery flow (RCOV-01). Add the conditional "Forgot password?" link to the login page (gated by a defensive guard and wired at ALL THREE login.html render sites so it never leaks on a POST re-render during first-run setup), register a GET /reset/confirm route so the confirm step is reachable directly, render the neutral request-confirmation state with an onward link (for both the first-mint and the live-token no-op paths), and add "Back to login" cross-navigation on both reset steps. Verify (do not re-plumb) the confirm-page inline field errors and the top-level token-error banner.

Purpose: A locked-out operator must be able to discover and complete recovery entirely from the browser, with reset pages that look and behave like login.html / setup.html. This is the user-facing half of Track A; Phase 72 already shipped the backend contracts this UI consumes.

Output: Two modified templates (login.html, reset.html), edits in routes.py (needs_setup context key at all three login.html render sites + new GET /reset/confirm handler), and new test functions appended to tests/test_reset.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-password-reset-ui/73-CONTEXT.md

<interfaces>
<!-- Locked Phase 72 backend contracts this UI consumes. Executor uses these directly; do NOT alter route logic beyond the edits named in Task 1. This plan is SELF-CONTAINED — every insertion point and class string the executor needs is inlined below or in the per-task <read_first>/<action> sections; there is NO external patterns file. -->

reset.html template context keys (passed by existing Phase 72 routes — do NOT change):
- step: "request" | "confirm"  — which form to render
- error: str | None            — page-level generic token error, e.g. "Invalid or expired reset token" (top banner)
- errors: dict | None          — per-field password errors, keyed "new_password" / "confirm_password"
- message: str | None          — neutral request-confirmation text (mint success OR live-token no-op). NEVER contains the token.

reset_request_post message-branch context shape (VERIFIED against routes.py — both the H1 live-token no-op branch at routes.py:1682-1693 AND the mint branch at routes.py:1727-1738 pass EXACTLY `context={"step": "request", "message": <neutral string>}`). There is NO token-valued key in either message context dict. The token local exists in the function but is NEVER placed into any TemplateResponse context — this is the D-02b / Phase 72 D-02 invariant the Task 2 context-boundary assertions pin.

login.html is rendered from THREE sites in routes.py (verified against source). Each ALREADY has an `auth` local in scope (auth = request.app.state.settings.auth) — reuse it; do NOT re-read app.state:
- login_page GET (routes.py:1235 assigns auth; context dict at routes.py:1254-1257): keys error, info, username, next_url
- login_post rate-limit re-render (routes.py:1318 assigns auth; context dict at routes.py:1335-1340, status_code=429, Retry-After header set after): keys error, info, username, next_url
- login_post invalid-credentials re-render (same function; auth from 1318 still in scope; context dict at routes.py:1374-1379): keys error, info, username, next_url
- needs_setup: bool — NEW key this plan adds at ALL THREE sites (Task 1), value auth.needs_setup.

login.html muted-text idiom to mirror for the new link (VERIFIED at login.html:10-11):
  {% if info %}
  <p class="text-triggarr-muted text-sm text-center mt-4" aria-live="polite">{{ info }}</p>
  {% endif %}
The Sign In button closes at login.html:40 (`</button>`); the form closes at login.html:41 (`</form>`). Insert the link block after `</button>` and before `</form>` (or immediately after `</form>` — discretionary).

Existing route to mirror for the new GET handler (routes.py:1634-1641):
  @router.get("/reset/request", response_class=HTMLResponse)
  async def reset_request_page(request: Request) -> HTMLResponse:
      return templates.TemplateResponse(request=request, name="reset.html", context={"step": "request"})

Middleware exemption (middleware.py:118 — NO change needed, and EXEMPT_PREFIXES is NOT touched): the dispatch predicate is
  path == "/reset" or path.startswith("/reset/") or any(path.startswith(p) for p in EXEMPT_PREFIXES)
EXEMPT_PREFIXES stays exactly ("/health", "/static", "/login", "/setup") (middleware.py:25). /reset/confirm is exempt via startswith("/reset/"). Do NOT add "/reset" to EXEMPT_PREFIXES (that would over-expose /resetXYZ); the exact-or-/reset/ predicate deliberately keeps /resetXYZ gated.

GATED-response shape for a NON-exempt unauthenticated path (verified against middleware.py dispatch, step 7, lines 160-164): when a configured app (needs_setup=False, method="Forms", no session cookie, no api-key) receives a request to a path the exemption does NOT cover (e.g. /resetXYZ), dispatch falls through to the fallback and returns:
- 302 RedirectResponse to "/login?next=..." (with "/login" in the Location header) when the request is a browser request (`Accept: text/html` present — `_is_browser` True), OR
- 401 JSONResponse {"detail": "Authentication required"} for a non-browser request.
It NEVER returns 404 when gated. A 404 would only occur if the path bypassed AuthMiddleware and reached the router (no matching route) — i.e. the exemption widened. Therefore 404 must NOT be treated as a gated/passing status in the regression test below.
`git diff middleware.py` must stay empty.

Confirm-page password error strings ALREADY produced by reset_confirm_post (routes.py:1791-1830) and already rendered by reset.html — verify, do not re-plumb:
- empty new password   -> errors["new_password"] = "New password is required"
- mismatch             -> errors["confirm_password"] = "Passwords do not match"
- over 72 bytes        -> errors["new_password"] = "Password must be 72 characters or fewer"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add needs_setup to all three login.html contexts and register GET /reset/confirm</name>
  <files>triggarr/web/routes.py, tests/test_reset.py</files>
  <read_first>
    - triggarr/web/routes.py:1232-1258 (login_page — `auth` assigned at line 1235; context dict at 1254-1257)
    - triggarr/web/routes.py:1315-1344 (login_post rate-limit re-render — `auth` assigned at line 1318; 429 context dict at 1335-1340; Retry-After set at 1343)
    - triggarr/web/routes.py:1368-1380 (login_post invalid-credentials re-render — same function, `auth` from 1318 still in scope; context dict at 1374-1379)
    - triggarr/web/routes.py:1634-1641 (reset_request_page — exact handler shape to mirror)
    - triggarr/web/middleware.py:110-164 (AuthMiddleware.dispatch — the exact-or-/reset/ exemption predicate at line 118 confirms /reset/confirm is already exempt; do NOT touch; EXEMPT_PREFIXES stays unchanged. Step 7 fallback at lines 160-164 is the GATED response for a non-exempt unauthenticated path: 302→/login for a browser request, 401 JSON otherwise — never 404.)
    - tests/test_reset.py:56-108 (_make_route_app, _make_reset_app, _configured_reset_auth helpers; default AuthConfig() yields needs_setup=True, _configured_reset_auth() yields needs_setup=False)
    - tests/test_reset.py:603-617 (test_reset_routes_unauthenticated — shape to mirror for the GET /reset/confirm reachability test)
    - tests/test_reset.py:620-666 (test_no_other_route_exposed — the Phase 72 locked M2 invariant; note its /resetXYZ assertion accepts 404 because it is a routing-level smoke check, NOT an auth-boundary proof. The new Phase 73 regression test below DELIBERATELY tightens this to assert the auth boundary — see action.)
    - .planning/phases/73-password-reset-ui/73-CONTEXT.md D-01a, D-02a
  </read_first>
  <behavior>
    - GET /reset/confirm with no session cookie (auth active) returns 200 and renders the confirm form (response text contains the "Reset Token" label and a "new_password" field).
    - GET /reset/confirm does NOT widen the auth exemption: a /resetXYZ-style path with no cookie is still blocked by AuthMiddleware AT THE AUTH BOUNDARY. Sending `Accept: text/html`, the request returns 302 with "/login" in the Location header (the verified browser-fallback of dispatch step 7). 404 is NOT an accepted/passing status — a 404 would mean the request bypassed auth and reached the router (no matching route), i.e. the exemption widened, which is the exact failure this test must catch.
    - All three login.html render sites pass needs_setup (verified indirectly via the Task 2 GET-direction tests AND the Task 2 setup-mode POST re-render tests, which prove BOTH the invalid-credentials re-render AND the 429 rate-limit re-render carry needs_setup=True so the link stays hidden).
  </behavior>
  <action>
    Make four edits to triggarr/web/routes.py, then append two tests to tests/test_reset.py.

    Edit (a) — login_page context (routes.py:1254-1257): add the key `needs_setup` with value `auth.needs_setup`. Reuse the existing `auth` local (already `request.app.state.settings.auth` at line 1235). Leave error, info, username, next_url unchanged.

    Edit (b) — login_post rate-limit re-render context (routes.py:1335-1340, the status_code=429 TemplateResponse): add `"needs_setup": auth.needs_setup`. Reuse the existing `auth` local (already `request.app.state.settings.auth` at line 1318). Leave error, info, username, next_url and the status_code/Retry-After handling unchanged.

    Edit (c) — login_post invalid-credentials re-render context (routes.py:1374-1379): add `"needs_setup": auth.needs_setup`. The `auth` local from line 1318 is still in scope (same function). Leave error, info, username, next_url unchanged.

    NOTE on the source-artifact suggestion: the finding text suggested deriving the login_post values from `request.app.state.settings.auth.needs_setup`. The actual code already binds `auth = request.app.state.settings.auth` at routes.py:1318 (verified), so `auth.needs_setup` is the correct in-scope expression at BOTH login_post sites and avoids re-reading app.state (CLAUDE.md convention). The behavioral contract (needs_setup present at all three sites) is unchanged; only the expression form differs.

    Edit (d) — new GET handler: directly after `reset_request_page` (ends line 1641) and before `@router.post("/reset/request")` at line 1644, add an async handler `reset_confirm_page` decorated `@router.get("/reset/confirm", response_class=HTMLResponse)`, returning `templates.TemplateResponse(request=request, name="reset.html", context={"step": "confirm"})`. Mirror reset_request_page exactly — differ only in path (/reset/confirm), function name (reset_confirm_page), step value ("confirm"), and docstring (note it is unauthenticated, exempt via the exact-or-/reset/ predicate — NOT via EXEMPT_PREFIXES). Keep GET handlers grouped together. Do NOT modify middleware.py — the exemption already covers this path.

    Tests — append two functions to tests/test_reset.py:
    1. `test_get_reset_confirm_reachable_unauthenticated(tmp_path)` — mirror test_reset_routes_unauthenticated: build via _make_reset_app(auth_config=_configured_reset_auth(), config_path=tmp_path / "triggarr.toml"); set auth_state["active"]=True, auth_state["method"]="Forms"; TestClient(app, follow_redirects=False); GET /reset/confirm. Assert status 200 and that the confirm form rendered (e.g. "Reset Token" in resp.text). Use _make_reset_app (not _make_route_app) so app.state.reset_token / last_reset_time are initialized.
    2. `test_reset_confirm_route_did_not_widen_exemption(tmp_path)` — focused proof the new route did not loosen the gate, asserting the AUTH BOUNDARY (not the routing outcome): build the same configured _make_reset_app, set auth_state active/Forms, TestClient(app, follow_redirects=False). Issue GET /resetXYZ WITH a browser Accept header — `client.get("/resetXYZ", headers={"Accept": "text/html"})` — so dispatch step 7 takes the browser-redirect branch. Assert `resp.status_code == 302` AND `"/login" in resp.headers.get("location", "")` — proving AuthMiddleware blocked the request at the auth boundary (the exact-or-/reset/ predicate did NOT widen to a bare /reset prefix). Add an explicit comment in the test that 404 must NOT be treated as passing: a 404 would mean the request bypassed auth and reached the router, i.e. the very widening failure this test guards against. Do NOT include 404 (or 200) in the accepted statuses. (Verified against middleware.py: a gated /resetXYZ returns 302→/login for a browser request, or 401 for a non-browser request — never 404. The 302→/login form is asserted here because it pins the browser fallback deterministically and matches the existing GET / redirect idiom in test_no_other_route_exposed.)
  </action>
  <verify>
    <automated>uv run pytest tests/test_reset.py::test_get_reset_confirm_reachable_unauthenticated tests/test_reset.py::test_reset_confirm_route_did_not_widen_exemption tests/test_reset.py::test_no_other_route_exposed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'async def reset_confirm_page' triggarr/web/routes.py` returns a match decorated with `@router.get("/reset/confirm"`.
    - Source: `grep -c 'needs_setup' triggarr/web/routes.py` shows `"needs_setup": auth.needs_setup` added at all THREE login.html context dicts (login_page + both login_post re-renders). Verify each of the three TemplateResponse(name="login.html", ...) contexts contains the key.
    - Source: `git diff triggarr/web/middleware.py` is empty (no middleware change); EXEMPT_PREFIXES is unchanged and `/reset` is NOT added to it.
    - Source: in test_reset_confirm_route_did_not_widen_exemption, the asserted statuses are EXACTLY `status_code == 302` plus `"/login" in location` — 404 and 200 are NOT in any accepted-status set (a 404-as-pass would mask the exemption-widening this test guards against).
    - Behavior: `uv run pytest tests/test_reset.py::test_get_reset_confirm_reachable_unauthenticated tests/test_reset.py::test_reset_confirm_route_did_not_widen_exemption tests/test_reset.py::test_no_other_route_exposed -x -q` exits 0.
    - Behavior: GET /reset/confirm unauthenticated returns 200; GET /resetXYZ unauthenticated (browser Accept) is blocked at the auth boundary — 302 with /login in Location, never 404 and never 200.
  </acceptance_criteria>
  <done>GET /reset/confirm renders the confirm step unauthenticated (200); all three login.html contexts emit needs_setup=auth.needs_setup; /resetXYZ stays gated at the auth boundary (302→/login, 404 explicitly NOT a pass); no middleware change; the new tests and existing reset tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional "Forgot password?" link (defensive guard) + reset.html message-state, onward link, and Back-to-login</name>
  <files>triggarr/templates/login.html, triggarr/templates/reset.html, tests/test_reset.py</files>
  <read_first>
    - triggarr/templates/login.html:10-41 (the info/error muted-text idiom — `<p class="text-triggarr-muted text-sm text-center mt-4">` at line 11 — and the Sign In `</button>` at line 40 / `</form>` at line 41 to insert the link block after)
    - triggarr/templates/reset.html:14-62 (the step=="request" block to extend, the step=="confirm" per-field errors already present, the closing card div)
    - triggarr/templates/setup.html:65-68 (the block-level anchor idiom to adapt for the onward link — render as muted secondary link, not green button)
    - triggarr/web/routes.py:1682-1693 (reset_request_post H1 no-op message branch — context is EXACTLY {"step": "request", "message": ...}, NO token key) and routes.py:1727-1738 (mint message branch — same EXACT shape). These are the source-of-truth context dicts the message-state context-boundary assertion (test 4) pins.
    - .planning/phases/73-password-reset-ui/73-CONTEXT.md D-01, D-01a, D-02, D-02b, D-03, D-03a, D-04
    - tests/test_reset.py:148-168 (test_token_not_in_response — structural shape for the message-state token-absence assertion against body + headers)
    - tests/test_reset.py:210-225 (test_live_token_request_is_noop — pre-seed a LIVE token to exercise the H1 no-op path for the second mandatory message-state test)
    - tests/test_reset.py:603-617 (configured-app GET /login shape via _make_reset_app)
  </read_first>
  <behavior>
    - GET /login with a configured (needs_setup=False) app: response text contains "Forgot password?".
    - GET /login with a default (needs_setup=True) app: response text does NOT contain "Forgot password?".
    - POST /login that re-renders via the INVALID-CREDENTIALS path during first-run setup (needs_setup=True): response text does NOT contain "Forgot password?" — the link must not leak on a POST re-render before auth is configured.
    - POST /login that re-renders via the 429 RATE-LIMIT path during first-run setup (needs_setup=True): response text does NOT contain "Forgot password?" — the link must not leak on the third (429) render site before auth is configured.
    - POST /reset/request first-mint on a configured app: response text contains the onward confirm link (href to /reset/confirm) and the neutral message; the "Request Reset Token" submit button is ABSENT alongside the confirmation; the token value appears in NEITHER the body NOR any header NOR the rendered TemplateResponse context.
    - POST /reset/request when a LIVE token already exists (H1 no-op): same message-state contract — neutral message + onward confirm link present, "Request Reset Token" button absent, token in neither body, header, nor context.
  </behavior>
  <action>
    Three template-side edits plus five appended tests.

    login.html — after the Sign In `</button>` (line 40) and before `</form>` (line 41) (or immediately after `</form>` per discretion), add a DEFENSIVE guard block `{% if needs_setup is defined and not needs_setup %}` containing a centered muted `<p class="text-triggarr-muted text-sm text-center mt-4">` (mirror the info-paragraph idiom at login.html:11, minus aria-live since this is static nav) wrapping `<a href="{{ request.url_for('reset_request_page') }}" class="hover:underline">Forgot password?</a>`. The guard MUST be `needs_setup is defined and not needs_setup` (NOT a bare `not needs_setup`) so any future render path that omits the key fails CLOSED (link hidden). This implements D-01 / D-01a.

    reset.html — message state (D-02 / D-02b): inside the `{% if step == "request" %}` branch, add a template-internal `{% if message %} ... {% else %} ...existing request form... {% endif %}` branch. When `message` is present: render NO "Request Reset Token" submit button; instead show the neutral `{{ message }}` text (centered muted, aria-live="polite") followed by an onward link to the confirm step: `<a href="{{ request.url_for('reset_confirm_page') }}" ...>Enter reset token</a>` styled as a secondary muted link (adapt the setup.html block anchor idiom but muted, not green). When `message` is absent: render the existing request `<p>` + `<form>` + button unchanged. NEVER reference or render a token variable — only `message`. Exact onward-link copy ("Enter reset token") is discretionary.

    reset.html — Back to login (D-04): at the bottom of the card (after the step branches, before the closing `</div>` on line 63), add a centered muted `<p>` with `<a href="{{ request.url_for('login_page') }}" class="hover:underline">Back to login</a>`, rendered for BOTH steps. This is new cross-nav (no existing analog) — use the muted-link idiom from login.html:11, drop aria-live.

    reset.html — confirm errors (D-03 / D-03a): VERIFY ONLY. The generic top banner `{% if error %}` (lines 10-12) and the per-field `errors.get("new_password")` / `errors.get("confirm_password")` blocks (lines 43-45, 52-54) already exist and are correct. Do NOT add new error plumbing; confirm they remain intact after your edits.

    Tests — append to tests/test_reset.py (ALL FIVE mandatory):
    1. `test_forgot_password_link_shown_when_configured(tmp_path)` — build via _make_reset_app(auth_config=_configured_reset_auth(), config_path=tmp_path / "triggarr.toml"), set auth_state active/Forms, GET /login, assert "Forgot password?" in resp.text.
    2. `test_forgot_password_link_absent_during_setup_get(tmp_path)` — build via _make_reset_app() with NO auth_config (default AuthConfig() → needs_setup=True; default app has active=False so /login is reachable), GET /login, assert "Forgot password?" not in resp.text.
    3. `test_forgot_password_link_absent_during_setup_post(tmp_path)` — build via _make_reset_app() with NO auth_config (needs_setup=True), TestClient(follow_redirects=False), POST /login with deliberately wrong credentials (data={"username": "x", "password": "wrong"}) so the INVALID-CREDENTIALS re-render fires (/login is in EXEMPT_PREFIXES, so the POST passes the middleware even during setup). Assert the response re-rendered login.html (e.g. "Sign In" in resp.text) AND "Forgot password?" not in resp.text — proving the link does not leak on a POST invalid-credentials re-render during first-run setup.
    4. `test_forgot_password_link_absent_during_setup_rate_limit(tmp_path)` — MANDATORY (Finding 3): proves the THIRD login.html render site (the 429 rate-limit re-render at routes.py:1335-1340) carries needs_setup=True during setup so the link stays hidden. Import `_record_failure` from triggarr.web.routes at the top of the file (alongside the existing `from triggarr.web.routes import auth_state, router`). Build via _make_reset_app() with NO auth_config (needs_setup=True). The TestClient resolves request.client.host to the literal "testclient"; deterministically push that IP over the limit by calling `_record_failure("testclient")` ten times (loop `for _ in range(10)`), matching `_MAX_ATTEMPTS=10` (verified at routes.py:1269). The conftest.py autouse `_reset_rate_limit_state` fixture clears `_login_failures` before/after each test (verified at tests/conftest.py:21-26), so this seeding does not leak. TestClient(app, follow_redirects=False), POST /login with any credentials (data={"username": "x", "password": "wrong"}) — the rate-limit check at routes.py:1329-1344 fires BEFORE credential verification, returning the 429 re-render. Assert `resp.status_code == 429` (proves the 429 render path was taken, not the invalid-credentials path) AND the response re-rendered login.html (e.g. "Sign In" in resp.text) AND "Forgot password?" not in resp.text — proving the 429 re-render carries needs_setup=True so the recovery link stays hidden during setup. Optionally also assert the inverse: with a configured app (_configured_reset_auth() → needs_setup=False), the same seeded-429 re-render DOES contain "Forgot password?" (the link is SHOWN on the 429 path when auth is configured).
    5. `test_request_confirmation_message_state(tmp_path)` — MANDATORY (Finding 2), covers BOTH paths plus the CONTEXT-boundary token-absence proof (split into two functions if clearer):
       (i) First-mint path: configured _make_reset_app, POST /reset/request, capture the minted token from app.state.reset_token[0]. Assert: the neutral confirmation message text renders (a non-empty message string present in resp.text); an onward link with `href` to `/reset/confirm` is present (assert `'/reset/confirm' in resp.text` or the url_for target); the "Request Reset Token" submit-button text is ABSENT from resp.text; the token value is in NEITHER resp.text NOR str(dict(resp.headers)).
       (ii) H1 live-token no-op path: configured _make_reset_app, pre-seed app.state.reset_token = ("livetoken_abc123xyz", time.monotonic() + 900) and app.state.last_reset_time = {} (mirror test_live_token_request_is_noop), POST /reset/request. Assert the SAME message-state contract: neutral message renders, onward `/reset/confirm` link present, "Request Reset Token" button absent, the live token value in NEITHER resp.text NOR str(dict(resp.headers)).
       (iii) CONTEXT-boundary token-absence (Finding 2 — the real invariant is "token NEVER appears in any template/response CONTEXT", not just body/headers): add an EXACT SOURCE assertion (option b) on reset_request_post, since the rendered context is not exposed via TestClient. Read the routes.py source text (e.g. `Path("triggarr/web/routes.py").read_text()` or `inspect.getsource(reset_request_post)`) and assert that BOTH message-branch `context=` dicts (the H1 no-op branch at ~routes.py:1685-1692 and the mint branch at ~routes.py:1730-1737) contain ONLY the keys `"step"` and `"message"` — i.e. assert the source contains `"step": "request"` and `"message":` within each message branch, and assert the token local name (`token`) does NOT appear as a context VALUE in either message-branch dict (e.g. assert there is no `: token` binding inside the message context dicts — the only `token` reference in the function body is the mint local and the log/file sinks, never a context value). State in the acceptance criteria exactly what is asserted: the two reset_request_post message-branch context dicts are keyed exactly {step, message} with no token-valued key, matching the verified source shape (routes.py:1685-1692 and 1730-1737, both `context={"step": "request", "message": ...}`).
    All other helpers/imports (time, _make_reset_app, _configured_reset_auth, auth_state, TestClient) already exist in test_reset.py — only `_record_failure` is a new import (test 4).
  </action>
  <verify>
    <automated>uv run pytest tests/test_reset.py -k "forgot_password or reset_confirm_reachable or request_confirmation_message" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "Forgot password?" triggarr/templates/login.html` returns a match inside a `{% if needs_setup is defined and not needs_setup %}` block linking to `reset_request_page` (the guard is the DEFENSIVE form, not a bare `not needs_setup`).
    - Source: `grep -n "needs_setup is defined and not needs_setup" triggarr/templates/login.html` returns exactly the link guard.
    - Source: `grep -n "Back to login" triggarr/templates/reset.html` returns a match linking to `login_page` (present for both steps).
    - Source: `grep -n "reset_confirm_page" triggarr/templates/reset.html` returns the onward link in the message branch of the request step.
    - Source: reset.html message branch references only `{{ message }}` — `grep -n "token" triggarr/templates/reset.html` shows only the confirm-step input field/label, never a rendered token value.
    - Source/Behavior (Finding 2 — CONTEXT boundary): test_request_confirmation_message_state asserts BOTH reset_request_post message-branch `context=` dicts are keyed exactly {"step", "message"} with NO token-valued key (an exact source assertion over routes.py:1685-1692 and 1730-1737), in addition to asserting the token is absent from resp.text and from str(dict(resp.headers)) on both the first-mint and H1 no-op paths.
    - Source/Behavior (Finding 3 — 429 site): test_forgot_password_link_absent_during_setup_rate_limit seeds `_record_failure("testclient")` x10, asserts the POST /login response is `status_code == 429` (the rate-limit render path) AND "Forgot password?" not in resp.text during setup (needs_setup=True), proving the third login.html render site carries needs_setup.
    - Behavior: `uv run pytest tests/test_reset.py -k "forgot_password or reset_confirm_reachable or request_confirmation_message" -x -q` exits 0 — covering link shown (GET configured), link absent (GET setup), link absent (POST invalid-credentials setup re-render), link absent (POST 429 rate-limit setup re-render), and message-state on BOTH first-mint and live-token no-op paths including the context-boundary token-absence proof.
  </acceptance_criteria>
  <done>login.html shows the conditional Forgot-password link under a defensive guard and never leaks it on EITHER POST re-render path (invalid-credentials AND 429 rate-limit) during setup; reset.html renders the neutral message + onward link in the request step for both mint and no-op paths, a Back-to-login link on both steps, preserves the existing error banner and per-field errors, and never surfaces a token in body, header, OR template context (proven by the exact source assertion); all five new template tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full reset suite green + ruff clean</name>
  <files>tests/test_reset.py</files>
  <read_first>
    - tests/test_reset.py (full file — confirm the appended Phase 73 tests sit alongside the Phase 72 suite and follow the established pytest-asyncio layout)
    - triggarr/web/routes.py (the Task 1 edits)
    - triggarr/templates/login.html, triggarr/templates/reset.html (the Task 2 edits)
    - CLAUDE.md (ruff config: E, F, I, UP, B, SIM; line length 120)
  </read_first>
  <action>
    Run the full reset test suite and the linter to prove Phase 73 changes did not regress Phase 72 behavior. Run `uv run pytest tests/test_reset.py -x -q` — all Phase 72 tests (token redaction, single-use, TTL, rate-limit, session rotation, unauthenticated reachability, no-other-route-exposed) plus the new Phase 73 tests (confirm reachability, /resetXYZ-stays-gated-at-the-auth-boundary, forgot-password shown/absent on GET, on POST invalid-credentials re-render, AND on POST 429 rate-limit re-render, message-state on mint and no-op paths including the context-boundary token-absence proof) must pass. Run `uv run ruff check triggarr/ tests/` — must be clean (note the new `_record_failure` import in test_reset.py must be used, or ruff F401 will fail). If any failure traces to the Task 1/Task 2 edits, fix the edit (not the existing Phase 72 tests, which encode locked contracts). Do NOT add tests here beyond what Tasks 1-2 created; this task is a green-gate, not new coverage. If a pre-existing failure unrelated to this phase surfaces, report it rather than masking it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reset.py -x -q && uv run ruff check triggarr/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run pytest tests/test_reset.py -x -q` exits 0 with all reset tests passing (Phase 72 + the new Phase 73 tests).
    - CLI: `uv run ruff check triggarr/ tests/` exits 0 (no violations, including no unused-import F401 for the new `_record_failure` import).
    - Source: `git diff --stat` shows changes confined to routes.py, login.html, reset.html, test_reset.py — no edits to middleware.py or any Phase 72 backend logic.
  </acceptance_criteria>
  <done>Entire reset suite passes; ruff is clean; the diff is confined to the four named files; no Phase 72 contract was altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → /login | First-run and locked-out users render the login page before any session exists; rendered on GET and on POST re-render (rate-limit 429, invalid-credentials) |
| unauthenticated browser → /reset/* | Recovery routes are deliberately reachable without a session (exempt via the exact-or-/reset/ predicate); untrusted input crosses here |
| route handler → reset.html / login.html context | Server-rendered Jinja under the global CSP nonce; whatever lands in context is reflected to an unauthenticated client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information Disclosure | reset.html message/onward-link branch + reset_request_post message context | mitigate | The request-confirmation branch renders ONLY `{{ message }}` (a fixed neutral string) — the plan forbids referencing any token-valued context variable in markup (Task 2 acceptance: grep shows no rendered token; message branch references only `message`). Defense extends to the CONTEXT boundary: Task 2 test 5(iii) adds an EXACT SOURCE assertion that BOTH reset_request_post message-branch `context=` dicts are keyed exactly {"step", "message"} with NO token-valued key (verified shape routes.py:1685-1692 and 1730-1737) — so a regression that injected the token into the TemplateResponse context (even if the template happened not to render it) is caught. Both message-state tests (first-mint AND H1 live-token no-op) also assert the token appears in NEITHER body NOR any header. Inherits Phase 72 D-02 (token never enters any response/header/context). test_token_not_in_response stays green (Task 3). |
| T-73-02 | Elevation of Privilege | new GET /reset/confirm route | mitigate | The route is reachable unauthenticated by design (inherits the exact-or-/reset/ predicate) but the plan makes NO middleware change and does NOT add `/reset` to EXEMPT_PREFIXES — `git diff middleware.py` must be empty (Task 1 + Task 3 acceptance). The new test_reset_confirm_route_did_not_widen_exemption asserts the AUTH BOUNDARY, not the routing outcome: GET /resetXYZ (browser Accept) returns 302 with /login in the Location header (verified dispatch step-7 browser fallback) — 404 is explicitly NOT a passing status, because a 404 would mean the request bypassed AuthMiddleware and reached the router, i.e. the exact widening this threat guards against. The existing Phase 72 test_no_other_route_exposed corroborates the exact-or-/reset/ predicate at the routing level (it accepts 404 as a smoke check); the new Phase 73 test deliberately tightens to the auth boundary so a bypass cannot pass as a 404. |
| T-73-03 | Information Disclosure | login.html "Forgot password?" link (GET + BOTH POST re-render paths: 429 rate-limit AND invalid-credentials) | mitigate | The link is wrapped in a DEFENSIVE guard `{% if needs_setup is defined and not needs_setup %}` so it is absent during first-run setup AND fails closed if any render path omits the key. All THREE login.html render sites pass needs_setup. Enforced by test_forgot_password_link_absent_during_setup_get (GET), test_forgot_password_link_absent_during_setup_post (POST invalid-credentials re-render during setup), AND test_forgot_password_link_absent_during_setup_rate_limit (POST 429 rate-limit re-render during setup — the third render site, seeded via _record_failure("testclient")x10 to force the 429 path), which together prove the link never advertises a recovery surface before auth is configured across ALL THREE render sites — including on a failed POST and on a rate-limited POST during first-run setup. |
| T-73-04 | Tampering | confirm-page password/token error rendering | accept | Token failures stay in the page-level generic banner; password failures stay inline per-field — both rendered from the locked Phase 72 `error`/`errors` contracts with no backend change. Generic token error reveals nothing about why a token was rejected (no oracle). Phase 73 only verifies the existing rendering (D-03/D-03a); rationale: no new attack surface, server-side validation unchanged. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure template + route + test edits against existing dependencies. No legitimacy audit required; nothing to mitigate. |

No high-severity (mitigate-required) threat is left unmitigated: T-73-01 (token leak), T-73-02 (exemption widening), and T-73-03 (link leak on a POST re-render during setup) are the adversarial-weight invariants from the security gate, and each carries a concrete, test-backed mitigation. T-73-01 now pins the token-absence invariant at the CONTEXT boundary (exact source assertion over both message-branch context dicts), not just body/headers. T-73-02 is proven at the auth boundary (302→/login), not the routing outcome, so a bypass cannot masquerade as a passing 404. T-73-03 now covers all THREE login render sites (GET + invalid-credentials POST + 429 rate-limit POST).
</threat_model>

<verification>
- `uv run pytest tests/test_reset.py -x -q` — full reset suite green (Phase 72 contracts + new Phase 73 tests).
- `uv run ruff check triggarr/ tests/` — clean (including the new `_record_failure` import being used).
- `grep -n "Forgot password?" triggarr/templates/login.html` — link present, gated on the defensive `needs_setup is defined and not needs_setup`.
- `grep -c 'needs_setup' triggarr/web/routes.py` — needs_setup present at all three login.html contexts.
- `grep -n "reset_confirm_page" triggarr/web/routes.py triggarr/templates/reset.html` — route registered and onward link wired.
- `git diff triggarr/web/middleware.py` — empty (no exemption change; EXEMPT_PREFIXES unchanged).
- `grep -n "Back to login" triggarr/templates/reset.html` — present on both steps.
</verification>

<success_criteria>
1. RCOV-01: The login page shows a "Forgot password?" link only when `needs_setup is defined and not needs_setup`; absent during first-run setup on the GET path AND on BOTH POST re-render paths — invalid-credentials AND 429 rate-limit (test-proven all three render sites). All three login.html render sites pass needs_setup.
2. Following that link reaches a reset-request page styled like login/setup; submitting it shows the neutral confirmation pointing the operator at logs/file, with an onward link to the confirm step — proven for both the first-mint and the H1 live-token no-op paths, with the token absent from body, headers, AND the rendered template context (exact source assertion over both message-branch context dicts).
3. GET /reset/confirm is reachable unauthenticated and renders the token + new-password form; a /resetXYZ-style path stays gated AT THE AUTH BOUNDARY (302→/login, never 404 — the exemption did not widen); field-level errors (empty / mismatch / over-72-byte) render inline and token errors render in the top banner (existing Phase 72 plumbing, verified).
4. "Back to login" cross-navigation exists on both reset steps.
5. The token value appears nowhere in any rendered markup, header, or template context; no middleware/auth-exemption widening (EXEMPT_PREFIXES unchanged); no Phase 72 backend logic changed.
</success_criteria>

<output>
Create `.planning/phases/73-password-reset-ui/73-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
