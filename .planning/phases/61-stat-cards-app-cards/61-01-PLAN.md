---
phase: 61-stat-cards-app-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/static/css/input.css
  - triggarr/templates/partials/stats_row.html
  - tests/test_stats_health.py
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04

must_haves:
  truths:
    - "All stat cards render with p-5 padding and text-[32px] hero numbers"
    - "Grab Rate card displays side-by-side mini progress bars with triggarr-radarr and triggarr-sonarr colors"
    - "Movies card shows ph-film-strip icon in orange, Series shows ph-television in blue, Albums shows ph-music-notes in green, Next Scan shows ph-clock-countdown in muted"
    - "Each stat card has a colored dot subtitle (e.g. In Radarr, In Sonarr)"
  artifacts:
    - path: "triggarr/static/css/input.css"
      provides: "triggarr-primary and triggarr-elevated color tokens"
      contains: "--color-triggarr-primary"
    - path: "triggarr/templates/partials/stats_row.html"
      provides: "Restyled stat cards matching artifact"
      contains: "text-[32px]"
    - path: "tests/test_stats_health.py"
      provides: "Updated assertions for new stat card classes"
      contains: "ph ph-chart-line-up"
  key_links:
    - from: "triggarr/templates/partials/stats_row.html"
      to: "triggarr/static/css/input.css"
      via: "Tailwind token references"
      pattern: "triggarr-primary"
---

<objective>
Restyle all stat cards in stats_row.html to match the AIDesigner artifact: hero numbers at text-[32px], Phosphor icons per card, side-by-side mini bars, colored dot subtitles, and p-5 uniform padding. Add missing CSS tokens (triggarr-primary, triggarr-elevated) to input.css.

Purpose: Delivers STAT-01 through STAT-04 -- the scaled, icon-rich stat cards matching the design artifact.
Output: Updated stats_row.html, input.css with new tokens, updated test_stats_health.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-stat-cards-app-cards/61-CONTEXT.md
@.planning/phases/61-stat-cards-app-cards/61-RESEARCH.md
@.planning/phases/61-stat-cards-app-cards/61-PATTERNS.md
@.planning/phases/61-stat-cards-app-cards/61-UI-SPEC.md

<interfaces>
<!-- Current stats_row.html htmx wiring (MUST PRESERVE lines 1-6): -->
```html
<div id="stats-row"
     hx-get="{{ request.url_for('partial_stats_row') }}"
     hx-trigger="every 30s"
     hx-swap="outerHTML"
     hx-include="[name='instance']"
     class="grid grid-cols-2 {% if instance_app_type %}md:grid-cols-3{% else %}md:grid-cols-5{% endif %} gap-3 mb-4">
```

<!-- Current input.css @theme tokens: -->
```css
@theme {
  --color-triggarr-green: #22c55e;
  --color-triggarr-green-dark: #16a34a;
  --color-triggarr-bg: #0f172a;
  --color-triggarr-card: #1e293b;
  --color-triggarr-card-elevated: #233346;
  --color-triggarr-border: #334155;
  --color-triggarr-text: #e2e8f0;
  --color-triggarr-muted: #94a3b8;
  --color-triggarr-radarr: #f59e0b;
  --color-triggarr-sonarr: #3b82f6;
  --color-triggarr-danger: #ef4444;
  --color-triggarr-primaryDark: #16a34a;
  --font-geist-mono: "Geist Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
}
```

<!-- Template variables available (from routes.py _build_stats_context): -->
- stats.overall_rate, stats.radarr_rate, stats.sonarr_rate, stats.lidarr_rate
- stats.movies_found, stats.movies_updated, stats.episodes_found, stats.episodes_updated
- stats.albums_found, stats.albums_updated
- time_to_grab (string)
- radarr_enabled, sonarr_enabled, lidarr_enabled
- instance_app_type (None or 'radarr'/'sonarr'/'lidarr')

<!-- Phosphor Icons: vendored at static/vendor/phosphor/, use <i class="ph ph-*"> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS tokens and update stats_row.html to artifact spec</name>
  <read_first>
    - triggarr/static/css/input.css
    - triggarr/templates/partials/stats_row.html
    - .planning/phases/61-stat-cards-app-cards/61-UI-SPEC.md
    - .planning/phases/61-stat-cards-app-cards/61-PATTERNS.md
  </read_first>
  <files>triggarr/static/css/input.css, triggarr/templates/partials/stats_row.html</files>
  <action>
**input.css changes:**

1. Add two new tokens to the `@theme` block (after `--color-triggarr-danger`):
   ```css
   --color-triggarr-primary: #22c55e;
   --color-triggarr-elevated: #233346;
   ```

2. The existing `.mini-bar` CSS class (height: 6px, border-radius: 3px) will no longer be used by stats_row.html. Leave the class in place for backward compatibility but the template will use pure Tailwind utilities instead.

**stats_row.html full restructure:**

Preserve the outermost `<div id="stats-row" ...>` with ALL htmx attributes (id, hx-get, hx-trigger, hx-swap, hx-include) unchanged. Update only the `class` attribute on that div and all inner HTML.

**Stats row grid class:** Change from `gap-3 mb-4` to `gap-4 mb-4`. Keep the existing conditional: `grid grid-cols-2 {% if instance_app_type %}md:grid-cols-3{% else %}md:grid-cols-5{% endif %} gap-4 mb-4`.

**Grab Rate Card (per D-01, D-04, D-09):**
- Wrapper: `bg-triggarr-card border border-triggarr-border rounded-lg p-5 flex flex-col justify-between shadow-sm`
- Keep the `{% if instance_app_type %}md:col-span-1{% else %}md:col-span-2{% endif %}` conditional on the wrapper.
- Remove the gradient overlay div (`absolute inset-0 bg-gradient-to-br...`) and the `relative overflow-hidden` classes -- artifact does not have them.
- Remove the health badge (Healthy/Warn/Critical pill) -- artifact does not have it.
- Label row: `<div class="flex justify-between items-start mb-4">`
  - Label: `<span class="text-xs font-bold tracking-widest uppercase text-triggarr-muted">Grab Rate</span>`
  - Icon: `<i class="ph ph-chart-line-up text-lg text-triggarr-primary"></i>`
- Hero number: `<div class="text-[32px] font-bold text-triggarr-text leading-none mb-4">` with `<span class="text-2xl text-triggarr-muted">%</span>` for the unit. Keep the existing Jinja2 logic for `stats.overall_rate` and the em-dash fallback.
- Mini bars: Replace the vertical `space-y-2` stack with a horizontal layout:
  ```html
  <div class="flex items-center justify-between gap-4 mt-2">
    {% if stats.radarr_rate is not none %}
    <div class="flex-1">
      <div class="flex justify-between text-[10px] text-triggarr-muted mb-1"><span>Radarr</span><span>{{ "%.0f" | format(stats.radarr_rate) }}%</span></div>
      <div class="w-full h-1 bg-triggarr-bg rounded-full overflow-hidden"><div class="h-full bg-triggarr-radarr rounded-full" style="width: {{ "%.0f" | format(stats.radarr_rate) }}%"></div></div>
    </div>
    {% endif %}
    {% if stats.sonarr_rate is not none %}
    <div class="flex-1">
      <div class="flex justify-between text-[10px] text-triggarr-muted mb-1"><span>Sonarr</span><span>{{ "%.0f" | format(stats.sonarr_rate) }}%</span></div>
      <div class="w-full h-1 bg-triggarr-bg rounded-full overflow-hidden"><div class="h-full bg-triggarr-sonarr rounded-full" style="width: {{ "%.0f" | format(stats.sonarr_rate) }}%"></div></div>
    </div>
    {% endif %}
    {% if stats.lidarr_rate is not none %}
    <div class="flex-1">
      <div class="flex justify-between text-[10px] text-triggarr-muted mb-1"><span>Lidarr</span><span>{{ "%.0f" | format(stats.lidarr_rate) }}%</span></div>
      <div class="w-full h-1 bg-triggarr-bg rounded-full overflow-hidden"><div class="h-full bg-triggarr-green rounded-full" style="width: {{ "%.0f" | format(stats.lidarr_rate) }}%"></div></div>
    </div>
    {% endif %}
  </div>
  ```
  Note: No `.mini-bar` class used. Pure Tailwind utilities for h-1 rounded-full bars.

**Movies Card (per D-01, D-02, D-09):**
- Wrapper: `bg-triggarr-card border border-triggarr-border rounded-lg p-5 flex flex-col justify-between shadow-sm`
- Label row: `<div class="flex justify-between items-start mb-4">`
  - Label: `<span class="text-xs font-bold tracking-widest uppercase text-triggarr-muted">Movies</span>`
  - Icon: `<i class="ph ph-film-strip text-lg text-triggarr-radarr"></i>`
- Hero number: `<div class="text-[32px] font-bold text-triggarr-text leading-none mb-3">{{ stats.movies_found + stats.movies_updated }}</div>`
- Subtitle: Replace `<p class="text-xs text-triggarr-muted mt-1">...found...upgraded</p>` with:
  ```html
  <div class="flex items-center text-xs text-triggarr-muted gap-1.5">
    <span class="w-1.5 h-1.5 rounded-full bg-triggarr-radarr opacity-80"></span>In Radarr
  </div>
  ```

**Episodes Card (per D-01, D-02, D-09):** Same structure as Movies but:
- Label text: "Series" (not "Episodes" -- match artifact naming)
- Icon: `<i class="ph ph-television text-lg text-triggarr-sonarr"></i>`
- Hero: `{{ stats.episodes_found + stats.episodes_updated }}`
- Subtitle dot: `bg-triggarr-sonarr` + "In Sonarr"

**Albums Card (per D-01, D-07):** Same structure as Movies but:
- Label text: "Albums"
- Icon: `<i class="ph ph-music-notes text-lg text-triggarr-green"></i>`
- Hero: `{{ stats.albums_found + stats.albums_updated }}`
- Subtitle dot: `bg-triggarr-green` + "In Lidarr"

**Time to Grab / Next Scan Card (per D-01, D-03):**
- This card maps to the artifact's "Next Scan" card. Keep the existing `time_to_grab` data wiring per D-03.
- Wrapper: same as other stat cards
- Label row: `<div class="flex justify-between items-start mb-4">`
  - Label: `<span class="text-xs font-bold tracking-widest uppercase text-triggarr-muted">Next Scan</span>`
  - Icon: `<i class="ph ph-clock-countdown text-lg text-triggarr-muted"></i>`
- Hero: `<div class="text-[32px] font-bold text-triggarr-text leading-none mb-3">{{ time_to_grab }}</div>`
- Subtitle:
  ```html
  <div class="flex items-center text-xs text-triggarr-muted gap-1.5">
    <i class="ph ph-calendar text-sm text-triggarr-primary"></i>Scheduled automatically
  </div>
  ```

Keep ALL Jinja2 conditionals for radarr_enabled/sonarr_enabled/lidarr_enabled and instance_app_type filtering unchanged.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/ tests/ && uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --minify 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - input.css contains `--color-triggarr-primary: #22c55e;`
    - input.css contains `--color-triggarr-elevated: #233346;`
    - stats_row.html contains `text-[32px] font-bold` (hero number class)
    - stats_row.html contains `ph ph-chart-line-up` (Grab Rate icon)
    - stats_row.html contains `ph ph-film-strip` (Movies icon)
    - stats_row.html contains `ph ph-television` (Series icon)
    - stats_row.html contains `ph ph-music-notes` (Albums icon)
    - stats_row.html contains `ph ph-clock-countdown` (Next Scan icon)
    - stats_row.html contains `tracking-widest` on all label spans
    - stats_row.html contains `h-1 bg-triggarr-bg rounded-full` (mini bar track)
    - stats_row.html contains `w-1.5 h-1.5 rounded-full bg-triggarr-radarr` (Movies subtitle dot)
    - stats_row.html contains `id="stats-row"` and `hx-trigger="every 30s"` (htmx preserved)
    - stats_row.html does NOT contain `text-4xl` (old hero size)
    - stats_row.html does NOT contain `mini-bar` class reference
    - stats_row.html does NOT contain `bg-gradient-to-br from-triggarr-green/5` (old gradient)
  </acceptance_criteria>
  <done>All stat cards use p-5 + text-[32px] hero numbers + Phosphor icons + colored dot subtitles + horizontal mini bars per artifact spec. CSS tokens triggarr-primary and triggarr-elevated exist.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_stats_health.py assertions for new stat card classes</name>
  <read_first>
    - tests/test_stats_health.py
    - triggarr/templates/partials/stats_row.html
  </read_first>
  <files>tests/test_stats_health.py</files>
  <action>
Update existing tests and add new tests in `tests/test_stats_health.py`:

**Update `test_grab_rate_hero_card_layout` (line 166):**
- Change assertion `"text-4xl font-bold"` to `"text-[32px] font-bold"`
- Keep `"md:col-span-2"` assertion
- Remove `"text-2xl text-triggarr-muted"` assertion (percentage span still exists, keep this one actually)
- Remove `"bg-gradient-to-br from-triggarr-green/5"` assertion (gradient removed)
- Add assertion: `"tracking-widest"` in response.text
- Add assertion: `"ph ph-chart-line-up"` in response.text

**Update `test_health_badge_renders` (line 181):**
- The health badge has been removed from the Grab Rate card per D-09 (artifact doesn't have it). Remove or skip this test. Replace it with a new test `test_grab_rate_has_phosphor_icon`:
  ```python
  def test_grab_rate_has_phosphor_icon(client):
      """Grab Rate card shows chart-line-up Phosphor icon (STAT-03)."""
      response = client.get("/")
      assert response.status_code == 200
      assert "ph ph-chart-line-up" in response.text
      assert "text-triggarr-primary" in response.text
  ```

**Update `test_per_app_bars_with_colors` (line 196):**
- Remove assertion `"mini-bar" in response.text`
- Add assertion `"h-1" in response.text` (new bar height)
- Add assertion `"bg-triggarr-bg rounded-full" in response.text` (bar track)
- Keep color assertions for `"bg-triggarr-radarr"` but change from hex `"#fb923c"` to checking for `"bg-triggarr-radarr"` in the bar fill. Also check `"bg-triggarr-sonarr"` is present.
- Remove `"text-orange-400"` assertion (label no longer uses that class)

**Update `test_stat_cards_have_shadow` (line 211):**
- Keep the assertion but adjust count. With Radarr+Sonarr+Lidarr enabled, we have 5 stat cards (Grab Rate + Movies + Series/Episodes + Albums + Next Scan) all with `shadow-sm`. Count >= 5 is still valid.

**Update `test_output_css_contains_mini_bar` (line 224):**
- Keep as-is (the `.mini-bar` CSS class still exists in input.css for backward compat).

**Add new test `test_stat_cards_have_phosphor_icons`:**
```python
def test_stat_cards_have_phosphor_icons(client):
    """All stat cards display Phosphor icons matching app type (STAT-03)."""
    response = client.get("/")
    assert response.status_code == 200
    assert "ph ph-chart-line-up" in response.text  # Grab Rate
    assert "ph ph-film-strip" in response.text  # Movies
    assert "ph ph-television" in response.text  # Series
    assert "ph ph-clock-countdown" in response.text  # Next Scan
```

**Add new test `test_stat_card_subtitles`:**
```python
def test_stat_card_subtitles(client):
    """Stat cards show colored dot + label subtitles (STAT-04)."""
    response = client.get("/")
    assert response.status_code == 200
    assert "w-1.5 h-1.5 rounded-full bg-triggarr-radarr" in response.text
    assert "In Radarr" in response.text
    assert "w-1.5 h-1.5 rounded-full bg-triggarr-sonarr" in response.text
    assert "In Sonarr" in response.text
    assert "Scheduled automatically" in response.text
```

**Add new test `test_stat_card_label_typography`:**
```python
def test_stat_card_label_typography(client):
    """Stat card labels use font-bold tracking-widest uppercase (STAT-04)."""
    response = client.get("/")
    assert response.status_code == 200
    assert "text-xs font-bold tracking-widest uppercase text-triggarr-muted" in response.text
```

**Add new test `test_mini_bars_horizontal_layout`:**
```python
def test_mini_bars_horizontal_layout(client):
    """Mini bars use horizontal flex layout with h-1 rounded-full bars (STAT-02)."""
    response = client.get("/")
    assert response.status_code == 200
    assert "flex items-center justify-between gap-4" in response.text
    assert "h-1 bg-triggarr-bg rounded-full overflow-hidden" in response.text
    assert "h-full bg-triggarr-radarr rounded-full" in response.text
```
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_stats_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_stats_health.py contains `"text-[32px] font-bold"` assertion (not `"text-4xl"`)
    - test_stats_health.py contains `def test_stat_cards_have_phosphor_icons`
    - test_stats_health.py contains `def test_stat_card_subtitles`
    - test_stats_health.py contains `"ph ph-chart-line-up"` assertion
    - test_stats_health.py contains `"w-1.5 h-1.5 rounded-full bg-triggarr-radarr"` assertion
    - test_stats_health.py does NOT contain `"text-4xl font-bold"` (old assertion)
    - `uv run pytest tests/test_stats_health.py -x -q` exits 0
  </acceptance_criteria>
  <done>All stat card tests pass with updated assertions for text-[32px], Phosphor icons, horizontal mini bars, and colored dot subtitles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan modifies only CSS and HTML templates with no new input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | stats_row.html Jinja2 variables | accept | All template variables are server-rendered; no new user input surfaces. Existing Jinja2 auto-escaping handles XSS. |
| T-61-02 | Information Disclosure | CSS tokens | accept | Color tokens are purely cosmetic; no sensitive data exposed. |
</threat_model>

<verification>
1. `uv run pytest tests/test_stats_health.py -x -q` -- all tests pass
2. `uv run ruff check triggarr/ tests/` -- no lint errors
3. `uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css --minify` -- CSS compiles without errors
4. Visual: stat cards show text-[32px] hero numbers, Phosphor icons, horizontal mini bars
</verification>

<success_criteria>
- All 4 STAT requirements (STAT-01 through STAT-04) are implemented
- stats_row.html matches artifact spec for card structure, icons, and typography
- input.css has triggarr-primary and triggarr-elevated tokens
- All test_stats_health.py tests pass
- No ruff violations
</success_criteria>

<output>
After completion, create `.planning/phases/61-stat-cards-app-cards/61-01-SUMMARY.md`
</output>
