---
phase: 61-stat-cards-app-cards
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - triggarr/templates/partials/app_card.html
  - tests/test_app_cards.py
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04

must_haves:
  truths:
    - "App cards have colored left borders matching app type (orange Radarr, blue Sonarr, green Lidarr, red unreachable)"
    - "App card header shows title and connection pill separated by a bottom border"
    - "Missing and Cutoff stats appear inside recessed sub-cards with bg-triggarr-bg/50"
    - "Search Now button is full-width with Phosphor magnifying glass icon that turns app-colored on hover"
  artifacts:
    - path: "triggarr/templates/partials/app_card.html"
      provides: "Restyled app cards matching artifact"
      contains: "border-l-triggarr-radarr"
    - path: "tests/test_app_cards.py"
      provides: "Updated assertions for new app card classes"
      contains: "bg-triggarr-bg/50"
  key_links:
    - from: "triggarr/templates/partials/app_card.html"
      to: "triggarr/static/css/input.css"
      via: "Tailwind token references (triggarr-elevated, triggarr-primary)"
      pattern: "bg-triggarr-elevated"
---

<objective>
Restyle app_card.html to match the AIDesigner artifact: sectioned layout (header/body/footer), app-type colored left borders, connection pill with border-b separator, recessed sub-cards for Missing/Cutoff, full-width Search Now button with app-colored hover, and redesigned unreachable card body.

Purpose: Delivers CARD-01 through CARD-04 -- refined app cards with colored accents and improved visual hierarchy.
Output: Updated app_card.html, updated test_app_cards.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-stat-cards-app-cards/61-CONTEXT.md
@.planning/phases/61-stat-cards-app-cards/61-RESEARCH.md
@.planning/phases/61-stat-cards-app-cards/61-PATTERNS.md
@.planning/phases/61-stat-cards-app-cards/61-UI-SPEC.md
@.planning/phases/61-stat-cards-app-cards/61-01-SUMMARY.md

<interfaces>
<!-- Current app_card.html htmx wiring (MUST PRESERVE): -->
```html
<div id="{{ app.card_id }}-card"
     hx-get="{{ request.url_for('partial_app_card', app_name=app.name, instance_name=app.instance) }}"
     hx-trigger="every 5s"
     hx-swap="outerHTML"
```

<!-- Template variables available (from routes.py _build_app_context): -->
- app.card_id, app.name, app.instance
- app.connected (True/False/None)
- app.unreachable_since
- app.last_run, app.next_run
- app.missing_count, app.cutoff_count, app.total_items
- app.missing_cursor, app.cutoff_cursor
- app.missing_searchable, app.missing_eligible, app.missing_monitored
- app.cutoff_searchable
- app.missing_pass, app.cutoff_pass
- app.skip_unreleased
- app.tag_warnings

<!-- CSS tokens available (after Plan 01): -->
- triggarr-primary (#22c55e), triggarr-elevated (#233346)
- triggarr-radarr (#f59e0b), triggarr-sonarr (#3b82f6), triggarr-green (#22c55e), triggarr-danger (#ef4444)
- triggarr-card (#1e293b), triggarr-bg (#0f172a), triggarr-border (#334155)
- triggarr-text (#e2e8f0), triggarr-muted (#94a3b8)

<!-- Tailwind JIT scanning note: -->
Dynamic class interpolation like `border-l-triggarr-{{ app.name }}` will NOT work.
Must use explicit full class strings in Jinja2 conditionals for Tailwind to scan them.

<!-- Phosphor Icons: vendored at static/vendor/phosphor/, use <i class="ph ph-*"> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure app_card.html to artifact sectioned layout</name>
  <read_first>
    - triggarr/templates/partials/app_card.html
    - .planning/phases/61-stat-cards-app-cards/61-UI-SPEC.md
    - .planning/phases/61-stat-cards-app-cards/61-PATTERNS.md
  </read_first>
  <files>triggarr/templates/partials/app_card.html</files>
  <action>
Full restructure of app_card.html. Preserve ALL htmx attributes on the outermost div (id, hx-get, hx-trigger, hx-swap) unchanged. Preserve ALL Jinja2 template variable references and conditional logic for data display.

**Outer wrapper class (per D-05, D-09, CARD-01):**
```html
<div id="{{ app.card_id }}-card"
     hx-get="{{ request.url_for('partial_app_card', app_name=app.name, instance_name=app.instance) }}"
     hx-trigger="every 5s"
     hx-swap="outerHTML"
     class="bg-triggarr-card border border-triggarr-border border-l-4
            {% if app.connected == false %}border-l-triggarr-danger{% elif app.name == 'radarr' %}border-l-triggarr-radarr{% elif app.name == 'sonarr' %}border-l-triggarr-sonarr{% elif app.name == 'lidarr' %}border-l-triggarr-green{% endif %}
            rounded-lg overflow-hidden flex flex-col shadow-sm card-hover
            {% if app.connected == false %}danger-stripes relative{% endif %}">
```
Key changes: Remove `p-5` (padding moves to sections). Left border color is now app-type-based (not connection-status-based). Red only for unreachable override per CARD-01.

**Connected card structure (3 sections):**

**Section 1: Header (per CARD-02, D-05)**
```html
  <!-- Header -->
  <div class="p-4 border-b border-triggarr-border/50 flex justify-between items-center{% if app.connected == false %} relative z-10{% endif %}">
    <h2 class="font-bold text-triggarr-text text-[15px]{% if app.connected == false %} text-triggarr-muted{% endif %}">{{ app.name | capitalize }}{% if app.instance != 'Default' %} <span class="text-sm font-normal text-triggarr-muted">/ {{ app.instance }}</span>{% endif %}</h2>
    {% if app.connected == true %}
      <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-primary/10 text-triggarr-primary border border-triggarr-primary/20">Connected</span>
    {% elif app.connected == false %}
      <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-danger/10 text-triggarr-danger border border-triggarr-danger/20">Unreachable</span>
    {% else %}
      <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-border/40 text-triggarr-muted border border-triggarr-border/40">Waiting...</span>
    {% endif %}
  </div>
```
Key changes: `rounded` (not `rounded-full`), `text-[10px] font-bold uppercase tracking-wider`, border on pill, removed dot-pulse animated dot. Connection pill uses `triggarr-primary` for connected state (not `triggarr-green/15`). Title uses `text-[15px] font-bold` (not `text-lg font-semibold`).

**Section 2: Body (connected state)**
```html
  {% if app.connected == false %}
  <!-- Unreachable body: centered error message (per artifact design.html:230-234) -->
  <div class="p-4 flex-1 relative z-10 flex flex-col justify-center items-center text-center">
    <i class="ph ph-warning-circle text-[40px] text-triggarr-danger/40 mb-3 block"></i>
    <span class="text-sm text-triggarr-muted block mb-1">API connection failed.</span>
    <span class="text-[10px] text-triggarr-muted/60 block">Check API key or network setup.</span>
  </div>
  {% else %}
  <!-- Connected/Waiting body -->
  <div class="p-4 flex-1">
    <!-- Tag warning badge (preserve existing) -->
    {% if app.tag_warnings %}
    <div class="bg-amber-500/15 text-amber-400 text-xs px-3 py-1.5 rounded mb-3 flex items-center gap-2">
      <svg class="w-3.5 h-3.5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z"/><line x1="12" y1="9" x2="12" y2="13"/><circle cx="12" cy="17" r="0.5"/></svg>
      <span>
        {% if app.tag_warnings | length == 1 %}
          {{ app.tag_warnings[0].field | capitalize }} tag '{{ app.tag_warnings[0].tag }}' not found
        {% else %}
          Tags not found: {% for w in app.tag_warnings %}'{{ w.tag }}' ({{ w.field }}){% if not loop.last %}, {% endif %}{% endfor %}
        {% endif %}
      </span>
    </div>
    {% endif %}

    <!-- Schedule row (per artifact: font-mono, mb-4, no border-b) -->
    <div class="text-[11px] font-mono text-triggarr-muted mb-4 flex justify-between">
      <span>Last run: <span class="text-triggarr-text">{% if app.last_run %}{{ app.last_run[11:19] }}{% else %}Never{% endif %}</span></span>
      <span>Next: <span class="text-triggarr-text">{% if app.connected == false %}&mdash;{% elif app.next_run %}{{ app.next_run[11:16] }}{% else %}&mdash;{% endif %}</span></span>
    </div>

    <!-- Stats sub-cards (per CARD-03, D-06) -->
    <div class="grid grid-cols-2 gap-3 mb-5{% if app.connected == false %} opacity-60{% endif %}">
      <div class="bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5">
        <span class="block text-[10px] text-triggarr-muted uppercase tracking-wider">Missing</span>
        <span class="block text-lg font-bold text-triggarr-text mt-1">
          {%- if app.missing_count is not none -%}{{ app.missing_count }}{%- else -%}&mdash;{%- endif -%}
        </span>
        {% if app.missing_count is not none %}
        <p class="text-[10px] text-triggarr-muted mt-0.5 flex items-center gap-1.5">
          <span>{{ app.missing_cursor }} of {{ app.missing_searchable if app.missing_searchable is not none else (app.missing_eligible if app.missing_eligible is not none else (app.missing_count if app.missing_count is not none else '?')) }}</span>
          {%- if app.missing_pass is defined and app.missing_pass > 0 %}
          <span class="text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded">pass {{ app.missing_pass }}</span>
          {%- endif %}
        </p>
        {%- if app.skip_unreleased and app.name == 'radarr' and app.missing_eligible is not none and app.missing_monitored is not none and app.missing_eligible < app.missing_monitored %}
        <p class="text-[10px] text-amber-400 mt-0.5">{{ app.missing_monitored - app.missing_eligible }} skipped</p>
        {%- endif %}
        {% endif %}
      </div>
      <div class="bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5">
        <span class="block text-[10px] text-triggarr-muted uppercase tracking-wider">Cutoff Unmet</span>
        <span class="block text-lg font-bold text-triggarr-text mt-1">
          {%- if app.cutoff_count is not none -%}{{ app.cutoff_count }}{%- else -%}&mdash;{%- endif -%}
        </span>
        {% if app.cutoff_count is not none %}
        <p class="text-[10px] text-triggarr-muted mt-0.5 flex items-center gap-1.5">
          <span>{{ app.cutoff_cursor }} of {{ app.cutoff_searchable if app.cutoff_searchable is not none else (app.cutoff_count if app.cutoff_count is not none else '?') }}</span>
          {%- if app.cutoff_pass is defined and app.cutoff_pass > 0 %}
          <span class="text-[10px] bg-triggarr-border/60 text-triggarr-muted px-1.5 py-0 rounded">pass {{ app.cutoff_pass }}</span>
          {%- endif %}
        </p>
        {% endif %}
      </div>
    </div>
  </div>
  {% endif %}
```
Key changes: Body is now a `p-4 flex-1` section. Schedule row uses `font-mono` and `mb-4` (no `border-b` -- border is now on header). Stats are in recessed sub-cards with `bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5`. Labels use `text-[10px] uppercase tracking-wider`. Values use `text-lg font-bold`. Simplified Missing/Cutoff value display to show count only (not "X of Y") in the hero position, with cursor progress in smaller text below. Unreachable body completely replaced with centered error message per artifact.

**Section 3: Footer (per CARD-04, D-08)**
```html
  <!-- Footer -->
  <div class="p-3 bg-triggarr-bg/30 border-t border-triggarr-border/50{% if app.connected == false %} relative z-10{% endif %}">
    {% if app.connected == false %}
      <button hx-post="{{ request.url_for('search_now', app_name=app.name, instance_name=app.instance) }}"
              hx-target="#{{ app.card_id }}-card"
              hx-swap="outerHTML"
              class="w-full flex items-center justify-center gap-2 py-2 rounded-md bg-triggarr-card hover:bg-triggarr-elevated border border-triggarr-border text-xs font-semibold transition-colors text-triggarr-muted">
        <i class="ph ph-arrows-clockwise"></i>Retry Connection
      </button>
    {% else %}
      <button hx-post="{{ request.url_for('search_now', app_name=app.name, instance_name=app.instance) }}"
              hx-target="#{{ app.card_id }}-card"
              hx-swap="outerHTML"
              class="w-full flex items-center justify-center gap-2 py-2 rounded-md bg-triggarr-elevated hover:bg-triggarr-border border border-triggarr-border text-xs font-semibold transition-colors text-triggarr-text group">
        <i class="ph ph-magnifying-glass {% if app.name == 'radarr' %}group-hover:text-triggarr-radarr{% elif app.name == 'sonarr' %}group-hover:text-triggarr-sonarr{% elif app.name == 'lidarr' %}group-hover:text-triggarr-green{% endif %} transition-colors"></i>Search Now
      </button>
    {% endif %}
  </div>
```
Key changes: Footer has `p-3 bg-triggarr-bg/30 border-t`. Connected Search Now button is full-width with `bg-triggarr-elevated` and Phosphor `ph-magnifying-glass` icon. Icon uses explicit Jinja2 conditionals for `group-hover:text-triggarr-radarr` / `group-hover:text-triggarr-sonarr` / `group-hover:text-triggarr-green` (full class strings for Tailwind scanning). Unreachable button uses `bg-triggarr-card` with `ph-arrows-clockwise` icon and "Retry Connection" text.

**Danger overlay for unreachable (per artifact design.html:225):**
Add immediately inside the outer div, before the header section, only for unreachable cards:
```html
  {% if app.connected == false %}
  <div class="absolute inset-0 bg-gradient-to-br from-triggarr-danger/5 to-transparent pointer-events-none"></div>
  {% endif %}
```
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - app_card.html contains `border-l-triggarr-radarr` (Radarr border color)
    - app_card.html contains `border-l-triggarr-sonarr` (Sonarr border color)
    - app_card.html contains `border-l-triggarr-green` (Lidarr border color)
    - app_card.html contains `border-l-triggarr-danger` (unreachable border color)
    - app_card.html contains `p-4 border-b border-triggarr-border/50` (header section)
    - app_card.html contains `font-bold text-triggarr-text text-[15px]` (title)
    - app_card.html contains `bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5` (recessed sub-cards)
    - app_card.html contains `text-lg font-bold text-triggarr-text` (sub-card values)
    - app_card.html contains `bg-triggarr-elevated hover:bg-triggarr-border` (Search Now button)
    - app_card.html contains `ph ph-magnifying-glass` (Search Now icon)
    - app_card.html contains `group-hover:text-triggarr-radarr` (app-colored hover)
    - app_card.html contains `ph ph-arrows-clockwise` (Retry icon)
    - app_card.html contains `ph ph-warning-circle` (unreachable body icon)
    - app_card.html contains `font-mono` (schedule row)
    - app_card.html contains `uppercase tracking-wider` (pill and label text)
    - app_card.html contains `id="{{ app.card_id }}-card"` and `hx-trigger="every 5s"` (htmx preserved)
    - app_card.html does NOT contain `p-5 shadow-sm card-hover` on outer div (p-5 removed from outer)
    - app_card.html does NOT contain `rounded-full bg-triggarr-green/15` (old pill style)
    - app_card.html does NOT contain `text-lg font-semibold` (old title style)
  </acceptance_criteria>
  <done>App cards use sectioned layout with colored left borders per app type, header with connection pill + border-b, recessed sub-cards, and full-width Search Now with app-colored hover icon.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_app_cards.py for new app card structure</name>
  <read_first>
    - tests/test_app_cards.py
    - triggarr/templates/partials/app_card.html
  </read_first>
  <files>tests/test_app_cards.py</files>
  <action>
Update existing tests and add new tests in `tests/test_app_cards.py`:

**Update `test_connected_pill_unified_shape` (line 126):**
- Change `"rounded-full bg-triggarr-green/15 text-triggarr-green"` to `"rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-primary/10 text-triggarr-primary"`
- Change `"inline-flex items-center gap-1.5"` to just check `"Connected"` is in response
- Add assertion: `"border border-triggarr-primary/20"` in response.text

**Update `test_unreachable_pill_unified_shape` (line 136):**
- Change `"rounded-full bg-red-500/15 text-red-400"` to `"rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-danger/10 text-triggarr-danger"`
- Add assertion: `"border border-triggarr-danger/20"` in response.text

**Update `test_waiting_pill_unified_shape` (line 148):**
- Change `"rounded-full bg-triggarr-border/40 text-triggarr-muted"` to `"rounded text-[10px] font-bold uppercase tracking-wider bg-triggarr-border/40 text-triggarr-muted"`

**Update `test_schedule_row_present` (line 161):**
- Change `"border-b border-triggarr-border/50 pb-3"` to `"font-mono text-triggarr-muted mb-4 flex justify-between"`
- Keep assertions for "Last run" content and "14:32:10" timestamp

**Update `test_unreachable_card_retry_button` (line 260):**
- Change `"bg-red-500/15 text-red-400"` to `"bg-triggarr-card hover:bg-triggarr-elevated"`
- Change `"Retry"` to `"Retry Connection"`
- Add: `"ph ph-arrows-clockwise"` in response.text

**Update `test_connected_card_search_now_button` (line 271):**
- Add assertion: `"ph ph-magnifying-glass"` in response.text
- Add assertion: `"bg-triggarr-elevated"` in response.text
- Add assertion: `"group-hover:text-triggarr-radarr"` in response.text (test uses radarr instance)
- Change `"Retry"` assertion: check that `"Retry Connection"` is NOT in response

**Update `test_connected_pill_has_dot_pulse` (line 285):**
- The dot-pulse animated dot has been removed from connection pills per artifact. Change this test to verify the NEW pill style instead. Replace with:
  ```python
  def test_connected_pill_has_border(client, test_app):
      """Connected pill has border and tracking-wider text (CARD-02)."""
      test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
      response = client.get("/partials/app-card/radarr/Default")
      assert response.status_code == 200
      assert "border border-triggarr-primary/20" in response.text
      assert "tracking-wider" in response.text
  ```

**Update `test_unreachable_pill_no_dot_pulse` (line 293):**
- Replace with test for unreachable pill border:
  ```python
  def test_unreachable_pill_has_danger_border(client, test_app):
      """Unreachable pill has danger-colored border (CARD-02)."""
      test_app.state.triggarr_state["radarr"]["Default"]["connected"] = False
      test_app.state.triggarr_state["radarr"]["Default"]["unreachable_since"] = "2026-04-13T12:00:00Z"
      response = client.get("/partials/app-card/radarr/Default")
      assert response.status_code == 200
      assert "border border-triggarr-danger/20" in response.text
  ```

**Add new test `test_app_card_border_color` (CARD-01):**
```python
def test_app_card_radarr_border_color(client, test_app):
    """Radarr app card has orange left border (CARD-01)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "border-l-triggarr-radarr" in response.text
    assert "border-l-triggarr-green" not in response.text  # no longer green for connected
```

**Add new test `test_app_card_unreachable_border_color` (CARD-01):**
```python
def test_app_card_unreachable_border_color(client, test_app):
    """Unreachable app card has red left border regardless of app type (CARD-01)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = False
    test_app.state.triggarr_state["radarr"]["Default"]["unreachable_since"] = "2026-04-13T12:00:00Z"
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "border-l-triggarr-danger" in response.text
```

**Add new test `test_card_header_border_bottom` (CARD-02):**
```python
def test_card_header_border_bottom(client, test_app):
    """App card header has border-bottom separator (CARD-02)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "p-4 border-b border-triggarr-border/50" in response.text
    assert "text-[15px]" in response.text  # title font size
```

**Add new test `test_recessed_subcards` (CARD-03):**
```python
def test_recessed_subcards(client, test_app):
    """Missing and Cutoff stats are in recessed sub-cards (CARD-03)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5" in response.text
    assert "text-lg font-bold text-triggarr-text" in response.text
    assert "text-[10px] text-triggarr-muted uppercase tracking-wider" in response.text
```

**Add new test `test_search_button_app_colored_hover` (CARD-04):**
```python
def test_search_button_app_colored_hover(client, test_app):
    """Search Now button has app-colored hover on icon (CARD-04)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "group-hover:text-triggarr-radarr" in response.text
    assert "ph ph-magnifying-glass" in response.text
    assert "bg-triggarr-elevated" in response.text
```

**Add new test `test_unreachable_body_error_message`:**
```python
def test_unreachable_body_error_message(client, test_app):
    """Unreachable card body shows centered error message with warning icon."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = False
    test_app.state.triggarr_state["radarr"]["Default"]["unreachable_since"] = "2026-04-13T12:00:00Z"
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "ph ph-warning-circle" in response.text
    assert "API connection failed." in response.text
    assert "Check API key or network setup." in response.text
```

**Add new test `test_footer_section_background`:**
```python
def test_footer_section_background(client, test_app):
    """Footer section has bg-triggarr-bg/30 background (CARD-04)."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = True
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "bg-triggarr-bg/30 border-t border-triggarr-border/50" in response.text
```

**Keep unchanged:** `test_card_has_hover_classes`, `test_unreachable_card_danger_stripes`, `test_connected_card_no_danger_stripes`, `test_unreachable_stats_opacity` (check if still valid -- unreachable body now shows error message, so opacity-60 may not apply). Actually, the unreachable body is now replaced entirely, so `test_unreachable_stats_opacity` should be updated: remove it or change to check for the centered error layout instead.

**Update `test_unreachable_stats_opacity` (line 246):**
Replace with:
```python
def test_unreachable_card_shows_error_not_stats(client, test_app):
    """Unreachable cards show error message instead of stats grid."""
    test_app.state.triggarr_state["radarr"]["Default"]["connected"] = False
    test_app.state.triggarr_state["radarr"]["Default"]["unreachable_since"] = "2026-04-13T12:00:00Z"
    response = client.get("/partials/app-card/radarr/Default")
    assert response.status_code == 200
    assert "API connection failed." in response.text
    assert "Missing" not in response.text  # stats grid not shown for unreachable
```

Also update `test_card_has_hover_classes`: The `shadow-sm` class is still on the outer wrapper and `card-hover` class is still present. This test should still pass as-is.

**Update `test_pass_pill_hidden_when_zero` (line 198):**
The pass pill uses `bg-triggarr-border/60` -- this is still used inside the sub-cards. The assertion checks for the class NOT being present when pass=0. This should still work since the sub-card wrappers use `border-triggarr-border/50` (different opacity). Verify the assertion is checking the right thing. Actually the pass pill class `bg-triggarr-border/60` is still used for pass pills inside sub-cards, so the logic is unchanged. Keep as-is.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_app_cards.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_app_cards.py contains `"border-l-triggarr-radarr"` assertion
    - test_app_cards.py contains `"border-l-triggarr-danger"` assertion
    - test_app_cards.py contains `def test_card_header_border_bottom`
    - test_app_cards.py contains `def test_recessed_subcards`
    - test_app_cards.py contains `def test_search_button_app_colored_hover`
    - test_app_cards.py contains `"bg-triggarr-bg/50 border border-triggarr-border/50 rounded p-2.5"` assertion
    - test_app_cards.py contains `"group-hover:text-triggarr-radarr"` assertion
    - test_app_cards.py contains `"ph ph-magnifying-glass"` assertion
    - test_app_cards.py does NOT contain `"rounded-full bg-triggarr-green/15"` (old pill)
    - test_app_cards.py does NOT contain `"rounded-full bg-red-500/15"` (old pill)
    - `uv run pytest tests/test_app_cards.py -x -q` exits 0
  </acceptance_criteria>
  <done>All app card tests pass with updated assertions for sectioned layout, app-type borders, recessed sub-cards, and app-colored Search Now button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan modifies only HTML templates with no new input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Tampering | app_card.html Jinja2 variables | accept | All template variables are server-rendered via _build_app_context(); no new user input surfaces. Existing Jinja2 auto-escaping handles XSS. |
| T-61-04 | Information Disclosure | app.name conditional logic | accept | App name values (radarr/sonarr/lidarr) are internal config values, not sensitive data. Used only for CSS class selection. |
</threat_model>

<verification>
1. `uv run pytest tests/test_app_cards.py -x -q` -- all tests pass
2. `uv run pytest tests/ -x -q` -- full suite passes
3. `uv run ruff check triggarr/ tests/` -- no lint errors
4. Visual: app cards show colored left borders, header with pill + border-b, recessed sub-cards, full-width Search Now button
</verification>

<success_criteria>
- All 4 CARD requirements (CARD-01 through CARD-04) are implemented
- app_card.html matches artifact spec for sectioned layout, borders, pills, sub-cards, and buttons
- All test_app_cards.py tests pass
- Full test suite passes
- No ruff violations
</success_criteria>

<output>
After completion, create `.planning/phases/61-stat-cards-app-cards/61-02-SUMMARY.md`
</output>
