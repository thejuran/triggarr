---
phase: 67-observability-csrf-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_middleware.py
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Missing Origin with matching Referer is explicitly asserted to pass (200)"
    - "Missing Referer with matching Origin is explicitly asserted to pass (200)"
    - "Both headers absent passes (existing test retained)"
    - "Scheme mismatch (https Origin vs http Host, same netloc) is asserted to ALLOW (200) with a documenting comment"
    - "Spoofed host (foreign host, suffix spoof, port mismatch) is asserted to REJECT (403)"
    - "All new tests drive through TestClient with crafted headers and assert response status only"
  artifacts:
    - path: "tests/test_middleware.py"
      provides: "OriginCheckMiddleware CSRF scenario coverage for all ROADMAP-named cases"
      contains: "test_post_scheme_mismatch_is_allowed"
  key_links:
    - from: "tests/test_middleware.py"
      to: "triggarr/web/middleware.py OriginCheckMiddleware"
      via: "TestClient(_make_app()) with crafted Origin/Referer/Host headers"
      pattern: "client\\.post"
---

<objective>
Extend the `OriginCheckMiddleware` test suite to explicitly cover every ROADMAP-named CSRF scenario: missing Origin, missing Referer, both absent, scheme mismatch, and spoofed host — all driven through `TestClient` with crafted headers, asserting status only, with NO reliance on internal middleware state (TEST-01).

Purpose: Regression-lock the netloc-equality CSRF guard against the adversarial header scenarios the ROADMAP names.
Output: Five new test functions in `tests/test_middleware.py`. The middleware itself is NOT modified (D-09).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-observability-csrf-test-coverage/67-CONTEXT.md
@.planning/phases/67-observability-csrf-test-coverage/67-RESEARCH.md
@.planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-05-31. Use directly — no exploration needed. -->

triggarr/web/middleware.py:72-86 (OriginCheckMiddleware dispatch — behavior under test, DO NOT modify):
  if request.method in ("POST", "PUT", "PATCH", "DELETE"):
      origin = request.headers.get("origin")
      referer = request.headers.get("referer")
      host = request.headers.get("host", "")
      if origin:
          if urlparse(origin).netloc != host:
              return Response("Forbidden", status_code=403)
      elif referer and urlparse(referer).netloc != host:
          return Response("Forbidden", status_code=403)
      # Neither header present: allow (same-origin browser behavior)
  # NOTE: scheme is NOT compared — urlparse(origin).netloc strips scheme.

tests/test_middleware.py:15-31 (existing harness — reuse the module-level client):
  def _make_app() -> FastAPI:
      app = FastAPI()
      app.add_middleware(OriginCheckMiddleware)
      @app.post("/test")
      async def post_endpoint():
          return {"status": "ok"}
      return app
  client = TestClient(_make_app())

tests/test_middleware.py:34-49 (existing test shape — match it):
  def test_post_matching_origin_passes():
      response = client.post("/test", headers={"Origin": "http://testserver", "Host": "testserver"})
      assert response.status_code == 200

Existing tests already present (do NOT duplicate): test_post_matching_origin_passes,
test_post_mismatched_origin_returns_403, test_post_matching_referer_passes,
test_post_mismatched_referer_returns_403, test_post_no_origin_no_referer_passes (both-absent — KEEP),
test_get_with_mismatched_origin_passes, plus two integration tests against /settings.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add the five ROADMAP-named OriginCheck CSRF scenario tests</name>
  <files>tests/test_middleware.py</files>
  <read_first>
    - tests/test_middleware.py (the `_make_app()` + module-level `client = TestClient(_make_app())` harness at 15-31; the existing test functions at 34-83 including `test_post_no_origin_no_referer_passes` at 70 which already covers "both absent"; the DEBT-02 integration section starting at line 86) — file being modified
    - triggarr/web/middleware.py:72-86 (`OriginCheckMiddleware.dispatch` — the netloc-vs-Host comparison that strips scheme) — source of truth for asserted behavior; NOT modified
    - .planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md §"tests/test_middleware.py" — the five exact test function bodies and docstrings
    - .planning/phases/67-observability-csrf-test-coverage/67-CONTEXT.md D-09/D-10/D-11/D-12 — scope, scheme-allow rationale, spoof-reject cases, no-internal-state rule
  </read_first>
  <action>
    Add five new test functions to tests/test_middleware.py after the existing OriginCheck tests (after line 83, before the DEBT-02 integration section). Reuse the existing module-level `client`; do not build a new harness. Each test crafts headers and asserts `response.status_code` only — no access to middleware internals (D-12).

    1. `test_post_missing_origin_with_matching_referer_passes` — headers `{"Referer": "http://testserver/page", "Host": "testserver"}` (no Origin) → assert 200. Explicit missing-Origin coverage (falls through to the Referer branch).
    2. `test_post_missing_referer_with_matching_origin_passes` — headers `{"Origin": "http://testserver", "Host": "testserver"}` (no Referer) → assert 200. Explicit missing-Referer coverage (Origin branch evaluated).
    3. `test_post_scheme_mismatch_is_allowed` — headers `{"Origin": "https://testserver", "Host": "testserver"}` → assert 200. This PINS current behavior per D-10. Include the full explanatory docstring: the middleware compares `urlparse(origin).netloc` (scheme stripped) against the raw Host, so `https://testserver` (netloc `testserver`) equals Host `testserver`; in Triggarr's single-origin model this is NOT a bypass (an attacker cannot make a browser emit a same-host Origin under a scheme they control); the comment must explicitly say "Do NOT change this test to assert 403 — scheme comparison is intentionally absent (D-10)" (Pitfall 9).
    4. `test_post_suffix_spoof_returns_403` — headers `{"Origin": "https://testserver.evil.com", "Host": "testserver"}` → assert 403 (netloc `testserver.evil.com` != `testserver`) (D-11).
    5. `test_post_port_mismatch_returns_403` — headers `{"Origin": "http://testserver:8080", "Host": "testserver"}` → assert 403 (netloc `testserver:8080` != `testserver`) (D-11).

    Do NOT add a "both absent" test — `test_post_no_origin_no_referer_passes` at line 70 already covers it (D-09). Do NOT modify triggarr/web/middleware.py (D-09: pure test work). The foreign-host reject case (`evil.com`) is already covered by the existing `test_post_mismatched_origin_returns_403`; the new suffix-spoof and port-mismatch tests extend it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_middleware.py -k "missing_origin or missing_referer or scheme_mismatch or suffix_spoof or port_mismatch or no_origin_no_referer" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_middleware.py contains the five new function names exactly: `test_post_missing_origin_with_matching_referer_passes`, `test_post_missing_referer_with_matching_origin_passes`, `test_post_scheme_mismatch_is_allowed`, `test_post_suffix_spoof_returns_403`, `test_post_port_mismatch_returns_403`.
    - `test_post_scheme_mismatch_is_allowed` asserts status 200 and its body/comment documents the intentional scheme-stripping behavior and the "do not change to 403" instruction.
    - Both spoof tests assert status 403.
    - No new test accesses middleware attributes/state — assertions are on `response.status_code` only.
    - `git diff --stat triggarr/web/middleware.py` shows no changes (middleware untouched).
    - `uv run pytest tests/test_middleware.py -x -q` passes and `uv run ruff check tests/test_middleware.py` exits 0.
  </acceptance_criteria>
  <done>All five ROADMAP-named CSRF scenarios are explicitly tested via TestClient, the scheme-mismatch ALLOW is pinned with a documenting comment, spoofed-host variants reject with 403, and the middleware is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → OriginCheckMiddleware | Untrusted cross-origin POST/PUT/PATCH/DELETE crosses here; CSRF control enforced via Origin/Referer vs Host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-07 | Spoofing | CSRF via spoofed Origin host | mitigate | New tests regression-lock 403 rejection of foreign-host, suffix-spoof, and port-mismatch Origins (D-11) — the netloc-equality guard cannot silently regress. |
| T-67-08 | Spoofing | CSRF via missing headers | accept | LOW — missing-header allow path is browser same-origin behavior, not a bypass; explicit tests assert the allow path (D-09) so it is documented and locked, not accidental. |
| T-67-09 | Spoofing | scheme-mismatch allow | accept | LOW by design (D-10) — single-origin threat model; an attacker cannot cause a browser to emit a same-host Origin under a controlled scheme. Test PINS the behavior with a documented rationale to prevent a misguided "fix". |
| T-67-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH.md: no new dependencies). |
</threat_model>

<verification>
- `uv run pytest tests/test_middleware.py -x -q` all green (existing + 5 new)
- `git diff triggarr/web/middleware.py` empty (D-09: no middleware change)
- `uv run ruff check tests/test_middleware.py` exits 0
</verification>

<success_criteria>
ROADMAP Phase 67 success criterion 3 satisfied: the OriginCheckMiddleware test suite covers missing Origin, missing Referer, both absent, scheme mismatch, and spoofed host; all tests pass and none rely on internal middleware state.
</success_criteria>

<output>
Create `.planning/phases/67-observability-csrf-test-coverage/67-03-SUMMARY.md` when done
</output>
