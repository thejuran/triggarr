---
phase: 67-observability-csrf-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/state.py
  - triggarr/search/engine.py
  - triggarr/web/routes.py
  - triggarr/templates/partials/app_card.html
  - tests/test_search.py
  - tests/test_state.py
  - tests/test_web.py
autonomous: true
requirements: [RES-02]
must_haves:
  truths:
    - "Each enabled app card shows a 'Last OK' timestamp (HH:MM:SS) or 'Never'"
    - "The 'Last OK' value is rendered amber when the last success is older than 2x the configured interval"
    - "A null last_success renders 'Never' in muted color, never amber"
    - "last_success is written only when a cycle reaches the connected=True end of the cycle fn"
    - "last_success persists across restart via state.json round-trip"
    - "D-01: per-instance last_success field added to AppState TypedDict + default None"
    - "D-02: last_success written in each engine cycle fn beside last_run at the connected=True success point (covers manual search_now too)"
    - "D-03: last_success_stale computed at render time in _build_app_context (None OR now - last_success > 2x per-instance search_interval)"
    - "D-04: Last OK entry on the app card schedule row using the [11:19] slice and reused amber token, never amber on null"
  artifacts:
    - path: "triggarr/state.py"
      provides: "last_success field in AppState TypedDict + default None"
      contains: "last_success"
    - path: "triggarr/search/engine.py"
      provides: "last_success write at each cycle success point"
      contains: "last_success"
    - path: "triggarr/web/routes.py"
      provides: "last_success_stale computation in _build_app_context"
      contains: "last_success_stale"
    - path: "triggarr/templates/partials/app_card.html"
      provides: "Last OK entry with amber stale flag on schedule row"
      contains: "Last OK"
  key_links:
    - from: "triggarr/search/engine.py"
      to: "triggarr/state.py AppState"
      via: 'ist["last_success"] write at cycle success'
      pattern: 'last_success.*=.*isoformat'
    - from: "triggarr/web/routes.py _build_app_context"
      to: "triggarr/templates/partials/app_card.html"
      via: "last_success + last_success_stale context keys"
      pattern: "last_success_stale"
---

<objective>
Surface a per-instance "last successful search" timestamp on each dashboard app card, visually flagged amber when older than 2x the configured search interval (RES-02).

Purpose: Users can see at a glance whether searches are succeeding per app/instance without inspecting logs.
Output: New `last_success` field threaded through state → engine → routes → template, plus unit and render tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-observability-csrf-test-coverage/67-CONTEXT.md
@.planning/phases/67-observability-csrf-test-coverage/67-RESEARCH.md
@.planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-05-31. Use directly — no exploration needed. -->

triggarr/state.py:50 (existing field — add last_success beside it):
  last_run: str | None  # ISO timestamp
  connected: bool | None

triggarr/state.py:76-78 (existing default — add last_success=None):
  def _default_instance_state() -> AppState:
      return AppState(missing_cursor=0, cutoff_cursor=0, last_run=None)

triggarr/state.py:142 (_merge_defaults — picks up last_success automatically, no change needed):
  merged_section[instance_name] = {**_default_instance_state(), **instance_data}

triggarr/search/engine.py:506 / 748 / 984 (existing last_run write at the three cycle success points):
  ist["last_run"] = datetime.now(UTC).isoformat().replace("+00:00", "Z")

triggarr/web/routes.py:16 (existing import — add timedelta):
  from datetime import UTC, datetime

triggarr/web/routes.py:242 (_build_app_context signature):
  def _build_app_context(request: Request, app_name: str, instance_name: str | None = None) -> dict | None:

triggarr/web/routes.py:278 (existing context dict entry — add last_success/last_success_stale beside it):
  "last_run": app_state.get("last_run"),

triggarr/templates/partials/app_card.html:51-53 (existing schedule row — extend it):
  <div class="text-[11px] font-mono text-triggarr-muted mb-4 flex justify-between">
    <span>Last run: <span class="text-triggarr-text">{% if app.last_run %}{{ app.last_run[11:19] }}{% else %}Never{% endif %}</span></span>
    <span>Next: <span class="text-triggarr-text">{% if app.next_run %}{{ app.next_run[11:16] }}{% else %}&mdash;{% endif %}</span></span>
  </div>
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add last_success field + write it at each engine cycle success point</name>
  <files>triggarr/state.py, triggarr/search/engine.py, tests/test_search.py, tests/test_state.py</files>
  <read_first>
    - triggarr/state.py (current AppState TypedDict at lines 43-61, `_default_instance_state` at 76-78, `_merge_defaults` at 128-150) — file being modified
    - triggarr/search/engine.py (the three cycle fn success points at lines 506, 748, 984 where `ist["last_run"]` is set; the `ist["connected"] = True` guards at 331/567/812) — file being modified
    - tests/test_search.py:241-271 `test_run_radarr_cycle_happy_path` — analog for the new last_success-on-success test
    - tests/test_state.py:24-45 `test_nested_state_round_trip` — analog for the persistence round-trip test
    - .planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md §"triggarr/state.py" and §"triggarr/search/engine.py — RES-02" — exact field + write patterns
  </read_first>
  <behavior>
    - Test 1 (test_state.py): AppState carrying last_success="2026-05-31T10:00:00Z" survives save_state → load_state round-trip with the value intact.
    - Test 2 (test_state.py): loading a state JSON with no last_success key yields `.get("last_success") is None` (verifies _merge_defaults fills the default).
    - Test 3 (test_search.py): a successful run_radarr_cycle (tag-free config, no get_tags_fn) sets `result["radarr"]["Default"]["last_success"] is not None`.
    - Test 4 (test_search.py): a run_radarr_cycle that early-returns on connection failure (httpx.ConnectError, the existing network-failure test pattern) leaves `result["radarr"]["Default"].get("last_success") is None`.
  </behavior>
  <action>
    RED then GREEN. Add `last_success: str | None` to the `AppState` TypedDict in state.py immediately after the `last_run` field (line 50), with a comment "ISO timestamp — last cycle that reached connected=True". Add `last_success=None` to the `_default_instance_state()` return at line 78 (per D-01). No change to save_state/load_state/_merge_defaults — the TypedDict is serialized whole via `json.dump` and `{**_default_instance_state(), **instance_data}` picks up the field automatically (Pitfall 5 / A2).

    In engine.py, at EACH of the three cycle success points (lines 506 Radarr, 748 Sonarr, 984 Lidarr), capture the ISO timestamp once into a `now_iso` local and assign both `ist["last_run"] = now_iso` and `ist["last_success"] = now_iso` (per D-02). The timestamp expression is the existing `datetime.now(UTC).isoformat().replace("+00:00", "Z")`. This location is reached ONLY when the cycle did not early-return on the connection-error path, so last_success semantically means "cycle reached the connected-True end" — distinct from last_run which is also only set here but documents "last attempt that completed".

    Do NOT touch the connection-error early-return paths (lines ~313-321 / ~549-557 / ~794-802) — last_success must stay None on those.
  </action>
  <verify>
    <automated>uv run pytest tests/test_state.py tests/test_search.py -k "last_success or round_trip or happy_path or network_failure" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - state.py contains `last_success: str | None` in AppState and `last_success=None` in `_default_instance_state()`.
    - `grep -c 'last_success' triggarr/search/engine.py` returns 3 (one write per cycle fn).
    - The four behavior tests pass.
    - `uv run ruff check triggarr/state.py triggarr/search/engine.py tests/test_search.py tests/test_state.py` exits 0.
  </acceptance_criteria>
  <done>last_success is a persisted AppState field, written at all three cycle success points and only on success; round-trip and success/failure unit tests pass.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Compute last_success_stale at render time in _build_app_context</name>
  <files>triggarr/web/routes.py, tests/test_web.py</files>
  <read_first>
    - triggarr/web/routes.py (`from datetime import UTC, datetime` at line 16; `_build_app_context` at 242-295 including the `enabled[instance_name]` resolution and early-return-None at ~262, the existing `"last_run": app_state.get("last_run")` context entry at 278) — file being modified
    - triggarr/models/config.py — `InstanceConfig.search_interval: int = 30` field (the per-instance interval, not a global)
    - .planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md §"triggarr/web/routes.py — RES-02: stale computation" — exact computation + import change
    - .planning/phases/67-observability-csrf-test-coverage/67-RESEARCH.md Pitfall 6 (timedelta import) and Pitfall 10 (search_interval resolution safety)
  </read_first>
  <behavior>
    - Test 1: app_state with last_success=None → context `last_success_stale` is True and `last_success` is None.
    - Test 2: app_state with a last_success timestamp older than 2x search_interval minutes → `last_success_stale` is True.
    - Test 3: app_state with a fresh last_success timestamp (within 2x interval) → `last_success_stale` is False.
  </behavior>
  <action>
    RED then GREEN. Change the routes.py import at line 16 from `from datetime import UTC, datetime` to `from datetime import UTC, datetime, timedelta` (per D-03 / Pitfall 6).

    In `_build_app_context`, after the `app_state` and `enabled[instance_name]` are resolved (the function already early-returns None at ~line 262 if `instance_name not in enabled`, so the lookup is safe — Pitfall 10) and before the `return` dict, compute: read `last_success = app_state.get("last_success")`; default `last_success_stale = True`; if `last_success is not None`, parse it with `datetime.fromisoformat(last_success.replace("Z", "+00:00"))`, read `instance_cfg = enabled[instance_name]`, set `threshold = timedelta(minutes=instance_cfg.search_interval * 2)`, and set `last_success_stale = (datetime.now(UTC) - ls_dt) > threshold`; wrap the parse/compare in `except (ValueError, TypeError): last_success_stale = True` (no bare except — CLAUDE.md). Formula per D-03: stale = (last_success is None) OR (now - last_success > 2 x search_interval_minutes).

    Add two keys to the returned context dict alongside `"last_run"`: `"last_success": last_success` and `"last_success_stale": last_success_stale`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web.py -k "last_success or stale" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - routes.py line 16 import includes `timedelta`.
    - `_build_app_context` return dict contains literal keys `"last_success"` and `"last_success_stale"`.
    - The three behavior tests pass (None→stale True, old→stale True, fresh→stale False).
    - `uv run ruff check triggarr/web/routes.py tests/test_web.py` exits 0.
  </acceptance_criteria>
  <done>_build_app_context exposes last_success and a render-time last_success_stale computed against 2x the per-instance search_interval; three boundary tests pass.</done>
</task>

<task type="execute">
  <name>Task 3: Render "Last OK" entry with amber stale flag on the app card</name>
  <files>triggarr/templates/partials/app_card.html, tests/test_web.py</files>
  <read_first>
    - triggarr/templates/partials/app_card.html (schedule row at lines 51-53 with the `[11:19]` slice and `Never` fallback; existing amber tag-warning badge at lines 38-48 using `bg-amber-500/15 text-amber-400`) — file being modified
    - .planning/phases/67-observability-csrf-test-coverage/67-PATTERNS.md §"triggarr/templates/partials/app_card.html" — exact markup + Pitfall 4 guard
    - .planning/phases/67-observability-csrf-test-coverage/67-RESEARCH.md Open Question 1 (three-spans-vs-second-row layout discretion) and Pitfall 4 (no amber on "Never")
  </read_first>
  <action>
    Add a "Last OK" entry to the schedule row. Render `app.last_success[11:19]` (HH:MM:SS, matching the existing `last_run[11:19]` slice) when `app.last_success` is truthy, else the literal "Never". The value span class is conditional: `text-amber-400` when `app.last_success_stale` is true, else `text-triggarr-text`. Per D-04, reuse the existing amber token `text-amber-400` (the tag-warning badge color) — do NOT invent a new color token. The "Never" case (null last_success) renders in `text-triggarr-muted` and MUST NOT be amber (Pitfall 4) — guard the `last_success_stale` check inside the outer `{% if app.last_success %}` so the `else`/Never branch never applies amber.

    Layout discretion (D-04 / Open Question 1): the existing row is `flex justify-between` with two spans. Add "Last OK" as a third span in that row. If three spans crowd the card at narrow width, instead render "Last OK" on a second `<div>` row directly beneath the existing "Last run / Next" row, mirroring its `text-[11px] font-mono text-triggarr-muted` classes. Either placement is acceptable; pick the one that reads cleanly. The card already auto-refreshes via `hx-trigger="every 5s"` — no new polling wiring.

    Add a TestClient render assertion in tests/test_web.py: a dashboard/card render with a stale last_success in state contains the string "Last OK" and the `text-amber-400` class on the value; a render with no last_success contains "Never" and does NOT apply `text-amber-400` to that entry.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web.py -k "card or last_ok or last_success" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app_card.html contains the literal "Last OK" and the `[11:19]` slice on `app.last_success`.
    - The amber class `text-amber-400` is applied only inside the `{% if app.last_success %}` truthy branch, gated by `app.last_success_stale`.
    - Render test asserts "Last OK" present, amber on a stale value, and no amber on the "Never" case.
    - `uv run pytest tests/test_web.py -x -q` passes and `uv run ruff check tests/test_web.py` exits 0.
  </acceptance_criteria>
  <done>The app card shows "Last OK HH:MM:SS" (or "Never"), amber only on a real stale timestamp; render tests assert the markup and the no-amber-on-Never guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → state.json | Timestamp values written to persisted state |
| _build_app_context → template | Context values rendered into dashboard HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure | last_success timestamp in rendered HTML | accept | LOW — a timestamp is not a secret; no API keys, credentials, or PII are in the value. The value is `datetime.now(UTC)` ISO string only. |
| T-67-02 | Tampering | last_success in state.json | accept | LOW — written only at the in-process cycle success point via the existing atomic state-write path; no external input reaches the field. |
| T-67-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (RESEARCH.md: no new dependencies). |
</threat_model>

<verification>
- `uv run pytest tests/test_state.py tests/test_search.py tests/test_web.py -x -q` all green
- `uv run ruff check triggarr/ tests/` exits 0
- Manual (pre-deploy, per 67-VALIDATION.md): dashboard card shows "Last OK", goes amber after > 2x interval without a success, "Never" never amber
</verification>

<success_criteria>
ROADMAP Phase 67 success criterion 1 satisfied: the dashboard shows a "last successful search" timestamp for each enabled app/instance, visually flagged stale (amber) when older than 2x the configured interval.
</success_criteria>

<output>
Create `.planning/phases/67-observability-csrf-test-coverage/67-01-SUMMARY.md` when done
</output>
