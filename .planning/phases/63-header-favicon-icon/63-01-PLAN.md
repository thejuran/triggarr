---
phase: 63
slug: header-favicon-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/static/favicon.svg
  - triggarr/static/favicon.ico
  - triggarr/static/favicon-16x16.png
  - triggarr/static/favicon-32x32.png
  - triggarr/static/apple-touch-icon.png
  - triggarr/static/android-chrome-192x192.png
  - triggarr/static/android-chrome-512x512.png
  - triggarr/templates/base.html
  - triggarr/static/css/output.css
  - tests/test_header_favicon.py
autonomous: false
requirements:
  - HDR-06
tags:
  - ui
  - favicon
  - header
  - static-assets

must_haves:
  truths:
    - "A cleaned-up favicon.svg master asset exists in triggarr/static/ and contains no <script> tags, event handlers, or external references"
    - "The full raster favicon bundle (favicon.ico + 5 PNGs) exists in triggarr/static/ with non-zero sizes and correct pixel dimensions (16, 32, 180, 192, 512) regenerated from the SVG master"
    - "base.html head declares favicon.svg as the primary <link rel=\"icon\" type=\"image/svg+xml\"> entry, placed before the .ico fallback so modern browsers prefer the scalable vector"
    - "The header left zone renders a 24x24px (w-6 h-6) app icon <img> referencing favicon.svg as the first child of a new gap-2 sub-flex wrapping the icon and the 'Triggarr' logo text"
    - "The version badge continues to sit at gap-3 from the Triggarr text because the outer left-zone flex keeps gap-3 w-64 shrink-0 and the new sub-flex counts as a single flex item inside it"
    - "output.css is recompiled so any Tailwind utilities referenced by the new markup (w-6, h-6, gap-2) are present in the compiled stylesheet"
    - "A new pytest module tests/test_header_favicon.py asserts asset-bundle existence, non-zero file sizes, SVG-primary link presence and ordering, and the header <img> markup"
  artifacts:
    - path: "triggarr/static/favicon.svg"
      provides: "Vector master favicon (rounded-square dark gradient, green film-reel-in-magnifying-glass mark)"
      contains: "viewBox=\"0 0 512 512\""
    - path: "triggarr/static/favicon.ico"
      provides: "Legacy multi-resolution ICO favicon (16+32 stacked) regenerated from the SVG master"
    - path: "triggarr/static/favicon-16x16.png"
      provides: "Regenerated 16x16 PNG favicon without the Mar 11 white-dot anti-aliasing artifact"
    - path: "triggarr/static/favicon-32x32.png"
      provides: "Regenerated 32x32 PNG favicon"
    - path: "triggarr/static/apple-touch-icon.png"
      provides: "Regenerated 180x180 PNG apple-touch-icon"
    - path: "triggarr/static/android-chrome-192x192.png"
      provides: "Regenerated 192x192 PNG for PWA / Android home-screen"
    - path: "triggarr/static/android-chrome-512x512.png"
      provides: "Regenerated 512x512 PNG for PWA splash / maskable icon"
    - path: "triggarr/templates/base.html"
      provides: "<head> SVG-primary <link rel=\"icon\"> + header left-zone icon <img> wrapped in gap-2 sub-flex"
      contains: "type=\"image/svg+xml\""
    - path: "triggarr/static/css/output.css"
      provides: "Compiled Tailwind utilities including w-6, h-6, gap-2 used by new header markup"
    - path: "tests/test_header_favicon.py"
      provides: "Asset-existence, non-empty-size, base.html markup, and rendered-HTML assertions for HDR-06"
      contains: "def test_favicon_bundle_exists"
  key_links:
    - from: "triggarr/templates/base.html <head>"
      to: "triggarr/static/favicon.svg"
      via: "<link rel=\"icon\" type=\"image/svg+xml\" href=\"{{ request.url_for('static', path='favicon.svg') }}\">"
      pattern: "type=\"image/svg+xml\""
    - from: "triggarr/templates/base.html header left zone"
      to: "triggarr/static/favicon.svg"
      via: "<img src=\"{{ request.url_for('static', path='favicon.svg') }}\" alt=\"\" class=\"w-6 h-6\">"
      pattern: "w-6 h-6"
    - from: "triggarr/templates/base.html left-zone outer flex"
      to: "new gap-2 icon+text sub-flex"
      via: "outer div keeps flex items-center gap-3 w-64 shrink-0; inner new div uses flex items-center gap-2"
      pattern: "flex items-center gap-2"
    - from: "tests/test_header_favicon.py"
      to: "triggarr/static/ favicon bundle"
      via: "STATIC_DIR / '<filename>' existence + st_size > 0 assertions"
      pattern: "STATIC_DIR"
---

<objective>
Close HDR-06 by shipping a cleaned-up favicon bundle (SVG master + regenerated raster fallbacks) and adding a 24x24 app icon beside the "Triggarr" logo text in the header. The SVG master eliminates the white-dot anti-aliasing artifacts that caused Phase 60 D-05 to defer this requirement; the regenerated raster bundle inherits clean edges from that master. The header icon sits at the leftmost position of the left zone, nested with the existing "Triggarr" span in a new gap-2 sub-flex so the version badge keeps its existing gap-3 spacing from the logo text.

Purpose: Gap closure for HDR-06 (deferred from Phase 60 D-05). Removes the last outstanding item from the v2.7 milestone header scope and fixes the Mar 11 favicon aliasing that produced a visible white dot on the 16x16 browser tab icon.
Output: New/updated static favicon bundle, two surgical edits in base.html (head + left zone), recompiled output.css, and a dedicated test module.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-header-favicon-icon/63-CONTEXT.md
@.planning/phases/63-header-favicon-icon/63-PATTERNS.md
@.planning/phases/60-foundation-header/60-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Current base.html head icon links (lines 7-11, read verbatim on 2026-04-17) -->
From triggarr/templates/base.html:
```html
  <link rel="icon" type="image/x-icon" href="{{ request.url_for('static', path='favicon.ico') }}">
  <link rel="icon" type="image/png" sizes="32x32" href="{{ request.url_for('static', path='favicon-32x32.png') }}">
  <link rel="icon" type="image/png" sizes="16x16" href="{{ request.url_for('static', path='favicon-16x16.png') }}">
  <link rel="apple-touch-icon" sizes="180x180" href="{{ request.url_for('static', path='apple-touch-icon.png') }}">
  <link rel="manifest" href="{{ request.url_for('static', path='site.webmanifest') }}">
```

<!-- Current base.html header left zone (lines 22-39, read verbatim on 2026-04-17) -->
From triggarr/templates/base.html:
```html
    <div class="px-6 py-4 flex items-center justify-between">
      {# Left zone: Logo + Version Badge + Update Badge (D-03: w-64 fixed width) #}
      <div class="flex items-center gap-3 w-64 shrink-0">
        <span class="text-triggarr-green font-bold text-xl tracking-tight">Triggarr</span>
        <button onclick="openChangelog()" type="button"
                class="font-geist-mono px-2 py-0.5 rounded-md bg-triggarr-card border border-triggarr-border text-triggarr-muted text-[10px] font-bold uppercase tracking-wider relative top-px hover:text-white transition-colors cursor-pointer"
                title="View changelog">
          v{{ triggarr_version }}
        </button>
        {% if update_info and update_info.update_available %}
        <a href="{{ update_info.html_url if update_info.get('html_url', '').startswith('https://github.com/') else '#' }}" target="_blank" rel="noopener"
           class="inline-flex items-center gap-1.5 text-xs bg-triggarr-green/15 text-triggarr-green px-2 py-0.5 rounded-full hover:bg-triggarr-green/25 transition-colors"
           title="Update available — click to view release">
          <span class="w-1.5 h-1.5 rounded-full bg-triggarr-green dot-pulse"></span>
          v{{ update_info.latest_version }} available
        </a>
        {% endif %}
      </div>
```

<!-- Existing PNG bundle (Mar 11, 01:06) dimensions + sizes to regenerate -->
| File | Current Size (bytes, Mar 11) | Expected Pixel Dimensions |
|------|------------------------------|---------------------------|
| favicon-16x16.png | 829 | 16 x 16 |
| favicon-32x32.png | 2158 | 32 x 32 |
| apple-touch-icon.png | 41612 | 180 x 180 |
| android-chrome-192x192.png | 45422 | 192 x 192 |
| android-chrome-512x512.png | 315455 | 512 x 512 |
| favicon.ico | 15406 | multi (16 + 32 stacked) |

<!-- favicon.svg root attributes (already in repo, read verbatim on 2026-04-17) -->
From triggarr/static/favicon.svg line 2:
```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512" role="img" aria-label="Cleaned video search favicon">
```
File contains a baked-in rounded-square dark gradient (#243553 -> #0F1A30, rx="64"), a magnifying glass (white + green strokes), and a green film-strip mark inside the lens. No <script>, no on*= handlers, no external <use xlink:href>.

<!-- Test module conventions from tests/test_header_redesign.py -->
From tests/test_header_redesign.py (lines 1-25):
```python
"""Phase 60 header redesign tests.

Verifies font discipline (FONT-01, FONT-02), header layout (HDR-01 through HDR-04),
and connection status pill (HDR-05). HDR-06 is deferred per D-05.
"""

from __future__ import annotations

import asyncio
import re
from unittest.mock import AsyncMock, MagicMock

import aiosqlite
import pytest
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.testclient import TestClient

from tests.conftest import make_settings
from triggarr.db import init_db, insert_search_entry
from triggarr.log_buffer import log_buffer
from triggarr.models.config import GeneralConfig
from triggarr.web.routes import STATIC_DIR, auth_state, router

TEMPLATES_DIR = STATIC_DIR.parent / "templates"
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User regenerates raster favicon bundle via realfavicongenerator.net</name>
  <files>
    triggarr/static/favicon.ico,
    triggarr/static/favicon-16x16.png,
    triggarr/static/favicon-32x32.png,
    triggarr/static/apple-touch-icon.png,
    triggarr/static/android-chrome-192x192.png,
    triggarr/static/android-chrome-512x512.png
  </files>
  <read_first>
    triggarr/static/favicon.svg (must exist before user uploads it),
    .planning/phases/63-header-favicon-icon/63-CONTEXT.md §Regeneration Workflow D-05,
    .planning/phases/63-header-favicon-icon/63-PATTERNS.md §Regeneration workflow pattern
  </read_first>
  <action>
    HUMAN-GATED CHECKPOINT (per D-05). Raster-asset generation is a human-gated action: Claude cannot run realfavicongenerator.net's in-browser image pipeline, and producing high-fidelity multi-resolution PNG/ICO fallbacks from the SVG master is out of scope for deterministic scripting.

Claude does the following BEFORE pausing for the user:
  </action>
  <what-built>
    Pre-checks verified by Claude before the prompt; after the checkpoint, Task 2 runs automated verification on the drop.
  </what-built>
  <how-to-verify>
Pre-check (Claude runs these before issuing the prompt; they must all print the shown output):

```bash
cd /Users/julianamacbook/triggarr
test -f triggarr/static/favicon.svg && echo "SVG_PRESENT"
test -s triggarr/static/favicon.svg && echo "SVG_NONEMPTY"
head -2 triggarr/static/favicon.svg | grep -q 'viewBox="0 0 512 512"' && echo "SVG_VIEWBOX_OK"
grep -c -i -E '<script|on[a-z]+\s*=|xlink:href' triggarr/static/favicon.svg
# Expected last line: 0
```

If any pre-check fails, STOP and tell the user the SVG master is missing or altered; do not proceed to the checkpoint prompt.

User-facing steps (paste verbatim when issuing the checkpoint):

1. Open https://realfavicongenerator.net in a browser.
2. Upload `triggarr/static/favicon.svg` as the source image.
3. On the configuration page, accept the defaults (the existing `site.webmanifest` already targets the default filenames and 192/512 android-chrome sizes). Do NOT change the filename prefixes.
4. Generate the package and download the ZIP.
5. Extract the ZIP. From its contents, copy ONLY these six files into `triggarr/static/`, overwriting the existing Mar 11 versions:
   - favicon.ico
   - favicon-16x16.png
   - favicon-32x32.png
   - apple-touch-icon.png
   - android-chrome-192x192.png
   - android-chrome-512x512.png
6. Do NOT copy the ZIP's `site.webmanifest` (the existing one is already correct) and do NOT copy any browserconfig.xml (not used by this project).
7. Leave `triggarr/static/favicon_io/` (the historical Mar 11 backup directory) untouched; it is out of scope for this phase.
8. When the six files are in place, reply to resume Claude.
  </how-to-verify>
  <acceptance_criteria>
    - `test -f triggarr/static/favicon.svg` exits 0 (master SVG still present after user activity)
    - All six target files listed above exist in `triggarr/static/`
    - None of the six files is zero-length
    - User confirms they did not modify `triggarr/static/site.webmanifest`
  </acceptance_criteria>
  <resume-signal>Reply with "bundle dropped" (or describe any deviation, e.g., "I kept the old favicon.ico because realfavicongenerator didn't include one").</resume-signal>
  <verify>
    <automated>test -f triggarr/static/favicon.svg && test -s triggarr/static/favicon.ico && test -s triggarr/static/favicon-16x16.png && test -s triggarr/static/favicon-32x32.png && test -s triggarr/static/apple-touch-icon.png && test -s triggarr/static/android-chrome-192x192.png && test -s triggarr/static/android-chrome-512x512.png && echo "DROP_RECEIVED"</automated>
  </verify>
  <done>User has replied with resume-signal (e.g., "bundle dropped") AND the six target files exist non-empty in triggarr/static/ AND triggarr/static/favicon.svg is still present. Control returns to the executor to run Task 2 (full dimension + safety verification). If the user reports any deviation in their resume message, escalate to the user before proceeding.</done>
</task>

<task type="auto">
  <name>Task 2: Verify regenerated favicon bundle (existence, non-empty, correct pixel dimensions)</name>
  <files>
    triggarr/static/favicon.svg,
    triggarr/static/favicon.ico,
    triggarr/static/favicon-16x16.png,
    triggarr/static/favicon-32x32.png,
    triggarr/static/apple-touch-icon.png,
    triggarr/static/android-chrome-192x192.png,
    triggarr/static/android-chrome-512x512.png
  </files>
  <read_first>
    triggarr/static/favicon.svg (first 2 lines only, to confirm viewBox),
    .planning/phases/63-header-favicon-icon/63-PATTERNS.md §Verification checklist excerpts
  </read_first>
  <action>
Run the following verification commands in order. All must pass before editing `base.html`. If any command fails, STOP and report the failing check to the user.

1. Confirm SVG master is still present, non-empty, and well-formed:
   ```bash
   cd /Users/julianamacbook/triggarr
   test -f triggarr/static/favicon.svg
   test -s triggarr/static/favicon.svg
   head -2 triggarr/static/favicon.svg | grep -q 'viewBox="0 0 512 512"'
   ```

2. Confirm SVG contains no executable or external content (static assertion, not a runtime scan):
   ```bash
   cd /Users/julianamacbook/triggarr
   # Must print exactly "0" (no matches for <script, on*= handlers, or xlink:href)
   grep -c -i -E '<script|on[a-z]+\s*=|xlink:href' triggarr/static/favicon.svg
   ```

3. Confirm all six raster files exist and are non-empty:
   ```bash
   cd /Users/julianamacbook/triggarr
   test -s triggarr/static/favicon.ico
   test -s triggarr/static/favicon-16x16.png
   test -s triggarr/static/favicon-32x32.png
   test -s triggarr/static/apple-touch-icon.png
   test -s triggarr/static/android-chrome-192x192.png
   test -s triggarr/static/android-chrome-512x512.png
   ```

4. Confirm each PNG has the expected pixel dimensions using `file(1)` (available on macOS + Linux). The expected substrings are exact:
   ```bash
   cd /Users/julianamacbook/triggarr
   file triggarr/static/favicon-16x16.png          | grep -q '16 x 16'
   file triggarr/static/favicon-32x32.png          | grep -q '32 x 32'
   file triggarr/static/apple-touch-icon.png       | grep -q '180 x 180'
   file triggarr/static/android-chrome-192x192.png | grep -q '192 x 192'
   file triggarr/static/android-chrome-512x512.png | grep -q '512 x 512'
   ```

5. Confirm `favicon.ico` is a valid ICO file (magic bytes `00 00 01 00`):
   ```bash
   cd /Users/julianamacbook/triggarr
   # `file` reports "MS Windows icon resource" for valid ICO files
   file triggarr/static/favicon.ico | grep -qi 'icon resource'
   ```

6. Spot-check white-dot regression fix: confirm the 16x16 PNG bytes differ from the historical Mar 11 backup at `triggarr/static/favicon_io/favicon-16x16.png` (the historical version is the one that carried the artifact; the new one must not be byte-identical):
   ```bash
   cd /Users/julianamacbook/triggarr
   if [ -f triggarr/static/favicon_io/favicon-16x16.png ]; then
     ! cmp -s triggarr/static/favicon-16x16.png triggarr/static/favicon_io/favicon-16x16.png && echo "16x16_REGENERATED"
   else
     echo "16x16_BACKUP_ABSENT_SKIP_CMP"
   fi
   ```

If all commands succeed, print "BUNDLE_OK" and proceed to Task 3. Do NOT attempt to regenerate the bundle yourself; that is the human-gated Task 1.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && test -f triggarr/static/favicon.svg && test -s triggarr/static/favicon.svg && head -2 triggarr/static/favicon.svg | grep -q 'viewBox="0 0 512 512"' && [ "$(grep -c -i -E '<script|on[a-z]+\s*=|xlink:href' triggarr/static/favicon.svg)" = "0" ] && test -s triggarr/static/favicon.ico && test -s triggarr/static/favicon-16x16.png && test -s triggarr/static/favicon-32x32.png && test -s triggarr/static/apple-touch-icon.png && test -s triggarr/static/android-chrome-192x192.png && test -s triggarr/static/android-chrome-512x512.png && file triggarr/static/favicon-16x16.png | grep -q '16 x 16' && file triggarr/static/favicon-32x32.png | grep -q '32 x 32' && file triggarr/static/apple-touch-icon.png | grep -q '180 x 180' && file triggarr/static/android-chrome-192x192.png | grep -q '192 x 192' && file triggarr/static/android-chrome-512x512.png | grep -q '512 x 512' && file triggarr/static/favicon.ico | grep -qi 'icon resource' && echo "BUNDLE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `triggarr/static/favicon.svg` exists, is non-empty, and contains `viewBox="0 0 512 512"` in its first two lines
    - Running `grep -c -i -E '<script|on[a-z]+\s*=|xlink:href' triggarr/static/favicon.svg` prints exactly `0`
    - `triggarr/static/favicon.ico` exists, is non-empty, and `file(1)` reports "icon resource" (case-insensitive)
    - `triggarr/static/favicon-16x16.png` exists, non-empty, `file(1)` reports `16 x 16`
    - `triggarr/static/favicon-32x32.png` exists, non-empty, `file(1)` reports `32 x 32`
    - `triggarr/static/apple-touch-icon.png` exists, non-empty, `file(1)` reports `180 x 180`
    - `triggarr/static/android-chrome-192x192.png` exists, non-empty, `file(1)` reports `192 x 192`
    - `triggarr/static/android-chrome-512x512.png` exists, non-empty, `file(1)` reports `512 x 512`
    - The new 16x16 PNG bytes differ from `triggarr/static/favicon_io/favicon-16x16.png` when that backup exists (proves regeneration happened)
    - Task prints `BUNDLE_OK` on stdout
  </acceptance_criteria>
  <done>All seven favicon assets verified present with correct dimensions and types, and the 16x16 regeneration is confirmed distinct from the historical white-dot version. Ready to edit `base.html` in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edit base.html head + header left zone, recompile Tailwind, add test module</name>
  <files>
    triggarr/templates/base.html,
    triggarr/static/css/output.css,
    tests/test_header_favicon.py
  </files>
  <read_first>
    triggarr/templates/base.html (read lines 1-50 before editing to confirm the current head and left-zone markup match the "interfaces" block in this plan's context),
    tests/test_header_redesign.py (read lines 1-40 to copy the import/fixture conventions for the new test module),
    .planning/phases/63-header-favicon-icon/63-PATTERNS.md §Template edits and §Test Module Conventions,
    triggarr/static/css/input.css (confirm no Tailwind config changes are needed — recompile only pulls new utilities from scanned sources)
  </read_first>
  <behavior>
    - Test: `test_favicon_bundle_exists` asserts every file in the seven-asset bundle (favicon.svg + favicon.ico + 5 PNGs) exists under `STATIC_DIR`.
    - Test: `test_favicon_files_non_empty` asserts `stat().st_size > 0` for every file in the bundle (catches accidental zero-byte drops).
    - Test: `test_favicon_svg_linked_as_primary_in_base_html` asserts `type="image/svg+xml"` appears in base.html and that the `favicon.svg` reference appears before the `favicon.ico` reference (index comparison).
    - Test: `test_header_icon_img_present_in_base_html` asserts base.html contains an `<img` tag referencing `favicon.svg` (either single- or double-quoted) and uses the `w-6 h-6` class for 24x24 render.
    - Test: `test_header_icon_subflex_uses_gap_2` asserts base.html contains the new inner `flex items-center gap-2` wrapper and that it precedes the "Triggarr" logo span in file order (verifies icon-then-text structure from D-09).
    - Test: `test_outer_left_zone_preserves_gap_3` asserts the outer left-zone flex still declares `flex items-center gap-3 w-64 shrink-0` (version-badge spacing invariant from D-08).
  </behavior>
  <action>
Execute the four sub-steps in the order listed. Do NOT reorder — the tests in 3c assert against the exact markup produced in 3a, and the CSS compile in 3b must happen before 3d runs pytest (otherwise class-reference assertions may still pass but the rendered dashboard would be visually broken).

### 3a. Edit `triggarr/templates/base.html` — two surgical insertions (per D-06 through D-09)

**Edit #1 (head):** In the `<head>` block (currently lines 7-11), INSERT a new SVG-primary `<link rel="icon">` as the FIRST icon entry, keeping all existing links in place. The resulting five-line `<link>` region must read exactly:

```html
  <link rel="icon" type="image/svg+xml" href="{{ request.url_for('static', path='favicon.svg') }}">
  <link rel="icon" type="image/x-icon" href="{{ request.url_for('static', path='favicon.ico') }}">
  <link rel="icon" type="image/png" sizes="32x32" href="{{ request.url_for('static', path='favicon-32x32.png') }}">
  <link rel="icon" type="image/png" sizes="16x16" href="{{ request.url_for('static', path='favicon-16x16.png') }}">
  <link rel="apple-touch-icon" sizes="180x180" href="{{ request.url_for('static', path='apple-touch-icon.png') }}">
  <link rel="manifest" href="{{ request.url_for('static', path='site.webmanifest') }}">
```

The SVG link MUST come before the `.ico` link so browsers that understand SVG icons prefer the scalable vector (closes HDR-06 / D-05).

**Edit #2 (header left zone):** In the left-zone div (currently lines 23-39, the `<div class="flex items-center gap-3 w-64 shrink-0">` that contains the Triggarr span, version button, and update-badge conditional), WRAP the `<span class="text-triggarr-green font-bold text-xl tracking-tight">Triggarr</span>` line in a new inner flex container and PREPEND an `<img>` as that container's first child. Do NOT change the outer div's classes, do NOT touch the version button or update-badge conditional, and keep all Jinja whitespace/comments as they are.

The left-zone div MUST end up reading exactly:

```html
      {# Left zone: Icon + Logo + Version Badge + Update Badge (D-03: w-64 fixed width) #}
      <div class="flex items-center gap-3 w-64 shrink-0">
        {# Icon + "Triggarr" text group with tighter gap-2 between them (D-07, D-08, D-09) #}
        <div class="flex items-center gap-2">
          <img src="{{ request.url_for('static', path='favicon.svg') }}" alt="" class="w-6 h-6">
          <span class="text-triggarr-green font-bold text-xl tracking-tight">Triggarr</span>
        </div>
        <button onclick="openChangelog()" type="button"
                class="font-geist-mono px-2 py-0.5 rounded-md bg-triggarr-card border border-triggarr-border text-triggarr-muted text-[10px] font-bold uppercase tracking-wider relative top-px hover:text-white transition-colors cursor-pointer"
                title="View changelog">
          v{{ triggarr_version }}
        </button>
        {% if update_info and update_info.update_available %}
        <a href="{{ update_info.html_url if update_info.get('html_url', '').startswith('https://github.com/') else '#' }}" target="_blank" rel="noopener"
           class="inline-flex items-center gap-1.5 text-xs bg-triggarr-green/15 text-triggarr-green px-2 py-0.5 rounded-full hover:bg-triggarr-green/25 transition-colors"
           title="Update available — click to view release">
          <span class="w-1.5 h-1.5 rounded-full bg-triggarr-green dot-pulse"></span>
          v{{ update_info.latest_version }} available
        </a>
        {% endif %}
      </div>
```

**Why this shape:** D-06 (direct SVG `<img>`, no inline bloat), D-07 (`w-6 h-6` = 24x24, heavier than 20px logo text), D-08 (`gap-2` inner, outer stays `gap-3`), D-09 (icon is first child of the grouped logo unit). The outer `w-64 shrink-0` is preserved so the absolute-centered nav (Zone 2) stays balanced with the right-zone `w-64`.

**Alt text:** `alt=""` is intentional (decorative — the adjacent "Triggarr" text serves screen readers). Do NOT replace with `alt="Triggarr"`.

### 3b. Recompile Tailwind CSS

```bash
cd /Users/julianamacbook/triggarr && uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css
```

This is a belt-and-suspenders step: `w-6`, `h-6`, and `gap-2` may already be present from prior phases (nav links use `gap-2` in Plan 60-02). If so, the recompile is a no-op on content but will still rewrite the file with identical or near-identical output. Do NOT skip it — if any utility happens to be new-to-the-compiled-stylesheet the rendered header will be silently broken without this step.

### 3c. Create `tests/test_header_favicon.py`

Write a new module at `tests/test_header_favicon.py` with the exact contents below. Follow project conventions from `tests/test_header_redesign.py`: `from __future__ import annotations`, import `STATIC_DIR` from `triggarr.web.routes`, derive `TEMPLATES_DIR = STATIC_DIR.parent / "templates"`. All tests stay synchronous (pure file/string assertions — no `client` fixture needed).

```python
"""Phase 63 header favicon (HDR-06) tests.

Verifies SVG primary <link>, raster fallback bundle existence and sizing,
and header icon <img> presence + structural invariants. Closes Phase 60 D-05.
"""

from __future__ import annotations

from triggarr.web.routes import STATIC_DIR

TEMPLATES_DIR = STATIC_DIR.parent / "templates"

FAVICON_BUNDLE = (
    "favicon.svg",
    "favicon.ico",
    "favicon-16x16.png",
    "favicon-32x32.png",
    "apple-touch-icon.png",
    "android-chrome-192x192.png",
    "android-chrome-512x512.png",
)


def test_favicon_bundle_exists():
    """HDR-06: Full favicon bundle (SVG master + raster fallbacks) present in static dir."""
    for name in FAVICON_BUNDLE:
        assert (STATIC_DIR / name).exists(), f"missing favicon asset: {name}"


def test_favicon_files_non_empty():
    """HDR-06: Every favicon bundle file is non-empty (catches zero-byte drops)."""
    for name in FAVICON_BUNDLE:
        path = STATIC_DIR / name
        assert path.stat().st_size > 0, f"favicon asset is empty: {name}"


def test_favicon_svg_linked_as_primary_in_base_html():
    """HDR-06: SVG <link> is declared and precedes the .ico fallback."""
    base_html = (TEMPLATES_DIR / "base.html").read_text()
    assert 'type="image/svg+xml"' in base_html
    assert "favicon.svg" in base_html
    svg_idx = base_html.index("favicon.svg")
    ico_idx = base_html.index("favicon.ico")
    assert svg_idx < ico_idx, "SVG favicon link must precede .ico fallback"


def test_header_icon_img_present_in_base_html():
    """HDR-06: Header contains an <img> referencing favicon.svg at w-6 h-6 sizing."""
    base_html = (TEMPLATES_DIR / "base.html").read_text()
    assert "<img" in base_html
    # Quoting can be ' or " depending on Jinja output; accept either.
    assert "path='favicon.svg'" in base_html or 'path="favicon.svg"' in base_html
    assert "w-6 h-6" in base_html


def test_header_icon_subflex_uses_gap_2():
    """HDR-06 / D-08, D-09: icon+text live in an inner flex gap-2 and icon precedes text."""
    base_html = (TEMPLATES_DIR / "base.html").read_text()
    assert "flex items-center gap-2" in base_html
    # The new <img> must appear before the Triggarr logo span in file order.
    img_idx = base_html.index("<img")
    logo_idx = base_html.index(">Triggarr<")
    assert img_idx < logo_idx, "favicon <img> must precede the Triggarr logo span"


def test_outer_left_zone_preserves_gap_3():
    """D-08 invariant: outer left-zone flex keeps gap-3 + w-64 shrink-0 (version-badge spacing)."""
    base_html = (TEMPLATES_DIR / "base.html").read_text()
    assert "flex items-center gap-3 w-64 shrink-0" in base_html
```

### 3d. Run tests, lint, and CSS-recompile-idempotence check

```bash
cd /Users/julianamacbook/triggarr && uv run pytest tests/test_header_favicon.py -x -q
cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/ tests/
# Sanity re-run: recompile once more and confirm no diff vs what we just wrote
cd /Users/julianamacbook/triggarr && uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css
```

All three commands must exit 0. If pytest fails, DO NOT edit the test assertions to match a broken template — fix the template to match the tests (tests encode the HDR-06 contract).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && grep -q 'type="image/svg+xml"' triggarr/templates/base.html && grep -q "favicon.svg" triggarr/templates/base.html && grep -q "w-6 h-6" triggarr/templates/base.html && grep -q 'alt=""' triggarr/templates/base.html && grep -q "flex items-center gap-2" triggarr/templates/base.html && grep -q "flex items-center gap-3 w-64 shrink-0" triggarr/templates/base.html && [ "$(grep -c 'favicon.svg' triggarr/templates/base.html)" -ge 2 ] && uv run tailwindcss -i triggarr/static/css/input.css -o triggarr/static/css/output.css && uv run pytest tests/test_header_favicon.py -x -q && uv run ruff check triggarr/ tests/ && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type="image/svg+xml"' triggarr/templates/base.html` exits 0 (SVG primary link present)
    - `grep -c 'favicon.svg' triggarr/templates/base.html` is at least 2 (one `<link>` in head, one `<img>` in header)
    - Index of first `favicon.svg` occurrence is less than index of first `favicon.ico` occurrence (SVG precedes ICO)
    - `grep -q "w-6 h-6" triggarr/templates/base.html` exits 0 (24x24 icon sizing, D-07)
    - `grep -q 'alt=""' triggarr/templates/base.html` exits 0 (decorative alt per D-06)
    - `grep -q "flex items-center gap-2" triggarr/templates/base.html` exits 0 (new inner sub-flex, D-08)
    - `grep -q "flex items-center gap-3 w-64 shrink-0" triggarr/templates/base.html` exits 0 (outer zone invariant preserved, D-08)
    - In file order, `<img` appears before `>Triggarr<` (icon is first child of the logo group, D-09)
    - `triggarr/static/css/output.css` is writable and recompiles without error
    - `tests/test_header_favicon.py` exists and contains `def test_favicon_bundle_exists`, `def test_favicon_svg_linked_as_primary_in_base_html`, `def test_header_icon_img_present_in_base_html`, `def test_header_icon_subflex_uses_gap_2`, `def test_outer_left_zone_preserves_gap_3`, `def test_favicon_files_non_empty`
    - `uv run pytest tests/test_header_favicon.py -x -q` exits 0 with all six tests passing
    - `uv run ruff check triggarr/ tests/` exits 0
    - base.html does NOT introduce any new `<script>` tag or JavaScript
    - `triggarr/static/site.webmanifest` is unchanged (no edits to manifest in this plan)
  </acceptance_criteria>
  <done>base.html head has SVG-primary favicon link, header left zone has a 24x24 favicon.svg <img> as the first child of a new gap-2 sub-flex that wraps the Triggarr logo, outer left-zone flex still uses gap-3 w-64 shrink-0 so the version badge sits untouched, output.css is freshly recompiled, and all six HDR-06 tests pass with no ruff regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied static assets -> browser | SVG + raster assets served from `/static/` are rendered by the browser on every page. SVG specifically can carry scripts / event handlers / external refs when loaded via `<iframe>` or `<object>`. |
| realfavicongenerator.net -> repo | Third-party tool produces the raster bundle. Bytes land in-repo via a human-mediated download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering (XSS via SVG) | `triggarr/static/favicon.svg` | mitigate | SVG is referenced ONLY via `<link rel="icon" type="image/svg+xml">` and `<img src>` — browsers do NOT execute `<script>` or `on*=` handlers in those contexts (unlike `<object>` or `<iframe>`). Additionally, Task 2 runs `grep -c -i -E '<script\|on[a-z]+\s*=\|xlink:href' triggarr/static/favicon.svg` and fails the plan if the count is non-zero. Inspection on 2026-04-17 confirmed the master SVG is clean (pure `<circle>`, `<line>`, `<rect>`, `<path>`, `<g>` markup). |
| T-63-02 | Information Disclosure (secrets baked into binary assets) | raster PNG/ICO bundle | mitigate | realfavicongenerator.net default output strips all PNG text chunks (tEXt/iTXt/zTXt/iCCP confirmed absent on the Mar 11 origin bundle, per CONTEXT.md §Metadata Scan). The user is instructed to accept defaults. No `SecretStr` or env-var payload could enter this path — these are rendered from a designer SVG, not from runtime config. N/A for auth secrets. |
| T-63-03 | Tampering (supply chain — malicious bytes from third-party tool) | realfavicongenerator.net output | accept | Low-value target: favicon rendering has no access to auth cookies, localStorage, or the DOM. Widely-used reputable tool (same origin as the Mar 11 bundle). Accept and rely on Task 2's `file(1)` dimension checks + ICO magic-byte check to catch gross substitution. |
| T-63-04 | Denial of Service (oversized assets hurting first-paint) | 512x512 PNG | accept | Existing 512x512 is 308 KB and has been in production since Mar 10 with no DoS impact. New bundle expected to be similar or smaller. No new risk introduced. |
| T-63-05 | Spoofing (favicon hijacking) / cache poisoning | `/static/favicon.*` served by FastAPI StaticFiles | accept (N/A) | Same-origin static mount, no CDN, no Cache-Control changes in this plan. Existing static-serve posture unchanged. |
| T-63-06 | Tampering (XSS via template) | `{{ request.url_for('static', path='favicon.svg') }}` | mitigate | Jinja2 autoescape is on (verified in Phase 60 Plan 02 T-60-04). `url_for` returns a Starlette URL object which renders as a URL-safe string. New `<img alt="">` takes no user input. No new XSS surface. |
| T-63-07 | Repudiation / integrity (accidental zero-byte drop) | any file in favicon bundle | mitigate | Task 2 runs `test -s` on every file; `test_favicon_files_non_empty` in Task 3 also enforces `st_size > 0` for every bundle member at CI time. |

**Block on:** high severity only. Mitigations above are either automated (Task 2 grep + Task 3 tests) or structural (browsers-dont-execute-SVG-in-link-or-img-contexts). No high-severity unmitigated threats remain.
</threat_model>

<verification>
- `triggarr/static/favicon.svg` exists, non-empty, viewBox `0 0 512 512`, zero script/handler/xlink matches (HDR-06 root-cause fix for D-05)
- All five PNG files exist at the correct pixel dimensions (16, 32, 180, 192, 512) per `file(1)` (HDR-06 bundle)
- `favicon.ico` exists and is a valid MS Windows icon resource (HDR-06 legacy fallback)
- The new 16x16 PNG bytes differ from the historical `favicon_io/favicon-16x16.png` backup (D-05 white-dot regression closed)
- `base.html` head declares `<link rel="icon" type="image/svg+xml">` BEFORE `<link rel="icon" type="image/x-icon">` (SVG-first ordering, HDR-06)
- `base.html` header left zone contains `<img src="...favicon.svg" alt="" class="w-6 h-6">` as the first child of a new `flex items-center gap-2` inner div (D-06, D-07, D-09)
- Outer left-zone flex still uses `flex items-center gap-3 w-64 shrink-0` (D-08 invariant — version badge spacing preserved)
- `triggarr/static/css/output.css` is recompiled after the template edits
- `tests/test_header_favicon.py` exists with six tests, all pass under `uv run pytest tests/test_header_favicon.py -x -q`
- `uv run ruff check triggarr/ tests/` is clean (CLAUDE.md lint requirement)
- No changes to `triggarr/static/site.webmanifest`, no new JavaScript, no new external dependencies
</verification>

<success_criteria>
- Users see a 24x24 cleaned-up app icon to the left of the "Triggarr" logo text in the header, with 8px between the icon and the logo text and the version badge still sitting 12px from the logo text (HDR-06 success criterion 2, AIDesigner-inspired SeedSyncarr placement)
- Browsers loading the dashboard pick up `favicon.svg` as the primary tab icon (modern browsers) or fall through to the regenerated `.ico` / PNG bundle (legacy browsers) — no white-dot artifact visible at 16x16 (HDR-06 success criterion 1, closes Phase 60 D-05)
- The three-zone header layout (w-64 left, absolute-centered nav, w-64 right) from Phase 60 is preserved exactly; the version badge, update badge, nav, and connection pill all stay visually identical to their Phase 60-62 state
- The full pytest suite for this phase passes (`tests/test_header_favicon.py`), ruff is clean, Tailwind is freshly compiled
- HDR-06 is no longer in the deferred list; Phase 63 closes the last outstanding v2.7 header requirement
</success_criteria>

<output>
After completion, create `.planning/phases/63-header-favicon-icon/63-01-SUMMARY.md`
</output>
