---
phase: 71-presentation-rewrite
plan: 05
type: execute
wave: 2
depends_on: ["71-02"]
files_modified:
  - README.md
  - Dockerfile
  - CONTRIBUTING.md
  - docs/repo-metadata.md
autonomous: true
requirements: [PREW-01, PREW-02, PREW-05, PREW-07]
must_haves:
  decisions:
    - "D-07: the standalone pip-install line must NOT hardcode ANY wheel filename/version — use a version-agnostic releases/latest path"
    - "D-13: repo-metadata (GitHub About ≤350 chars, topics/tags, homepage) is delivered as copy-paste text in docs/repo-metadata.md for the maintainer to apply manually; About one-liner reconciled with the benefit-led README one-liner and SeedSyncarr framing"
  truths:
    - "A cold visitor sees a centered header (4 badges + benefit-led tagline) and a representative screenshot above the fold within 30 seconds"
    - "A Quick Start section near the top gives a minimal compose block + 'visit http://localhost:8484'"
    - "The standalone pip install line is fully version-agnostic — it hardcodes NO wheel filename (no 2.7.2, no 2.8.1, no 2.9.0); it links the GitHub latest-release page OR derives the asset URL at runtime from the releases/latest API"
    - "The systemd unit includes StateDirectory=triggarr so first start does not fail on a missing /var/lib/triggarr"
    - "The Docker first-run sys.exit(1) + restart behavior is explained as expected, not a crash loop"
    - "Tag-based filtering fail-open behavior is documented"
    - "The README SSRF/URL-validation claim is PRECISE: loopback IP literals + link-local/unspecified/multicast IP literals + known cloud-metadata hosts/IPs are blocked at config load and via the web form (the web form additionally rejects loopback IP literals); arbitrary DNS hostnames are NOT resolved at validation time and remain an accepted residual risk (network-layer egress controls are the mitigation). The claim does NOT say 'the web form rejects loopback' broadly or 'cloud-metadata/link-local hostnames are blocked' for arbitrary DNS"
    - "README preserves the External-auth/direct-access trust-boundary wording and the X-Forwarded-Proto/Uvicorn/ASGI-scheme/trusted_proxy_ips wording that test_docs_accuracy.py requires"
    - "A Related Projects section cross-links SeedSyncarr (the highest-signal trivially-fixable gap)"
    - "The Tailwind version is aligned between Dockerfile and committed output.css, and dev docs pin it"
    - "GitHub repo-metadata text (About ≤350 chars, topics, homepage) is drafted as copy-paste text for manual application"
    - "Every README ```toml``` block still parses as valid Settings"
  artifacts:
    - path: "README.md"
      provides: "Full rewrite in D-04 section order with all D-05/D-06/D-07 fixes"
      contains: "## Quick Start"
    - path: "Dockerfile"
      provides: "TAILWINDCSS_VERSION aligned to v4.2.2 (matches output.css header)"
      contains: "TAILWINDCSS_VERSION"
    - path: "CONTRIBUTING.md"
      provides: "Tailwind dev command pins TAILWINDCSS_VERSION"
      contains: "TAILWINDCSS_VERSION"
    - path: "docs/repo-metadata.md"
      provides: "Copy-paste GitHub About / topics / homepage text (PREW-05)"
      contains: "About"
  key_links:
    - from: "README.md SSRF claim (Security Model)"
      to: "triggarr/web/validation.py + triggarr/models/config.py (plan 02 behavior)"
      via: "prose accuracy — precise net behavior statement from 71-02-SUMMARY.md"
      pattern: "config file|config-load|triggarr.toml"
    - from: "README.md Related Projects"
      to: "github.com/thejuran/seedsyncarr"
      via: "cross-link mirroring SeedSyncarr's complement description"
      pattern: "seedsyncarr"
    - from: "Dockerfile TAILWINDCSS_VERSION"
      to: "triggarr/static/css/output.css header version"
      via: "version alignment"
      pattern: "v4\\.2\\.2"
---

<objective>
Rewrite README.md to survive the Phase 70 teardown and mirror SeedSyncarr's structure (D-04..D-07),
align the Tailwind version between Dockerfile/output.css and dev docs (D-07), and draft the GitHub
repo-metadata copy-paste text (PREW-05/D-13). This is the largest presentation deliverable and the
core of PREW-01/PREW-07.

Purpose: Genuine quality must be evident within 30 seconds; the two sibling repos must read as one
coherent author; every cited correctness fix from the three Phase 70 artifacts must land — without
recreating the very HIGH findings this phase exists to close (a hardcoded wheel filename; an
overstated SSRF claim).

Output: A fully rewritten README.md, an aligned Dockerfile + CONTRIBUTING.md Tailwind command, and
a new docs/repo-metadata.md holding the maintainer's copy-paste About/topics/homepage text.

DEPENDS ON plan 02: the README SSRF claim (Security Model section) must state the PRECISE
post-code-change behavior recorded in 71-02-SUMMARY.md, so the validation widening must have landed
first.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-presentation-rewrite/71-PATTERNS.md
@.planning/phases/71-presentation-rewrite/71-RESEARCH.md
@.planning/phases/71-presentation-rewrite/71-02-SUMMARY.md
@.planning/phases/70-presentation-discovery/70-CRITIQUE.md
@.planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md

<interfaces>
<!-- VERIFIED 2026-06-02. Anchors for the rewrite. -->

Current README.md (277 lines), section anchors:
- line 1 `# Triggarr`; lines 3-4 two badges (CI + custom Docker badge with encoded slashes)
- line 6 dry one-liner; line 8 the better benefit sentence (buried)
- line 10 `## Table of Contents` (DROP per D-04)
- line 20 Features; line 34 Screenshots (`?v=2` refs at 36-40); line 42 Install
  (44 Docker, 80 Standalone pip, 99 systemd); line 121 Configuration Reference
  (TOML blocks ~128-185 — MUST stay valid Settings); line 189 Security Model
  (212 plaintext caveat already present; 215 SSRF claim to update); line 269 Development
  (275 tailwindcss watch command)
- line 78 Docker first-run wording (needs sys.exit(1) explanation)
- line 85 `pip install ...triggarr-2.7.2-...whl` (stale hardcoded wheel — the codex HIGH; MUST become
  version-agnostic, NOT re-pinned to 2.8.1)
- line 109 `Environment=TRIGGARR_CONFIG_DIR=/var/lib/triggarr` (add StateDirectory=triggarr after)

Header / badges / Related Projects target text: 71-RESEARCH.md §6 (verified SeedSyncarr badge URLs
and the Related Projects mirror block) and 71-PATTERNS.md "README.md — full rewrite".

Dockerfile:9 `ENV TAILWINDCSS_VERSION=v4.2.1`; output.css:1 header `tailwindcss v4.2.2`
→ align Dockerfile to v4.2.2 (the committed artifact is authoritative; 71-RESEARCH.md Open Q1 / A3).
CONTRIBUTING.md:20 the same tailwindcss watch command.

Covering tests that constrain the rewrite (do NOT break) — tests/test_docs_accuracy.py applies to
BOTH README and SECURITY.md; the README rewrite must keep ALL of these green:
- test_readme_toml_examples_parse_and_validate_as_settings (lines 85-100): every ```toml``` block
  must parse as TOML AND validate as Triggarr Settings.
- test_external_auth_docs_require_upstream_authz_and_blocked_direct_access (lines 32-41): README
  (normalized lowercase) must contain `external`, `authentication`, `authorization`, `direct access`,
  and `blocked` OR `firewalled` — the External-auth trust-boundary wording must survive the reorder.
- test_secure_cookie_docs_use_asgi_scheme_not_direct_forwarded_proto_trust (lines 43-65): README
  must contain `x-forwarded-proto`, `asgi request scheme`, `uvicorn`, and `trusted_proxy_ips`, AND
  must NOT contain any of the forbidden direct-trust phrasings (e.g. "triggarr trusts x-forwarded-proto",
  "carries `x-forwarded-proto: https`", "routes trust `x-forwarded-proto`", "directly trusts
  `x-forwarded-proto`"). Cookie-trust must be described as flowing through the Uvicorn/ASGI scheme
  behind the trusted_proxy_ips allow-list — not direct app-route header trust.
- test_tracked_docs_do_not_reintroduce_stale_auth_or_config_dir_claims (lines 68-83): do NOT
  reintroduce any retired stale claim (e.g. "no built-in authentication", config-dir-not-configurable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full README.md rewrite in D-04 order with all D-05/D-06/D-07 fixes + screenshot refs + Related Projects + PRECISE SSRF claim</name>
  <files>README.md</files>
  <read_first>
    - README.md — the ENTIRE current file (277 lines). Preserve all accurate content (Configuration
      Reference TOML, Security Model substance, External-auth trust-boundary wording, reverse-proxy/
      Synology guidance, the X-Forwarded-Proto/Uvicorn/ASGI/trusted_proxy_ips proxy wording) while
      reordering.
    - tests/test_docs_accuracy.py — READ FIRST, the WHOLE file (101 lines). Four tests gate this
      rewrite (all apply to README): the TOML-block test (lines 85-100), the External-auth /
      direct-access test (lines 32-41), the forwarded-proto/Uvicorn/ASGI/trusted_proxy_ips test
      (lines 43-65, including its `forbidden_claims` list of direct-trust phrasings to AVOID), and the
      stale-claims test (lines 68-83). Every ```toml``` block must stay valid Settings; the External-auth
      and proxy wording must SURVIVE the reorder verbatim-in-substance; no forbidden direct-trust
      phrasing and no retired stale claim may be introduced.
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — "README.md — full rewrite"
      (lines 412-456): target section order, header pattern, screenshot-ref change, Tailwind line.
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md — §2 (install/quickstart accuracy:
      exact pip/systemd/Docker/tag-filter fixes) and §6 (badge URLs + Related Projects mirror block).
    - .planning/phases/70-presentation-discovery/70-CRITIQUE.md — Gripes 1,2,3,6,7,8,9,10,11.
    - .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md — divergence signals 1-7,11.
    - .planning/phases/71-presentation-rewrite/71-02-SUMMARY.md — the EXACT precise SSRF behavior
      statement to quote (do NOT broaden it).
  </read_first>
  <action>
    Rewrite README.md top-to-bottom in the D-04 section order: (1) centered `<div align="center">`
    header with the `# Triggarr` H1, the FOUR badges (CI, Release shields.io/github/v/release/
    thejuran/triggarr, Docker simplified `img.shields.io/badge/docker-ghcr.io-blue`, License
    shields.io/github/license/thejuran/triggarr) using the exact URL forms in 71-RESEARCH.md §6, and
    the benefit-led tagline directly under it (D-06 direction; final copy at executor discretion);
    (2) above-the-fold representative screenshot (`docs/screenshots/dashboard.png`, DROP the `?v=2`
    cache-buster, descriptive alt text); (3) `## Quick Start` — a minimal compose block derived from
    the current Docker block + "visit http://localhost:8484"; (4) `## Features` (honest list; fix the
    Tag-based filtering bullet to document fail-open per 71-RESEARCH.md §2e); (5) `## How It Works`;
    (6) `## Install` (full Docker + standalone reference); (7) `## Configuration Reference` (keep TOML
    blocks valid Settings); (8) `## Screenshots` (fuller set, clean alt text, no `?v=`);
    (9) `## Security Model`; (10) `## Related Projects`; (11) `## Contributing / Development`. DROP
    the Table of Contents (D-04).

    Apply the correctness fixes:
    - PIP INSTALL (D-07, codex HIGH): the standalone install MUST be fully version-agnostic and MUST
      NOT hardcode ANY wheel filename or version number — not the old `2.7.2`, not the current `2.8.1`,
      not a future `2.9.0`. Re-pinning to a newer stale filename would recreate the exact Phase 70
      HIGH finding. Use one of: (a) link users to the GitHub latest-release page
      (https://github.com/thejuran/triggarr/releases/latest) and instruct them to download the wheel
      from there, OR (b) a curl-to-`releases/latest` API + pip pattern that derives the asset
      download URL at runtime (e.g. query the GitHub releases/latest API, extract the `.whl`
      browser_download_url, then `pip install` it). The README MUST NOT contain any literal matching
      `triggarr-<major>.<minor>.<patch>-py3-none-any.whl`.
    - SYSTEMD (codex HIGH): add `StateDirectory=triggarr` to the systemd unit after the
      TRIGGARR_CONFIG_DIR Environment line so first start does not fail on a missing /var/lib/triggarr.
    - DOCKER FIRST-RUN (D-07 / critique Gripe 8): add one sentence to the Docker first-run text
      explaining the `sys.exit(1)` + `restart: unless-stopped` behavior is expected on first boot
      (writes a default config then exits for the operator to fill in), not a crash loop.
    - TAG-FILTER (codex MEDIUM): document the tag-based filtering fail-open behavior in the Features
      and/or relevant section per 71-RESEARCH.md §2e.
    - SSRF / URL-VALIDATION CLAIM (codex HIGH, the core honesty fix): update the Security Model claim
      (current line ~215) to the PRECISE post-plan-02 behavior quoted from 71-02-SUMMARY.md. The claim
      MUST state: loopback IP literals, link-local / unspecified / multicast IP literals, and known
      cloud-metadata hostnames/IPs are blocked both at config load (triggarr.toml at startup) and via
      the web settings form; the web settings form ADDITIONALLY rejects loopback IP literals; arbitrary
      DNS hostnames are NOT resolved at validation time and remain an accepted residual risk, with
      network-layer egress controls as the mitigation. The claim MUST NOT say "the web form rejects
      loopback" without the "IP literal" qualifier (false for the DNS name `localhost`), MUST NOT claim
      "cloud-metadata and link-local hostnames are blocked" broadly (only known metadata hostnames in
      the block-list + IP literals are; arbitrary DNS names are not resolved), and MUST NOT overstate
      coverage. KEEP the existing at-rest-plaintext caveat.
    - PROXY / FORWARDED-PROTO wording (test_docs_accuracy invariant): the rewrite MUST preserve the
      X-Forwarded-Proto / `asgi request scheme` / Uvicorn / `trusted_proxy_ips` wording describing
      cookie-trust flowing through the Uvicorn/ASGI scheme behind the trusted-proxy allow-list, and
      MUST NOT introduce any of the forbidden direct-trust phrasings from test_docs_accuracy.py:54-62.
    - EXTERNAL-AUTH wording (test_docs_accuracy invariant): preserve the External-auth-mode
      trust-boundary wording — upstream authentication AND authorization required, direct access to
      port 8484 must be blocked/firewalled.

    Add the `## Related Projects` SeedSyncarr cross-link mirroring SeedSyncarr's complement line
    (71-RESEARCH.md §6). In the Development section, prepend `TAILWINDCSS_VERSION=v4.2.2` to the
    tailwindcss watch command. Do NOT reintroduce any retired stale claim from test_docs_accuracy.py:71-80.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_accuracy.py -x -q && uv run pytest tests/test_community_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Quick Start` and `## Related Projects` and `## Security Model`
    - `README.md` does NOT contain `## Table of Contents`
    - `README.md` does NOT match the regex `triggarr-[0-9]+\.[0-9]+\.[0-9]+-py3-none-any\.whl` (no hardcoded wheel filename — NOT 2.7.2, NOT 2.8.1, NOT any version): `grep -Eq 'triggarr-[0-9]+\.[0-9]+\.[0-9]+-py3-none-any\.whl' README.md` returns NON-zero (no match)
    - `README.md` standalone install references `releases/latest` (the GitHub latest-release page or the releases/latest API) — `grep -q 'releases/latest' README.md` exits 0
    - `README.md` contains `StateDirectory=triggarr`
    - `README.md` contains `seedsyncarr` (the Related Projects cross-link)
    - `README.md` contains all four badge URLs: `actions/workflows/ci.yml/badge.svg`, `github/v/release/thejuran/triggarr`, `badge/docker-ghcr.io-blue`, `github/license/thejuran/triggarr`
    - `README.md` contains `TAILWINDCSS_VERSION=v4.2.2` on the tailwindcss watch line
    - `README.md` screenshot refs do NOT contain `?v=` (grep for `png?v=` returns nothing)
    - `README.md` Security Model SSRF claim is PRECISE: it references config-file/startup validation (grep -iE 'config file|config-load|triggarr.toml|startup' within the Security Model section matches), qualifies the web-form loopback rejection with "IP literal", and states arbitrary DNS hostnames are not resolved / remain a residual risk. It does NOT contain a broad unqualified "rejects loopback" or "metadata and link-local hostnames are blocked" claim.
    - `README.md` Features section documents tag-filter fail-open (contains `fail-open` or "searches all" wording near the tag-filtering bullet)
    - `uv run pytest tests/test_docs_accuracy.py -x -q` exits 0 (ALL four tests: TOML blocks valid; External-auth/direct-access wording present; forwarded-proto/Uvicorn/ASGI/trusted_proxy_ips wording present with no forbidden direct-trust phrasing; no stale claims)
  </acceptance_criteria>
  <done>README fully rewritten in D-04 order with all teardown/codex fixes; pip install fully version-agnostic (no wheel filename); SSRF claim precise; External-auth + proxy wording preserved; the entire tests/test_docs_accuracy.py + community-health suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Align Dockerfile Tailwind pin, fix CONTRIBUTING.md Tailwind command, and draft repo-metadata copy-paste text</name>
  <files>Dockerfile, CONTRIBUTING.md, docs/repo-metadata.md</files>
  <read_first>
    - Dockerfile — line 9 `ENV TAILWINDCSS_VERSION=v4.2.1`.
    - triggarr/static/css/output.css — line 1 header confirming `tailwindcss v4.2.2` (the committed
      artifact, authoritative per 71-RESEARCH.md Open Q1 / A3).
    - CONTRIBUTING.md — line ~20, the `uv run tailwindcss ... --watch` dev command.
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — "CONTRIBUTING.md — Tailwind version
      alignment" (lines 484-495) and the README Tailwind fix note.
    - .planning/phases/71-presentation-rewrite/71-CONTEXT.md — D-13 (repo-metadata: About ≤350 chars,
      topics, homepage; reconcile About one-liner with the new benefit-led README one-liner and
      SeedSyncarr's About framing).
    - .planning/phases/70-presentation-discovery/70-CONSISTENCY-AUDIT.md — signal 11 (one-liner) for
      About-text alignment.
  </read_first>
  <action>
    In Dockerfile: change `ENV TAILWINDCSS_VERSION=v4.2.1` to `v4.2.2` so the build pin matches the
    committed output.css header (the shipped artifact is authoritative; regenerating CSS is not
    needed). In CONTRIBUTING.md: prepend `TAILWINDCSS_VERSION=v4.2.2` to the tailwindcss watch
    command at line ~20 (matching the README fix), so contributors regenerate CSS with the same
    version. Create docs/repo-metadata.md (new file) containing the PREW-05 copy-paste text for the
    maintainer to apply manually in the GitHub web UI: an About description (≤350 chars, reconciled
    with the new benefit-led README one-liner and consistent with SeedSyncarr's About framing), a
    topics/tags list (executor's discretion — e.g. radarr, sonarr, lidarr, arr, self-hosted, docker,
    automation, homelab, media-automation), and the homepage link. Clearly label the file as
    "manual-apply: maintainer pastes these into GitHub → repo Settings/About" (D-13 — cannot be
    applied from the session). State the About char count to confirm the ≤350 limit.
  </action>
  <verify>
    <automated>grep -q "TAILWINDCSS_VERSION=v4.2.2" Dockerfile && grep -q "TAILWINDCSS_VERSION=v4.2.2" CONTRIBUTING.md && test -f docs/repo-metadata.md && uv run pytest tests/test_community_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` contains `TAILWINDCSS_VERSION=v4.2.2` and no longer contains `v4.2.1`
    - `CONTRIBUTING.md` contains `TAILWINDCSS_VERSION=v4.2.2` on its tailwindcss command line
    - `docs/repo-metadata.md` exists, contains an `About` section, a topics/tags list, a homepage link, and a manual-apply note
    - The About description in docs/repo-metadata.md is ≤350 characters (the file states the count)
    - `uv run pytest tests/test_community_health.py -x -q` exits 0 (CONTRIBUTING.md accuracy intact)
    - `uv run ruff check` is not applicable to these files; no Python changed
  </acceptance_criteria>
  <done>Dockerfile + CONTRIBUTING Tailwind version aligned to v4.2.2; repo-metadata copy-paste text drafted for manual application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits Markdown, a Dockerfile build-arg pin, and a new docs file. No runtime code changes
and no new attack surface are introduced at execution time.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | Information Disclosure (false-security claim) | README Security Model SSRF wording | mitigate | The SSRF/URL-validation claim is updated to match the PRECISE post-plan-02 code behavior (loopback IP literals + link-local/unspecified/multicast IP literals + known metadata hosts blocked at config load and web form; web form additionally rejects loopback IP literals; arbitrary DNS not resolved = accepted residual risk), eliminating the stale overstatement (codex HIGH README:215) WITHOUT introducing a new overstatement. Accuracy is enforced by reviewer source-assertion + the depends_on:71-02 ordering + the test_docs_accuracy.py invariants. |
| T-71-03 | Tampering (supply-chain, stale install instruction) | README standalone pip-install line | mitigate | The install line is made fully version-agnostic (releases/latest page or API-derived asset URL) so it cannot rot into a stale/missing-wheel instruction; the no-hardcoded-wheel acceptance criterion (regex non-match) blocks recreating the Phase 70 HIGH finding. |
| (none) | — | Dockerfile build pin / repo-metadata text | accept | Tailwind version alignment is a build-reproducibility fix, not a security boundary; repo-metadata is copy-paste text. No new attack surface. |

Credential-scrub note: docs/repo-metadata.md and the rewritten README must contain NO real API
keys, hostnames, or credentials — only placeholders (`<radarr-api-key>` etc.), consistent with the
existing docs convention.
</threat_model>

<verification>
- `uv run pytest tests/test_docs_accuracy.py tests/test_community_health.py -x -q` exits 0 (all four docs-accuracy tests + community health).
- README in D-04 order; ToC dropped; all D-07 correctness fixes present; pip install version-agnostic (no wheel filename regex match); SSRF claim precise; External-auth + proxy wording preserved.
- Dockerfile + CONTRIBUTING Tailwind aligned to v4.2.2.
- docs/repo-metadata.md present with ≤350-char About + topics + homepage + manual-apply note.
- No real secrets/hostnames in any edited file.
</verification>

<success_criteria>
- README rewritten to survive the teardown (PREW-01) and reconciled with SeedSyncarr (PREW-07).
- Standalone install hardcodes NO wheel filename (D-07); SSRF claim precise and honest.
- Screenshot refs/alt text updated, `?v=` dropped (PREW-02 refs-only; capture deferred to walkthrough).
- Repo-metadata copy-paste text drafted (PREW-05).
- TOML-accuracy + External-auth + forwarded-proto + community-health tests green.
</success_criteria>

<output>
Create `.planning/phases/71-presentation-rewrite/71-05-SUMMARY.md` when done. Note that PREW-02
screenshot CAPTURE is deferred to the milestone-end NAS walkthrough (only refs/alt text updated
here), that PREW-05 repo-metadata requires manual application by the maintainer, and confirm the
pip-install line hardcodes no wheel filename and the SSRF claim quotes the precise 71-02-SUMMARY.md
behavior without broadening it.
</output>
