---
phase: 71-presentation-rewrite
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_validation.py
  - tests/test_config.py
autonomous: true
requirements: [PREW-01]
must_haves:
  decisions:
    - "D-02: config-load SSRF validation is the relaxed variant (loopback/localhost permitted; cloud-metadata + link-local still blocked) — the RED tests here assert exactly that contract"
    - "D-03: a covering config-load validation test is required (metadata/link-local rejected, loopback + private-LAN allowed) and no existing validate_arr_url / web-form test is deleted or skipped"
  truths:
    - "A failing TestValidateArrUrlConfig class exists describing the relaxed config-load SSRF contract (loopback allowed, metadata/link-local blocked)"
    - "Failing InstanceConfig integration tests exist describing config-load rejection of metadata/link-local and acceptance of loopback/localhost/private-LAN"
    - "A test asserts config-load URL validation runs independent of `enabled` (a disabled instance with an unsafe URL is still rejected) — the intended-hardening behavior"
    - "A combined-case test (metadata host + apikey= query) asserts the apikey rejection fires, proving reject_apikey_in_url runs before/independently of validate_url_ssrf"
    - "No existing TestValidateArrUrl test or web-form validation test is modified or skipped"
  artifacts:
    - path: "tests/test_validation.py"
      provides: "TestValidateArrUrlConfig class (RED stub) + import of validate_arr_url_config"
      contains: "class TestValidateArrUrlConfig"
    - path: "tests/test_config.py"
      provides: "InstanceConfig config-load SSRF integration tests (RED stubs)"
      contains: "test_instance_config_metadata_url_raises"
  key_links:
    - from: "tests/test_validation.py"
      to: "triggarr.web.validation.validate_arr_url_config"
      via: "import (will fail until plan 02 adds the function)"
      pattern: "validate_arr_url_config"
    - from: "tests/test_config.py"
      to: "triggarr.models.config.InstanceConfig.url validator"
      via: "InstanceConfig construction with metadata/loopback URLs"
      pattern: "pytest.raises\\(ValidationError\\)"
---

<objective>
Author the RED-phase test stubs for the config-load SSRF validation contract (D-01/D-02/D-03)
BEFORE any implementation exists. This is the Nyquist Wave 0 requirement from 71-VALIDATION.md:
the tests must exist and fail (ImportError / no validator) so plan 02 implements against a
fixed contract rather than writing tests after the fact.

Purpose: Lock the expected behavior of the new relaxed config-load validator and its
InstanceConfig field_validator into executable tests first. Plan 02 (TDD GREEN) makes them pass.

Output: Two extended test files — a new TestValidateArrUrlConfig unit class in
tests/test_validation.py and the InstanceConfig integration tests in tests/test_config.py
(including a disabled-instance case and a combined apikey+metadata ordering case).
At the end of THIS plan the new tests FAIL (RED) because validate_arr_url_config and the
validate_url_ssrf field_validator do not exist yet. That is the correct, expected state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-presentation-rewrite/71-PATTERNS.md
@.planning/phases/71-presentation-rewrite/71-VALIDATION.md

<interfaces>
<!-- Contract the tests assert against. validate_arr_url_config does NOT exist yet (plan 02 adds it). -->
<!-- Signature the tests must call (from 71-RESEARCH.md Pattern 2 / 71-PATTERNS.md): -->

Target (to be implemented in plan 02), tests written against it now:
```
# triggarr/web/validation.py
def validate_arr_url_config(url: str) -> tuple[bool, str]:
    # relaxed: loopback ALLOWED; metadata + link-local + unspecified + multicast BLOCKED
    # empty/whitespace url -> (True, "")

# triggarr/models/config.py — InstanceConfig gains a second @field_validator("url")
# named validate_url_ssrf, defined AFTER reject_apikey_in_url, calling validate_arr_url_config.
# field_validators run on the `url` field regardless of `enabled` (Pydantic validates the
# field whenever it is set), so a disabled instance with an unsafe URL is still rejected.
```

Existing InstanceConfig construction signature (from tests/test_config.py analogs, VERIFIED):
```
InstanceConfig(url=<str>, api_key=SecretStr("k"), enabled=True)
```
The existing SEC-02 apikey tests (VERIFIED tests/test_config.py:250-268) raise with
`pytest.raises(ValidationError, match="apikey=")`. The combined-case test below reuses that
`match="apikey="` to prove the apikey rejection fires even when the host is also a metadata host.
tests/test_config.py already imports `from pydantic import SecretStr, ValidationError` (VERIFIED line 13).
tests/test_validation.py currently imports from triggarr.web.validation:
safe_int, safe_log_level, validate_arr_url, validate_instance_name (add validate_arr_url_config).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestValidateArrUrlConfig RED stub to tests/test_validation.py</name>
  <files>tests/test_validation.py</files>
  <read_first>
    - tests/test_validation.py — full file. Read the existing import block (line ~5) and the
      TestValidateArrUrl class (lines 11-141) to mirror its one-method-per-case style, its
      `ok, err = ...; assert ok is ...` shape, and its substring error assertions
      (`assert "blocked" in err.lower()`). Do NOT use @pytest.mark.parametrize (the analog does not).
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — section "tests/test_validation.py —
      add TestValidateArrUrlConfig class" (lines 153-223): exact import line to add and the new
      class skeleton (7 methods).
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md — §1 "D-03 minimum test coverage"
      table (lines 316-327): the six unit cases that MUST be covered.
  </read_first>
  <behavior>
    - test_empty_string_allowed: validate_arr_url_config("") -> (True, "")
    - test_loopback_ipv4_allowed: validate_arr_url_config("http://127.0.0.1:7878") -> (True, "")
    - test_localhost_hostname_allowed: validate_arr_url_config("http://localhost:7878") -> (True, "")
    - test_private_192_168_allowed: validate_arr_url_config("http://192.168.1.100:7878") -> (True, "")
    - test_cloud_metadata_ip_blocked: validate_arr_url_config("http://169.254.169.254/latest/meta-data") -> ok is False
    - test_link_local_ip_blocked: validate_arr_url_config("http://169.254.42.42") -> ok is False, "blocked" in err.lower()
    - test_gcp_metadata_hostname_blocked: validate_arr_url_config("http://metadata.google.internal") -> ok is False
  </behavior>
  <action>
    Add `validate_arr_url_config` to the existing `from triggarr.web.validation import (...)` import
    block in tests/test_validation.py (keep alphabetical/existing ordering as the file uses).
    Add a new class `TestValidateArrUrlConfig` immediately AFTER `TestValidateArrUrl` closes
    (~line 141) and BEFORE `TestSafeInt` (~line 147), containing the seven methods enumerated in
    <behavior>, each mirroring the analog's `ok, err = validate_arr_url_config(<url>)` then
    `assert` shape. Use substring assertions for error text (`assert "blocked" in err.lower()`),
    never exact-match. This is the RED stub — the import of validate_arr_url_config WILL fail
    until plan 02 adds the function; that failure is the expected end state of this task.
    Do NOT touch the existing TestValidateArrUrl class.
  </action>
  <verify>
    <automated>uv run pytest tests/test_validation.py::TestValidateArrUrlConfig -x -q 2>&1 | grep -qiE "error|failed|no attribute|cannot import" && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_validation.py contains the string `class TestValidateArrUrlConfig`
    - tests/test_validation.py contains the string `validate_arr_url_config` in the import block
    - tests/test_validation.py contains all seven method names from <behavior>
    - The existing `class TestValidateArrUrl` is byte-for-byte unchanged (no methods removed/renamed/skipped)
    - `uv run pytest tests/test_validation.py::TestValidateArrUrlConfig -x -q` exits NON-zero (RED: import/collection error because validate_arr_url_config does not exist yet)
    - `uv run pytest tests/test_validation.py::TestValidateArrUrl -x -q` still exits 0 (existing strict tests untouched and importable — NOTE: if the new import breaks module collection, this is expected to also fail at RED; in that case verify the class text is present and unmodified by reading the file)
  </acceptance_criteria>
  <done>TestValidateArrUrlConfig class present with 7 methods + import line added; class fails to collect/run only because the not-yet-implemented function is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add InstanceConfig config-load SSRF integration RED stubs to tests/test_config.py (incl. disabled-instance + apikey-ordering cases)</name>
  <files>tests/test_config.py</files>
  <read_first>
    - tests/test_config.py — read the import block (confirm `from pydantic import SecretStr,
      ValidationError` at ~line 13), the SEC-02 apikey tests (`test_instance_url_rejects_apikey`
      ~lines 250-268, which raises with `pytest.raises(ValidationError, match="apikey=")`, and
      `test_instance_url_accepts_without_apikey` ~lines 271-286) for the
      `with pytest.raises(ValidationError): InstanceConfig(url=..., api_key=SecretStr("k"),
      enabled=True)` shape, and confirm where the SEC-02 block ends (~line 286) so the new tests
      land before `test_multi_instance_radarr` (~line 289).
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — section "tests/test_config.py — add
      SSRF integration tests" (lines 227-302): the four exact test functions to add and the
      placement note (the two extra cases below are additive on top of those four).
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md — §1 D-03 integration rows
      (test_instance_config_loopback_url_valid, test_instance_config_metadata_url_raises).
  </read_first>
  <behavior>
    - test_instance_config_loopback_url_valid: InstanceConfig(url="http://127.0.0.1:7878", ...) constructs OK; cfg.url == "http://127.0.0.1:7878"
    - test_instance_config_localhost_url_valid: InstanceConfig(url="http://localhost:7878", ...) constructs OK
    - test_instance_config_metadata_url_raises: InstanceConfig(url="http://169.254.169.254/latest/meta-data", ...) raises ValidationError
    - test_instance_config_link_local_url_raises: InstanceConfig(url="http://169.254.42.42", ...) raises ValidationError
    - test_instance_config_disabled_instance_metadata_url_still_raises: InstanceConfig(url="http://169.254.169.254/latest/meta-data", api_key=SecretStr("k"), enabled=False) STILL raises ValidationError — config-load URL validation runs independent of `enabled` (intended hardening; a disabled instance with a genuinely-unsafe URL is still rejected at startup)
    - test_instance_config_metadata_url_with_apikey_rejects_apikey_first: InstanceConfig(url="http://169.254.169.254?apikey=secret", ...) raises ValidationError with `match="apikey="` — proves reject_apikey_in_url runs before/independently of validate_url_ssrf (the metadata block also applies, but the apikey message surfaces because that validator is defined first)
  </behavior>
  <action>
    Add a new clearly-commented section "D-01/D-02/D-03: InstanceConfig config-load SSRF validation
    (relaxed variant)" after the SEC-02 block (~line 286) and before `test_multi_instance_radarr`
    (~line 289), containing the six module-level test functions enumerated in <behavior>, each
    using the existing `InstanceConfig(url=..., api_key=SecretStr("k"), enabled=True)` construction
    shape (the disabled-instance case uses `enabled=False`). For the generic raising cases
    (metadata, link-local, disabled-instance) use `with pytest.raises(ValidationError):` (omit
    `match=` — the error text is generic "Blocked address"/"Blocked hostname", or optionally
    `match="Blocked"`). For the combined apikey+metadata case use
    `with pytest.raises(ValidationError, match="apikey="):` so it asserts the apikey rejection
    fires first (proving validator ordering, not just that *some* ValidationError is raised).
    Do NOT add @pytest.mark.asyncio (synchronous). These are RED stubs: until plan 02 adds the
    validate_url_ssrf field_validator on InstanceConfig, the metadata/link-local/disabled-instance
    `*_raises` tests will FAIL because no ValidationError is raised. (The apikey-ordering case may
    already PASS at RED, because reject_apikey_in_url already exists — that is acceptable; its job is
    to lock the ordering proof so it does not silently weaken when validate_url_ssrf lands.)
  </action>
  <verify>
    <automated>uv run pytest "tests/test_config.py::test_instance_config_metadata_url_raises" "tests/test_config.py::test_instance_config_link_local_url_raises" "tests/test_config.py::test_instance_config_disabled_instance_metadata_url_still_raises" -q 2>&1 | grep -qiE "failed|error|did not raise" && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config.py contains all six function names from <behavior>
    - `uv run pytest tests/test_config.py::test_instance_config_metadata_url_raises -q` exits NON-zero (RED: no ValidationError raised yet — DID NOT RAISE)
    - `uv run pytest tests/test_config.py::test_instance_config_disabled_instance_metadata_url_still_raises -q` exits NON-zero at RED (no validator yet) — proves the disabled-instance case is a genuine new assertion, not a no-op
    - `uv run pytest tests/test_config.py::test_instance_config_metadata_url_with_apikey_rejects_apikey_first -q` uses `match="apikey="` so it asserts the specific message, not just that any ValidationError is raised
    - `uv run pytest tests/test_config.py::test_instance_config_loopback_url_valid -q` exits 0 (loopback already constructs fine pre-implementation — passes even at RED, which is acceptable)
    - No existing test in tests/test_config.py is renamed, deleted, or marked skip/xfail
    - `uv run ruff check tests/test_config.py` exits 0
  </acceptance_criteria>
  <done>Six InstanceConfig SSRF integration tests added (incl. disabled-instance + apikey-ordering); the metadata/link-local/disabled-instance raising tests fail (RED) because the field_validator does not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config file → InstanceConfig (config load) | Untrusted-ish operator-set URL string crosses into the HTTP-client target; the SSRF surface this phase closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Spoofing / Information Disclosure | `InstanceConfig.url` config-load path | mitigate | This plan writes the executable contract (RED tests) for the relaxed config-load validator; plan 02 implements it (V5 Input Validation). No production code changes here. |

This is a test-only plan. It adds no new attack surface and ships no runtime code. The threat it
documents is mitigated by plan 02; this plan only fixes the expected behavior in tests first.
</threat_model>

<verification>
- New test classes/functions present in both files.
- New SSRF tests are RED (metadata/link-local/disabled-instance raising tests fail; import of validate_arr_url_config fails) — the expected Wave-0 state.
- All pre-existing tests in both files remain present and unmodified (no skips/xfails/deletes).
- `uv run ruff check tests/` exits 0.
</verification>

<success_criteria>
- tests/test_validation.py has TestValidateArrUrlConfig (7 methods) + the new import.
- tests/test_config.py has the six InstanceConfig SSRF integration tests (incl. disabled-instance + apikey-ordering).
- No existing TestValidateArrUrl or web-form validation test touched (D-03 constraint).
- The new tests fail in exactly the way that proves the contract is not yet implemented (RED).
</success_criteria>

<output>
Create `.planning/phases/71-presentation-rewrite/71-01-SUMMARY.md` when done. Record which tests
are RED and confirm no existing tests were modified, so plan 02 can drive them GREEN.
</output>
