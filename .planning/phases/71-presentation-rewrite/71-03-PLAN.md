---
phase: 71-presentation-rewrite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug-report.yml
  - tests/test_github_templates.py
autonomous: true
requirements: [PREW-04]
must_haves:
  decisions:
    - "D-10: bug-report.yml version dropdown updated (v2.8.1, v2.8, v2.7, v2.6, v2.5, v2.4, Older) and App Type dropdown gains Lidarr + All; CONTRIBUTING.md / PR template / LICENSE confirmed present and accurate"
  truths:
    - "A user on any current Triggarr version (v2.8.1 down to v2.4) can select an accurate version in the bug-report dropdown instead of falling back to 'Older'"
    - "A Lidarr user filing a bug can select 'Lidarr' (and 'All') in the App Type dropdown"
    - "The github-template snapshot tests assert the NEW dropdown contents and stay green"
    - "CONTRIBUTING.md, PR template, feature-request.yml, config.yml, LICENSE are confirmed present and accurate (no drift introduced)"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug-report.yml"
      provides: "Current version dropdown (v2.8.1..v2.4, Older) + App Type dropdown (Radarr/Sonarr/Lidarr/All)"
      contains: "Lidarr"
    - path: "tests/test_github_templates.py"
      provides: "Snapshot assertions updated to the new dropdown options"
      contains: "v2.8.1"
  key_links:
    - from: "tests/test_github_templates.py test_version_dropdown_options"
      to: ".github/ISSUE_TEMPLATE/bug-report.yml version options"
      via: "substring assertion over file content"
      pattern: "v2\\.8\\.1"
    - from: "tests/test_github_templates.py test_app_type_dropdown_options"
      to: ".github/ISSUE_TEMPLATE/bug-report.yml App Type options"
      via: "substring assertion over file content"
      pattern: "Lidarr"
---

<objective>
Fix the community-health gaps the Phase 70 teardown flagged (D-10 / critique Gripes 4 & 5):
the bug-report version dropdown tops out at v2.3 and the App Type dropdown omits Lidarr. Update
the dropdowns AND the snapshot tests that assert their contents in the same plan, so the suite
stays green. Confirm the other community-health files are present and free of drift (PREW-04).

Purpose: A Lidarr user is a first-class supported user (Features list, config example, one-liner);
the template must not force them to mis-select. Stale version options signal an unmaintained repo.

Output: Updated bug-report.yml dropdowns + updated test_github_templates.py assertions; a
confirmation note (in the SUMMARY) that CONTRIBUTING.md / PR template / feature-request.yml /
config.yml / LICENSE are accurate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-presentation-rewrite/71-PATTERNS.md

<interfaces>
<!-- VERIFIED 2026-06-02. The test file asserts EXACT current options; both must move together. -->

.github/ISSUE_TEMPLATE/bug-report.yml (VERIFIED):
- version dropdown options (lines 9-14 region): `v2.3, v2.2, v2.1, Older`  → replace with
  `v2.8.1, v2.8, v2.7, v2.6, v2.5, v2.4, Older`
- App Type dropdown options (lines 31-34 region): `Radarr, Sonarr, Both` → replace with
  `Radarr, Sonarr, Lidarr, All`

tests/test_github_templates.py (VERIFIED):
- line 32: `for version in ("v2.3", "v2.2", "v2.1", "Older"):` → must assert the NEW versions
- line 42: `for app in ("Radarr", "Sonarr", "Both"):` → must assert `Radarr, Sonarr, Lidarr, All`
- Other tests in this file (test_valid_yaml, test_has_dropdowns, test_deployment_dropdown_options,
  config-excerpt redaction, feature-request, config.yml, PR template) must remain untouched/green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bug-report.yml version + App Type dropdowns and their snapshot assertions</name>
  <files>.github/ISSUE_TEMPLATE/bug-report.yml, tests/test_github_templates.py</files>
  <read_first>
    - tests/test_github_templates.py — READ FIRST (covering test). Confirm the exact assertion
      tuples at line 32 (`("v2.3","v2.2","v2.1","Older")`) and line 42 (`("Radarr","Sonarr","Both")`)
      and the `test_deployment_dropdown_options` test (line 35) so it is NOT broken by these edits.
    - .github/ISSUE_TEMPLATE/bug-report.yml — full file. Confirm the version dropdown block and the
      App Type dropdown block and their exact YAML indentation (the `options:` list under
      `attributes:` for each `dropdown` element).
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — ".github/ISSUE_TEMPLATE/bug-report.yml"
      (lines 349-408): the exact replacement option lists AND the exact test assertion replacements.
    - .planning/phases/70-presentation-discovery/70-CRITIQUE.md — Gripes 4 & 5 (the source findings).
  </read_first>
  <action>
    In .github/ISSUE_TEMPLATE/bug-report.yml: replace the version dropdown `options` list with, in
    order, `v2.8.1, v2.8, v2.7, v2.6, v2.5, v2.4, Older` (per D-10). Replace the App Type dropdown
    `options` list with `Radarr, Sonarr, Lidarr, All` (adds Lidarr, replaces "Both" with "All" per
    D-10 — "All" is the broader accurate choice for a multi-app deployment). Preserve all surrounding
    YAML structure, labels, ids, and indentation exactly; only the two `options:` lists change.
    In tests/test_github_templates.py: update `test_version_dropdown_options` (line ~32) to assert
    the new version tuple `("v2.8.1","v2.8","v2.7","v2.6","v2.5","v2.4","Older")` and
    `test_app_type_dropdown_options` (line ~42) to assert `("Radarr","Sonarr","Lidarr","All")`. Do
    NOT touch any other test or any other dropdown (deployment dropdown stays as-is).
  </action>
  <verify>
    <automated>uv run pytest tests/test_github_templates.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.github/ISSUE_TEMPLATE/bug-report.yml` contains `Lidarr`
    - `.github/ISSUE_TEMPLATE/bug-report.yml` contains `v2.8.1` and `v2.4` and contains `- All`
    - `.github/ISSUE_TEMPLATE/bug-report.yml` no longer contains the standalone option `- v2.3` (grep for `- v2.3` returns nothing) and no longer contains `- Both`
    - `tests/test_github_templates.py` contains `v2.8.1` and `Lidarr` in its assertion tuples
    - `uv run pytest tests/test_github_templates.py -x -q` exits 0
    - YAML still parses: `python -c "import yaml,sys; yaml.safe_load(open('.github/ISSUE_TEMPLATE/bug-report.yml'))"` exits 0
  </acceptance_criteria>
  <done>Both dropdowns updated; both snapshot tests updated and green; YAML valid.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm remaining community-health files present and accurate (PREW-04)</name>
  <files>tests/test_community_health.py</files>
  <read_first>
    - CONTRIBUTING.md, .github/pull_request_template.md, .github/ISSUE_TEMPLATE/feature-request.yml,
      .github/ISSUE_TEMPLATE/config.yml, LICENSE — skim each for drift (wrong version refs, dead
      links, wrong app list). Per 71-RESEARCH.md §4 these are confirmed present + accurate; this task
      is audit-and-confirm. Note: CONTRIBUTING.md's Tailwind dev-command line is fixed in plan 05
      (do NOT edit CONTRIBUTING.md here — it is owned by plan 05 to avoid a file conflict).
    - tests/test_community_health.py — the covering tests (TestContributing, TestSecurity,
      TestLicense). Confirm what strings they require so nothing regresses.
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md §4 — the confirmed file inventory.
  </read_first>
  <action>
    Audit-and-confirm only — do NOT edit any file in this task. Verify CONTRIBUTING.md, the PR
    template, feature-request.yml, config.yml, and LICENSE are present and free of factual drift
    (LICENSE year 2026 / MIT; config.yml `blank_issues_enabled: false` + Discussions link;
    PR template's tests/lint/Docker checklist). Run the community-health test suite to confirm the
    accuracy invariants hold. If genuine drift is found that is NOT the CONTRIBUTING.md Tailwind line
    (plan 05's responsibility) and NOT bug-report.yml (task 1), record it in the SUMMARY as a
    follow-up rather than editing here (keeps file ownership clean for parallel waves). If no drift,
    state "all four community-health files confirmed accurate" in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_community_health.py tests/test_github_templates.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_community_health.py -x -q` exits 0
    - `LICENSE` exists and contains `MIT` and `2026`
    - `.github/ISSUE_TEMPLATE/config.yml` contains `blank_issues_enabled: false`
    - The SUMMARY records an explicit present-and-accurate confirmation for CONTRIBUTING.md, PR template, feature-request.yml, config.yml, LICENSE (or lists drift found as a follow-up)
    - No file other than the SUMMARY is modified in this task
  </acceptance_criteria>
  <done>All four community-health file groups confirmed present and accurate; community-health tests green; no unowned files edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits GitHub issue-template YAML and a test file. No runtime code, no new attack surface,
no trust boundary is crossed at execution time.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | community-health docs/YAML | accept | No new attack surface — issue-template dropdown content and a snapshot test. The only security-adjacent note is the existing `test_config_excerpt_redaction_warning` invariant in test_github_templates.py, which this plan leaves untouched. |
</threat_model>

<verification>
- `uv run pytest tests/test_github_templates.py tests/test_community_health.py -x -q` exits 0.
- bug-report.yml dropdowns updated; YAML valid.
- No file outside files_modified (+ the SUMMARY) edited.
</verification>

<success_criteria>
- Version dropdown current (v2.8.1..v2.4, Older); App Type includes Lidarr + All.
- Snapshot tests assert new options and pass.
- Remaining community-health files confirmed present and accurate (PREW-04).
</success_criteria>

<output>
Create `.planning/phases/71-presentation-rewrite/71-03-SUMMARY.md` when done. Record the
present-and-accurate confirmation for all community-health files and any drift deferred to a
follow-up.
</output>
