---
phase: 71-presentation-rewrite
plan: 06
type: execute
wave: 2
depends_on: ["71-02"]
files_modified:
  - SECURITY.md
autonomous: true
requirements: [PREW-03, PREW-07]
must_haves:
  decisions:
    - "D-08: SECURITY.md enumerates the v2.8/v2.8.1 hardening as a confident 'what we do' list (CSP script-src nonce, session-secret rotation on password change / CWE-613, apikey= URL rejection, Basic-auth control-char validation, session-secret startup length check)"
    - "D-09: SECURITY.md states the at-rest-plaintext caveat plainly (SecretStr protects repr/log/HTML, NOT disk; secrets are plaintext in triggarr.toml — protect with file permissions + volume security), mirroring README:212"
  truths:
    - "SECURITY.md enumerates the v2.8/v2.8.1 hardening as a confident 'what we do' list: CSP script-src nonce (no unsafe-inline), session-secret rotation on password change (CWE-613), apikey= URL rejection, Basic-auth control-char validation, session-secret startup length check"
    - "SECURITY.md states the at-rest-plaintext caveat plainly: SecretStr protects repr/log/HTML exposure, NOT at-rest secrecy; secrets are plaintext in triggarr.toml, protect with file permissions + volume security"
    - "The SECURITY.md SSRF claim is PRECISE: link-local/unspecified/multicast IP literals + known cloud-metadata hosts/IPs are blocked BOTH at config load (triggarr.toml at startup) AND via the web form; loopback IP literals + the DNS name 'localhost' are PERMITTED at config load (relaxed variant for same-host *arr) and blocked ONLY by the web form; arbitrary DNS hostnames are NOT resolved at validation time and remain an accepted residual risk (network-layer egress controls are the mitigation). It does NOT state loopback is blocked at config load, does NOT say 'rejects loopback' unqualified, and does NOT claim 'metadata/link-local hostnames blocked' for arbitrary DNS"
    - "SECURITY.md preserves the External-auth/direct-access trust-boundary wording and the X-Forwarded-Proto/Uvicorn/ASGI-scheme/trusted_proxy_ips wording that test_docs_accuracy.py requires"
    - "All existing TestSecurity content invariants stay green (SecretStr, CSRF, SSRF, clamping, atomic, PUID, redact, 2.x/1.x table, advisories link)"
  artifacts:
    - path: "SECURITY.md"
      provides: "Reconciled threat model with v2.8/v2.8.1 hardening + at-rest caveat + accurate SSRF scope"
      contains: "nonce"
  key_links:
    - from: "SECURITY.md SSRF section"
      to: "triggarr/web/validation.py + triggarr/models/config.py (plan 02 behavior)"
      via: "prose accuracy — precise config-load + web-form scope from 71-02-SUMMARY.md"
      pattern: "config|triggarr.toml|startup"
    - from: "SECURITY.md Credential Protection section"
      to: "the at-rest-plaintext reality"
      via: "explicit caveat mirroring README:212"
      pattern: "plaintext"
---

<objective>
Reconcile SECURITY.md with the v2.8/v2.8.1 hardening and state the at-rest-plaintext caveat plainly
(D-08/D-09 / critique Gripe 12 / codex MEDIUM SECURITY.md:18,28 + HIGH SECURITY.md:39). Honesty here
is a credibility win with the skeptical reader, and the hardening enumeration is a maturity signal.

Purpose: A security-conscious viewer reading SECURITY.md should see the most recent hardening layer
named explicitly and get accurate expectations about at-rest secret storage and the SSRF scope —
closing the gap between what the code does and what the doc claims, WITHOUT swapping one overstatement
(stale broad SSRF claim) for another (a too-broad block-list claim).

Output: An updated SECURITY.md that enumerates the v2.8/v2.8.1 hardening, adds the plaintext caveat,
and states the now-accurate PRECISE SSRF scope, while preserving every existing TestSecurity invariant
and the External-auth / forwarded-proto wording the docs-accuracy tests require.
DEPENDS ON plan 02 so the SSRF claim states the post-code-change behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-presentation-rewrite/71-PATTERNS.md
@.planning/phases/71-presentation-rewrite/71-RESEARCH.md
@.planning/phases/71-presentation-rewrite/71-02-SUMMARY.md
@.planning/phases/70-presentation-discovery/70-CRITIQUE.md
@.planning/phases/70-presentation-discovery/70-CODEX-REVIEW.md

<interfaces>
<!-- VERIFIED 2026-06-02. SECURITY.md is 60 lines; edit in-place. -->

Current SECURITY.md anchors:
- line 7-8 supported-versions table (`2.x | Yes`, `1.x | No`) — KEEP (TestSecurity asserts 2.x + 1.x)
- line 14 GSVR advisories link — KEEP (TestSecurity asserts the advisories URL)
- line 28 `### Credential Protection`; line 30 SecretStr bullet; line 31 Loguru redaction; 32 UI masking
- line 34 `### Web Security`; line 37 CSRF; line 39 SSRF claim (UPDATE per plan 02 behavior);
  line 40 input clamping
- line 48 `### Container Hardening`; line 51 PUID/PGID
- line 55 `## Deployment Recommendations`
- External-auth / direct-access wording and the X-Forwarded-Proto / ASGI-scheme / Uvicorn /
  trusted_proxy_ips wording also live in this file — see the docs-accuracy invariants below; both
  must SURVIVE this edit (the edit is additive/corrective, not a structural rewrite).

Confirmed v2.8/v2.8.1 hardening to enumerate (D-08, from 71-RESEARCH.md §3 with file:line):
- CSP `script-src` nonce per request, no `unsafe-inline` — web/middleware.py:43-57
- Session-secret rotation on password change (CWE-613) — web/routes.py:1449-1481 (v2.8.1)
- `apikey=` URL rejection at model construction — models/config.py reject_apikey_in_url
- Basic-auth control-character validation — web/middleware.py:177-186
- Session-secret startup length check — startup.py:49-69

TestSecurity (tests/test_community_health.py:50-93) REQUIRES these substrings to remain present:
`2.x`, `1.x`, the advisories URL, `SecretStr`, `CSRF`, `SSRF`, `clamping`, `atomic`, `PUID`,
`redact`. The rewrite must not remove any of them.

test_docs_accuracy.py invariants that ALSO gate SECURITY.md (do NOT break — read the whole file):
- test_external_auth_docs_require_upstream_authz_and_blocked_direct_access (lines 32-41): SECURITY.md
  (normalized lowercase) must contain `external`, `authentication`, `authorization`, `direct access`,
  and `blocked` OR `firewalled`.
- test_secure_cookie_docs_use_asgi_scheme_not_direct_forwarded_proto_trust (lines 43-65): SECURITY.md
  must contain `x-forwarded-proto`, `asgi request scheme`, `uvicorn`, and `trusted_proxy_ips`, AND must
  NOT contain any forbidden direct-trust phrasing (lines 54-62). Keep cookie-trust described as flowing
  through the Uvicorn/ASGI scheme behind the trusted_proxy_ips allow-list.
- test_tracked_docs_do_not_reintroduce_stale_auth_or_config_dir_claims (lines 68-83): introduce no
  retired stale claim.

PRECISE post-plan-02 SSRF claim to write (quote 71-02-SUMMARY.md; do NOT broaden):
"URL inputs use an allow-list of schemes (http/https). Link-local / unspecified / multicast IP
literals, and known cloud-metadata hostnames/IPs (e.g. 169.254.169.254, metadata.google.internal)
are blocked both when loaded from triggarr.toml at startup and when submitted via the web settings
form. Loopback IP literals and the DNS name `localhost` are PERMITTED when loaded from triggarr.toml
(relaxed config-load variant — running an *arr app on the same host is a legitimate pattern) and are
blocked ONLY by the web settings form. Arbitrary DNS hostnames are not resolved at validation time and
remain an accepted residual risk (DNS rebinding); network-layer egress controls are the appropriate
mitigation."
Must keep the literal token `SSRF`. Must NOT state loopback is blocked at config load (it is
permitted there), MUST NOT say "rejects loopback" without the "IP literal" qualifier, and must NOT
claim "cloud-metadata and link-local hostnames are blocked" for arbitrary DNS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate v2.8/v2.8.1 hardening, add at-rest-plaintext caveat, and update the SSRF claim to the precise scope</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md — the ENTIRE current file (60 lines). Note every section and the exact bullets so
      the edit is additive/corrective, not a structural rewrite, and no required substring is dropped
      (including the External-auth and X-Forwarded-Proto/ASGI/trusted_proxy_ips wording).
    - tests/test_community_health.py — lines 50-93 (TestSecurity). READ FIRST (covering test):
      confirm every asserted substring (SecretStr, CSRF, SSRF, clamping, atomic, PUID, redact,
      2.x, 1.x, advisories URL) so none is removed.
    - tests/test_docs_accuracy.py — READ FIRST, the WHOLE file. The External-auth/direct-access test
      (lines 32-41) and the forwarded-proto/Uvicorn/ASGI/trusted_proxy_ips test (lines 43-65, with its
      forbidden-claims list) apply to SECURITY.md; the stale-claims test (lines 68-83) also applies.
      None of this wording may be removed, and no forbidden direct-trust phrasing or stale claim may
      be introduced.
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md — §3 (confirmed hardening items with
      file:line, the at-rest reality, and the post-D-01/D-02 SSRF behavior).
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — "SECURITY.md — reconcile" (lines
      460-481): the SSRF line replacement, the caveat text, and the hardening list to enumerate.
    - .planning/phases/70-presentation-discovery/70-CRITIQUE.md Gripe 12 + 70-CODEX-REVIEW.md
      SECURITY.md:18,28,39 findings — the source findings.
    - .planning/phases/71-presentation-rewrite/71-02-SUMMARY.md — the PRECISE net SSRF behavior to
      quote verbatim (do NOT broaden it).
  </read_first>
  <action>
    Edit SECURITY.md in place (preserve structure + every TestSecurity-required substring + the
    External-auth and forwarded-proto/ASGI/trusted_proxy_ips wording the docs-accuracy tests require):
    (1) In Web Security, replace the SSRF bullet (line ~39) with the PRECISE post-plan-02 claim from
    the <interfaces> block — it must (a) keep the literal token `SSRF`, (b) state the block-list
    applies to link-local/unspecified/multicast IP literals + KNOWN cloud-metadata hostnames/IPs,
    blocked BOTH at config load (triggarr.toml at startup) AND via the web settings form, (c) state
    that loopback IP literals AND the DNS name `localhost` are PERMITTED at config load (relaxed
    variant) and blocked ONLY by the web settings form (qualified with "IP literal" — NOT a bare
    "rejects loopback", which is false for the DNS name `localhost`; and NOT stating loopback is
    blocked at config load, which is false), and (d) state that arbitrary DNS hostnames are NOT resolved at validation time and remain an
    accepted residual risk (DNS rebinding), mitigated by network-layer egress controls. Do NOT write a
    broad "cloud-metadata and link-local hostnames are blocked" claim — only known metadata hostnames
    in the block-list and IP literals are blocked. (2) Add the v2.8/v2.8.1 hardening as a confident
    "what we do" enumeration (D-08) — name CSP `script-src` nonce (no `unsafe-inline`), session-secret
    rotation on password change (CWE-613), `apikey=` URL rejection, Basic-auth control-character
    validation, and the session-secret startup length check; place them in the Web Security and/or
    Credential Protection subsections where each fits, framed as maturity signals not a jargon wall.
    Ensure the existing CSP reference is upgraded to name the nonce explicitly (so the file now
    contains `nonce`). (3) In Credential Protection (line ~28-32), add the at-rest-plaintext caveat
    plainly (D-09): SecretStr protects repr/log/HTML exposure, NOT at-rest secrecy — API keys, auth
    credentials, and the session secret are plaintext in `triggarr.toml`; protect them with file
    permissions (`0600`, set by Triggarr on write) and volume security. Mirror the README:212 caveat
    wording. Do NOT remove the SecretStr / redaction / UI-masking bullets (TestSecurity needs them),
    and do NOT remove or alter the External-auth or X-Forwarded-Proto/ASGI/trusted_proxy_ips wording.
    Do NOT downgrade Triggarr's leading signals (GSVR reporting, full threat model) — they are where
    Triggarr is AHEAD of SeedSyncarr (consistency audit signals 8, 9) and stay as-is (PREW-07).
  </action>
  <verify>
    <automated>uv run pytest tests/test_community_health.py::TestSecurity tests/test_docs_accuracy.py -x -q && grep -q "nonce" SECURITY.md && grep -qi "plaintext" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `SECURITY.md` contains `nonce` (CSP script-src nonce documented)
    - `SECURITY.md` contains `CWE-613` (session-secret rotation) and `control` (Basic-auth control-char validation) and `startup` (session-secret length check)
    - `SECURITY.md` contains a plaintext at-rest caveat (contains `plaintext` and `triggarr.toml` in the Credential Protection context)
    - `SECURITY.md` SSRF bullet is PRECISE: it references config-file/startup validation (grep -iE 'triggarr.toml|config|startup' near the SSRF bullet matches), qualifies the web-form loopback rejection with "IP literal", and states arbitrary DNS hostnames are not resolved / remain a residual risk. It does NOT contain an unqualified "rejects loopback" or a broad "metadata and link-local hostnames are blocked" claim for arbitrary DNS.
    - `SECURITY.md` still contains every TestSecurity substring: `SecretStr`, `CSRF`, `SSRF`, `clamping`, `atomic`, `PUID`, `redact`, `2.x`, `1.x`, and `github.com/thejuran/triggarr/security/advisories/new`
    - `SECURITY.md` still contains the External-auth wording (external / authentication / authorization / direct access / blocked|firewalled) and the X-Forwarded-Proto / `asgi request scheme` / Uvicorn / `trusted_proxy_ips` wording with no forbidden direct-trust phrasing
    - `uv run pytest tests/test_community_health.py::TestSecurity -x -q` exits 0
    - `uv run pytest tests/test_docs_accuracy.py -x -q` exits 0 (External-auth + forwarded-proto + stale-claims invariants all hold for SECURITY.md)
  </acceptance_criteria>
  <done>SECURITY.md enumerates v2.8/v2.8.1 hardening, states the at-rest caveat, and the SSRF claim is precise (no overstatement); all TestSecurity and docs-accuracy invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits a single Markdown policy file. No runtime code, no new attack surface at execution.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | Information Disclosure (false-security claim) | SECURITY.md SSRF + Credential Protection wording | mitigate | The SSRF claim (SECURITY.md:39) is updated to the PRECISE post-plan-02 behavior (link-local/unspecified/multicast IP literals + known metadata hosts blocked at BOTH config load and web form; loopback IP literals + localhost permitted at config load and blocked ONLY by the web form; arbitrary DNS not resolved = accepted residual risk), and the Credential Protection section gains an explicit at-rest-plaintext caveat (codex MEDIUM SECURITY.md:18,28) — both eliminate overstated-protection claims WITHOUT introducing a new too-broad claim. Accuracy is enforced by reviewer source-assertion + depends_on:71-02 ordering + the test_docs_accuracy.py invariants. |

The hardening enumeration (D-08) names existing, already-shipped controls; it documents the current
posture and introduces no new attack surface.
</threat_model>

<verification>
- `uv run pytest tests/test_community_health.py::TestSecurity tests/test_docs_accuracy.py -x -q` exits 0.
- SECURITY.md names CSP nonce, CWE-613 session rotation, apikey= rejection, control-char validation, startup length check.
- At-rest-plaintext caveat present; SSRF claim precise (IP-literal-qualified, DNS-not-resolved residual risk noted).
- External-auth + forwarded-proto/ASGI/trusted_proxy_ips wording preserved.
- Triggarr's leading signals (GSVR, threat-model depth) preserved.
</verification>

<success_criteria>
- SECURITY.md reconciled with v2.8/v2.8.1 hardening + at-rest caveat (PREW-03).
- SSRF claim precise and honest to the post-plan-02 code behavior (no overstatement).
- External-auth + forwarded-proto invariants preserved; Triggarr's ahead-of-SeedSyncarr security signals preserved (PREW-07).
- All TestSecurity + docs-accuracy invariants green.
</success_criteria>

<output>
Create `.planning/phases/71-presentation-rewrite/71-06-SUMMARY.md` when done. Confirm the SSRF claim
quotes the precise 71-02-SUMMARY.md behavior without broadening it, and that the External-auth +
forwarded-proto wording survived the edit.
</output>
