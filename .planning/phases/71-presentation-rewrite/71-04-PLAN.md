---
phase: 71-presentation-rewrite
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [PREW-06]
must_haves:
  decisions:
    - "D-11: CHANGELOG.md IS the in-app changelog source (triggarr/changelog.py reads it) — the v2.9.0 release-notes entry and the in-app changelog are the same single edit in the expected Features/Fixes format"
    - "D-12: the v2.9.0 entry lists user-facing changes (SSRF/config-load URL-validation hardening, Phase 69 manual-search failure-counter fix, docs/presentation overhaul as Documentation bullets); internal-only mechanics only where user-relevant; date stamped at execution time"
  truths:
    - "CHANGELOG.md has a v2.9.0 entry at the top in the exact format the changelog parser expects, listing user-facing changes only"
    - "The in-app changelog (which renders CHANGELOG.md via triggarr/changelog.py) parses and displays the v2.9.0 entry without error"
    - "The v2.9.0 entry covers the SSRF/config-load URL-validation hardening (D-01), the manual-search failure-counter fix (Phase 69 CHARD-02), and the docs/presentation overhaul"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v2.9.0 release-notes + in-app-changelog entry"
      contains: "## v2.9.0"
  key_links:
    - from: "triggarr/changelog.py parse_changelog"
      to: "CHANGELOG.md v2.9.0 block"
      via: "version-header + category + bullet regex parsing"
      pattern: "## v2\\.9\\.0"
---

<objective>
Write the v2.9.0 release-notes entry in CHANGELOG.md (D-11/D-12). CHANGELOG.md IS the in-app
changelog source (triggarr/changelog.py renders it from disk, Tautulli model), so this single
edit satisfies both "release notes" and "in-app changelog" for PREW-06.

Purpose: A clean, user-facing v2.9.0 entry that a visitor (and the in-app changelog viewer) reads
as a coherent summary of what changed — not an internal milestone log.

Output: One `## v2.9.0 (<date>)` block inserted at the top of CHANGELOG.md, in the strict parser
format, listing only user-relevant changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-presentation-rewrite/71-PATTERNS.md

<interfaces>
<!-- changelog.py parser format (VERIFIED from changelog.py regex + CHANGELOG.md). -->
Version header regex: ^##\s+(.+)$            → line must be `## v2.9.0 (YYYY-MM-DD)`, nothing else
Category   regex:     ^\*\s+([^:]+):\s*$      → line must be `* Security:` — NOTHING after the colon
Bullet     regex:     ^\s+\*\s+(.+)$          → `  * text` — two-space indent before `*`
- Optional one-paragraph summary after the version header (blank line before first `* Category:`).
- Most-recent existing entry `## v2.8.1 (2026-05-31)` is the canonical single-category template.
- "Documentation" is a valid category name (parser accepts any name).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the v2.9.0 entry at the top of CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md — read the top ~40 lines: the `## v2.8.1 (2026-05-31)` entry (single-category
      template) and the `## v2.8.0 (2026-06-01)` entry (multi-category template) for exact spacing —
      blank line after version header, blank line after summary, blank line between a category line
      and its bullets.
    - tests/test_changelog.py — confirm the parser tests use a SAMPLE_CHANGELOG constant (not the
      live file), so editing CHANGELOG.md does not break parser unit tests; the live-file render is
      validated separately below.
    - .planning/phases/71-presentation-rewrite/71-PATTERNS.md — "CHANGELOG.md — add ## v2.9.0 entry"
      (lines 305-345): the exact entry skeleton and the Security/Fixes/Documentation bullet content.
    - .planning/phases/71-presentation-rewrite/71-RESEARCH.md §5 — D-12 user-facing content mapping
      and the Pitfall 3 category-line format rule.
  </read_first>
  <action>
    Insert a new `## v2.9.0 (<date>)` entry as the FIRST entry in CHANGELOG.md (above `## v2.8.1`),
    matching the existing format exactly. Use the execution date in `YYYY-MM-DD` form (run `date +%F`
    — do NOT invent or hardcode a date, per D-12). Include a one-sentence user-facing summary
    paragraph, then these categories with two-space-indented bullets:
      * Security: — URL validation for *arr instances now applies at config-load time, not only via
        the web UI; cloud-metadata and link-local addresses are blocked on startup while loopback
        addresses are permitted for same-host deployments (the D-01/D-02 net behavior).
      * Fixes: — manual-search failures now increment/reset the consecutive-failure counter the same
        way scheduled cycles do (the Phase 69 CHARD-02/SAFETY-03 fix), so a recovered instance is no
        longer prematurely paused.
      * Documentation: — full README rewrite (benefit-led intro, accurate Quick Start, corrected pip
        install + systemd unit, documented tag-filtering fail-open) and SECURITY.md reconciled with
        the v2.8/v2.8.1 hardening + at-rest-plaintext caveat.
    Category lines MUST be exactly `* Security:` / `* Fixes:` / `* Documentation:` with nothing after
    the colon (Pitfall 3). Keep internal-only milestone mechanics (gitleaks fingerprint repair,
    fastapi/starlette dep bump, .orchestrator.json gitignore) OUT — per D-12, include only where
    user-relevant. The exact final wording is at the executor's discretion within this scope.
  </action>
  <verify>
    <automated>uv run python -c "from triggarr.changelog import read_changelog, parse_changelog; from pathlib import Path; html = parse_changelog(Path('CHANGELOG.md').read_text(), latest_only=True); assert 'v2.9.0' in html, 'v2.9.0 not parsed as latest'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains `## v2.9.0 (` followed by a `YYYY-MM-DD` date matching today (`date +%F`)
    - The v2.9.0 block is the FIRST `## v` entry in the file (appears before `## v2.8.1`)
    - The block contains category lines `* Security:`, `* Fixes:`, `* Documentation:` each ending exactly at the colon (grep -E '^\* (Security|Fixes|Documentation):$' CHANGELOG.md matches all three)
    - `parse_changelog(CHANGELOG.md content, latest_only=True)` returns HTML containing `v2.9.0` (the automated verify command above exits 0)
    - `uv run pytest tests/test_changelog.py -x -q` exits 0 (parser unit tests still green)
    - The block does NOT mention gitleaks, .orchestrator.json, or the dependency bump as standalone bullets (internal-only, excluded per D-12)
  </acceptance_criteria>
  <done>v2.9.0 entry present at top in valid parser format; in-app changelog renders it as the latest; user-facing scope only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits a Markdown file rendered read-only by the in-app changelog viewer. No new attack
surface. The only correctness invariant is the parser format (a malformed category line would
render as a stray bullet — guarded by the automated render check above).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | CHANGELOG.md / in-app changelog render | accept | Docs-only content edit; changelog.py renders it read-only with HTML escaping already in place (test_parse_html_escaping_in_bullets). No new surface. |
</threat_model>

<verification>
- In-app changelog parses v2.9.0 as the latest entry (automated check).
- `uv run pytest tests/test_changelog.py -x -q` exits 0.
- Date is execution-time, not invented.
</verification>

<success_criteria>
- v2.9.0 entry at top of CHANGELOG.md in strict parser format.
- User-facing changes only (SSRF config-load, manual-search counter fix, docs overhaul).
- In-app changelog renders it without error.
</success_criteria>

<output>
Create `.planning/phases/71-presentation-rewrite/71-04-SUMMARY.md` when done.
</output>
