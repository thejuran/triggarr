---
phase: 68-code-track-hostile-reader-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md
autonomous: true
requirements: [CDISC-01, CDISC-02, CDISC-03, CDISC-04, CDISC-05]

must_haves:
  truths:
    - "A skeptical engineer can read one artifact (68-FINDINGS.md) and see every discovery-source result with each finding classified FOLD-IN or PARKED"
    - "ruff was run whole-tree (triggarr/ tests/) and its result is recorded as a section"
    - "Shield (Semgrep SAST + gitleaks working-tree + dependency audit) was run and each component's result is recorded"
    - "gitleaks scanned the FULL git history honoring .gitleaksignore; either real exposures are recorded with commit SHAs FOLD-IN, or an explicit 'history scan clean, N commits scanned' line is present"
    - "The six entry-point files were skimmed with hostile framing and notes captured"
    - "Every FOLD-IN row carries a file:line or commit SHA and an actionable description; every PARKED row carries a written rationale"
    - "The two curated known items (.orchestrator.json gitignore gap; SAFETY-03 at scheduler.py:~325) appear in the artifact"
    - "The config-knob UI debt (DEBT-03/06/07/08) does NOT appear as FOLD-IN"
  artifacts:
    - path: ".planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md"
      provides: "Single triaged findings artifact; FOLD-IN rows are Phase 69's CHARD-04 fix checklist"
      contains: "## Fold-In Summary"
  key_links:
    - from: ".planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "cross-check reconciliation (known items recorded, not re-discovered)"
      pattern: "SAFETY-03"
    - from: ".planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md"
      to: ".gitleaksignore"
      via: "history scan honors the 4-entry allowlist"
      pattern: "gitleaksignore"
---

<objective>
Run a deliberate "this is on Reddit and a skeptical r/selfhosted engineer is reading it" hostile pass over Triggarr's whole code surface AND its full git history, then capture and classify every result into one triaged findings artifact, `68-FINDINGS.md`. This phase writes NO source code — it runs read-only discovery tools and records + triages their output.

Purpose: The FOLD-IN rows of this artifact ARE the fix checklist that Phase 69 (Code-track hardening, CHARD-04) consumes directly, without re-investigation. The classification produced here is the gate that defines Phase 69's fix scope. The git-history scan is the launch-visible-risk addition unique to an already-public repo with a long commit history.

Output: `.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md` — one structured Markdown file, one section per discovery source, every finding classified FOLD-IN or PARKED with a one-line rationale, plus a fold-in summary list for Phase 69.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked classification bar and artifact structure — READ BEFORE CLASSIFYING ANYTHING
@.planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md

# Source design spec — §3.1 (code-track discovery), D-3 (launch-visible bound), D-4 (curated subset), D-5 (parked config-knob debt), D-8 (toolchain)
@docs/superpowers/specs/2026-06-02-launch-hardening-design.md

# Cross-check catalogue: known debt with file:line pointers — record what's catalogued, spend effort finding what ISN'T
@.planning/codebase/CONCERNS.md

# The 4-entry allowlist the full-history scan honors
@.gitleaksignore

<classification_bar>
<!-- LOCKED in 68-CONTEXT.md D-04. Apply to EVERY finding. -->
FOLD-IN if: (a) launch-visible — something a skeptical r/selfhosted engineer browsing the repo / README / `git log` would actually see and ding — OR (b) any real security or secret exposure, regardless of visibility.
PARK if: pure-internal nitpick (style the linter doesn't enforce, invisible debt, cosmetic item) — with written rationale.

HARD RULE: The config-knob UI debt (DEBT-03 history cap, DEBT-06 drain timeout, DEBT-07 request timeout, DEBT-08 page size) is pre-parked by spec D-5. It MUST NOT be folded in even if a tool surfaces it. If surfaced, record it PARKED with rationale "spec D-5: invisible to launch reader, parked to v2."
</classification_bar>

<known_curated_items>
<!-- Expected to appear; CONFIRM and LOCATE, do not re-litigate. -->
1. `.orchestrator.json` is untracked and NOT git-ignored (confirmed 2026-06-02 via `git check-ignore .orchestrator.json` returning nothing). Repo-hygiene FOLD-IN. NB: `.DS_Store`, `triggarr/.DS_Store`, and `.playwright-mcp/` are ALREADY git-ignored — do NOT re-flag them.
2. SAFETY-03: manual search via `/search-now/{app}/{instance}` bypasses the per-job consecutive-failure counter; the `# TODO` lives at `scheduler.py:~325` and the handler at `routes.py:~876`. Correctness asymmetry FOLD-IN.
</known_curated_items>

<scan_provenance>
<!-- Confirmed at planning time 2026-06-02. -->
Branch: launch-hardening. gitleaks 8.30.1, semgrep installed, pip-audit installed, uv installed, Shield plugin 0.3.1 at ~/.claude/plugins/cache/shield-security/shield. Full history = 1021 commits (use `git rev-list --count HEAD` for the live number at run time). ruff config: pyproject.toml, rules E/F/I/UP/B/SIM, line-length 120.
</scan_provenance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 68-FINDINGS.md with provenance header and empty per-source sections</name>
  <files>.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md</files>
  <read_first>
    - .planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md (D-01/D-02 artifact location + required per-finding fields)
    - docs/superpowers/specs/2026-06-02-launch-hardening-design.md (§3.1 source ordering)
  </read_first>
  <action>
    Create the findings artifact at the path in files. Write a `## Scan Provenance` header capturing: run date, branch (launch-hardening), tool versions captured at run time (run `gitleaks version`, `semgrep --version`, `pip-audit --version`, `uv --version` and record the actual output), the live full-history commit count (run `git rev-list --count HEAD`), and the ruff ruleset (E/F/I/UP/B/SIM, line-length 120 from pyproject.toml). Then create one empty section per discovery source in this order, each with a placeholder table whose columns satisfy 68-CONTEXT.md D-02 (description, file:line-or-SHA, classification FOLD-IN/PARKED, one-line rationale): `## ruff`, `## Shield (Semgrep)`, `## Shield (gitleaks working-tree)`, `## Shield (dependency audit)`, `## gitleaks (full history)`, `## entry-point skim`. Finally create an empty `## Fold-In Summary` section (the Phase 69 CHARD-04 checklist) and an empty `## Cross-check against CONCERNS.md` section. Do not run any scan tools in this task — only build the scaffold. Per CONTEXT.md "Claude's Discretion", the exact column layout is yours as long as D-02's four fields are present.
  </action>
  <verify>
    <automated>test -f .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md && grep -q '## Scan Provenance' .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md && for s in 'ruff' 'Shield (Semgrep)' 'Shield (gitleaks working-tree)' 'Shield (dependency audit)' 'gitleaks (full history)' 'entry-point skim' 'Fold-In Summary'; do grep -qF "## $s" .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md || { echo "MISSING section: $s"; exit 1; }; done && echo "scaffold OK"</automated>
  </verify>
  <acceptance_criteria>
    - 68-FINDINGS.md exists at the phase-directory path.
    - It contains a `## Scan Provenance` header with run date, branch, real captured tool versions, the live commit count, and the ruff ruleset.
    - It contains all six per-source section headers in spec order plus `## Fold-In Summary` and `## Cross-check against CONCERNS.md`.
    - Each per-source section has a table whose columns include description, file:line-or-SHA, classification, and rationale.
    - No scan tools were run in this task; sections are empty placeholders.
  </acceptance_criteria>
  <done>Scaffold committed in the working tree with provenance header, all six per-source sections, the fold-in summary section, and the cross-check section — ready to be filled by later tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Run ruff whole-tree and the Shield triad; capture + classify into their four sections</name>
  <files>.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md</files>
  <read_first>
    - .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md (the scaffold from Task 1)
    - .planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md (the LOCKED classification bar D-04; D-05 allowlist note)
    - .planning/codebase/CONVENTIONS.md (to judge "is this a real smell or an established pattern" before classifying)
    - .gitleaksignore (the 4 test-fixture allowlist entries the working-tree gitleaks scan honors)
  </read_first>
  <action>
    Run `uv run ruff check triggarr/ tests/` and record the result in the `## ruff` section. If the tree is clean, state that explicitly ("ruff clean, 0 violations") rather than leaving the section empty. If violations surface, record each (or, per CONTEXT.md "Claude's Discretion", a summary count for a large class of the same rule) with rule code, file:line, and a FOLD-IN/PARKED classification using the locked bar — a bare `except` or other convention violation in launch-visible code is FOLD-IN; a pure style nit the linter does not enforce is PARKED. Then run the Shield triad via the Shield plugin skill (the `shield:shield` / `shield:audit` skill at ~/.claude/plugins/cache/shield-security/shield, which wraps Semgrep SAST + gitleaks working-tree secrets + dependency/pip-audit); if the skill is unavailable in-session, fall back to invoking the underlying tools directly: `semgrep --config auto triggarr/` for SAST, `gitleaks detect --source . --no-banner` for working-tree secrets (this respects .gitleaksignore by default), and `uv run pip-audit` (or `pip-audit`) for the dependency audit. Record results into the three sections `## Shield (Semgrep)`, `## Shield (gitleaks working-tree)`, and `## Shield (dependency audit)` respectively. For each component state clean explicitly if nothing surfaces. Classify every surfaced finding FOLD-IN or PARKED per the locked bar: any real secret or SAST security finding is FOLD-IN regardless of visibility; a known-vulnerable dependency is FOLD-IN (launch-visible via a dependency scan and a real security exposure); a low-severity informational SAST hit on an established, intentional pattern is PARKED with rationale. Cross-reference CONVENTIONS.md before deciding a flagged pattern is a real smell. Do NOT fix anything — record only.
  </action>
  <verify>
    <automated>f=.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md; for s in 'ruff' 'Shield (Semgrep)' 'Shield (gitleaks working-tree)' 'Shield (dependency audit)'; do start=$(grep -n "^## $s" "$f" | head -1 | cut -d: -f1); [ -n "$start" ] || { echo "no section $s"; exit 1; }; body=$(tail -n +$((start+1)) "$f" | sed '/^## /q' | grep -v '^## ' | grep -v '^[[:space:]]*$'); [ -n "$body" ] || { echo "EMPTY section: $s"; exit 1; }; done; echo "ruff+Shield sections populated"</automated>
  </verify>
  <acceptance_criteria>
    - The `## ruff` section records the whole-tree `uv run ruff check triggarr/ tests/` result; if clean, it says so explicitly.
    - The three Shield sections (Semgrep, gitleaks working-tree, dependency audit) each record their component's result; clean components are stated explicitly, not omitted.
    - Every surfaced finding in these four sections carries a file:line (or package name+version for a dependency) and a FOLD-IN/PARKED classification with a one-line rationale that applies the locked D-04 bar.
    - Any real secret, SAST security finding, or known-vulnerable dependency is classified FOLD-IN.
    - No source files were modified; only 68-FINDINGS.md changed.
  </acceptance_criteria>
  <done>The ruff and three Shield sections are populated with real captured tool output, every surfaced finding classified per the locked bar, clean results stated explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Run gitleaks over FULL git history honoring .gitleaksignore; triage every non-allowlisted hit highest-priority</name>
  <files>.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md</files>
  <read_first>
    - .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md (scaffold + prior sections)
    - .planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md (D-05: full-history scan, rotate-then-document remediation, explicit clean statement)
    - .gitleaksignore (the 4 allowlisted test-fixture files — hits in these are NOT real exposures)
  </read_first>
  <action>
    Run gitleaks across the entire commit history (not just the working tree): `gitleaks detect --source . --log-opts=--all --no-banner` — the `--log-opts=--all` flag scans all commits across all refs and `.gitleaksignore` is honored by default. Capture the live full-history commit count with `git rev-list --count HEAD` for the "N commits scanned" statement. Record results in the `## gitleaks (full history)` section. Two outcomes, per D-05: (1) If NO hit surfaces outside the 4 allowlisted test-fixture files, state the clean result EXPLICITLY in the artifact — a line of the form "history scan clean, N commits scanned" (substitute the live N) — do not silently omit. (2) For ANY hit NOT covered by the .gitleaksignore allowlist, treat it as a potential real exposure and triage it HIGHEST-PRIORITY: record it FOLD-IN with the commit SHA, the file and rule, and a rotate-then-document remediation note (the repo is already public, so assume the credential is exposed; the note states "rotate the credential, then add to .gitleaksignore only after rotation"). Distinguish allowlisted hits from real ones — if gitleaks reports a hit in one of the 4 allowlisted files, note it as allowlisted/test-fixture, not a real exposure. Do NOT rotate or modify anything in this phase — only record the finding and the prescribed remediation for Phase 69.
  </action>
  <verify>
    <automated>f=.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md; start=$(grep -n '^## gitleaks (full history)' "$f" | head -1 | cut -d: -f1); [ -n "$start" ] || { echo "no history section"; exit 1; }; body=$(tail -n +$((start+1)) "$f" | sed '/^## /q' | grep -v '^## ' | grep -v '^[[:space:]]*$'); echo "$body" | grep -Eqi 'history scan clean, [0-9]+ commits scanned|FOLD-IN' || { echo "history section lacks explicit clean line OR a FOLD-IN exposure row"; exit 1; }; echo "history section conclusive"</automated>
  </verify>
  <acceptance_criteria>
    - The `## gitleaks (full history)` section reflects a scan run with `--log-opts=--all` over the full commit history honoring .gitleaksignore.
    - The section is conclusive: it contains EITHER an explicit "history scan clean, N commits scanned" line (with the live commit count) OR at least one FOLD-IN exposure row.
    - Every non-allowlisted hit (if any) is recorded FOLD-IN with the commit SHA, file, rule, and a rotate-then-document remediation note.
    - Hits in the 4 .gitleaksignore-allowlisted test-fixture files are distinguished as allowlisted/test-fixture, not real exposures.
    - No credential was rotated and no file other than 68-FINDINGS.md changed in this task.
  </acceptance_criteria>
  <done>The full-history gitleaks section is conclusive — either an explicit clean-with-count statement or commit-SHA-anchored FOLD-IN exposure rows with rotate-then-document remediation notes.</done>
</task>

<task type="auto">
  <name>Task 4: Hostile-reader skim of the six entry-point files; capture smells into the skim section</name>
  <files>.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md</files>
  <read_first>
    - triggarr/web/routes.py
    - triggarr/search/scheduler.py
    - triggarr/config.py
    - triggarr/db.py
    - triggarr/auth.py
    - triggarr/startup.py
    - .planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md (D-06: ~2-minute hostile skim per file, NOT a full correctness audit; the smell list)
    - .planning/codebase/CONVENTIONS.md (to distinguish an established pattern from a real smell before flagging)
  </read_first>
  <action>
    Skim each of the six entry-point files with the "what would a skeptical engineer flag in ~2 minutes" lens defined in CONTEXT.md D-06 — this is a hostile-reader surface pass, NOT a full per-file correctness audit (deep correctness is the turingmind deep-review gate's job on Phase 69's diff). For each file, look for the D-06 smell set: bare `except`, secrets in log lines or HTTP responses, obvious injection/SSRF, dead code, alarming `TODO`/`FIXME`, leaked internals in responses. Record notes per file in the `## entry-point skim` section with file:line anchors for anything flagged. Apply the locked classification bar to each smell: a bare-except or a raw secret in a log line in launch-visible code is FOLD-IN; an established, documented pattern that merely looks unusual (cross-checked against CONVENTIONS.md — e.g. SecretStr/`.get_secret_value()` at HTTP-client init, the loguru redacting sink, the intentional asyncio.Lock single-worker model) is NOT a finding and should be noted as "verified intentional pattern" or omitted, not flagged. Confirm and locate the SAFETY-03 `# TODO` at `scheduler.py:~325` and the `search_now` handler at `routes.py:~876` while skimming scheduler.py and routes.py — record SAFETY-03 as a FOLD-IN row with both file:line anchors and a one-line description (manual search bypasses the consecutive-failure counter). If a file is clean of new smells, state that explicitly. Time-box per CONTEXT.md D-06 — do not expand any single file into a deep audit.
  </action>
  <verify>
    <automated>f=.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md; start=$(grep -n '^## entry-point skim' "$f" | head -1 | cut -d: -f1); [ -n "$start" ] || { echo "no skim section"; exit 1; }; body=$(tail -n +$((start+1)) "$f" | sed '/^## /q'); for base in routes scheduler config db auth startup; do echo "$body" | grep -q "$base" || { echo "skim missing file: $base"; exit 1; }; done; echo "$body" | grep -qi 'scheduler.py' && echo "$body" | grep -Eqi 'SAFETY-03|consecutive-failure|search_failures' || { echo "SAFETY-03 not recorded in skim"; exit 1; }; echo "skim section covers all six files + SAFETY-03"</automated>
  </verify>
  <acceptance_criteria>
    - The `## entry-point skim` section references all six entry-point files by name (routes.py, scheduler.py, config.py, db.py, auth.py, startup.py).
    - Each file has skim notes; files clean of new smells state that explicitly.
    - Every flagged smell carries a file:line anchor and a FOLD-IN/PARKED classification per the locked bar.
    - SAFETY-03 is recorded as a FOLD-IN row anchored at both `scheduler.py:~325` (the `# TODO`) and `routes.py:~876` (the `search_now` handler) with a one-line description.
    - Established intentional patterns (SecretStr discipline, loguru redacting sink, single-worker asyncio.Lock) verified against CONVENTIONS.md are NOT flagged as findings.
    - The skim stayed a surface pass (no file expanded into a deep correctness audit); no source files modified.
  </acceptance_criteria>
  <done>The entry-point skim section covers all six files with hostile-framing notes and file:line anchors, SAFETY-03 recorded FOLD-IN with both anchors, intentional patterns explicitly cleared rather than flagged.</done>
</task>

<task type="auto">
  <name>Task 5: Final triage pass — reconcile against CONCERNS.md, confirm known items, write the Fold-In Summary for Phase 69</name>
  <files>.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md</files>
  <read_first>
    - .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md (all sections now populated)
    - .planning/codebase/CONCERNS.md (the standing known-debt catalogue to reconcile against)
    - .planning/phases/68-code-track-hostile-reader-discovery/68-CONTEXT.md (D-03 actionability bar; D-04 fold-in/parked bar; D-05 history; the pre-park HARD RULE on DEBT-03/06/07/08)
    - .planning/REQUIREMENTS.md (confirm DEBT-03/06/07/08 and UI-01..03 are the v2-parked items that must NOT fold in)
  </read_first>
  <action>
    Do the consolidating triage pass that makes the artifact safe for Phase 69 to consume blind. First, walk every finding row across all six sections and confirm each is classified exactly FOLD-IN or PARKED — no unclassified rows. Enforce the actionability bar (CONTEXT.md D-03): every FOLD-IN row must be specific enough to fix without re-investigation (a concrete file:line or commit SHA + a concrete description); every PARKED row must state a one-line rationale. Fix any row that fails this bar in place. Second, fill the `## Cross-check against CONCERNS.md` section: reconcile the discovery findings against the standing catalogue — for each relevant CONCERNS.md item note whether the pass re-confirmed it, and explicitly confirm the two curated known items are recorded (`.orchestrator.json` gitignore gap as a repo-hygiene FOLD-IN; SAFETY-03 at scheduler.py:~325 + routes.py:~876 as a correctness FOLD-IN). Third, enforce the HARD RULE: verify the config-knob UI debt DEBT-03/06/07/08 does NOT appear anywhere as FOLD-IN — if any tool or skim surfaced one, ensure it is PARKED with rationale "spec D-5: invisible to launch reader, parked to v2"; likewise UI-01..03 stay out of fold-in. Fourth, write the `## Fold-In Summary` section: a single consolidated list of every FOLD-IN row (description + file:line-or-SHA) collected from all sections — this list IS Phase 69's CHARD-04 fix checklist, so it must be copy-paste-actionable. Add the two curated known items to that list if not already captured by a tool/skim section. Do NOT add any finding to fold-in that fails the D-04 bar; do NOT fix any code — this remains discovery-only.
  </action>
  <verify>
    <automated>f=.planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md; grep -q '## Cross-check against CONCERNS.md' "$f" || { echo "no cross-check section"; exit 1; }; grep -qi 'orchestrator.json' "$f" || { echo ".orchestrator.json not recorded"; exit 1; }; grep -Eqi 'SAFETY-03|consecutive-failure' "$f" || { echo "SAFETY-03 not recorded"; exit 1; }; start=$(grep -n '^## Fold-In Summary' "$f" | head -1 | cut -d: -f1); body=$(tail -n +$((start+1)) "$f" | grep -v '^[[:space:]]*$'); echo "$body" | grep -qiE 'orchestrator.json' && echo "$body" | grep -qiE 'SAFETY-03|consecutive-failure|search_failures' || { echo "Fold-In Summary missing a curated known item"; exit 1; }; debt=$(grep -niE 'DEBT-0[3678]' "$f" | grep -i 'FOLD-IN' || true); [ -z "$debt" ] || { echo "VIOLATION: config-knob debt marked FOLD-IN: $debt"; exit 1; }; echo "final triage conclusive"</automated>
  </verify>
  <acceptance_criteria>
    - Every finding row across all six sections is classified exactly FOLD-IN or PARKED (no unclassified rows).
    - Every FOLD-IN row has a concrete file:line or commit SHA plus an actionable description (D-03 satisfied); every PARKED row has a one-line rationale.
    - The `## Cross-check against CONCERNS.md` section reconciles discovery findings against the catalogue and confirms both curated known items are recorded.
    - The `.orchestrator.json` gitignore gap and SAFETY-03 (scheduler.py:~325 + routes.py:~876) both appear, and both are present in the `## Fold-In Summary`.
    - The `## Fold-In Summary` is a single consolidated, copy-paste-actionable list of all FOLD-IN rows — Phase 69's CHARD-04 checklist.
    - No config-knob UI debt (DEBT-03/06/07/08) and no UI-01..03 item is classified FOLD-IN anywhere.
    - No source code was modified — the only changed file is 68-FINDINGS.md.
  </acceptance_criteria>
  <done>68-FINDINGS.md is fully triaged: every finding classified, the cross-check section reconciles against CONCERNS.md, both curated known items are recorded and appear in a consolidated copy-paste Fold-In Summary, and the pre-parked config-knob debt is confirmed absent from fold-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery tools → findings artifact | Tool output (ruff, semgrep, gitleaks, pip-audit) is captured verbatim into a Markdown file; no code is executed from the codebase, no packages are installed. |
| git history → secret exposure surface | The full-history gitleaks scan reads every commit; any surfaced secret is, by assumption (public repo), already exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Information Disclosure | full-history gitleaks scan output | mitigate | A surfaced secret is recorded by commit SHA + rule in the artifact with a rotate-then-document remediation note for Phase 69; the artifact does NOT reproduce the raw secret value verbatim (record the locator, not the credential). |
| T-68-02 | Tampering | discovery toolchain integrity | accept | Tools are pre-installed, confirmed at planning (gitleaks 8.30.1, semgrep, pip-audit, Shield 0.3.1); this phase installs NO packages, so the supply-chain install vector does not apply. |
| T-68-03 | Repudiation | scan reproducibility | mitigate | The `## Scan Provenance` header records date, branch, tool versions, and commit count so any reader can re-run and reproduce the same pass. |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this phase (discovery-only, read-only tools already present); the Package Legitimacy Gate does not apply. No `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
Phase-level checks (all read-only; this phase writes no source):

1. `test -f .planning/phases/68-code-track-hostile-reader-discovery/68-FINDINGS.md` — the single artifact exists.
2. All six per-source sections (`ruff`, `Shield (Semgrep)`, `Shield (gitleaks working-tree)`, `Shield (dependency audit)`, `gitleaks (full history)`, `entry-point skim`) are present and populated (not placeholder-empty).
3. The full-history section is conclusive — either "history scan clean, N commits scanned" with the live N, or commit-SHA-anchored FOLD-IN exposure rows.
4. Every FOLD-IN row carries a file:line or commit SHA; every PARKED row carries a rationale.
5. Both curated known items (`.orchestrator.json` gitignore gap; SAFETY-03 at scheduler.py:~325) appear in the artifact and in the Fold-In Summary.
6. No DEBT-03/06/07/08 row is classified FOLD-IN (HARD RULE check).
7. `git status --porcelain triggarr/ tests/` shows no source changes — confirm the phase touched only the findings artifact.
</verification>

<success_criteria>
- A single triaged findings artifact `68-FINDINGS.md` exists in the phase directory with a provenance header and one section per discovery source (CDISC-05).
- ruff was run whole-tree (`uv run ruff check triggarr/ tests/`) and its result is recorded (CDISC-01).
- Shield (Semgrep SAST + gitleaks working-tree secrets + dependency audit) was run and each component's result is recorded (CDISC-02).
- gitleaks scanned the FULL git history honoring `.gitleaksignore`; the result is conclusive (explicit clean-with-count OR commit-SHA FOLD-IN exposures) (CDISC-03).
- The six entry-point files were skimmed with hostile framing and notes captured with file:line anchors (CDISC-04).
- Every finding is classified FOLD-IN or PARKED; every FOLD-IN is actionable; every PARKED has a rationale; the Fold-In Summary is Phase 69's copy-paste CHARD-04 checklist (CDISC-05).
- The two curated known items are confirmed and recorded; the pre-parked config-knob debt is absent from fold-in.
</success_criteria>

<output>
Create `.planning/phases/68-code-track-hostile-reader-discovery/68-01-SUMMARY.md` when done. The SUMMARY must state, for the downstream Phase 69 planner: the count of FOLD-IN findings, whether the history scan was clean or surfaced exposures, and a pointer to the `## Fold-In Summary` section of 68-FINDINGS.md as the CHARD-04 fix checklist.
</output>
