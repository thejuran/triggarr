---
phase: 33-config-model-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - triggarr/models/config.py
  - tests/test_config.py
autonomous: true
requirements:
  - INST-01
  - INST-02

must_haves:
  truths:
    - "Settings model accepts a dict of named InstanceConfig objects for radarr field"
    - "Settings model accepts a dict of named InstanceConfig objects for sonarr field"
    - "Validation rejects more than 5 instances per app type"
    - "Each InstanceConfig validates search counts when enabled"
    - "has_enabled_app returns true when any instance across any app type is enabled with a URL"
    - "SecretStr on api_key is preserved in InstanceConfig"
  artifacts:
    - path: "triggarr/models/config.py"
      provides: "InstanceConfig model, updated Settings with dict[str, InstanceConfig] fields"
      contains: "class InstanceConfig"
      exports: ["InstanceConfig", "Settings", "GeneralConfig", "get_config_dir"]
    - path: "tests/test_config.py"
      provides: "Tests for multi-instance model validation"
      contains: "test_multi_instance"
  key_links:
    - from: "triggarr/models/config.py"
      to: "tests/test_config.py"
      via: "InstanceConfig and Settings imports"
      pattern: "from triggarr\\.models\\.config import.*InstanceConfig"
---

<objective>
Create the multi-instance config model: rename ArrConfig to InstanceConfig, change Settings.radarr and Settings.sonarr from single ArrConfig to dict[str, InstanceConfig], add validation (max 5 per app type), update has_enabled_app, and keep a deprecated ArrConfig alias for transition.

Purpose: INST-01 and INST-02 require multiple named instances per app type. The model is the foundation for everything else in v2.3.
Output: Updated triggarr/models/config.py with InstanceConfig and dict-based Settings, plus comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-config-model-migration/CONTEXT.md
@.planning/phases/33-config-model-migration/33-RESEARCH.md

@triggarr/models/config.py
@tests/test_config.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InstanceConfig model and Settings with dict fields</name>
  <files>triggarr/models/config.py, tests/test_config.py</files>
  <behavior>
    - Test: InstanceConfig with valid fields creates successfully (url, api_key as SecretStr, enabled, search_interval, search_missing_count, search_cutoff_count)
    - Test: InstanceConfig rejects both search counts = 0 when enabled (existing validator behavior preserved)
    - Test: InstanceConfig allows both counts = 0 when disabled
    - Test: Settings accepts radarr as dict[str, InstanceConfig] with multiple named instances (e.g., {"4K Radarr": {...}, "Default": {...}})
    - Test: Settings accepts sonarr as dict[str, InstanceConfig] with multiple named instances
    - Test: Settings accepts empty dicts for radarr and sonarr (fresh install scenario)
    - Test: Settings rejects more than 5 radarr instances (ValidationError)
    - Test: Settings rejects more than 5 sonarr instances (ValidationError)
    - Test: has_enabled_app returns True when one instance in radarr dict is enabled with a URL
    - Test: has_enabled_app returns True when one instance in sonarr dict is enabled with a URL
    - Test: has_enabled_app returns False when all instances are disabled or have empty URLs
    - Test: SecretStr api_key value does not appear in str(), repr(), or model_dump_json() of InstanceConfig
    - Test: ArrConfig alias still works (backward compatibility for transition)
  </behavior>
  <action>
    RED phase: Write all tests listed in behavior block into tests/test_config.py. Add new test functions prefixed with `test_` and using descriptive names matching the behavior. Use `from triggarr.models.config import InstanceConfig, Settings, ArrConfig`. Tests MUST fail because InstanceConfig does not exist yet.

    GREEN phase: Update triggarr/models/config.py:
    1. Rename class ArrConfig to InstanceConfig (keep all fields and validators identical)
    2. Add `ArrConfig = InstanceConfig` alias after the class for backward compat
    3. Change Settings fields:
       - `radarr: dict[str, InstanceConfig] = {}` (was `radarr: ArrConfig = ArrConfig()`)
       - `sonarr: dict[str, InstanceConfig] = {}` (was `sonarr: ArrConfig = ArrConfig()`)
    4. Add model_validator(mode="after") `validate_instances` on Settings that checks `len(instances) > 5` for each app type and raises ValueError
    5. Update `has_enabled_app` property to iterate dict values:
       ```python
       @property
       def has_enabled_app(self) -> bool:
           for app_type in ("radarr", "sonarr"):
               for cfg in getattr(self, app_type).values():
                   if cfg.enabled and cfg.url.strip():
                       return True
           return False
       ```
    6. Add helper methods for downstream consumers (Phase 34 will use these):
       ```python
       def get_enabled_instances(self, app_type: str) -> dict[str, InstanceConfig]:
           return {name: cfg for name, cfg in getattr(self, app_type).items() if cfg.enabled and cfg.url.strip()}
       ```
    7. Keep `settings_customise_sources` unchanged
    8. Update all exports: ensure InstanceConfig, ArrConfig alias, Settings, GeneralConfig, get_config_dir are importable

    REFACTOR: Clean up any redundancy. Ensure docstrings reflect multi-instance semantics.

    IMPORTANT: Do NOT update conftest.py, startup.py, scheduler.py, engine.py, or routes.py in this task. Those are handled in Plan 02 or Phase 34.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_config.py -x -q -k "instance or multi_instance or max_instances or has_enabled or ArrConfig"</automated>
  </verify>
  <done>
    - InstanceConfig class exists with all fields from old ArrConfig
    - ArrConfig alias points to InstanceConfig
    - Settings.radarr and Settings.sonarr are dict[str, InstanceConfig]
    - Max 5 instance validation works
    - has_enabled_app iterates dict values correctly
    - get_enabled_instances helper method exists
    - All new tests pass, no ruff violations
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/julianamacbook/triggarr && uv run pytest tests/test_config.py -x -q
cd /Users/julianamacbook/triggarr && uv run ruff check triggarr/models/config.py tests/test_config.py
```
</verification>

<success_criteria>
- InstanceConfig model with all per-instance fields (url, api_key, enabled, search_interval, search_missing_count, search_cutoff_count)
- Settings.radarr and Settings.sonarr are dict[str, InstanceConfig] with default empty dict
- Validation: max 5 per app type, search count validation on enabled instances
- has_enabled_app works with dict-based model
- ArrConfig backward-compat alias exists
- All tests in test_config.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-config-model-migration/33-01-SUMMARY.md`
</output>
