---
milestone: v2.11
audited: 2026-06-04
status: passed
scores:
  requirements: 11/11
  phases: 1/1
  integration: 11/11
  flows: 1/1
gaps: {}
tech_debt:
  - phase: 76-never-searched-first-search-queue
    items:
      - "SUMMARY frontmatter: 76-01 and 76-02 have requirements_completed: [] (not populated by the planner); coverage is independently proven by VERIFICATION.md + REQUIREMENTS.md traceability, so this is a metadata-population gap only."
      - "VALIDATION.md frontmatter nyquist_compliant: false / wave_0_complete: false were not flipped post-execution, despite every test the validation strategy specified being written and passing (1089 green). Status-flag drift, not a coverage gap."
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 0
  overall: partial
---

# Milestone v2.11 — Audit Report

**Milestone:** v2.11 Never-Searched-First Search Queue Priority
**Audited:** 2026-06-04
**Status:** ✅ PASSED

Replaced the integer search cursor (`missing_cursor`/`cutoff_cursor`) + `slice_batch` with an
ordered per-instance "searched-log" on `AppState` and a pure `prioritize_batch()` dispatcher
(never-searched-first, top-up oldest-searched-first, mark-on-attempt, reset-per-pass,
prune-to-eligible, commit-at-cycle-end). Single phase (76), 3 plans.

## Scores

| Dimension | Score |
|-----------|-------|
| Requirements satisfied | 11/11 |
| Phases verified | 1/1 |
| Cross-phase integration | 11/11 connections wired |
| E2E flows | 1/1 complete |

## Requirements Coverage (3-source cross-reference)

All 11 requirements **satisfied** — confirmed across VERIFICATION.md (status: passed, 9/9
must-haves, each QUEUE ID with file:line evidence), REQUIREMENTS.md traceability (11/11 mapped to
Phase 76, all Complete, 0 unmapped), and the integration checker (all 11 wired live).

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| QUEUE-01 | Ordered searched-log per queue in state.json | satisfied | `missing_searched`/`cutoff_searched` state.py:48-49; round-trip tests |
| QUEUE-02 | Per-app key; Sonarr `seriesId:seasonNumber` composite | satisfied | per-site key_fn; Sonarr "123:0"/"123:1"/"123:2" tests |
| QUEUE-03 | Cursor fields removed; strip-on-load, no migration | satisfied | `_merge_defaults` pop state.py:152-153; `test_strip_on_load_save_round_trip` |
| QUEUE-04 | Never-searched-first (fetch order) | satisfied | `prioritize_batch` engine.py:183; `test_prioritize_batch_unsearched_first` |
| QUEUE-05 | Top-up oldest-searched-first | satisfied | engine.py:188-194; `test_prioritize_batch_topup_oldest_first` |
| QUEUE-06 | Cold-start equivalence | satisfied | `test_prioritize_batch_cold_start_fixed_expectation` (slice_batch-free) |
| QUEUE-07 | 6 sites rewired; slice_batch removed | satisfied | 6 call sites; `grep -rq slice_batch` empty |
| QUEUE-08 | Mark-on-attempt | satisfied | append before loop; `test_run_radarr_cycle_mark_on_attempt` |
| QUEUE-09 | Pass-reset clears log + bumps `*_pass` | satisfied | `bool(batch)` guard engine.py:201; pass-reset tests |
| QUEUE-10 | Prune-to-eligible each cycle | satisfied | engine.py:179; `test_prioritize_batch_prune_departed_items` |
| QUEUE-11 | Commit only at cycle end (single save_state) | satisfied | write-back post-loop; save_state once in `_run_one_cycle` |

## Cross-Phase Integration

Integration checker: **11/11 connections wired, 0 orphaned, 0 missing, 0 broken flows.** Full
chain verified: scheduler.py → engine.py (`run_*_cycle` → `prioritize_batch`) → state.py
(`_merge_defaults` strip + `save_state` at cycle end) → routes.py (`_build_app_context` sources
the dashboard numerator from `len(searched-log)` — the deep-review fix) → app_card.html. No
consumer reads a removed cursor key. Count-only refresh (`refresh_*_counts`) confirmed
queue-independent (never reads/writes the searched-log). E2E flow matches the live NAS
walkthrough behavior.

## Verification & Review

- **Phase 76 VERIFICATION.md:** status `passed`, 9/9 must-haves verified against live code.
- **Deep review (.turingmind/REVIEW.md):** ✅ APPROVED — 2 findings raised (1 warning: dashboard
  half-migration; 1 medium: negative-batch_size slice), both fixed and re-verified.
- **Codex adversarial review (plan stage):** 2 design blockers (stale-key persistence; broken
  runtime checkpoint) + 2 mediums caught and resolved before execution.
- **Live NAS walkthrough:** 0 bugs — dashboard "X of N" numerator confirmed live (the review-fix
  surface), never-searched-first dispatch + Sonarr composite-key path exercised via scheduled
  cycles, refresh-counts queue-independence confirmed, no secrets exposed.
- **Test suite:** 1089 passing, ruff clean.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Note |
|-------|---------------|-----------|------|
| 76 | exists | partial | `nyquist_compliant` flag not flipped post-execution; all specified tests written + passing (1089 green). Status-flag drift, not a coverage gap. |

## Tech Debt (non-blocking)

1. **SUMMARY frontmatter population** — 76-01/02 have `requirements_completed: []`. Coverage is
   independently proven; metadata-only gap.
2. **VALIDATION.md status flags** — `nyquist_compliant: false` / `wave_0_complete: false` not
   updated post-execution despite passing tests. Status-flag drift.

Neither blocks completion. Both are bookkeeping, not functional gaps.

## Verdict

✅ **PASSED** — all 11 requirements satisfied, single phase verified, cross-phase integration
clean (11/11), E2E flow complete and live-confirmed. No critical gaps, no unsatisfied
requirements, no orphans. Two minor non-blocking metadata items noted. Ready to complete.
