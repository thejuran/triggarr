---
phase: 22-rename-to-triggarr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/__init__.py
  - triggarr/__main__.py
  - triggarr/config.py
  - triggarr/db.py
  - triggarr/state.py
  - triggarr/startup.py
  - triggarr/logging.py
  - triggarr/log_buffer.py
  - triggarr/correlation.py
  - triggarr/tracking.py
  - triggarr/clients/__init__.py
  - triggarr/clients/base.py
  - triggarr/clients/radarr.py
  - triggarr/clients/sonarr.py
  - triggarr/models/__init__.py
  - triggarr/models/config.py
  - triggarr/search/__init__.py
  - triggarr/search/engine.py
  - triggarr/search/scheduler.py
  - triggarr/web/__init__.py
  - triggarr/web/middleware.py
  - triggarr/web/routes.py
  - triggarr/web/validation.py
  - tests/conftest.py
  - tests/test_clients.py
  - tests/test_config.py
  - tests/test_correlation.py
  - tests/test_db.py
  - tests/test_log_buffer.py
  - tests/test_logging.py
  - tests/test_middleware.py
  - tests/test_scheduler.py
  - tests/test_search.py
  - tests/test_startup.py
  - tests/test_state.py
  - tests/test_tracking.py
  - tests/test_validation.py
  - tests/test_web.py
  - pyproject.toml
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-06

must_haves:
  truths:
    - "Python package is named triggarr (directory and pyproject.toml)"
    - "All imports reference triggarr, not fetcharr"
    - "All tests pass with the new package name"
  artifacts:
    - path: "triggarr/__init__.py"
      provides: "Package root"
    - path: "triggarr/__main__.py"
      provides: "Entry point"
    - path: "pyproject.toml"
      provides: "Package metadata"
      contains: 'name = "triggarr"'
  key_links:
    - from: "tests/*.py"
      to: "triggarr/"
      via: "import statements"
      pattern: "from triggarr"
    - from: "pyproject.toml"
      to: "triggarr/__main__"
      via: "project.scripts"
      pattern: 'triggarr = "triggarr.__main__:main"'
---

<objective>
Rename the Python package from fetcharr to triggarr and update all imports.

Purpose: The project has been renamed to Triggarr. The Python package directory and all import references must reflect the new name for code to function correctly.
Output: Renamed triggarr/ package directory, updated pyproject.toml, all imports in source and tests updated, tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branch and rename package directory with all import updates</name>
  <files>
    triggarr/ (entire directory, renamed from fetcharr/)
    pyproject.toml
  </files>
  <action>
    1. Create a git branch named `rename-to-triggarr` from current HEAD:
       `git checkout -b rename-to-triggarr`

    2. Rename the package directory:
       `git mv fetcharr triggarr`

    3. Update pyproject.toml:
       - `name = "fetcharr"` -> `name = "triggarr"`
       - `fetcharr = "fetcharr.__main__:main"` -> `triggarr = "triggarr.__main__:main"`

    4. Find-and-replace ALL import references in triggarr/ source files.
       In every .py file under triggarr/, replace:
       - `from fetcharr.` -> `from triggarr.`
       - `import fetcharr.` -> `import triggarr.`
       - `"fetcharr."` -> `"triggarr."` (string references to module paths)

       Key files with internal imports:
       - triggarr/config.py (imports from triggarr.models.config)
       - triggarr/startup.py (imports from triggarr.config, triggarr.clients, etc.)
       - triggarr/search/engine.py (imports from triggarr.clients, triggarr.db, etc.)
       - triggarr/search/scheduler.py (imports from triggarr.search.engine, triggarr.config, etc.)
       - triggarr/web/routes.py (imports from triggarr.config, triggarr.db, etc.)
       - triggarr/web/middleware.py (imports from triggarr modules)
       - triggarr/tracking.py (imports from triggarr.correlation, triggarr.db, etc.)
       - triggarr/correlation.py (imports from triggarr.models)
       - triggarr/clients/*.py (imports from triggarr.models, triggarr.clients.base)
       - triggarr/models/*.py (possible internal imports)
       - triggarr/__main__.py (imports from triggarr)

    5. Also update any string references like logger names that contain "fetcharr" to "triggarr".
       Check for patterns like: `logger.bind(name="fetcharr")`, `getLogger("fetcharr")`, or similar.

    Do NOT touch: Dockerfile, docker-compose.yml, entrypoint.sh, README.md, CLAUDE.md, CI/CD workflows, or HTML templates -- those are handled in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && test -d triggarr && ! test -d fetcharr && python -c "import ast; [ast.parse(open(f).read()) for f in __import__('glob').glob('triggarr/**/*.py', recursive=True)]" && echo "OK"</automated>
  </verify>
  <done>fetcharr/ directory no longer exists, triggarr/ directory exists with all source files, pyproject.toml references triggarr, all .py files parse without syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Update all test imports and verify tests pass</name>
  <files>
    tests/conftest.py
    tests/test_clients.py
    tests/test_config.py
    tests/test_correlation.py
    tests/test_db.py
    tests/test_log_buffer.py
    tests/test_logging.py
    tests/test_middleware.py
    tests/test_scheduler.py
    tests/test_search.py
    tests/test_startup.py
    tests/test_state.py
    tests/test_tracking.py
    tests/test_validation.py
    tests/test_web.py
  </files>
  <action>
    1. In every file under tests/, find-and-replace:
       - `from fetcharr.` -> `from triggarr.`
       - `import fetcharr.` -> `import triggarr.`
       - `"fetcharr.` -> `"triggarr.` (string references used in mock paths like `"fetcharr.config.load_config"`)
       - `'fetcharr.` -> `'triggarr.` (single-quoted mock paths)

       CRITICAL: Mock patch paths must match the new module name exactly.
       For example: `@patch("fetcharr.search.engine.SearchEngine")` -> `@patch("triggarr.search.engine.SearchEngine")`

    2. Run `uv sync --extra dev` to reinstall the package under the new name.

    3. Run `uv run ruff check triggarr/ tests/` -- fix any import sort violations caused by the rename.

    4. Run `uv run pytest tests/ -x -q` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/ -x -q && uv run ruff check triggarr/ tests/</automated>
  </verify>
  <done>All test files import from triggarr (zero references to "from fetcharr" or "import fetcharr" in tests/), ruff passes with zero violations, all tests pass</done>
</task>

</tasks>

<verification>
- `grep -r "from fetcharr" triggarr/ tests/` returns zero results
- `grep -r "import fetcharr" triggarr/ tests/` returns zero results
- `uv run pytest tests/ -x -q` passes all tests
- `uv run ruff check triggarr/ tests/` reports zero violations
- `git branch --show-current` shows `rename-to-triggarr`
</verification>

<success_criteria>
- Python package directory is triggarr/ (fetcharr/ does not exist)
- pyproject.toml name is "triggarr" with correct entry point
- All source and test imports reference triggarr
- All tests pass, ruff clean
- Work is on the rename-to-triggarr branch
</success_criteria>

<output>
After completion, create `.planning/phases/22-rename-to-triggarr/22-01-SUMMARY.md`
</output>
