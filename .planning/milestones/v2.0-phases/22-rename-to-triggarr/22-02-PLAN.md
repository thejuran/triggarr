---
phase: 22-rename-to-triggarr
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - entrypoint.sh
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - CLAUDE.md
  - README.md
  - triggarr/templates/base.html
  - triggarr/templates/dashboard.html
  - triggarr/templates/settings.html
  - triggarr/templates/history.html
  - triggarr/templates/partials/app_card.html
  - triggarr/templates/partials/search_log.html
  - triggarr/templates/partials/history_results.html
  - triggarr/templates/partials/log_viewer.html
  - triggarr/templates/partials/stats_row.html
  - triggarr/static/css/input.css
autonomous: true
requirements:
  - RENAME-03
  - RENAME-04
  - RENAME-05

must_haves:
  truths:
    - "Docker image publishes to ghcr.io/thejuran/triggarr"
    - "Dockerfile, docker-compose.yml, and entrypoint.sh reference triggarr"
    - "README and CLAUDE.md say Triggarr throughout"
    - "HTML templates show Triggarr in titles and user-facing text"
    - "CI workflow lints triggarr/ (not fetcharr/)"
  artifacts:
    - path: "Dockerfile"
      provides: "Docker build for triggarr package"
      contains: "COPY triggarr/ triggarr/"
    - path: "docker-compose.yml"
      provides: "Compose config with triggarr image"
      contains: "ghcr.io/thejuran/triggarr"
    - path: ".github/workflows/release.yml"
      provides: "Release pipeline publishing triggarr image"
      contains: "ghcr.io/thejuran/triggarr"
    - path: "README.md"
      provides: "User-facing documentation"
      contains: "Triggarr"
    - path: "CLAUDE.md"
      provides: "Developer instructions"
      contains: "triggarr"
  key_links:
    - from: "Dockerfile"
      to: "triggarr/"
      via: "COPY directive"
      pattern: "COPY triggarr/ triggarr/"
    - from: "entrypoint.sh"
      to: "triggarr"
      via: "python -m triggarr"
      pattern: "python -m triggarr"
    - from: ".github/workflows/ci.yml"
      to: "triggarr/"
      via: "ruff check path"
      pattern: "ruff check triggarr/"
---

<objective>
Update all Docker, CI/CD, documentation, and template files to reference Triggarr.

Purpose: Complete the rename by updating all non-Python-import references -- Docker image name, CI paths, user-facing strings in templates and docs.
Output: Every file in the repo references triggarr/Triggarr instead of fetcharr/Fetcharr.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rename-to-triggarr/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Docker, CI/CD, and config files</name>
  <files>
    Dockerfile
    docker-compose.yml
    entrypoint.sh
    .github/workflows/ci.yml
    .github/workflows/release.yml
  </files>
  <action>
    1. Dockerfile -- replace all occurrences:
       - `COPY fetcharr/ fetcharr/` -> `COPY triggarr/ triggarr/`  (both stage 1 and stage 2)
       - `fetcharr/static/css/input.css` -> `triggarr/static/css/input.css`
       - `fetcharr/static/css/output.css` -> `triggarr/static/css/output.css`
       - The COPY --from=builder line similarly updated

    2. docker-compose.yml -- replace:
       - `image: ghcr.io/thejuran/fetcharr:latest` -> `image: ghcr.io/thejuran/triggarr:latest`
       - `container_name: fetcharr` -> `container_name: triggarr`
       - Comment at top: "Fetcharr" -> "Triggarr"
       - `fetcharr_config` volume name -> `triggarr_config` (both in service volumes and top-level volumes)

    3. entrypoint.sh -- replace:
       - `groupadd ... fetcharr` -> `groupadd ... triggarr`
       - `useradd ... fetcharr` -> `useradd ... triggarr`
       - Comment "Drop privileges and exec into Fetcharr" -> "Drop privileges and exec into Triggarr"
       - `python -m fetcharr` -> `python -m triggarr`

    4. .github/workflows/ci.yml -- replace:
       - `uv run ruff check fetcharr/ tests/` -> `uv run ruff check triggarr/ tests/`
       - `tags: fetcharr:ci-test` -> `tags: triggarr:ci-test`

    5. .github/workflows/release.yml -- replace:
       - `images: ghcr.io/thejuran/fetcharr` -> `images: ghcr.io/thejuran/triggarr`
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && ! grep -r 'fetcharr' Dockerfile docker-compose.yml entrypoint.sh .github/workflows/ci.yml .github/workflows/release.yml && echo "OK"</automated>
  </verify>
  <done>Zero references to "fetcharr" in Dockerfile, docker-compose.yml, entrypoint.sh, ci.yml, release.yml</done>
</task>

<task type="auto">
  <name>Task 2: Update docs, templates, and user-facing strings</name>
  <files>
    CLAUDE.md
    README.md
    triggarr/templates/base.html
    triggarr/templates/dashboard.html
    triggarr/templates/settings.html
    triggarr/templates/history.html
    triggarr/templates/partials/app_card.html
    triggarr/templates/partials/search_log.html
    triggarr/templates/partials/history_results.html
    triggarr/templates/partials/log_viewer.html
    triggarr/templates/partials/stats_row.html
    triggarr/static/css/input.css
  </files>
  <action>
    1. CLAUDE.md -- replace throughout:
       - "Fetcharr" -> "Triggarr" (display name)
       - "fetcharr" -> "triggarr" (package/command references)
       - Update commands: `uv run ruff check triggarr/ tests/`, `docker build -t triggarr:local .`
       - `ghcr.io/thejuran/fetcharr` -> `ghcr.io/thejuran/triggarr`

    2. README.md -- replace throughout:
       - Title: "# Fetcharr" -> "# Triggarr"
       - All "Fetcharr" -> "Triggarr" (proper noun usage)
       - All "fetcharr" -> "triggarr" (technical references: package name, image, config, commands)
       - Badge URLs: `thejuran/fetcharr` -> `thejuran/triggarr` in both CI badge and Docker badge
       - Docker compose example: use triggarr image name, container name, volume name
       - Config file path: `/config/fetcharr.toml` -> `/config/triggarr.toml`
       - Env var prefix: `FETCHARR_GENERAL__LOG_LEVEL` -> `TRIGGARR_GENERAL__LOG_LEVEL`
       - Development commands: `ruff check triggarr/`, `docker build -t triggarr:local`
       - CSS path in dev command: `triggarr/static/css/...`

    3. HTML templates -- in ALL template files, replace:
       - Any "Fetcharr" in page titles, headings, alt text, tooltips -> "Triggarr"
       - Any "fetcharr" in CSS class names or IDs -> "triggarr" (if any exist)
       - Check base.html for <title> tag and site name

    4. triggarr/static/css/input.css -- replace any "fetcharr" comment references -> "triggarr"

    5. Also check triggarr/models/config.py for the TOML config filename.
       If it references `fetcharr.toml` as the default config filename, update to `triggarr.toml`.
       Similarly check triggarr/config.py for any hardcoded "fetcharr" references in default paths or env var prefixes.

    6. Check triggarr/logging.py and triggarr/startup.py for any user-facing log messages containing "fetcharr" or "Fetcharr" and update to triggarr/Triggarr.

    7. Final sweep: `grep -rn 'fetcharr\|Fetcharr' triggarr/ tests/ *.md *.yml *.toml Dockerfile docker-compose.yml entrypoint.sh .github/` to confirm zero remaining references (excluding .planning/).

    8. Run tests one final time: `uv run pytest tests/ -x -q` and `uv run ruff check triggarr/ tests/`
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && grep -rn 'fetcharr' triggarr/ tests/ CLAUDE.md README.md pyproject.toml Dockerfile docker-compose.yml entrypoint.sh .github/ 2>/dev/null | grep -v '.planning/' | wc -l | xargs test 0 -eq && uv run pytest tests/ -x -q && echo "OK"</automated>
  </verify>
  <done>Zero references to "fetcharr" or "Fetcharr" in any source, test, config, or doc file (excluding .planning/). All tests pass. README shows "Triggarr" as project name. Docker image targets ghcr.io/thejuran/triggarr.</done>
</task>

</tasks>

<verification>
- `grep -rn 'fetcharr' triggarr/ tests/ *.md *.yml *.toml Dockerfile docker-compose.yml entrypoint.sh .github/` returns zero results
- `uv run pytest tests/ -x -q` passes all tests
- `uv run ruff check triggarr/ tests/` reports zero violations
- `docker build -t triggarr:local .` builds successfully (Dockerfile references triggarr/)
</verification>

<success_criteria>
- All Docker/CI files reference triggarr
- release.yml publishes to ghcr.io/thejuran/triggarr
- README.md title is "Triggarr", all references updated
- CLAUDE.md commands reference triggarr
- HTML templates show "Triggarr" in user-facing text
- Config filename is triggarr.toml
- Zero "fetcharr" references remain outside .planning/
- All tests pass, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/22-rename-to-triggarr/22-02-SUMMARY.md`
</output>
