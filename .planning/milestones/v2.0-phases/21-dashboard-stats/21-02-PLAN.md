---
phase: 21-dashboard-stats
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - fetcharr/templates/partials/history_results.html
  - fetcharr/templates/partials/search_log.html
  - fetcharr/templates/settings.html
  - fetcharr/web/routes.py
  - tests/test_web.py
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04, STATS-05]

must_haves:
  truths:
    - "Search history entries display color-coded outcome badges: grabbed=green, partial=amber, unresolved=gray"
    - "Outcome badges have tooltips explaining each state"
    - "History filter bar includes grabbed, partial, unresolved as filterable pills"
    - "Search log on dashboard shows color-coded outcome badges for new outcomes"
    - "Settings page has 4 new inputs in General section: tracking window, timeout, page size, max history rows"
    - "Settings form saves new config fields and they take effect immediately"
  artifacts:
    - path: "fetcharr/templates/partials/history_results.html"
      provides: "Outcome badges with colors and tooltips, filter pills for new outcomes"
      contains: "grabbed"
    - path: "fetcharr/templates/partials/search_log.html"
      provides: "Color-coded outcome badges for all outcome types"
      contains: "partial"
    - path: "fetcharr/templates/settings.html"
      provides: "4 new form inputs in General section"
      contains: "tracking_window_minutes"
    - path: "fetcharr/web/routes.py"
      provides: "save_settings reads new form fields from POST data"
      contains: "tracking_window_minutes"
  key_links:
    - from: "fetcharr/templates/settings.html"
      to: "fetcharr/web/routes.py"
      via: "POST /settings form submission"
      pattern: "tracking_window_minutes"
    - from: "fetcharr/templates/partials/history_results.html"
      to: "/partials/history-results"
      via: "hx-get with outcome filter param"
      pattern: "outcome.*grabbed"
---

<objective>
Add color-coded outcome badges to search history/log, extend history filter bar with new outcomes, and wire new config fields into the settings form.

Purpose: Completes the Phase 21 UI -- users see per-item tracking results at a glance and can configure all tracking-related settings.
Output: Updated history templates with outcome badges, settings form with 4 new inputs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-stats/21-CONTEXT.md
@.planning/phases/21-dashboard-stats/21-01-SUMMARY.md
@fetcharr/templates/partials/history_results.html
@fetcharr/templates/partials/search_log.html
@fetcharr/templates/settings.html
@fetcharr/web/routes.py
</context>

<interfaces>
<!-- From 21-CONTEXT.md locked decisions -->

Outcome badge colors (traffic light scheme):
- searched = blue (existing, keep as-is)
- failed = red (existing, keep as-is)
- grabbed = green (new)
- partial = amber/yellow (new)
- unresolved = gray (new)

Tooltips:
- grabbed: "Download detected within tracking window"
- partial: "Some episodes grabbed, not all"
- unresolved: "No grabs detected before window expired"

Settings fields to add to General section:
- tracking_window_minutes (int, hint: "Minutes to wait for grabs after search")
- request_timeout (float, hint: "Seconds for outbound HTTP requests")
- page_size (int, hint: "Items per page for *arr API requests")
- max_history_rows (int, hint: "0 = unlimited. Max resolved rows in search history.")

From fetcharr/web/routes.py save_settings:
```python
# Currently preserves these values (not editable in UI):
"max_history_rows": current_settings.general.max_history_rows,
"request_timeout": current_settings.general.request_timeout,
"page_size": current_settings.general.page_size,
"tracking_window_minutes": current_settings.general.tracking_window_minutes,
"tracking_delay_seconds": current_settings.general.tracking_delay_seconds,
```

From fetcharr/templates/partials/history_results.html:
```html
<!-- Existing outcome filter only has: 'searched', 'failed' -->
{% for o_val in ['searched', 'failed'] %}

<!-- Existing badge colors only handle: failed=red, else=green -->
{% if entry.outcome == 'failed' %}bg-red-500/20 text-red-400
{% else %}bg-fetcharr-green/20 text-fetcharr-green{% endif %}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add outcome badges with colors and tooltips to history and search log templates</name>
  <files>fetcharr/templates/partials/history_results.html, fetcharr/templates/partials/search_log.html</files>
  <action>
In both `history_results.html` and `search_log.html`, update the outcome badge span to use a traffic-light color scheme with tooltips for all 5 outcome values.

Replace the existing two-way conditional badge in BOTH templates with a 5-way conditional:

```html
<span class="text-xs px-1.5 py-0.5 rounded
       {% if entry.outcome == 'grabbed' %}bg-green-500/20 text-green-400
       {% elif entry.outcome == 'partial' %}bg-amber-500/20 text-amber-400
       {% elif entry.outcome == 'unresolved' %}bg-gray-500/20 text-gray-400
       {% elif entry.outcome == 'failed' %}bg-red-500/20 text-red-400
       {% else %}bg-blue-500/20 text-blue-400{% endif %}"
       {% if entry.outcome == 'grabbed' %}title="Download detected within tracking window"
       {% elif entry.outcome == 'partial' %}title="Some episodes grabbed, not all"
       {% elif entry.outcome == 'unresolved' %}title="No grabs detected before window expired"
       {% endif %}>
    {{ entry.outcome or 'searched' }}
</span>
```

In `history_results.html` ONLY, extend the outcome filter pill loop to include the 3 new outcomes. Change:
```
{% for o_val in ['searched', 'failed'] %}
```
to:
```
{% for o_val in ['searched', 'grabbed', 'partial', 'unresolved', 'failed'] %}
```

And update the filter pill color conditional to match all 5 outcomes:
```html
{% if o_val in active_outcomes %}
  {% if o_val == 'grabbed' %}bg-green-500/20 text-green-400
  {% elif o_val == 'partial' %}bg-amber-500/20 text-amber-400
  {% elif o_val == 'unresolved' %}bg-gray-500/20 text-gray-400
  {% elif o_val == 'failed' %}bg-red-500/20 text-red-400
  {% else %}bg-blue-500/20 text-blue-400{% endif %}
{% else %}
  bg-fetcharr-card text-fetcharr-muted border border-fetcharr-border hover:text-white
{% endif %}
```

Do NOT change any other part of these templates (pagination, text search, app/queue filters).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_web.py -x -q</automated>
  </verify>
  <done>All 5 outcome values render with correct color-coded badges and tooltips. History filter bar shows 5 outcome pills (searched, grabbed, partial, unresolved, failed). Search log on dashboard shows same color-coded badges.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new config fields into settings form and save handler</name>
  <files>fetcharr/templates/settings.html, fetcharr/web/routes.py, tests/test_web.py</files>
  <action>
In `settings.html`, add 4 new form inputs to the existing General section, AFTER the "Hard Max Items Per Cycle" input. Each follows the existing pattern (label + input + hint):

1. **Max History Rows** -- `name="max_history_rows"`, type=number, value="{{ max_history_rows }}", min=0, max=100000
   - Hint: "0 = unlimited. Max resolved rows kept in search history."

2. **Request Timeout (seconds)** -- `name="request_timeout"`, type=number, value="{{ request_timeout }}", min=5, max=300, step=1
   - Hint: "Timeout for outbound HTTP requests to *arr apps."

3. **Page Size** -- `name="page_size"`, type=number, value="{{ page_size }}", min=10, max=500
   - Hint: "Items per page when querying *arr APIs."

4. **Tracking Window (minutes)** -- `name="tracking_window_minutes"`, type=number, value="{{ tracking_window_minutes }}", min=5, max=1440
   - Hint: "How long to wait for grabs after a search before marking unresolved."

Use the same Tailwind classes as existing inputs: `w-full bg-fetcharr-bg border border-fetcharr-border rounded px-3 py-2 text-sm`
Use the same hint pattern: `<p class="text-xs text-fetcharr-muted mt-1">hint text</p>`

In `routes.py` `save_settings`, update the general config dict to read from form data instead of preserving current values:
```python
"general": {
    "log_level": safe_log_level(form.get("log_level")),
    "hard_max_per_cycle": safe_int(form.get("hard_max_per_cycle"), 0, 0, 1000),
    "max_history_rows": safe_int(form.get("max_history_rows"), 1000, 0, 100_000),
    "request_timeout": safe_int(form.get("request_timeout"), 30, 5, 300),
    "page_size": safe_int(form.get("page_size"), 50, 10, 500),
    "tracking_window_minutes": safe_int(form.get("tracking_window_minutes"), 60, 5, 1440),
    "tracking_delay_seconds": current_settings.general.tracking_delay_seconds,
},
```

Note: `tracking_delay_seconds` stays preserved (not user-editable per CONTEXT.md -- only the 4 listed fields get form inputs). `request_timeout` uses `safe_int` which returns int -- that's fine since GeneralConfig.request_timeout is float and Pydantic will coerce.

Add tests:
1. Test settings page renders new form fields (check for "tracking_window_minutes" in response body)
2. Test POST /settings with new fields saves correctly (verify new_settings values after save)
3. Test outcome badge colors appear in history partial response (insert entries with grabbed/partial/unresolved outcomes, verify color classes in response)

Run ruff: `uv run ruff check fetcharr/ tests/ --fix`
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/ -x -q && uv run ruff check fetcharr/ tests/</automated>
  </verify>
  <done>Settings page shows 4 new config inputs with hint text. Form submission saves all new values to TOML config. Changes take effect immediately via hot-reload. All tests pass, zero ruff violations.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` -- all tests pass
2. `uv run ruff check fetcharr/ tests/` -- zero violations
3. History page shows 5 outcome filter pills and color-coded badges
4. Settings page shows 4 new General config inputs
5. Saving settings persists new field values
</verification>

<success_criteria>
- Outcome badges use traffic-light colors: grabbed=green, partial=amber, unresolved=gray, failed=red, searched=blue
- Each new badge has a tooltip explaining the state
- History filter bar has 5 outcome pills (searched, grabbed, partial, unresolved, failed)
- Settings General section has inputs for tracking_window_minutes, request_timeout, page_size, max_history_rows
- Settings save correctly writes new fields to TOML and reloads without restart
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-dashboard-stats/21-02-SUMMARY.md`
</output>
