---
phase: 20.1-deep-review-security
plan: 01
subsystem: database
tags: [sqlite, aiosqlite, migration, row-factory, exception-handling]

# Dependency graph
requires:
  - phase: 17-database-tracking
    provides: "Migration system, row_factory pattern, shared connection"
provides:
  - "Safe row_factory handling with try/finally guards"
  - "Guarded migration backup (no crash on fresh install)"
  - "Correct DEFAULT 'searched' for v1 migration outcome column"
  - "Narrow exception suppression (sqlite3.OperationalError only)"
affects: [20.2-deep-review-quality]

# Tech tracking
tech-stack:
  added: []
  patterns: ["try/finally for shared mutable state on async connection", "Path.exists() guard before file operations"]

key-files:
  created: []
  modified: [fetcharr/db.py, tests/test_db.py]

key-decisions:
  - "Monkeypatch Path.exists() in fresh-install test since aiosqlite creates file before run_migrations"

patterns-established:
  - "try/finally around any db.row_factory mutation on shared connection"
  - "Guard shutil.copy2 with Path.exists() to handle missing source files"
  - "Use sqlite3.OperationalError (not bare Exception) in contextlib.suppress for ALTER TABLE"

requirements-completed: [DRSEC-01, DRSEC-04, DRSEC-05, DRSEC-06]

# Metrics
duration: 4min
completed: 2026-02-25
---

# Phase 20.1 Plan 01: DB Safety Fixes Summary

**try/finally guards on row_factory mutations, backup guard for fresh installs, correct v1 DEFAULT, and narrow suppress scope in db.py**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-26T00:36:18Z
- **Completed:** 2026-02-26T00:41:04Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Wrapped all three row_factory set/reset sites in try/finally to prevent async race conditions
- Guarded shutil.copy2 backup with db_path.exists() to prevent FileNotFoundError on fresh install
- Changed v1 migration outcome DEFAULT from NULL to 'searched' for correct v4 backfill semantics
- Narrowed contextlib.suppress(Exception) to sqlite3.OperationalError in _migrate_v1 and _migrate_v2
- Added 4 new tests covering row_factory cleanup, fresh-install migration, and idempotent suppress

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix row_factory race, migration backup, DEFAULT, and suppress scope in db.py** - `75e8153` (fix)
2. **Task 2: Add tests for row_factory cleanup, fresh-install migration, and narrow suppress** - `677e5a0` (test)

## Files Created/Modified
- `fetcharr/db.py` - try/finally around row_factory, db_path.exists() guard, 'searched' DEFAULT, sqlite3.OperationalError suppress
- `tests/test_db.py` - 4 new tests for deep-review safety fixes

## Decisions Made
- Monkeypatched Path.exists() in fresh-install test because aiosqlite creates the file during init_db's CREATE TABLE before run_migrations runs, making a true "no file" scenario impossible through init_db

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed fresh-install test assertion**
- **Found during:** Task 2 (test_run_migrations_fresh_install)
- **Issue:** Test assumed init_db on fresh path would not create a backup, but init_db creates the file via CREATE TABLE before run_migrations checks db_path.exists(), so backup was always created
- **Fix:** Used monkeypatch on Path.exists() to return False for db_path, directly testing the guard logic
- **Files modified:** tests/test_db.py
- **Verification:** Test passes, correctly verifies no backup created when db_path.exists() returns False
- **Committed in:** 677e5a0

**2. [Rule 1 - Bug] Fixed lint violations in tests**
- **Found during:** Task 2
- **Issue:** Unused imports (run_migrations, PropertyMock, sqlite3) and unsorted import block
- **Fix:** Removed unused imports, organized import order per ruff I001
- **Files modified:** tests/test_db.py
- **Verification:** uv run ruff check tests/test_db.py passes clean
- **Committed in:** 677e5a0

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both auto-fixes necessary for test correctness and lint compliance. No scope creep.

## Issues Encountered
- Task 1 code changes were already committed from a prior execution attempt (75e8153). Verified all changes present, skipped re-implementation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All four database safety issues resolved and tested
- Ready for 20.1-02 (security fixes) or 20.2 (quality fixes)
- No blockers

---
*Phase: 20.1-deep-review-security*
*Completed: 2026-02-25*
