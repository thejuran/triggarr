---
phase: 20.1-deep-review-security
verified: 2026-02-25T01:10:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 20.1: Deep Review — Security Verification Report

**Phase Goal:** All security vulnerabilities and data-correctness bugs identified by deep code review are resolved before shipping
**Verified:** 2026-02-25T01:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | row_factory mutations on the shared connection are protected by try/finally so concurrent async coroutines cannot leave it in the wrong state | VERIFIED | `db.py` lines 226-235, 302-321, 356-366: all three sites have `db.row_factory = aiosqlite.Row` followed immediately by `try:` block and `finally: db.row_factory = None` |
| 2 | Fresh install (no database file) runs migrations without FileNotFoundError | VERIFIED | `db.py` line 61: `if db_path.exists():` guards `shutil.copy2`; test `test_run_migrations_fresh_install` passes with monkeypatched `Path.exists()` returning False |
| 3 | Migration v1 sets outcome DEFAULT 'searched' so v4 backfill only catches truly pre-migration rows | VERIFIED | `db.py` line 79: `("outcome", "'searched'")` with single-quoted string literal; `test_backfill_sets_unresolved` confirms pre-v1 rows get `'searched'` outcome |
| 4 | Migration functions suppress only sqlite3.OperationalError, not disk-full or permission errors | VERIFIED | `db.py` lines 80, 88: `contextlib.suppress(sqlite3.OperationalError)` in both `_migrate_v1` and `_migrate_v2`; `import sqlite3` present at line 14 |
| 5 | All dynamic values in hx-get URL attributes use the urlencode filter to prevent reflected XSS | VERIFIED | `history_results.html` lines 19, 42, 65, 129: all four hx-get attribute sites apply `| urlencode` to every dynamic value (`new_apps`, `active_apps`, `active_queues`, `active_outcomes`, `new_queues`, `new_outcomes`, `search_text`) |
| 6 | Rate limiter timestamp is written inside the search_lock, with a re-check after acquiring, preventing concurrent bypass | VERIFIED | `routes.py` lines 378-385: `async with request.app.state.search_lock:` contains re-check at lines 380-384 then `request.app.state.last_search_time[app_name] = now` at line 385; no write occurs before the lock |
| 7 | Exception details stored in search history use sanitized type-based summaries, not raw str(exc) | VERIFIED | `engine.py` lines 26-40: `_sanitize_exc` helper defined; lines 263, 297, 414, 452: all four `insert_search_entry` failure calls use `detail=_sanitize_exc(exc)`; no `str(exc)[:200]` remains in engine.py |
| 8 | sourceTitle from external APIs is truncated to 200 chars before storage in the detail field | VERIFIED | `tracking.py` line 168: `source = (matched_grabs[0].sourceTitle or "unknown")[:200]` |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `fetcharr/db.py` | Safe row_factory handling, guarded backup, correct defaults, narrow suppress | VERIFIED | try/finally at all 3 sites; `if db_path.exists()` guard; `('searched')` DEFAULT; `sqlite3.OperationalError` suppress; `import sqlite3` present |
| `tests/test_db.py` | Tests for row_factory cleanup, fresh-install migration, narrow suppress | VERIFIED | 4 new tests at lines 654-749: `test_row_factory_restored_after_successful_calls`, `test_row_factory_restored_on_exception`, `test_run_migrations_fresh_install`, `test_migration_suppresses_only_operational_error` |
| `fetcharr/templates/partials/history_results.html` | XSS-safe URL attributes with urlencode filter | VERIFIED | All 4 hx-get URL sites apply `| urlencode` to all dynamic values (lines 19, 42, 65, 129) |
| `fetcharr/web/routes.py` | Rate limiter race fix with re-check inside lock | VERIFIED | Double-checked locking pattern: optimistic pre-check + authoritative re-check + timestamp write all correctly sequenced inside `search_lock` |
| `fetcharr/search/engine.py` | Sanitized exception details in search history entries | VERIFIED | `_sanitize_exc` helper at lines 26-40; all 4 failure insert calls use it; no raw `str(exc)` storage |
| `fetcharr/tracking.py` | sourceTitle truncation for defense-in-depth | VERIFIED | `[:200]` slice on `matched_grabs[0].sourceTitle` at line 168 |
| `tests/test_web.py` | Rate limiter concurrent-protection test | VERIFIED | `test_search_now_rate_limit_concurrent_protection` at line 462: validates first call succeeds (200), second within window is rejected (429) |
| `tests/test_tracking.py` | _sanitize_exc unit tests | VERIFIED | 3 tests at lines 351-376: `test_sanitize_exc_http_status_error`, `test_sanitize_exc_timeout`, `test_sanitize_exc_generic`; imported from `fetcharr.search.engine` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `fetcharr/db.py:get_recent_searches` | `db.row_factory` | try/finally guard | WIRED | Line 226: set; line 227: `try:`; line 234: `finally: db.row_factory = None` |
| `fetcharr/db.py:get_search_history` | `db.row_factory` | try/finally guard | WIRED | Line 302: set; line 303: `try:` covering both queries; line 321: `finally: db.row_factory = None` |
| `fetcharr/db.py:get_trackable_entries` | `db.row_factory` | try/finally guard | WIRED | Line 356: set; line 357: `try:`; line 365: `finally: db.row_factory = None` |
| `fetcharr/db.py:run_migrations` | `shutil.copy2` | path.exists() guard | WIRED | Line 61: `if db_path.exists():` gates line 62: `shutil.copy2(db_path, backup_path)` |
| `fetcharr/web/routes.py:search_now` | `request.app.state.last_search_time` | write inside search_lock | WIRED | `async with request.app.state.search_lock:` at line 378; timestamp write at line 385 is inside the block |
| `fetcharr/search/engine.py` | `fetcharr/db.py:insert_search_entry` | sanitized detail parameter | WIRED | All 4 failure call sites use `detail=_sanitize_exc(exc)`: lines 263, 297, 414, 452 |

### Requirements Coverage

All 8 requirement IDs from both plans are traced and accounted for. No orphaned requirements for Phase 20.1 exist in REQUIREMENTS.md.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DRSEC-01 | 20.1-01 | row_factory mutation on shared DB connection uses try/finally | SATISFIED | try/finally at all 3 row_factory sites in db.py |
| DRSEC-02 | 20.1-02 | Template URL attributes use urlencode filter to prevent reflected XSS | SATISFIED | All 4 hx-get dynamic sites have `| urlencode` in history_results.html |
| DRSEC-03 | 20.1-02 | Rate limiter re-checks timestamp inside search_lock | SATISFIED | Double-checked locking in routes.py search_now; timestamp write inside lock |
| DRSEC-04 | 20.1-01 | Migration backup guarded with path.exists() | SATISFIED | `if db_path.exists():` at line 61 of db.py |
| DRSEC-05 | 20.1-01 | Migration v1 DEFAULT changed from NULL to 'searched' | SATISFIED | `("outcome", "'searched'")` at line 79 of db.py |
| DRSEC-06 | 20.1-01 | contextlib.suppress(Exception) narrowed to sqlite3.OperationalError | SATISFIED | Both migrate functions use `contextlib.suppress(sqlite3.OperationalError)` |
| DRSEC-07 | 20.1-02 | Exception details in search history sanitized | SATISFIED | `_sanitize_exc` helper replaces all 4 `str(exc)[:200]` occurrences |
| DRSEC-08 | 20.1-02 | sourceTitle in tracking detail field truncated to 200 chars | SATISFIED | `[:200]` on sourceTitle in tracking.py line 168 |

**Requirement traceability in REQUIREMENTS.md:** All 8 DRSEC requirements are marked `[x]` and mapped to Phase 20.1 in the traceability table. No orphaned requirements.

### Anti-Patterns Found

None. Scan of all modified files found:

- No `TODO`, `FIXME`, `XXX`, `HACK`, or `PLACEHOLDER` comments in modified files
- No `return null`, `return {}`, or stub implementations
- No `str(exc)[:200]` remaining in engine.py (all replaced with `_sanitize_exc`)
- No `contextlib.suppress(Exception)` remaining in db.py (both narrowed)
- No unguarded `shutil.copy2` remaining (guarded by `if db_path.exists()`)
- No `row_factory = aiosqlite.Row` without an enclosing `try/finally` block

### Human Verification Required

None. All phase deliverables are programmatically verifiable:

- XSS fix is a static template change (grep-verifiable)
- Race condition fix is a code structure change (grep-verifiable)
- Exception sanitization is a pure function (unit-tested)
- truncation is a slice operation (grep + test-verifiable)
- All 86 tests pass including the 7 new tests added by this phase

### Test Run Results

```
86 passed in 1.01s
```

Breakdown of new tests added this phase:
- `test_row_factory_restored_after_successful_calls` — PASS
- `test_row_factory_restored_on_exception` — PASS
- `test_run_migrations_fresh_install` — PASS
- `test_migration_suppresses_only_operational_error` — PASS
- `test_search_now_rate_limit_concurrent_protection` — PASS
- `test_sanitize_exc_http_status_error` — PASS
- `test_sanitize_exc_timeout` — PASS
- `test_sanitize_exc_generic` — PASS

Ruff check: zero violations across all modified files.

### Gaps Summary

No gaps. All 8 observable truths verified. All 6 artifacts confirmed substantive and wired. All 6 key links confirmed. All 8 requirements satisfied. Zero anti-patterns found. 86/86 tests pass.

---

_Verified: 2026-02-25T01:10:00Z_
_Verifier: Claude (gsd-verifier)_
