---
phase: 47-test-hardening-state-search-edge-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_config.py
  - tests/test_state.py
  - tests/test_db.py
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04

must_haves:
  truths:
    - "Broken TOML config (syntax error, missing field, wrong type) raises a clean exception without corrupting any files"
    - "Corrupt SQLite file (random bytes, locked DB) raises an identifiable error without silent data loss"
    - "Invalid JSON state files (truncated, wrong structure, empty) recover gracefully to default state"
    - "Config migration handles partial v2.2 configs and unknown fields without crashing"
  artifacts:
    - path: "tests/test_config.py"
      provides: "TOML error and migration edge-case tests"
      contains: "test_toml_syntax_error_raises_decode_error"
    - path: "tests/test_state.py"
      provides: "JSON state corruption recovery tests"
      contains: "test_state_truncated_json_recovers"
    - path: "tests/test_db.py"
      provides: "SQLite corruption tests"
      contains: "test_init_db_corrupt_file"
  key_links:
    - from: "tests/test_config.py"
      to: "triggarr/config.py"
      via: "load_settings() and _migrate_v22_to_v23()"
      pattern: "load_settings|_migrate_v22_to_v23|_is_v22_format"
    - from: "tests/test_state.py"
      to: "triggarr/state.py"
      via: "load_state()"
      pattern: "load_state"
    - from: "tests/test_db.py"
      to: "triggarr/db.py"
      via: "init_db()"
      pattern: "init_db"
---

<objective>
Add unhappy-path tests for corrupt/invalid persistent state: broken TOML config files, corrupt SQLite databases, invalid JSON state files, and config migration edge cases.

Purpose: Verify Triggarr handles corrupt persistent state without crashing or losing data -- completing STATE-01 through STATE-04 requirements.
Output: New tests in test_config.py, test_state.py, and test_db.py -- all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-test-hardening-state-search-edge-cases/47-RESEARCH.md

@triggarr/config.py
@triggarr/state.py
@triggarr/db.py
@tests/test_config.py
@tests/test_state.py
@tests/test_db.py

<interfaces>
From triggarr/config.py:
```python
def load_settings(config_path: Path) -> Settings:
    """Load and return Settings from a TOML config file."""
    with open(config_path, "rb") as f:
        data = tomllib.load(f)
    return Settings(**data)
    # NOTE: No try/except -- TOMLDecodeError and ValidationError propagate to caller

def _is_v22_format(data: dict) -> bool:
    """Check if parsed TOML data uses the v2.2 flat config format."""

def _migrate_v22_to_v23(data: dict) -> dict:
    """Transform v2.2 flat config data to v2.3 multi-instance format."""

def detect_and_migrate_v22(config_path: Path) -> bool:
    """Detect and migrate v2.2 config format to v2.3."""
```

From triggarr/state.py:
```python
def load_state(state_path: Path = STATE_PATH) -> TriggarrState:
    """Load state from JSON. Catches json.JSONDecodeError and OSError, returns defaults."""

class TriggarrState(TypedDict, total=False):
    radarr: dict[str, AppState]
    sonarr: dict[str, AppState]
    lidarr: dict[str, AppState]
    search_log: list
```

From triggarr/db.py:
```python
async def init_db(db: aiosqlite.Connection, db_path: Path) -> None:
    """Create base tables, run migrations."""

async def get_schema_version(db: aiosqlite.Connection) -> int:
    """Read current schema version from schema_version table."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broken TOML config and config migration edge-case tests (STATE-01, STATE-03, STATE-04)</name>
  <files>tests/test_config.py, tests/test_state.py</files>
  <read_first>
    - tests/test_config.py (current tests, imports, TOML fixtures)
    - tests/test_state.py (existing corrupt recovery test at line 247)
    - triggarr/config.py (load_settings, _is_v22_format, _migrate_v22_to_v23 signatures)
    - triggarr/state.py (load_state error handling at line 169-174)
  </read_first>
  <action>
**STATE-01 -- Add to tests/test_config.py (after the existing migration tests, before the class-based test blocks):**

Add 3 tests for broken TOML config:

1. `test_toml_syntax_error_raises_decode_error` -- Write `"[general\nlog_level = 'info'"` (missing closing bracket) to a tmp file, call `load_settings(path)`, assert `pytest.raises(tomllib.TOMLDecodeError)`.

2. `test_toml_missing_required_api_key_raises_validation_error` -- Write valid TOML with a radarr instance that has `url = "http://radarr:7878"` and `enabled = true` but NO `api_key` field. Call `load_settings(path)`, assert `pytest.raises(pydantic.ValidationError)`. Import `pydantic` at top of file.

3. `test_toml_wrong_type_raises_validation_error` -- Write valid TOML with `[radarr."Default"]\nurl = "http://radarr:7878"\napi_key = "key"\nenabled = "yes"` (string instead of bool). Call `load_settings(path)`, assert `pytest.raises(pydantic.ValidationError)`.

**STATE-04 -- Add to tests/test_config.py (after the existing migration tests):**

Add 4 tests for config migration edge cases:

4. `test_migrate_v22_partial_radarr_only` -- Call `_migrate_v22_to_v23({"general": {"log_level": "info"}, "radarr": {"url": "http://r:7878", "api_key": "k", "enabled": True}})` (no sonarr key at all). Assert result has `radarr.Default` with the values and `sonarr` is `{}`.

5. `test_migrate_v22_unknown_extra_fields_preserved` -- Call `_migrate_v22_to_v23({"general": {"log_level": "info", "unknown_field": 42}, "radarr": {"url": "http://r:7878", "api_key": "k", "enabled": True, "custom": "val"}})`. Assert `general.unknown_field == 42` and `radarr.Default.custom == "val"` -- migration preserves unknown fields by wrapping, not filtering.

6. `test_is_v22_format_missing_general` -- Call `_is_v22_format({"radarr": {"url": "http://r:7878"}})`. Assert returns True (general section not required for format detection).

7. `test_migrate_v22_mixed_nested_and_flat_only_detects_flat` -- Call `_is_v22_format({"radarr": {"Default": {"url": "http://r:7878", "api_key": "k"}}, "sonarr": {"url": "http://s:8989"}})`. This has radarr in v2.3 format but sonarr in v2.2 format. Assert returns True (sonarr has flat keys).

**STATE-03 -- Add to tests/test_state.py (after the existing `test_state_corrupt_recovers_to_defaults` at line 247):**

Add 4 tests for invalid JSON state:

8. `test_state_truncated_json_recovers` -- Write `'{"radarr":'` to state file, call `load_state(path)`, assert returns default state (radarr={}, sonarr={}, search_log=[]).

9. `test_state_wrong_structure_list_recovers` -- Write `'[1, 2, 3]'` to state file, call `load_state(path)`. Since json.load succeeds (it's valid JSON), this goes through _merge_defaults. If it crashes, assert the crash type. If it recovers, assert defaults. Read _merge_defaults to determine behavior first.

10. `test_state_empty_file_recovers` -- Write `''` (empty string) to state file, call `load_state(path)`, assert returns default state (empty file triggers json.JSONDecodeError which is caught).

11. `test_state_wrong_nested_type_recovers` -- Write `'{"radarr": "not_a_dict", "sonarr": {}}'` to state file, call `load_state(path)`. This is valid JSON so load succeeds. Read _merge_defaults to verify it handles non-dict radarr gracefully. If it errors, document the behavior.

IMPORTANT: For tests 9 and 11, read `triggarr/state.py` `_merge_defaults()` function before writing the test to understand what happens when the structure is wrong but JSON is valid. If `_merge_defaults` crashes on wrong types, the test should verify the exception type. If it handles it, verify the output.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_config.py tests/test_state.py -x -q -k "syntax_error or missing_required_api_key or wrong_type or partial_radarr_only or unknown_extra_fields or missing_general or mixed_nested or truncated or wrong_structure or empty_file or wrong_nested"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_toml_syntax_error_raises_decode_error" tests/test_config.py returns 1
    - grep -c "def test_toml_missing_required_api_key_raises_validation_error" tests/test_config.py returns 1
    - grep -c "def test_toml_wrong_type_raises_validation_error" tests/test_config.py returns 1
    - grep -c "def test_migrate_v22_partial_radarr_only" tests/test_config.py returns 1
    - grep -c "def test_migrate_v22_unknown_extra_fields_preserved" tests/test_config.py returns 1
    - grep -c "def test_is_v22_format_missing_general" tests/test_config.py returns 1
    - grep -c "def test_migrate_v22_mixed_nested_and_flat_only_detects_flat" tests/test_config.py returns 1
    - grep -c "def test_state_truncated_json_recovers" tests/test_state.py returns 1
    - grep -c "def test_state_empty_file_recovers" tests/test_state.py returns 1
    - uv run pytest tests/test_config.py tests/test_state.py -x -q exits 0
    - uv run ruff check tests/test_config.py tests/test_state.py exits 0
  </acceptance_criteria>
  <done>All 11 new tests pass. Broken TOML raises clean TOMLDecodeError or ValidationError. Invalid JSON state recovers to defaults. Config migration handles partial and unexpected input.</done>
</task>

<task type="auto">
  <name>Task 2: Corrupt SQLite database tests (STATE-02)</name>
  <files>tests/test_db.py</files>
  <read_first>
    - tests/test_db.py (existing DB tests, imports, fixtures)
    - triggarr/db.py (init_db, get_schema_version, run_migrations signatures)
  </read_first>
  <action>
Add 3 tests for corrupt SQLite to tests/test_db.py (at the end of the file, in a new section marked with a comment `# --- Corrupt/invalid database tests ---`):

1. `test_init_db_corrupt_file_raises_database_error` -- Write 256 random bytes to a `.db` file in tmp_path. Open with `aiosqlite.connect(path)`. Call `init_db(db, path)`. Assert `pytest.raises(aiosqlite.DatabaseError)` or `sqlite3.DatabaseError` (aiosqlite wraps sqlite3 errors). The error should occur on the first CREATE TABLE IF NOT EXISTS statement. Import `sqlite3` at top if not already imported.

    NOTE: `aiosqlite.connect()` itself does NOT validate the file -- the error occurs on the first SQL execution. So the connect succeeds but `init_db` raises.

2. `test_init_db_locked_database` -- Create a valid DB by running init_db normally. Then simulate a lock: open a second connection, start a transaction with `BEGIN EXCLUSIVE`, and while that transaction is held, try to run init_db on a third connection with `PRAGMA busy_timeout = 0` (zero timeout so it fails immediately instead of waiting). Assert the appropriate `sqlite3.OperationalError` with "database is locked" message. Clean up all connections.

    IMPORTANT: Use `db.execute("PRAGMA busy_timeout = 0")` on the third connection before calling init_db. This ensures the test is not flaky -- it will fail immediately rather than waiting.

3. `test_get_schema_version_on_empty_db` -- Create a fresh aiosqlite connection to a new file (no init_db called). Call `get_schema_version(db)`. Since the function does `CREATE TABLE IF NOT EXISTS schema_version`, it should succeed and return 0 (no row exists, but the function creates the table and inserts a default). Read the actual implementation of `get_schema_version` to verify the return value -- it may return 0 from the `fetchone()` or need special handling for None row.

All tests must be async (the file uses pytest-asyncio auto mode).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_db.py -x -q -k "corrupt_file or locked_database or schema_version_on_empty"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_init_db_corrupt_file_raises_database_error" tests/test_db.py returns 1
    - grep -c "def test_init_db_locked_database" tests/test_db.py returns 1
    - grep -c "def test_get_schema_version_on_empty_db" tests/test_db.py returns 1
    - grep "PRAGMA busy_timeout = 0" tests/test_db.py returns a match
    - uv run pytest tests/test_db.py -x -q exits 0
    - uv run ruff check tests/test_db.py exits 0
  </acceptance_criteria>
  <done>All 3 SQLite corruption tests pass. Corrupt file raises DatabaseError. Locked DB raises OperationalError. Empty DB schema version returns cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> App | Config files (TOML, JSON, SQLite) read from disk may be corrupt or malicious |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | triggarr.toml | accept | Test-only phase: verifies existing behavior (exception propagation). No production code changes. |
| T-47-02 | Tampering | state.json | accept | Existing recovery logic (json.JSONDecodeError catch -> defaults) already handles this. Tests verify it. |
| T-47-03 | Denial of Service | SQLite DB | accept | Corrupt DB prevents startup -- acceptable for a single-user daemon. Tests document the behavior. |
</threat_model>

<verification>
```bash
# All existing + new tests pass
cd /Users/julianamacbook/triggarr && uv run pytest tests/ -x -q

# Lint clean
uv run ruff check triggarr/ tests/
```
</verification>

<success_criteria>
- 14 new tests added across test_config.py, test_state.py, test_db.py
- All tests pass (including existing 466+ tests)
- No production code changes
- Ruff lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/47-test-hardening-state-search-edge-cases/47-01-SUMMARY.md`
</output>
