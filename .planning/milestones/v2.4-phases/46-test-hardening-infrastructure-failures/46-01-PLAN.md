---
phase: 46-test-hardening-infrastructure-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_clients.py
  - tests/test_search.py
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04

must_haves:
  truths:
    - "Tests prove DNS failure returns False from validate_connection and aborts cycle gracefully"
    - "Tests prove SSL error returns False from validate_connection and aborts cycle gracefully"
    - "Tests prove TimeoutException during cycle fetch aborts gracefully with connected=False"
    - "Tests prove unreachable_since is set on first failure and preserved on subsequent failures"
    - "Tests prove all search commands failing mid-cycle still completes the cycle without crash"
  artifacts:
    - path: "tests/test_clients.py"
      provides: "DNS failure, SSL error, and logging tests for validate_connection"
      contains: "test_validate_connection_dns_failure"
    - path: "tests/test_search.py"
      provides: "DNS/SSL/timeout cycle abort tests, mid-cycle all-fail tests"
      contains: "test_run_radarr_cycle_dns_failure"
  key_links:
    - from: "tests/test_clients.py"
      to: "triggarr/clients/base.py"
      via: "httpx.ConnectError with DNS/SSL messages"
      pattern: "ConnectError.*Name or service not known|SSL"
    - from: "tests/test_search.py"
      to: "triggarr/search/engine.py"
      via: "AsyncMock side_effect for cycle-level failures"
      pattern: "side_effect=httpx\\."
---

<objective>
Add connection failure tests covering DNS resolution failures, SSL/TLS errors, timeout during cycle fetch, unreachable_since state tracking, and mid-cycle total failure scenarios.

Purpose: Fill the gaps identified in CONN-01 through CONN-04 where existing tests cover basic ConnectError but not DNS-specific, SSL-specific, timeout-during-cycle, or all-items-fail scenarios.
Output: New tests appended to test_clients.py and test_search.py, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-test-hardening-infrastructure-failures/46-RESEARCH.md

@triggarr/clients/base.py
@triggarr/search/engine.py
@tests/test_clients.py
@tests/test_search.py
@tests/conftest.py

<interfaces>
<!-- Key error handling signatures the executor needs -->

From triggarr/clients/base.py:
```python
# validate_connection catches these individually:
except httpx.HTTPStatusError as exc:  # 401 -> error log, others -> warning log
except httpx.ConnectError:            # "Connection refused at configured URL"
except httpx.TimeoutException:        # "Connection timed out (30s)"
except pydantic.ValidationError:      # "Unexpected API response format"
```

From triggarr/search/engine.py (run_radarr_cycle, run_sonarr_cycle, run_lidarr_cycle):
```python
# Fetch-level catch (aborts cycle):
except (httpx.HTTPError, pydantic.ValidationError) as exc:
    ist["connected"] = False
    if not ist.get("unreachable_since"):
        ist["unreachable_since"] = datetime.now(UTC).isoformat().replace("+00:00", "Z")

# Per-item search catch (skip and continue):
except (httpx.HTTPError, pydantic.ValidationError):
    # logs warning, increments skip counter, continues to next item
```

From tests/test_search.py helpers:
```python
def _cycle_settings(missing_count=2, cutoff_count=2) -> Settings
def _cycle_instance_config(missing_count=2, cutoff_count=2) -> InstanceConfig
def _make_test_state() -> TriggarrState
```

From tests/test_clients.py:
```python
class _ConcreteClient(ArrClient):  # minimal concrete subclass for testing base class
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-level connection failure gap tests</name>
  <files>tests/test_clients.py</files>
  <read_first>
    - tests/test_clients.py (current state, existing patterns, _ConcreteClient helper)
    - triggarr/clients/base.py (validate_connection error handling signatures)
  </read_first>
  <action>
Append the following tests to `tests/test_clients.py`, after the existing `test_validate_connection_timeout` test:

**CONN-02: DNS failure at client level**
- `test_validate_connection_dns_failure`: MockTransport handler raises `httpx.ConnectError("[Errno -2] Name or service not known")`. Assert `validate_connection()` returns `False`. Follow existing try/finally pattern with `await client.close()`.

**CONN-03: SSL error at client level**
- `test_validate_connection_ssl_error`: MockTransport handler raises `httpx.ConnectError("[SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed")`. Assert `validate_connection()` returns `False`. Follow existing try/finally pattern.

**CONN-01 gap: Logging verification on connection failure**
- `test_validate_connection_connect_error_logs_warning`: Use `io.StringIO` + `logger.add(sink, format="{message}", level="WARNING")` pattern (from test_startup.py). Trigger ConnectError via MockTransport. Assert `"Connection refused" in sink.getvalue()` and `client._app_name in sink.getvalue()`. Add `import io` and `from loguru import logger` to imports if not already present.

All three tests follow the exact pattern of existing `test_validate_connection_connect_error` (line ~282 of test_clients.py): create `_ConcreteClient`, set `_app_name = "Test"`, replace `_client` with `httpx.AsyncClient(transport=transport, base_url="http://test")`, try/finally with close.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_clients.py -x -q -k "dns_failure or ssl_error or connect_error_logs"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_clients.py contains `async def test_validate_connection_dns_failure`
    - tests/test_clients.py contains `async def test_validate_connection_ssl_error`
    - tests/test_clients.py contains `async def test_validate_connection_connect_error_logs_warning`
    - test_validate_connection_dns_failure asserts `result is False`
    - test_validate_connection_ssl_error asserts `result is False`
    - test_validate_connection_connect_error_logs_warning asserts `"Connection refused" in sink.getvalue()`
    - `uv run pytest tests/test_clients.py -x -q -k "dns_failure or ssl_error or connect_error_logs"` exits 0 with 3 passed
    - `uv run ruff check tests/test_clients.py` exits 0
  </acceptance_criteria>
  <done>Three new client-level tests pass: DNS failure returns False, SSL error returns False, ConnectError logs warning with app name</done>
</task>

<task type="auto">
  <name>Task 2: Cycle-level connection failure gap tests</name>
  <files>tests/test_search.py</files>
  <read_first>
    - tests/test_search.py (current state, existing cycle test patterns, _make_test_state/_cycle_settings/_cycle_instance_config helpers)
    - triggarr/search/engine.py (run_radarr_cycle, run_sonarr_cycle error handling including unreachable_since logic at lines ~310-321)
  </read_first>
  <action>
Append the following tests to `tests/test_search.py`, after the existing cycle network failure tests.

**CONN-02 cycle-level: DNS failure during fetch**
- `test_run_radarr_cycle_dns_failure`: `client.get_wanted_missing = AsyncMock(side_effect=httpx.ConnectError("[Errno -2] Name or service not known"))`. Assert `result["radarr"]["Default"]["connected"] is False`. Follow existing tmp_path + aiosqlite + init_db pattern.

**CONN-03 cycle-level: SSL error during fetch**
- `test_run_radarr_cycle_ssl_error`: `client.get_wanted_missing = AsyncMock(side_effect=httpx.ConnectError("[SSL: CERTIFICATE_VERIFY_FAILED]"))`. Assert `result["radarr"]["Default"]["connected"] is False`.

**CONN-01 gap: Timeout during cycle fetch**
- `test_run_radarr_cycle_timeout_aborts`: `client.get_wanted_missing = AsyncMock(side_effect=httpx.TimeoutException("timed out"))`. Assert `result["radarr"]["Default"]["connected"] is False`.

**CONN-01 gap: unreachable_since tracking**
- `test_run_radarr_cycle_sets_unreachable_since`: ConnectError on fetch. Assert `result["radarr"]["Default"]["unreachable_since"]` is not None (a string).
- `test_run_radarr_cycle_preserves_unreachable_since`: Pre-set `state["radarr"]["Default"]["unreachable_since"] = "2026-01-01T00:00:00Z"`. Trigger ConnectError. Assert `result["radarr"]["Default"]["unreachable_since"] == "2026-01-01T00:00:00Z"` (preserved, not overwritten).

**CONN-04 gap: All search commands fail mid-cycle**
- `test_run_radarr_cycle_all_searches_fail`: `client.get_wanted_missing = AsyncMock(return_value=[{"id": 1, "monitored": True, "title": "M1"}, {"id": 2, "monitored": True, "title": "M2"}])`, `client.get_wanted_cutoff = AsyncMock(return_value=[])`, `client.get_library_count = AsyncMock(return_value=100)`, `client.get_tags = AsyncMock(return_value=[])`, `client.search_movies = AsyncMock(side_effect=httpx.ConnectError("instance down"))`, `client.get_grab_history = AsyncMock(return_value=[])`. Assert cycle completes (does not raise), `result["radarr"]["Default"]["connected"] is True` (fetch succeeded), and cursor position advances (the items were attempted even though all failed).

All tests use `tmp_path` for DB, `await aiosqlite.connect(db_path)`, `await init_db(db, db_path)`, `_make_test_state()`, `_cycle_settings()`, `_cycle_instance_config()`, and close DB in finally block.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_search.py -x -q -k "dns_failure or ssl_error or timeout_aborts or unreachable_since or all_searches_fail"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_search.py contains `async def test_run_radarr_cycle_dns_failure`
    - tests/test_search.py contains `async def test_run_radarr_cycle_ssl_error`
    - tests/test_search.py contains `async def test_run_radarr_cycle_timeout_aborts`
    - tests/test_search.py contains `async def test_run_radarr_cycle_sets_unreachable_since`
    - tests/test_search.py contains `async def test_run_radarr_cycle_preserves_unreachable_since`
    - tests/test_search.py contains `async def test_run_radarr_cycle_all_searches_fail`
    - `uv run pytest tests/test_search.py -x -q -k "dns_failure or ssl_error or timeout_aborts or unreachable_since or all_searches_fail"` exits 0 with 6 passed
    - `uv run ruff check tests/test_search.py` exits 0
    - `uv run pytest tests/ -x -q` full suite still passes
  </acceptance_criteria>
  <done>Six new cycle-level tests pass: DNS failure aborts, SSL error aborts, timeout aborts, unreachable_since set on first failure, unreachable_since preserved on repeat failure, all-searches-fail completes without crash</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | This plan adds only test code -- no production code changes, no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | tests/* | accept | Test files contain no secrets, no production impact. Mock API keys are dummy values ("test-key") |
| T-46-02 | Information Disclosure | test output | accept | Tests use mock transports only, never contact real instances |
</threat_model>

<verification>
1. `uv run pytest tests/test_clients.py -x -q -k "dns_failure or ssl_error or connect_error_logs"` -- 3 new client-level tests pass
2. `uv run pytest tests/test_search.py -x -q -k "dns_failure or ssl_error or timeout_aborts or unreachable_since or all_searches_fail"` -- 6 new cycle-level tests pass
3. `uv run pytest tests/ -x -q` -- full suite green (no regressions)
4. `uv run ruff check tests/test_clients.py tests/test_search.py` -- no lint violations
</verification>

<success_criteria>
- 9 new tests pass covering CONN-01 gaps (timeout cycle + unreachable_since + logging), CONN-02 (DNS), CONN-03 (SSL), CONN-04 (all-fail mid-cycle)
- Full test suite remains green
- No lint violations
</success_criteria>

<output>
After completion, create `.planning/phases/46-test-hardening-infrastructure-failures/46-01-SUMMARY.md`
</output>
