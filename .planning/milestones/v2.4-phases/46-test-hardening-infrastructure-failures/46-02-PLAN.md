---
phase: 46-test-hardening-infrastructure-failures
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - tests/test_clients.py
  - tests/test_search.py
  - tests/test_startup.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "Tests prove malformed JSON (non-parseable) at client and cycle level is handled without crash"
    - "Tests prove 403 Forbidden and 502 Bad Gateway return False from validate_connection"
    - "Tests prove 403/502 during cycle fetch aborts cycle gracefully"
    - "Tests prove Sonarr version detection handles future versions, empty strings, missing keys, beta suffixes, and ConnectError"
    - "Tests prove truncated pagination (server lies about totalRecords) terminates without infinite loop"
  artifacts:
    - path: "tests/test_clients.py"
      provides: "Malformed JSON, 403/502 validate_connection, truncated pagination tests"
      contains: "test_validate_connection_403"
    - path: "tests/test_search.py"
      provides: "Malformed JSON and 403/502 during cycle tests"
      contains: "test_run_radarr_cycle_403_aborts"
    - path: "tests/test_startup.py"
      provides: "Sonarr version detection edge case tests"
      contains: "test_detect_api_version_future_major"
  key_links:
    - from: "tests/test_clients.py"
      to: "triggarr/clients/base.py"
      via: "HTTPStatusError and DecodingError simulation"
      pattern: "HTTPStatusError|DecodingError"
    - from: "tests/test_startup.py"
      to: "triggarr/clients/sonarr.py"
      via: "detect_api_version edge cases"
      pattern: "detect_api_version"
---

<objective>
Add bad API response tests covering malformed JSON handling, unexpected HTTP status codes (403, 502), Sonarr v3/v4 version detection edge cases, and truncated paginated responses.

Purpose: Fill the gaps identified in API-01 through API-04 where existing tests cover basic cases but not invalid JSON, 403/502 status codes, version string edge cases, or server-lies-about-totalRecords scenarios.
Output: New tests appended to test_clients.py, test_search.py, and test_startup.py, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-test-hardening-infrastructure-failures/46-RESEARCH.md
@.planning/phases/46-test-hardening-infrastructure-failures/46-01-SUMMARY.md

@triggarr/clients/base.py
@triggarr/clients/sonarr.py
@triggarr/search/engine.py
@tests/test_clients.py
@tests/test_search.py
@tests/test_startup.py
@tests/conftest.py

<interfaces>
<!-- Key error handling signatures the executor needs -->

From triggarr/clients/base.py validate_connection:
```python
except httpx.HTTPStatusError as exc:
    if exc.response.status_code == 401:
        logger.error("{app}: API key is invalid (401 Unauthorized)")
    else:
        logger.warning("{app}: Unexpected HTTP error: {exc}")
    return False
```

From triggarr/clients/base.py get_paginated (pagination termination):
```python
# Lock totalRecords from first page
if total_records is None:
    total_records = data.totalRecords
# Terminate when records empty or all collected
if len(data.records) == 0 or page * effective_page_size >= total_records:
    break
```

From triggarr/clients/sonarr.py detect_api_version:
```python
data = response.json()
version = data["version"]
if version.startswith("4"):
    return "v4"
return "v3"
# catches: (httpx.HTTPError, pydantic.ValidationError, KeyError) -> returns "v3"
```

From triggarr/search/engine.py cycle fetch error handling:
```python
except (httpx.HTTPError, pydantic.ValidationError) as exc:
    ist["connected"] = False
    # ... sets unreachable_since, returns state
```

HTTPStatusError constructor: `httpx.HTTPStatusError(message, request=request, response=response)`
where request = `httpx.Request("GET", url)` and response = `httpx.Response(status_code, request=request)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-level bad API response tests</name>
  <files>tests/test_clients.py</files>
  <read_first>
    - tests/test_clients.py (current state after Plan 01, existing patterns)
    - triggarr/clients/base.py (validate_connection lines 208-256, get_paginated lines 148-202, _request_with_retry lines 42-77)
  </read_first>
  <action>
Append the following tests to `tests/test_clients.py`:

**API-02: 403 Forbidden at validate_connection**
- `test_validate_connection_403`: MockTransport returns `httpx.Response(403)`. Assert `validate_connection()` returns `False`. Note: validate_connection calls `response.raise_for_status()` which raises HTTPStatusError for 403. The except branch logs "Unexpected HTTP error" (not the 401 branch).

**API-02: 502 Bad Gateway at validate_connection**
- `test_validate_connection_502`: MockTransport returns `httpx.Response(502)`. Assert returns `False`.

**API-01: Invalid JSON (not parseable) from paginated endpoint**
- `test_get_paginated_invalid_json`: MockTransport returns `httpx.Response(200, content=b"not json at all", headers={"content-type": "application/json"})`. Call `client.get_paginated("/items")`. This will fail when `response.json()` is called. The `PaginatedResponse.model_validate(response.json())` call should raise an error. Assert that an exception is raised (either `json.JSONDecodeError` or `httpx.DecodingError` -- use `pytest.raises(Exception)` broadly, then narrow based on what actually fires). The test proves the exception propagates to the caller (cycle-level handler).

**API-04: Truncated pagination (totalRecords=10, only 3 returned)**
- `test_get_paginated_truncated_response`: MockTransport returns `{"page": 1, "pageSize": 50, "sortKey": "id", "totalRecords": 10, "records": [{"id": 1}, {"id": 2}, {"id": 3}]}`. Since `1*50 >= 10` is True, pagination terminates after page 1. Assert `len(result) == 3` (returns what was actually received, not what totalRecords promised).

**API-04: Mid-pagination empty page**
- `test_get_paginated_mid_pagination_empty_page`: First request returns `{"page": 1, "pageSize": 2, "sortKey": "id", "totalRecords": 6, "records": [{"id": 1}, {"id": 2}]}`. Second request returns `{"page": 2, "pageSize": 2, "sortKey": "id", "totalRecords": 6, "records": []}`. Pagination terminates because `len(data.records) == 0`. Assert `len(result) == 2`.

**API-04: Missing records key in paginated response**
- `test_get_paginated_missing_records_key`: MockTransport returns `{"page": 1, "pageSize": 50, "sortKey": "id", "totalRecords": 5}` (no "records" key). This should raise `pydantic.ValidationError` from `PaginatedResponse.model_validate()`. Assert `pytest.raises(pydantic.ValidationError)`.

All tests follow existing _ConcreteClient + MockTransport + try/finally pattern.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_clients.py -x -q -k "403 or 502 or invalid_json or truncated or empty_page or missing_records"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_clients.py contains `async def test_validate_connection_403`
    - tests/test_clients.py contains `async def test_validate_connection_502`
    - tests/test_clients.py contains `async def test_get_paginated_invalid_json`
    - tests/test_clients.py contains `async def test_get_paginated_truncated_response`
    - tests/test_clients.py contains `async def test_get_paginated_mid_pagination_empty_page`
    - tests/test_clients.py contains `async def test_get_paginated_missing_records_key`
    - `uv run pytest tests/test_clients.py -x -q -k "403 or 502 or invalid_json or truncated or empty_page or missing_records"` exits 0 with 6 passed
    - `uv run ruff check tests/test_clients.py` exits 0
  </acceptance_criteria>
  <done>Six new client-level tests pass: 403 returns False, 502 returns False, invalid JSON raises, truncated pagination returns 3 items, mid-pagination empty terminates, missing records key raises ValidationError</done>
</task>

<task type="auto">
  <name>Task 2: Cycle-level bad API response tests</name>
  <files>tests/test_search.py</files>
  <read_first>
    - tests/test_search.py (current state after Plan 01, existing cycle patterns)
    - triggarr/search/engine.py (run_radarr_cycle lines 275-375, per-item search error handling)
  </read_first>
  <action>
Append the following tests to `tests/test_search.py`:

**API-02 cycle-level: 403 during fetch**
- `test_run_radarr_cycle_403_aborts`: Create `request = httpx.Request("GET", "http://test/api/v3/wanted/missing")` and `response = httpx.Response(403, request=request)`. Set `client.get_wanted_missing = AsyncMock(side_effect=httpx.HTTPStatusError("Forbidden", request=request, response=response))`. Assert `result["radarr"]["Default"]["connected"] is False`.

**API-02 cycle-level: 502 during fetch**
- `test_run_radarr_cycle_502_aborts`: Same pattern with 502 status. Assert `result["radarr"]["Default"]["connected"] is False`.

**API-02 cycle-level: 403 during per-item search**
- `test_run_radarr_cycle_403_per_item_skip`: `client.get_wanted_missing = AsyncMock(return_value=[{"id": 1, "monitored": True, "title": "M1"}, {"id": 2, "monitored": True, "title": "M2"}])`, `client.get_wanted_cutoff = AsyncMock(return_value=[])`, `client.get_library_count = AsyncMock(return_value=100)`, `client.get_tags = AsyncMock(return_value=[])`. Create request/response for 403. Set `client.search_movies = AsyncMock(side_effect=httpx.HTTPStatusError("Forbidden", request=httpx.Request("POST", "http://test"), response=httpx.Response(403, request=httpx.Request("POST", "http://test"))))`, `client.get_grab_history = AsyncMock(return_value=[])`. Assert cycle completes without raising and `result["radarr"]["Default"]["connected"] is True` (fetch succeeded, only search failed).

**API-01 cycle-level: Malformed JSON from fetch**
- `test_run_radarr_cycle_malformed_json_aborts`: `client.get_wanted_missing = AsyncMock(side_effect=pydantic.ValidationError.from_exception_data("PaginatedResponse", []))`. Assert `result["radarr"]["Default"]["connected"] is False`. (pydantic.ValidationError is caught by the cycle-level handler.)

All tests use `tmp_path`, `aiosqlite.connect`, `init_db`, `_make_test_state()`, `_cycle_settings()`, `_cycle_instance_config()`. Add `import pydantic` to imports if not present.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_search.py -x -q -k "403_aborts or 502_aborts or 403_per_item or malformed_json_aborts"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_search.py contains `async def test_run_radarr_cycle_403_aborts`
    - tests/test_search.py contains `async def test_run_radarr_cycle_502_aborts`
    - tests/test_search.py contains `async def test_run_radarr_cycle_403_per_item_skip`
    - tests/test_search.py contains `async def test_run_radarr_cycle_malformed_json_aborts`
    - `uv run pytest tests/test_search.py -x -q -k "403_aborts or 502_aborts or 403_per_item or malformed_json_aborts"` exits 0 with 4 passed
    - `uv run ruff check tests/test_search.py` exits 0
  </acceptance_criteria>
  <done>Four new cycle-level tests pass: 403 aborts cycle, 502 aborts cycle, 403 per-item skip continues, malformed JSON aborts cycle</done>
</task>

<task type="auto">
  <name>Task 3: Sonarr version detection edge case tests</name>
  <files>tests/test_startup.py</files>
  <read_first>
    - tests/test_startup.py (current state, existing detect_api_version tests at lines ~155-260)
    - triggarr/clients/sonarr.py (detect_api_version at lines 28-51: catches httpx.HTTPError, pydantic.ValidationError, KeyError)
  </read_first>
  <action>
Append the following tests to `tests/test_startup.py`, after the existing `test_detect_api_version_*` tests:

**API-03: Future major version**
- `test_detect_api_version_future_major`: Mock `_client.get` to return response with JSON `{"version": "5.0.0.100"}`. Since `"5.0.0.100".startswith("4")` is False, assert `detect_api_version()` returns `"v3"`.

**API-03: Empty version string**
- `test_detect_api_version_empty_version`: Mock `_client.get` to return response with JSON `{"version": ""}`. Empty string doesn't startswith("4"), so assert returns `"v3"`.

**API-03: Missing version key**
- `test_detect_api_version_missing_version_key`: Mock `_client.get` to return response with JSON `{"appName": "Sonarr"}` (no "version" key). This triggers `KeyError` which is caught. Assert returns `"v3"`.

**API-03: Beta version format**
- `test_detect_api_version_beta_format`: Mock `_client.get` to return response with JSON `{"version": "4.0.0-beta1"}`. Since `"4.0.0-beta1".startswith("4")` is True, assert returns `"v4"`.

**API-03: ConnectError during version detection**
- `test_detect_api_version_connect_error_fallback`: Mock `_client.get` to raise `httpx.ConnectError("refused")`. Since `httpx.ConnectError` is a subclass of `httpx.HTTPError`, it IS caught. Assert returns `"v3"`.

Each test creates a `SonarrClient(base_url="http://test", api_key="key")`, replaces `client._client` with a mock that returns the appropriate response or raises, calls `await client.detect_api_version()`, and asserts the return value. Use try/finally with `await client.close()`.

For the mock: use `AsyncMock` for `client._client.get`. The return value mock needs `.raise_for_status()` as a no-op method and `.json()` returning the dict. Pattern:
```python
mock_response = AsyncMock()
mock_response.raise_for_status = lambda: None
mock_response.json = lambda: {"version": "5.0.0.100"}
client._client = AsyncMock()
client._client.get = AsyncMock(return_value=mock_response)
```
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_startup.py -x -q -k "future_major or empty_version or missing_version_key or beta_format or connect_error_fallback"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_startup.py contains `async def test_detect_api_version_future_major`
    - tests/test_startup.py contains `async def test_detect_api_version_empty_version`
    - tests/test_startup.py contains `async def test_detect_api_version_missing_version_key`
    - tests/test_startup.py contains `async def test_detect_api_version_beta_format`
    - tests/test_startup.py contains `async def test_detect_api_version_connect_error_fallback`
    - `uv run pytest tests/test_startup.py -x -q -k "future_major or empty_version or missing_version_key or beta_format or connect_error_fallback"` exits 0 with 5 passed
    - `uv run ruff check tests/test_startup.py` exits 0
    - `uv run pytest tests/ -x -q` full suite still passes
  </acceptance_criteria>
  <done>Five new Sonarr version detection tests pass: future major returns v3, empty version returns v3, missing key returns v3, beta format returns v4, ConnectError falls back to v3. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | This plan adds only test code -- no production code changes, no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Tampering | tests/* | accept | Test files contain no secrets, no production impact. Mock API keys are dummy values |
| T-46-04 | Information Disclosure | test output | accept | Tests use mocks only, never contact real instances |
</threat_model>

<verification>
1. `uv run pytest tests/test_clients.py -x -q -k "403 or 502 or invalid_json or truncated or empty_page or missing_records"` -- 6 new client tests pass
2. `uv run pytest tests/test_search.py -x -q -k "403_aborts or 502_aborts or 403_per_item or malformed_json_aborts"` -- 4 new cycle tests pass
3. `uv run pytest tests/test_startup.py -x -q -k "future_major or empty_version or missing_version_key or beta_format or connect_error_fallback"` -- 5 new version tests pass
4. `uv run pytest tests/ -x -q` -- full suite green
5. `uv run ruff check tests/test_clients.py tests/test_search.py tests/test_startup.py` -- no lint violations
</verification>

<success_criteria>
- 15 new tests pass covering API-01 (malformed JSON), API-02 (403/502), API-03 (version edge cases), API-04 (truncated pagination)
- Full test suite remains green
- No lint violations
</success_criteria>

<output>
After completion, create `.planning/phases/46-test-hardening-infrastructure-failures/46-02-SUMMARY.md`
</output>
