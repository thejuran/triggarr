---
phase: 24-hardening-config-validation-and-temp-file-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triggarr/models/config.py
  - triggarr/web/routes.py
  - tests/test_config_dir.py
  - tests/test_web.py
autonomous: true
requirements:
  - HARDEN-01
  - HARDEN-02
  - HARDEN-03
  - HARDEN-04

must_haves:
  truths:
    - "Setting TRIGGARR_CONFIG_DIR to a relative path like '../tmp' causes startup to fail with a clear error"
    - "Setting TRIGGARR_CONFIG_DIR to a traversal path like '/config/../../etc' causes startup to fail with a clear error"
    - "If os.replace fails during settings save, the temp file is cleaned up"
    - "Module-level constants (CONFIG_DIR, CONFIG_PATH, STATE_PATH) freeze constraint is documented"
    - "Tests verify frozen constant behavior"
  artifacts:
    - path: "triggarr/models/config.py"
      provides: "Path validation in get_config_dir()"
      contains: "resolve"
    - path: "triggarr/web/routes.py"
      provides: "Temp file cleanup on os.replace failure"
      contains: "os.unlink"
    - path: "tests/test_config_dir.py"
      provides: "Tests for path validation and frozen constants"
  key_links:
    - from: "triggarr/models/config.py"
      to: "triggarr/__main__.py"
      via: "get_config_dir() called at import time"
      pattern: "get_config_dir"
    - from: "triggarr/web/routes.py"
      to: "triggarr/state.py"
      via: "Same atomic write pattern with cleanup"
      pattern: "os\\.unlink"
---

<objective>
Harden config path validation and temp file cleanup based on deep code review findings.

Purpose: Prevent silent misconfiguration from relative/traversal paths in TRIGGARR_CONFIG_DIR, and prevent temp file leaks on os.replace failure in settings save.
Output: Validated config paths, safe temp file handling, documented freeze constraints, test coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@triggarr/models/config.py
@triggarr/web/routes.py
@triggarr/state.py
@tests/test_config_dir.py
@tests/test_web.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add path validation and fix temp file cleanup</name>
  <files>triggarr/models/config.py, triggarr/web/routes.py, tests/test_config_dir.py, tests/test_web.py</files>
  <behavior>
    - Test: get_config_dir() with relative path (e.g., "../tmp") raises ValueError
    - Test: get_config_dir() with traversal path (e.g., "/config/../../etc") that resolves outside expected dir raises ValueError
    - Test: get_config_dir() with absolute non-traversal path (e.g., "/data/triggarr") succeeds
    - Test: get_config_dir() with default (unset env var) returns Path("/config") as before
    - Test: settings save cleans up temp file when os.replace raises OSError
  </behavior>
  <action>
    1. In `triggarr/models/config.py`, update `get_config_dir()`:
       - Call `.resolve()` on the resulting Path
       - Check `path.is_absolute()` BEFORE resolve (raw value must be absolute)
       - If raw value is not absolute, raise `ValueError(f"TRIGGARR_CONFIG_DIR must be an absolute path, got: {raw}")`
       - After resolve, check that the resolved path equals `Path(raw).resolve()` (no traversal that escapes -- actually simpler: just require the raw string starts with `/` since resolve handles `..` components, and the absolute check catches relative paths)
       - Return the resolved path
       - This is a fail-fast: the app will crash at import time with a clear message if misconfigured

    2. In `triggarr/web/routes.py` around lines 310-314, wrap `os.replace` in try/except matching the pattern from `state.py:save_state()`:
       ```python
       try:
           os.replace(tmp.name, str(config_path))
       except OSError:
           with contextlib.suppress(OSError):
               os.unlink(tmp.name)
           raise
       ```
       Add `import contextlib` at top of file if not already present.

    3. Add docstring note to `CONFIG_DIR`, `CONFIG_PATH` in `triggarr/models/config.py` and `STATE_PATH` in `triggarr/state.py` explaining the freeze constraint:
       Add a module-level comment block after the constant assignments:
       ```python
       # NOTE: CONFIG_DIR and CONFIG_PATH are evaluated once at first import.
       # Changing TRIGGARR_CONFIG_DIR after import has no effect on these constants.
       # Functions accept path parameters to allow testing without module reload.
       ```
       Same pattern for STATE_PATH in state.py.

    4. Add tests in `tests/test_config_dir.py`:
       - `test_relative_path_rejected`: Set TRIGGARR_CONFIG_DIR to "relative/path", assert ValueError raised
       - `test_traversal_path_rejected`: Set TRIGGARR_CONFIG_DIR to "../etc", assert ValueError raised (it's relative)
       - `test_absolute_path_with_dotdot_resolves`: Set to "/config/../data", verify it resolves to Path("/data") and succeeds (absolute path with .. is fine -- it resolves cleanly)
       - `test_frozen_constants_not_affected_by_env_change`: Import CONFIG_DIR, then change env var, call get_config_dir() -- show the function returns new value but CONFIG_DIR is still the old value (documenting the freeze behavior)

    5. Add test in `tests/test_web.py`:
       - `test_save_settings_cleans_temp_on_replace_failure`: Mock os.replace to raise OSError, verify temp file is cleaned up after the exception
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/test_config_dir.py tests/test_web.py -x -q</automated>
  </verify>
  <done>
    - get_config_dir() rejects relative paths with clear ValueError
    - Absolute paths with .. components resolve correctly
    - Settings save cleans up temp file on os.replace failure
    - Module-level freeze constraint documented in comments
    - All new tests pass, existing tests unbroken
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite and lint</name>
  <files></files>
  <action>
    Run the full test suite and linter to confirm no regressions:
    1. `uv run pytest tests/ -x -q` -- all tests pass
    2. `uv run ruff check triggarr/ tests/` -- no lint violations
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/triggarr && uv run pytest tests/ -x -q && uv run ruff check triggarr/ tests/</automated>
  </verify>
  <done>Full test suite passes, zero ruff violations</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` passes with all tests green
2. `uv run ruff check triggarr/ tests/` shows zero violations
3. Setting `TRIGGARR_CONFIG_DIR=../tmp` and importing config raises ValueError
</verification>

<success_criteria>
- Relative/traversal TRIGGARR_CONFIG_DIR values rejected at startup with clear error
- Temp file leaked scenario in routes.py fixed with try/except/unlink pattern
- Freeze constraint documented in both config.py and state.py
- Test coverage for path validation, frozen constants, and temp file cleanup
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/24-hardening-config-validation-and-temp-file-cleanup/24-01-SUMMARY.md`
</output>
