# GitHub Repo Metadata — Copy-Paste Text

**Manual apply:** Paste these values into GitHub > repo Settings > About (the gear icon next to the repository description on the main page). This file cannot be applied from a CI session.

---

## About (repository description)

Paste this into the "Description" field under Settings > About:

```
Radarr, Sonarr, and Lidarr never auto-search for missing or upgrade-eligible media — Triggarr does. Scheduled searches, closed-loop grab tracking, and a web dashboard. Docker-first.
```

**Character count: 181** (limit: 350)

---

## Topics / Tags

Paste each of these into the "Topics" field under Settings > About (GitHub accepts up to 20):

```
radarr
sonarr
lidarr
arr
self-hosted
docker
automation
homelab
media-automation
python
fastapi
htmx
scheduler
```

---

## Homepage

Paste this into the "Website" field under Settings > About:

```
https://github.com/thejuran/triggarr
```

(Point to the GitHub repo itself unless a separate project site is published.)

---

## Notes

- The About one-liner is reconciled with the benefit-led README tagline (D-06) and is consistent with SeedSyncarr's About framing ("sync files from your seedbox... fast, automated").
- The topics list covers the primary discovery paths for homelab users searching for *arr automation tools.
- Homepage can be updated to a docs site or release page if one is created in a future milestone.
