---
phase: 20.2-deep-review-quality
plan: 02
subsystem: api, database, config
tags: [pydantic, aiosqlite, httpx, ruff, exception-handling, migration]

# Dependency graph
requires:
  - phase: 20.1-deep-review-security
    provides: "Exception sanitization, rate limiter fix, XSS defense"
provides:
  - "Specific exception types in scheduler tracking handler"
  - "Gap-tolerant migration loop using sorted keys"
  - "Consistent async with cursor pattern in db.py"
  - "tracking_delay_seconds config field (renamed from tracking_poll_seconds)"
  - "ArrConfig at_least_one_search_count model validator"
  - "Zero ruff violations across entire codebase"
affects: [config, scheduler, database, tracking]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Specific exception tuple (httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error, OSError) for tracking handler"
    - "sorted(MIGRATIONS.keys()) iteration for gap-tolerant migrations"
    - "Pydantic model_validator(mode='after') for cross-field config validation"

key-files:
  created: []
  modified:
    - "fetcharr/search/scheduler.py"
    - "fetcharr/db.py"
    - "fetcharr/models/config.py"
    - "fetcharr/web/routes.py"
    - "fetcharr/config.py"
    - "tests/test_config.py"
    - "tests/test_db.py"
    - "tests/test_web.py"
    - "tests/test_middleware.py"
    - "tests/test_tracking.py"

key-decisions:
  - "OSError included in tracking exception tuple for filesystem error coverage"
  - "model_validator raises ValueError wrapped in Pydantic ValidationError -- tests match accordingly"
  - "Pre-existing test_search.py failure from DRSEC-07 sanitization is out of scope (already in deferred-items.md)"

patterns-established:
  - "Exception handler specificity: tracking handler catches known failure modes, programming bugs bubble to outer handler"
  - "Migration loop iteration: always use sorted(MIGRATIONS.keys()) to tolerate version gaps"

requirements-completed: [DRQUAL-03, DRQUAL-07, DRQUAL-09, DRQUAL-10, DRQUAL-11, DRQUAL-12]

# Metrics
duration: 13min
completed: 2026-02-25
---

# Phase 20.2 Plan 02: Scheduler, DB & Config Fixes Summary

**Narrowed scheduler exception scope, gap-tolerant migration loop, consistent cursor, config field rename, and ArrConfig model validator with zero ruff violations**

## Performance

- **Duration:** 13 min
- **Started:** 2026-02-26T00:56:47Z
- **Completed:** 2026-02-26T01:10:00Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- DRQUAL-03: Tracking exception handler now catches specific types (httpx.HTTPError, pydantic.ValidationError, aiosqlite.Error, OSError) instead of bare Exception
- DRQUAL-07: Migration loop uses sorted(MIGRATIONS.keys()) to tolerate version gaps safely
- DRQUAL-09: get_schema_version uses async with for cursor, consistent with rest of db.py
- DRQUAL-10: Zero ruff violations across fetcharr/ and tests/
- DRQUAL-11: tracking_poll_seconds renamed to tracking_delay_seconds with accurate docstring across all source and test files
- DRQUAL-12: ArrConfig model validator ensures at least one search count > 0 when enabled

## Task Commits

Each task was committed atomically:

1. **Task 1: Narrow scheduler exception scope, fix migration loop and cursor in db.py** - `c1806a5` (fix)
2. **Task 2: Rename tracking_poll_seconds, reinstate model validator, fix ruff, add tests** - `e558700` (feat)

## Files Created/Modified
- `fetcharr/search/scheduler.py` - Added httpx/pydantic imports, narrowed tracking exception tuple
- `fetcharr/db.py` - Gap-tolerant migration loop with sorted keys, async with cursor in get_schema_version
- `fetcharr/models/config.py` - Renamed tracking_delay_seconds, added model_validator import and at_least_one_search_count
- `fetcharr/web/routes.py` - Updated two tracking_poll_seconds references to tracking_delay_seconds
- `fetcharr/config.py` - Updated DEFAULT_CONFIG template comment
- `tests/test_config.py` - Added 2 model validator tests
- `tests/test_db.py` - Added migration gap tolerance test
- `tests/test_web.py` - Updated mock settings field name
- `tests/test_middleware.py` - Updated mock settings field name
- `tests/test_tracking.py` - Restored pytest import needed by externally-added test

## Decisions Made
- OSError included in tracking exception tuple to cover filesystem errors (e.g., DB file access issues)
- Model validator test uses pydantic.ValidationError (not ValueError) since Pydantic wraps the inner ValueError
- Pre-existing test_search.py failures from DRSEC-07 sanitization were not fixed (out of scope, already documented in deferred-items.md)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed model validator test to use ValidationError**
- **Found during:** Task 2 (add tests)
- **Issue:** Plan's test example used `pytest.raises(ValueError)` but Pydantic wraps ValueError in ValidationError
- **Fix:** Changed to `pytest.raises(ValidationError, match="At least one")`
- **Files modified:** tests/test_config.py
- **Verification:** Test passes, correctly validates both rejection and allowance cases
- **Committed in:** e558700 (Task 2 commit)

**2. [Rule 3 - Blocking] Restored pytest import in test_tracking.py**
- **Found during:** Task 2 (fix ruff)
- **Issue:** External worktree added test using `pytest.raises()` at line 388 after original ruff flagged the import as unused
- **Fix:** Kept pytest import since it is now used by the externally-added test
- **Files modified:** tests/test_tracking.py
- **Verification:** ruff check passes, all tracking tests pass
- **Committed in:** e558700 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered
- Files were reverted multiple times by what appeared to be linter notifications reverting to original content -- required re-applying all changes
- Pre-existing test failure in test_search.py (DRSEC-07 sanitization) confirmed as pre-existing and out of scope

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 6 DRQUAL requirements resolved (03, 07, 09, 10, 11, 12)
- Phase 20.2 (Deep Review -- Quality) is fully complete
- Codebase has zero ruff violations and robust exception handling patterns
- Pre-existing test_search.py failures should be addressed in a future phase

## Self-Check: PASSED

All 11 files verified present. Both task commits (c1806a5, e558700) verified in git history.

---
*Phase: 20.2-deep-review-quality*
*Completed: 2026-02-25*
