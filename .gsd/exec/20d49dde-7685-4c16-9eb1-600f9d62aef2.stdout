## README.md
271 lines; 27 term matches
17: - [Reverse Proxy](#reverse-proxy)
58: - "127.0.0.1:8484:8484"  # Localhost only -- use Tailscale or reverse proxy for remote access
139: # The web setup flow creates auth.username, auth.password_hash,
140: # auth.api_key, and auth.session_secret. Do not copy example secrets.
141: [auth]
142: method = "Forms"                   # Forms, Basic, External, or Disabled
181: Startup-level environment variables such as `TRIGGARR_CONFIG_DIR`, `TRUSTED_PROXY_IPS`, and `ROOT_PATH` are read by the process before the TOML settings are loaded; they are not stored in `triggarr.toml`.
185: Triggarr includes application authentication by default. A fresh config starts in setup mode; the first browser visit redirects to `/setup`, where you create the local username/password and receive a generated API key. A
189: Configured in `[auth]` as `method`:
191: - `Forms` (default) — browser login page, bcrypt password hash, signed 30-day session cookie, logout, password change, and login rate limiting.
192: - `Basic` — HTTP Basic credentials are accepted and can establish the same signed session cookie.
193: - `External` — Triggarr trusts an upstream proxy or SSO layer and lets requests through; use this only when direct access to port 8484 is blocked.
194: - `Disabled` — all routes are accessible without Triggarr auth and a warning is logged periodically. Prefer `External` for reverse-proxy deployments.
196: Requests may also authenticate with `X-Api-Key` when `auth.api_key` is set. The setup flow and security settings page generate API keys; do not paste real keys into examples or logs.
200: - **Application routes** are deny-by-default after setup unless auth is `External` or `Disabled`
201: - **API keys and auth secrets** are stored as `SecretStr`, redacted from logs, and not exposed in normal settings HTML
202: - **Password hashes** use bcrypt, and session cookies are signed with a generated session secret
214: Keep the compose example's localhost bind (`127.0.0.1:8484:8484`) unless Triggarr is behind Tailscale, a VPN, or a reverse proxy. For proxy/SSO deployments, set `auth.method = "External"` only after confirming the proxy 
218: When running Triggarr behind a reverse proxy (Nginx, Caddy, Traefik, etc.), configure `TRUSTED_PROXY_IPS` so Triggarr trusts the `X-Forwarded-For` and `X-Forwarded-Proto` headers from your proxy. Without this, Triggarr c
224: | `TRUSTED_PROXY_IPS` | `127.0.0.1` | Comma-separated list of trusted proxy IPs or CIDR subnets. Set this to the IP address or subnet of your reverse proxy. **Do not use `*`** — it allows any client to forge its apparent
227: **Example: Nginx reverse proxy on the same Docker network**
237: - TRUSTED_PROXY_IPS=172.18.0.1       # Prefer the specific proxy IP; use a CIDR only on fully trusted Docker networks
245: If your proxy runs on the Docker host (e.g. host-networked Nginx), set `TRUSTED_PROXY_IPS=172.17.0.1` (the default Docker gateway) or the specific IP your proxy uses to reach Triggarr.
247: **Example: subpath behind a reverse proxy**
251: - TRUSTED_PROXY_IPS=172.18.0.1
255: Then configure your proxy to forward `/triggarr` to Triggarr's port 8484.
257: When no proxy is configured, the default trust list (`127.0.0.1`) ensures forwarded headers are only honored from localhost.

## SECURITY.md
60 lines; 10 term matches
18: Triggarr is a single-process automation daemon that connects to configured Radarr, Sonarr, and Lidarr instances. It includes application authentication by default, protects stored service credentials, and is designed for
23: - **Supported auth modes** -- `Forms` (default), `Basic`, `External`, and `Disabled` are configured in `[auth].method`. Use `External` only behind a trusted reverse proxy or SSO layer; `Disabled` intentionally removes Tr
24: - **Session cookies** -- browser sessions are signed, HTTP-only, SameSite=Lax cookies with a 30-day max age. Cookies are marked secure when the request is HTTPS or carries `X-Forwarded-Proto: https` from an IP in `TRUSTE
25: - **API access** -- requests may authenticate with `X-Api-Key` when `auth.api_key` is configured. API keys are generated by setup/security settings flows and compared using timing-safe checks.
30: - **SecretStr for API keys and auth secrets** -- instance API keys, auth API keys, password hashes, and session secrets are stored as Pydantic `SecretStr` fields. Secret values are only unwrapped at explicit use sites su
32: - **Settings UI redaction** -- normal settings pages do not expose stored API keys or auth secrets in HTML.
36: - **Deny-by-default route protection** -- after setup, non-exempt routes require a valid session, API key, configured Basic credentials, or an explicitly configured External/Disabled mode.
57: - Keep the Docker port bound to localhost (`127.0.0.1:8484:8484`) unless Triggarr is behind Tailscale, a VPN, or a reverse proxy.
58: - Set `TRUSTED_PROXY_IPS` to specific proxy IPs or CIDR ranges when using forwarded headers. Do not use `*`, because it lets any client spoof its apparent IP address.
59: - Prefer `auth.method = "External"` for reverse-proxy or SSO deployments where the proxy is the only path to Triggarr.

## TODO.md
5 lines; 0 term matches

## triggarr/models/config.py
165 lines; 5 term matches
92: When method is 'Disabled', all endpoints are accessible without auth.
95: method: Literal["Forms", "Basic", "External", "Disabled"] = "Forms"
108: """True when auth is explicitly disabled via config."""
115: Sections: [general], [auth], [radarr], [sonarr].
125: auth: AuthConfig = AuthConfig()

## triggarr/web/routes.py
1422 lines; 74 term matches
29: from triggarr.auth import (
70: # Shared mutable dict for auth display state. Plan 03's route handlers populate it
71: # from settings on startup and after config changes. Templates use auth_state.active
73: auth_state: dict = {"active": False}
74: templates.env.globals["auth_state"] = auth_state
77: def _is_secure_context(request: Request) -> bool:
78: """True when the request arrived over HTTPS (direct or via reverse proxy)."""
84: def _sync_auth_state(settings: SettingsModel) -> None:
85: """Update auth_state dict for template conditional rendering (D-11)."""
86: auth_state["active"] = settings.auth.method in ("Forms", "Basic") and not settings.auth.needs_setup
87: auth_state["method"] = settings.auth.method
210: auth = settings.auth
211: result["auth"] = {
212: "method": auth.method,
213: "username": auth.username,
214: "password_hash": auth.password_hash.get_secret_value(),
215: "api_key": auth.api_key.get_secret_value(),
216: "session_secret": auth.session_secret.get_secret_value(),
405: "auth_method": settings.auth.method,
406: "auth_is_disabled": settings.auth.is_disabled,
407: "auth_api_key_set": bool(settings.auth.api_key.get_secret_value()),
408: "auth_username": settings.auth.username,
580: _sync_auth_state(new_settings)
1028: When auth is already configured (needs_setup=False), redirects to /login
1032: auth = request.app.state.settings.auth
1033: if not auth.needs_setup:
1046: If auth is already configured (race condition or stale form submission),
1049: auth = request.app.state.settings.auth
1050: if not auth.needs_setup:
1078: new_auth = AuthConfig(
1086: # Persist to TOML -- merge auth with existing config
1091: if not current_settings.auth.needs_setup:
1094: # Build new settings with auth configured
1095: updated = current_settings.model_copy(update={"auth": new_auth})
1103: # Update auth_state for nav bar logout visibility
1104: _sync_auth_state(request.app.state.settings)
1110: # Auto-login: set session cookie (D-01)
1117: response.set_cookie(
1123: secure=_is_secure_context(request),
1131: """Render login form, or redirect to dashboard if already authenticated (D-06)."""
... 34 more matches

## triggarr/web/middleware.py
190 lines; 34 term matches
15: from triggarr.auth import COOKIE_MAX_AGE, sign_session, validate_session, verify_password
18: # Paths that bypass authentication entirely.
80: """Deny-all authentication middleware with path whitelist.
82: Every non-exempt request must pass one of the authentication checks
87: 3. method == External -> pass through
88: 4. valid session cookie -> pass through
98: """Gate every non-exempt route through authentication checks."""
101: # Exempt paths pass through without auth
105: auth = request.app.state.settings.auth
108: if auth.needs_setup:
114: if auth.is_disabled:
118: "Authentication is disabled -- all requests are unauthenticated. "
119: "Set auth.method in triggarr.toml to enable."
124: # Step 3: External -> pass through
125: if auth.method == "External":
128: # Step 4: Valid session cookie -> pass through
129: cookie = request.cookies.get("triggarr_session")
130: username = validate_session(cookie, auth.session_secret.get_secret_value())
136: stored_key = auth.api_key.get_secret_value()
140: # Step 6: Basic auth mode -> check Authorization header
141: if auth.method == "Basic":
142: return await self._handle_basic_auth(request, auth, call_next)
157: async def _handle_basic_auth(
158: request: Request, auth: AuthConfig, call_next: RequestResponseEndpoint
160: """Decode Basic auth header, verify credentials, set session cookie on success."""
161: authorization = request.headers.get("authorization", "")
162: if authorization.startswith("Basic "):
164: decoded = base64.b64decode(authorization[6:]).decode("utf-8")
166: if secrets.compare_digest(username, auth.username) and verify_password(
167: password, auth.password_hash.get_secret_value()
171: username, auth.session_secret.get_secret_value()
173: _secure = request.url.scheme == "https" or request.headers.get(
176: response.set_cookie(
182: secure=_secure,

## tests/test_auth.py: MISSING
## tests/test_web.py
1925 lines; 0 term matches

## tests/test_startup.py
489 lines; 0 term matches

## .gsd/milestones/M001/slices/S02/S02-SUMMARY.md
8 lines; 2 term matches
3: Unit `complete-slice` for `M001/S02` failed to produce this artifact after idle recovery exhausted all retries.
5: **Reason**: Deterministic policy rejection for complete-slice "M001/S02": edit: HARD BLOCK: unit "complete-slice" runs under tools-policy "planning-dispatch" — cannot edit "README.md" — writes are restricted to .gsd/. Th

## .gsd/milestones/M001/slices/S02/S02-PLAN.md
61 lines; 14 term matches
1: # S02: S02
4: **Demo:** After this: README and adjacent docs explain current Docker/standalone setup, nested multi-instance config, auth/security behavior, and no longer point to stale missing configurable-config work.
10: - Security/auth section matches actual code behavior and avoids obsolete absolute claims.
27: - [x] **T01: Audited README and adjacent docs against verified auth, config-dir, multi-instance, release, and Lidarr behavior.** `est:1h`
29: - Files: `README.md`, `SECURITY.md`, `CONTRIBUTING.md`, `docker-compose.yml`, `CHANGELOG.md`, `TODO.md`, `triggarr/models/config.py`, `triggarr/models/config.py`, `triggarr/auth.py`, `triggarr/web/middleware.py`, `tests/
30: - Verify: `rg -n "no authentication|TRIGGARR_CONFIG_DIR|\[radarr\]|\[sonarr\]|\[lidarr\]|/config|mellow-tinkering-creek|latest/download" README.md SECURITY.md CONTRIBUTING.md docker-compose.yml CHANGELOG.md TODO.md` plus
32: - [x] **T02: Updated README install, config, and security guidance to match current config-dir, multi-instance, and auth behavior.** `est:1.5h`
35: - Verify: `rg -n "no authentication|\[radarr\]\s*$|\[sonarr\]\s*$|\[lidarr\]\s*$|mellow-tinkering-creek" README.md` should show no stale/contradictory matches except intentional explanatory context.
37: - [x] **T03: Retired the stale configurable config-directory TODO and reconciled backlog/security docs with current path and auth behavior.** `est:45m`
43: Fix the slice-level reviewer findings in README.md and SECURITY.md without changing runtime code. Required corrections: (1) adjust SECURITY.md session-cookie wording to say cookies are marked secure when the request is H
45: - Verify: rg -n "The Docker image defaults `TRIGGARR_CONFIG_DIR`|inappropriate public IPs|forwarded as HTTPS by a trusted proxy|use 172\.18\.0\.0/16 for the full subnet|mellow-tinkering-creek|Hardcoded `/config/` paths p
56: - triggarr/auth.py
58: - tests/test_auth_config.py
59: - tests/test_auth_routes.py

