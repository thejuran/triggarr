## triggarr/web/routes.py
77: def _is_secure_context(request: Request) -> bool:
81: return request.headers.get("x-forwarded-proto", "") == "https"
1117: response.set_cookie(
1123: secure=_is_secure_context(request),
1222: # NOTE: client.host is resolved from X-Forwarded-For by uvicorn when proxy_headers=True.
1223: # If TRUSTED_PROXY_IPS=*, rate limiting can be bypassed by spoofing X-Forwarded-For.
1254: response.set_cookie(
1260: secure=_is_secure_context(request),
1289: secure=_is_secure_context(request),
1299: _ALLOWED_AUTH_METHODS = {"Forms", "Basic", "External"}

## triggarr/web/middleware.py
87: 3. method == External -> pass through
124: # Step 3: External -> pass through
125: if auth.method == "External":
174: "x-forwarded-proto", ""
176: response.set_cookie(

## triggarr/__main__.py
29: """Return trusted proxy IPs for uvicorn forwarded_allow_ips.
31: Reads the TRUSTED_PROXY_IPS env var. Defaults to 127.0.0.1.
33: return os.environ.get("TRUSTED_PROXY_IPS", "127.0.0.1")
64: "TRUSTED_PROXY_IPS=* trusts ALL proxies — only use this if Triggarr is "
81: proxy_headers=True,
82: forwarded_allow_ips=get_trusted_proxy_ips(),

## tests/test_auth_routes.py
13: test_login_set_cookie_max_age_30_days, test_login_get_rejects_open_redirect_next,
354: set_cookie = response.headers.get("set-cookie", "")
355: assert "triggarr_session" in set_cookie
408: set_cookie = response.headers.get("set-cookie", "")
409: assert "triggarr_session" in set_cookie
469: set_cookie = response.headers.get("set-cookie", "")
470: assert "triggarr_session" in set_cookie
472: assert 'max-age=0' in set_cookie.lower() or '="";' in set_cookie
599: """POST /settings/security with method=External updates config and redirects."""
610: data={"auth_method": "External"},
774: def test_login_set_cookie_max_age_30_days(tmp_path: Path):
785: set_cookie = response.headers.get("set-cookie", "")
786: assert "max-age=2592000" in set_cookie.lower()

## tests/test_auth_middleware.py
179: # External mode tests
184: """When method is External, all requests pass through unconditionally."""
185: auth = _configured_auth(method="External")
429: """X-Api-Key still works in External mode (External passes through anyway)."""
430: auth = _configured_auth(method="External")

## tests/test_root_path.py
5: Also verifies proxy_headers is enabled so X-Forwarded-Proto is respected.
45: def test_proxy_headers_enabled() -> None:
46: """Uvicorn config includes proxy_headers so X-Forwarded-Proto is respected."""
52: assert "proxy_headers=True" in source
53: assert "forwarded_allow_ips" in source
57: """When TRUSTED_PROXY_IPS is not set, forwarded_allow_ips defaults to 127.0.0.1."""
58: monkeypatch.delenv("TRUSTED_PROXY_IPS", raising=False)
65: """When TRUSTED_PROXY_IPS is set, that value is used for forwarded_allow_ips."""
66: monkeypatch.setenv("TRUSTED_PROXY_IPS", "172.18.0.0/16")

## README.md
142: method = "Forms"                   # Forms, Basic, External, or Disabled
181: Startup-level environment variables such as `TRIGGARR_CONFIG_DIR`, `TRUSTED_PROXY_IPS`, and `ROOT_PATH` are read by the process before the TOML settings are loaded; they are not stored in `triggarr.toml`.
193: - `External` — Triggarr trusts an upstream proxy or SSO layer and lets requests through; use this only when direct access to port 8484 is blocked.
194: - `Disabled` — all routes are accessible without Triggarr auth and a warning is logged periodically. Prefer `External` for reverse-proxy deployments.
200: - **Application routes** are deny-by-default after setup unless auth is `External` or `Disabled`
214: Keep the compose example's localhost bind (`127.0.0.1:8484:8484`) unless Triggarr is behind Tailscale, a VPN, or a reverse proxy. For proxy/SSO deployments, set `auth.method = "External"` only after confirming the proxy is the sole path to Triggarr.
218: When running Triggarr behind a reverse proxy (Nginx, Caddy, Traefik, etc.), configure `TRUSTED_PROXY_IPS` so Triggarr trusts the `X-Forwarded-For` and `X-Forwarded-Proto` headers from your proxy. Without this, Triggarr cannot determine the real client IP or protocol, which affects logging and scheme-aware behavior.
224: | `TRUSTED_PROXY_IPS` | `127.0.0.1` | Comma-separated list of trusted proxy IPs or CIDR subnets. Set this to the IP address or subnet of your reverse proxy. **Do not use `*`** — it allows any client to forge its apparent IP address. |
237: - TRUSTED_PROXY_IPS=172.18.0.1       # Prefer the specific proxy IP; use a CIDR only on fully trusted Docker networks
245: If your proxy runs on the Docker host (e.g. host-networked Nginx), set `TRUSTED_PROXY_IPS=172.17.0.1` (the default Docker gateway) or the specific IP your proxy uses to reach Triggarr.
251: - TRUSTED_PROXY_IPS=172.18.0.1

## SECURITY.md
23: - **Supported auth modes** -- `Forms` (default), `Basic`, `External`, and `Disabled` are configured in `[auth].method`. Use `External` only behind a trusted reverse proxy or SSO layer; `Disabled` intentionally removes Triggarr auth and should be limited to already-private networks.
24: - **Session cookies** -- browser sessions are signed, HTTP-only, SameSite=Lax cookies with a 30-day max age. Cookies are marked secure when the request is HTTPS or carries `X-Forwarded-Proto: https` from an IP in `TRUSTED_PROXY_IPS`; configure that trust list so forwarded headers are only honored from expected proxies.
36: - **Deny-by-default route protection** -- after setup, non-exempt routes require a valid session, API key, configured Basic credentials, or an explicitly configured External/Disabled mode.
58: - Set `TRUSTED_PROXY_IPS` to specific proxy IPs or CIDR ranges when using forwarded headers. Do not use `*`, because it lets any client spoof its apparent IP address.
59: - Prefer `auth.method = "External"` for reverse-proxy or SSO deployments where the proxy is the only path to Triggarr.

